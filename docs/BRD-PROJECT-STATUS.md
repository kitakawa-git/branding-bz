# BRD / branding.bz プロジェクトステータス

> **このファイルは Claude Code・Cowork・Claude Projects の共通ハブです。**
> Claude Code: `/sync-status` スキルで更新
> Cowork: 直接読み書き
> Claude Projects: ナレッジとしてアップロード（週1回推奨）

**最終更新:** 2026-09-02
**更新者:** Claude Code（/sync-status）。前回更新者の記録: Cowork（/sync-status。2026-08-31（その4）：**資料側に残っていた2件を修正し、営業資料 v2 を上書き保存した**（Drive `ID_bzサービス開発/260831_branding-bz_営業資料_v2.pptx`・コード変更なし）。①**社名**＝p15「株式会社 ID INC.」→「ID INC.（アイディ株式会社）」。p16 は先に修正済みだったので実変更は p15 の1箇所。②**p4 の Free 記述**＝フッターを「Freeプランで各ツール月3回まで、生成結果を画面で確認できます。／PDF出力とブランド掲示への反映は Standard 以上。回数も無制限になります。」に、冒頭副文を「Standard 以上なら、PDF出力と本体への反映まで一本道です。」に差し替え。**書く前に `entitlements.ts` を再確認**し `pdfExport` / `portalSync` / `buildToolsUnlimited` の3つとも `free:false` / `standard:true` であることを実測した。⚠️ **p9 の Free「ブランド掲示 編集＋閲覧」と p5 のブランド掲示「Free〜」バッジは据え置き**＝`brandGuidelinesEdit/View` は `free:true` で、Standard 以上になるのは「ツールの出力を掲示へ自動反映する」（`portalSync`）ところだけ。両者は別物なので矛盾しない。③**上田さん作成の Keynote 原本は破棄**が決まったため、**営業資料の正本は pptx 1本**になった。原本にしか無かった画像（表紙コラージュ・スクショ・ロゴ・会社概要のグラデーション・QR・Podcastロゴ）と Keynote 書き出しPDFを Drive `ID_bzサービス開発/260831_営業資料_素材/` に退避済み。④**LPの柱セクションを Claude in Chrome で目視確認**（前回撮影できなかった箇所）。⚠️ **このLPは JS からスクロール・寸法計測ができない**（`scrollIntoView` / `window.scrollTo` が効かず `document.scrollingElement.scrollHeight` がビューポート高と同値で返る）が、**マウスホイールのスクロールは正常に効く**。**TSエラー0件・console.log を含むファイル18件**。⚠️ **未 push 13件**（うち5件は別セッションの 2026-08-25 分。`main` なので push＝本番デプロイになる。指示待ち）。以下は前回の記録：Claude Code（/sync-status。2026-08-31（その3）：**営業資料 v2 とサイトを全項目で突き合わせ、食い違い3件を直した**（`4d8e2ee`・**未 push**）。資料は pptx16枚を全文抽出して比較。**一致していたのは** 4プランの価格・想定規模・メンバー上限（5/50/300/無制限）／1人あたり月額（Standard ¥396〜1,980・Premium ¥199〜1,196）／機能のプラン割り当て（`entitlements.ts` の実装とも一致）／レイヤー構成／機能一覧のグループ順と lead／用語wiki 280語／初期費用・データエクスポート・SSL・PWA／所在地。**食い違いは5件**あり、うち3件をサイト側で修正した。①**Free で PDF出力・掲示反映ができないのに、資料は「一本道」と書いていた**＝`pdfExport` / `portalSync` はどちらも `free:false`（`entitlements.ts:60`）で、API も `guardCompanyFeature` で実際に止めている。**FAQ には正しく書いてあったが料金ページには書いていなかった**ので、Free カードの注記に「生成結果は画面で確認できます。PDF出力・本体への反映は Standard 以上」を追加。注記行は元から60px確保済みでレイアウトは不変。⚠️ **資料側の p4 の記述は誤りなので Cowork で修正が要る**。そもそも資料に寄せる（Free に開放する）か実装に寄せるかは商品判断。②**料金ページのアップセル帯だけプラン名称が古かった**＝Premium「浸透する」／Enterprise「計測＋伴走」がカードの副題と食い違ったまま取り残されていた。カード側に揃えて「構築＋浸透＋発信」「すべてを、伴走つきで」に。**計測を横串に戻した直後なので「計測＋伴走」は特に紛らわしかった**。4つとも折り返し無しを実測。③**マイクロフィードバックが機能一覧に無かった**＝名刺ページで収集（`app/card/[slug]/page.tsx:556`）→ `brand_micro_feedbacks` → 管理画面のブランドスコアで集計、という**実装済みの機能なのに features の発信グループから漏れていた**（FAQ には言及あり）。発信を3件に。④**資料のなかで社名が2通りに割れている**（p15「株式会社 ID INC.」／p16「ID 株式会社」）＝正は「ID INC.（アイディ株式会社）」。**Cowork で資料側の修正が要る**。⑤**導入フローの「審査」がサイトだけ無い**＝資料 p12 は STEP2 に審査を明記しているが、サイトのFAQ「導入にどのくらい時間がかかりますか？」は審査に触れず「最短即日・3ステップ」。**北川さんの指示で保留**とし、サイト・資料とも触っていない。**TSエラー0件・`npm run build`（`next build --webpack`）緑・console.log を含むファイル18件**。⚠️ **未 push 12件**（指示により push しない）。以下は前回の記録：2026-08-31（その2）：**レイヤー構成を「構築 → 浸透 → 発信。計測が、その全部を貫く」に直した**（`9236cc7`・**未 push**）。⚠️ **同日の `103b575` で入れた「構築 → 浸透 → 計測。発信が横串」は逆だったので破棄**。北川さんの指示で計測を横串に戻した。**計測が横串である根拠は実装そのもの**＝インナースコアは浸透の測定、アウタースコア（名刺閲覧・マイクロFB）は発信の測定、理解度テストの設問は `brand_guidelines` / `brand_visuals` / `brand_personas`（構築で定義したブランドデータ）を正解キーに生成される。**計測は3つすべてを参照しないと成立しない**。逆に発信は構築の下流・計測の上流であって横断していない。①**LP**＝Hero副文と柱の見出しを3語に戻し、**柱は3枚のまま維持**。⚠️ **計測を4枚目のカードにすると「4番目のステップ」に見えてしまう**ので、3枚の下に幅いっぱいの帯を新設した（営業資料 p3 と同じ構図）。帯の上端に3枚を貫く線（`bg-gradient-to-r` の細線）を引き、**見た目そのものが横串の説明になる**ようにしている。実測で 1280px：カード371×326×3が1行／**帯は left 64px・width 1152px でグリッドと完全一致**／すき間20px（＝カード間の gap-5 と同値）／横あふれ無し。②**features**＝計測の2機能を浸透グループから独立させ、**発信のあと・サポートの直前**に置いた（ステップではないので構築→浸透→発信の流れから外す）。lead は「構築・浸透・発信の3つを、横串で数字にする。」。PageHero副文と meta description 2箇所にも横串であることを明記。③**ドキュメント3件**（glossary／overview／CLAUDE.md）を同じ内容に。overview には**なぜ計測が横串なのか（どのテーブルを参照するか）まで残した**＝根拠が無いと次にまた反転する。⚠️ **「4レイヤー構造」という呼称はやめた**（3ステップ＋横串なので数え方が揺れる）。glossary の「旧称『3レイヤー構造』は使わない」の一文も削除。④**項目2（/signup 統一）・項目3（用語wiki 280語・特商法の住所）は `103b575` で対応済みだったので確認のみ**（実画面で「無料で始める」3箇所すべて /signup・280語・番地入り住所を確認）。⚠️ **社名は指示書に「現状維持」とあったが、その後の北川さんの明示指示で「ID INC.（アイディ株式会社）」に修正済み（`9ea43c4`）なので戻していない**。**目視確認は Cowork 側（Claude in Chrome）で完了**＝柱3枚が横一列で揃い、計測の帯は左右端がグリッドと一致・カード間と同じ間隔・上端に3枚を貫く細い線あり。/features のグループ順と計測の lead も想定どおり。⚠️ **このLPは JS からスクロール・寸法計測ができない**（`scrollIntoView` / `window.scrollTo` が効かず、`document.scrollingElement.scrollHeight` がビューポート高と同値で返る）。**ホイールのスクロールは効く**ので目視はホイール操作で行う。**TSエラー0件・`npm run build`（`next build --webpack`）緑・console.log を含むファイル18件**。⚠️ **未 push**（指示により push しない）。以下は前回の記録：2026-08-31：**営業資料を正としてサイトの表記を揃えた**（`103b575`・**未 push**）。指示書は Drive の `260831_サイト_資料整合修正_指示書_v1.md`、正の資料は `260831_branding-bz_営業資料_v2.pptx`。①**レイヤー構成を3区分→4区分に**＝正の構造は「構築 → 浸透 → 計測。発信が、その全部を貫く」。LP は Hero副文・柱セクションの見出しを4語にし、柱を3枚→4枚（はかる を追加）。**グリッドは `md:grid-cols-3` → `sm:grid-cols-2 lg:grid-cols-4`**＝md で4列にすると1枚が狭すぎるので中間幅は2列で畳む。実測で 1280px＝4列1段（273×377px・全カード同高）／900px＝2列2段／横あふれ無しを確認。⚠️ **「社員サーベイで浸透度をスコア化」は計測の話なので ひろげる の body から はかる へ移した**（同じ文が2レイヤーに跨ると区分の意味が消える）。features は計測の2機能（インナースコア＋推移／統合ブランドスコア）が浸透グループに同居していたので独立グループに切り出し、順を 構築→浸透→計測→発信→サポート に。PageHero副文に加えて **meta description 2箇所（`ブランドの構築・浸透・発信を支える機能`）も4区分に直した**＝指示書には無かったが、画面が4グループになった時点で説明文だけ3区分のまま残ると同じ不整合が再発するため。②**ドキュメントも同時に更新**＝`docs/glossary.md`（3レイヤー構造→4レイヤー構造・旧称は「使わない」と明記）／`docs/branding-bz-overview.md`（表を4行に。**「計測は独立レイヤーではなく横串」という記述を、横串は発信という書き方に改めた**）／`CLAUDE.md` 冒頭（3項目→4項目）。**片方だけ直すと社内の正が二重になる**ため必ずセットで直す。⚠️ CLAUDE.md は別セッションが末尾に共通ルールを追記中だったので、**`git diff` から自分の hunk だけを取り出して `git apply --cached` で staged した**（`git add -p` は対話式で使えない）。③**「無料で始める」の遷移先を /signup に統一** ＝`/tools/colors/auth` は `/portal/auth` にリダイレクトするため、**未登録者が /tools/colors から入ると必ずログイン画面で行き止まりになっていた**。料金ページの Free プランとページ末尾CTAの2箇所。LP Hero は元から /signup。④**事実誤り2件**＝用語wiki **238語→280語**（本番DB実測 `select count(*) from wiki_terms where status='published'` = 280）／特商法の所在地に番地が無かったので `神奈川県川崎市高津区久本3-6-7-303` に（特商法は住所を全部記載する必要がある）。✅ **社名は同日中に確定・修正済み**＝北川さんの確認により **正式名称は「ID INC.（アイディ株式会社）」**（長音符なし）。サイトは全5箇所が「アイディー株式会社」（長音符あり）で誤っていたので、特商法・利用規約・プライバシーポリシーの3ページを修正した（`0e2e0b6`）。資料側の「ID 株式会社」も正式名称ではない。3ページとも実画面で「アイディー」0件を確認。⚠️ **審査フローの事前開示とFAQ「導入にどのくらい時間がかかりますか？」は対象外**として現状維持。**TSエラー0件・`npm run build` 緑・console.log を含むファイル18件**。⚠️ **未 push**（指示により push しない）。ワークツリーには別セッションの作業（`scripts/wiki-seed/` 2件と CLAUDE.md 末尾の追記）が残っているので触っていない。以下は前回の記録：2026-08-25：**市場調査スコアの2件の修正＋PWA更新トーストの廃止**（未 push）。①**競合の自動抽出から集計上の受け皿行を除いた** `e3221de`＝2026年GT表の `q1_AC` には「その他 13.6%」「あてはまるものはない 11.8%」が**コード付きの行として実在**し、会社でないのに競合として拾っていた。`benchmark.competitorMax` と順位の母数を汚す（受け皿の値が競合トップになりうる）。判定は market-extras が持っていた `NOISE_LABEL` を流用。ただし **market-extras は market-auto-map を import しているため逆向きは循環する**ので、`lib/brand-score/market-labels.ts` に切り出して両者が参照する形にした。除外は列ラベル規約側（`proposeByColumn`）と想起側の両方。想起は選択肢がそのまま社名の並びになるので受け皿行が混ざりやすい。**「その他」は完全一致のときだけ落とす**（`その他の医療機器メーカー` は競合に残す）ことをテストで固定した。②**段階構成が変わった年を素で比べないようにした** `9979885`＝市場浸透スコアは「スコアが出ている段階の平均」なので、設問構成が変われば分母が変わる。**5段階の年と4段階の年を素で引くと、実態が動いていなくても差が出る**（推奨20点が抜けたぶん平均が 52→60 に上がるのを「浸透が進んだ」と読む）。trend API に `scored_stages` を追加し、`lib/brand-score/market-trend.ts` で隣接2点の共通段階を出して**その段階だけで `computeMarketScore` を掛け直して増減を取る**。共通が `MIN_SCORED_STAGES`(3) 未満なら増減を出さない。市場調査の一覧に「前回比」の行と構成差の注記、スコア推移グラフ（`BrandScoreView`＝管理画面とポータル共通）にも注記を出す。※ **`portal/market-survey` には推移表示が無い**（単一調査の結果ビューのみ）ので、ポータル側の該当箇所は `BrandScoreView` にした。※ 増減は整数になる（`computeMarketScore` が `clamp`＝`Math.round` を通す既存挙動）。③**「新しいバージョンがあります」の更新トーストを廃止** `5d9f1ce`＝押しても押さなくても次の遷移で反映されるもので、判断させる意味が無い。ただし消すだけだと `skipWaiting:false` のまま新SWが永久に待機して古い資産が配られ続けるので、**あわせて `skipWaiting:true`（即時有効化）に変更**。`PWAUpdatePrompt` → `PWARegister` に改名（SW登録と ChunkLoadError リカバリだけを持つ）。リカバリは残す＝即時有効化にしたぶん、開きっぱなしのタブが古いチャンクを取りに行って失敗する経路はむしろ増えるため。`96c92e9` は改名時に旧ファイルの削除をコミットし損ねていたぶんの後始末。**TSエラー0件・console.log を含むファイル18件・関連テスト5本通過**。以下は前回の記録：2026-08-21：**本番が 504 になった middleware の修正**。**2026-08-21 15:09 JST に Vercel で MIDDLEWARE_INVOCATION_TIMEOUT（hnd1::knrsh-1787292599032）が出てサイト全体が落ちた**。⚠️ **Supabase Auth 自体は正常だった**＝edge_logs の実測で障害時刻の `/auth/v1/user` は avg 122ms / max 160ms、24時間集計でも n=2723・p50 0ms・p99 71ms・max 161ms。リクエストが Supabase に到達した形跡がログに無いため、**Vercel edge → Supabase 間の一時的なハングと推定**。原因側はこちらで制御できないので、**1回の外部通信のハングがサイト全域の 504 に増幅される構造のほう**を塞いだ。①`getUser()` に4秒のタイムアウト＝custom fetch に `AbortSignal.timeout` を渡して接続自体を打ち切る（Promise を放置するだけだと通信が生き続けて実行時間を食う）。abort が効かない経路のために `Promise.race` も重ね、敗者のタイマーは `finally` で `clearTimeout`（放置すると同じ問題を自分で作る）。失敗は握りつぶして `supabaseResponse` を返す＝セッション更新は「できたら嬉しい」程度のもので、これで公開ページを落とす理由が無い。**4秒の根拠**は実測 p99 71ms / max 161ms に対して約25倍の余裕。延ばしても正常時の待ち時間は変わらず、ハング時の失敗検知が遅くなるだけ。②matcher を `/admin` `/superadmin` `/portal` `/mypage` と API だけに絞った。API から **cron・card-view・analytics** を除外したのは「**cookie を持たない呼び出しなので middleware を通す意味が無い**」ため。※ `@supabase/ssr` の `getUser()` はセッション cookie が無ければ `AuthSessionMissingError` を即返し**ネットワーク往復をしない**ので、未ログイン訪問者は元々 Auth を叩いていない（当初「公開アクセス数ぶん Auth を叩く」と書いたのは誤り）。除外してもリクエストの cookie は載ったままなので、各ルート側でセッションを読む処理は従来どおり動く（更新されないだけ）。**確認**: localhost:3004 で公開ページ8本が 200、認証経路4本も従来どおり、除外した API も挙動不変。ビルド後の `middleware-manifest.json` で matcher が5本だけになっていることも確認。TSエラー0件。以下は前回の記録：2026-08-18：**用語wiki の詳細定義を平均108字→670字に拡充し Markdown 構造で描画**（`90da0d6`／`f6a6237`）。①Cowork 側で全230語の `long_def` を「### 見出し＋段落」の Markdown に書き換え。実データの記法は見出しと素の段落だけ（230語を実測して箇条書き・太字・リンク・表・コードは0件）なので **react-markdown / remark-gfm は追加せず自前パーサ**（`lib/wiki/long-def.ts`）で分解し、React の子要素として描画＝`dangerouslySetInnerHTML` を使わないため XSS の余地が構造的に無い②見出し3本以上で**目次**を表示（実データは全語4〜5本）。アンカー id は日本語見出しを避けて `sec-1..` の連番、スクロール位置は globals.css の `scroll-margin-top:5rem` をそのまま利用③`components/wiki/LongDefinition.tsx` は **Server Component のままでクライアントJSの増加ゼロ**。見出しの無い旧データが来ても段落として描画されるのでDB更新前後どちらでも壊れない④DB更新は `scripts/update-wiki-longdef.ts` で **`long_def` と `updated_at` だけを UPDATE**（230件）。`seed-wiki.ts` を使わないのは、子テーブルを作り直すと本番で削除済みの引用8件が復活するため。実行後に quotes=151／sources=240／relations=653 が不変であることを確認済み⑤**再生成された terms.json で引用8件が復活していたので再除去**（159→151）。DB は元から151のままなので公開表示に影響なし⑥見出し階層は h1=用語名／h2=セクション名／h3=定義内小見出し。JSON-LD の `description` と meta description は `short_def` のままで変更なし。**TSエラー0件・build緑（用語231＋カテゴリ7ページを事前生成）**。以下は前回 2026-08-16（その14）の記録：**ダッシュボードの「私たちの『らしさ』」カードを、サイドバー見出しのスイッチで畳めるようにした** `630f71b`。同じ5項目がサイドバーに常時あるので、慣れた人にはショートカットカードが場所を取るだけになる。一方で入ったばかりの人には「どこに何があるか」の地図として要る。**どちらか一方に決めず、見る人が切り替えられるようにした**。①**既定は非表示**＝表示を選んだときだけ localStorage に残す（`portal-rashisa-card-shown`）。端末ごとの見た目の好みなので DB には持たない＝あちらは管理者が全員に効かせる会社設定で、性質が違う。②**状態は context で共有**（`components/portal/rashisa-card-visibility.tsx` 新設）＝スイッチはサイドバー、カードはダッシュボードと置き場所が離れている。ヘッダー検索の `PortalSearchProvider` と同じ考え方で、`PortalLayoutClient` の両方を含む層に Provider を置く。③**開閉をアニメーションさせた**＝条件レンダリングだと間のスペースが一瞬で詰まる。**常時マウントしたまま `grid-rows-[0fr]↔[1fr]` で高さを動かす**（中身は `min-h-0 overflow-hidden` の箱に入れる）。⚠️ `max-height` を使わないのは、閉じ切るまでの速さが中身の量で変わるため。実測で `grid-template-rows`／`margin-bottom`／`opacity` の3つが揃って 300ms で補間（高さ 240px⇄0px・下余白 16px⇄0px）。④**スイッチと見出しの上下中心**＝**箱の中心は `items-center` で既に一致していた（差 0.3px）**。ずれて見えるのは、和文は行ボックスの下側に余白が多く**字面の中心が箱の中心より 0.6px 上**に来るため。`-translate-y-[0.5px]` で字面中心との差 0.07px に。⚠️ **「ずれている」という指摘を、まず実測してから直した**＝目分量で1px動かすと逆方向に外す。⑤スイッチはサイドバーにあるので**ダッシュボード以外の画面でも押せる**（効果はダッシュボードにだけ出る）。ダッシュボード表示中だけ出すかは保留。**TSエラー0件・console.log を含むファイル17件・デプロイ green（`630f71b`）**。以下は前回の記録：2026-08-16（その13）：**スーパー管理の整理**。①**新規登録の承認を企業一覧の上のアコーディオンに移した** `dd2f1da`＝承認待ちの企業は承認すれば下の一覧に並ぶもので、別ページに分けているとサイドバーのバッジだけが気づく手段だった。「新規登録の承認」「競合ドメイン ブロックリスト」の2つを、管理画面の「招待リンク」と同じ体裁のアコーディオンで置く。承認待ちがあるときだけ開いた状態で出す（0件なら畳む）。サイドバーの項目は削除し未対応バッジは企業一覧へ。旧 URL は転送。②**「残存アカウントの削除」をスーパー管理へ移設** `c48d02a`＝旧実装は `profiles` の削除に会社の絞り込みが無く、認可も「どこかの会社の管理者か」しか見ていなかったため、**別の会社の管理者がメールアドレスさえ知っていれば他社の名刺プロフィールを消せた**（該当4件）。内部状態の復旧手段を契約者に削除ボタンとして渡していた点も含めて作り直し、`/superadmin/orphan-accounts` で一覧から選ぶ形に。対象は「members にも admin_users にも紐づかない auth ユーザー」だけで profiles には触れない。溜まっていた**孤立アカウント7件を削除**（テスト残骸3件＋Google ログインで入っただけの3件＋1件。バックアップ済み）。発生源は塞がっている（Google の孤児は oauth-gate が自動削除、サインアップとメンバー作成は失敗時にロールバック）ため**サイドバーからは外し画面だけ残した** `94a696c`。③**cospark を削除**＝独自ドメインでない登録（kodai2007furu@icloud.com）。approval_status は active で却下フローの対象外だったため、`companies` を参照する全62 FK を走査して実際に行のあるテーブルだけを特定してから削除（バックアップ済み・却下メールは送っていない）。あわせて **cospark / うま屋ラーメン / atelier Kiitos の is_demo を false に訂正**（オーガニック登録だった）。④**「企業」を「ブランド」に寄せた** `0cc228e` `544e6f4` `268e9aa` `8c6e223`＝サイドバー・パンくず・一覧・詳細・閲覧ビュー・API のエラー文まで表示語を統一し、アイコンも Building2 から管理画面の「基本情報」と同じ Sparkles に揃えた。DB のテーブル名（companies）は変えていない。⑤**企業詳細をタブ化** `ab8bb78`（基本情報／ブランドオントロジー）、**サイドバーに「サイト管理」の見出し** `82540f2`、**メニューの「アカウント」→「メンバー」** `3093978`、**却下確認をブラウザ confirm から AlertDialog へ**、**アカウント管理の「有効」が1文字ずつ折り返すのを修正**、**表示設定のアイコンを ToggleLeft に**。⑥**デザイントークンの重複を1件だけ統合** `b9a2527`＝同値のトークンは11組あったが、統合したのは `--ds-accent-primary`→`--ds-app-accent-hover` のみ。残り10組（shadcn の --background/--card/--popover など）は**役割が違って値がたまたま一致しているだけ**で、まとめると個別に調整できなくなるため残した。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-16（その12）：**戻り導線・表の折り返し・タイムラインの余白と検索を整えた**。①**パンくずから一覧へ戻れるようにした** `1290923` `c3da1a1` `8fea784`＝詳細画面に入ると戻り方が「ブラウザの戻る」しかなく、親の一覧が文字として出ているのに押せなかった。スーパー管理は `section` を `string` から `{label, href}` に変え、ポータルは `parent?: {label, href}` を足した（お知らせ詳細＝「お知らせ一覧」→ `/portal/announcements`）。**ラベルは遷移先の画面名に合わせる**＝「お知らせ」だとカテゴリ名にも読めて、押した先が一覧だと分からない。あわせてページ内に置いていた「← 戻る」リンクを削除 `c60c8ad`＝パンくずと同じ働きの導線が同じ画面に2つあると、どちらが正か迷う。②**一覧表の折り返しを止めた** `2261882` `c8cae9b` `89150bd`＝列名・構築度バッジ・社名が2行に折れて行の高さが揃わず、縦に目で追えなかった。列名とバッジは `whitespace-nowrap`、社名は伸び続ける可能性があるので `truncate` ＋ `title` 属性（全文はホバーで出す）。**社名だけ扱いを分けた**＝固定文言は nowrap で足りるが、可変長を nowrap にすると表が横に伸びる。③**タイムラインの余白を12pxに揃えた** `f52e79c` `1183979`＝投稿カードの内側とカード間。⚠️ `p-3` を書いても16pxになる＝`globals.css` が「角丸＋枠＋`p-3`〜`p-8`」のカードを16pxに統一しているため。**方向指定（`px-3 py-3`）が既定の逃げ道**で、これはルール側にコメントで明記されている。いいね/コメントは PC だけ1段階詰め（`min-h-11 md:min-h-9`）＝44px はモバイルのタップ標準なので、モバイル側は縮めない。④**ポータルの検索をヘッダーの虫眼鏡に移した** `40e83ff` `98a97f5`＝ページ内の検索欄が投稿の上に常時居座り、探すつもりが無い人にも1行ぶんの幅を取っていた。3案（インライン展開／ドロップダウン／全画面オーバーレイ）をモックで比較してインライン展開を採用。**ヘッダーとページの間は React context**（`PortalSearchProvider` ＋ `useRegisterPortalSearch(placeholder)`）＝検索したい画面が自分で名乗り出る形にすると、ヘッダー側にページ名の分岐を持たずに済む。展開後は**虫眼鏡をフィールドの内側・左**に置く（1つの箱が伸びる動きにする。別々の要素だと「アイコンが消えて欄が出た」に見える）。空のまま blur で畳む。⑤**フィルタのバッジを横1列＋横スクロールに** `a9a6ff0`＝折り返すとカテゴリが増えるたびに投稿が下へ押し出される。⚠️ **スクロールバーは隠している**ので、マウス利用者には「右に続きがある」合図が無い（Shift+ホイールで動く）。フェードや矢印を足すかは保留。⑥別セッションの動き：スーパー管理の「企業」→**「ブランド」**への言い換え `0cc228e` `544e6f4`、アイコンを管理画面「基本情報」と同じ `Sparkles` に統一 `268e9aa` `8c6e223`、企業詳細のタブ分け `ab8bb78`、新規登録の承認を企業一覧上のアコーディオンへ `dd2f1da`、孤立アカウントの整理 `c48d02a` `94a696c`、サイドバーに「サイト管理」見出し `82540f2`。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-16（その11）：**ポータルのスコア表示をカード内カードに整え、お知らせを未読のみにした**。①**スコア表示を「カードの中にカード」に** `ac3ae22`→`fd9876d`＝最初は枠1本＋罫線で割ったが狙いは入れ子だったので作り直した。外側をグレー地の器（枠＋角丸＋余白）にし、中の2枚は白カードのまま。ポータルの「私たちの『らしさ』」と同じ入れ子・同じ地色の関係に揃えた。②**器に「ブランドスコア」の見出しを追加＋グラフを縦中央に** `ac0d363`＝見出しは2カラムをまたぐ行として先頭に置く（order 既定 0 なのでインナー md:order-1／推移 md:order-2 より前に来る）。見出し下は 12px（行間だけ `md:gap-y-3`、列間は 16px のまま）。グラフは隣のインナースコアカードのほうが背が高いぶん下に余白が余っていたので、`CardContent` を縦フレックスにして見出しの下の残り高さいっぱいの箱を作り、その中央に置いた（上下 18px で均等）。③**最新のお知らせを未読のみ3件に** `c0e22b1`＝読んだお知らせが残り続けると「新着がある」ように見えて本当の新着が埋もれる。既読は消して未読だけを出す（全件は「すべて見る」から `/portal/announcements`）。⚠️ 取得を `limit(3)`→`limit(20)` に広げた＝3件のままだと**新しい3件が既読なだけで、その先に未読が残っていても何も出なくなる**。既読判定は一覧ページと同じ `announcement_reads`（user_id + company_id）。**全件既読になるとカードごと消える（「すべて見る」導線も消える）が、これは仕様として承認済み**。④別セッションの2件：`bea7cb5` 表示設定のアイコンを ToggleLeft に／`871c719` 新規登録の承認の確認をブラウザ confirm からダイアログに。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-16（その10）：**新 split の見え方を実画面で詰めた**。①**スコア推移の凡例に「総合」「アウター」が残っていた** `2577224`＝basic では系列データを null にしていたが `<Line>` 自体は描画していたため、Recharts が凡例だけ3系列とも出していた。Premium の画面に使えないはずの系列名が見えていた。⚠️ **直前の修正で「枠だけ残す作りはやめる」と決めてコメントにも書いた、その同じ間違いをグラフの系列側で繰り返した。**`isFull` で `<Line>` ごと出し分ける。②**ポータルのスコア表示を2カラムに** `ad1af6c`＝Premium のポータルは出すカードがインナーと推移の2枚だけで縦に間延びしていたので、左＝インナースコア／右＝スコア推移に並べた。JSX の順序は変えず親をグリッドにして `order` で入れ替えている（大きなブロックを動かすと事故るため）。適用は `readOnly && !isFull` に限定＝full はインナー×アウターの対比が主役でそこに推移を混ぜると組が崩れる、管理画面は縦に読ませる面なので変えない。③**系列が1本のときは説明文と凡例を出さない** `df859d0`＝凡例は線の読み分けに使われず名前を繰り返すだけ、「塗りつぶした点／白い点」の説明も複数系列を見比べるときの手がかりなので1本では情報を足さない。④**ブランドスコアの塊だけ下余白が mb-8（32px）だった** `2e902cd`＝他のカードは全て mb-4（16px）。1箇所だけ倍で空いて見えていたので揃えた。⑥**ポータルの Premium で総合スコアカードを消した** `252e4b7`＝full では「総合＝インナー×50%＋アウター×50%」で別の数字だが、basic は合成相手がいないのでインナースコアそのもの＝下のカードと同じ 77.4 が1画面に2回出ていた。ポータルは Enterprise 導線も出さない（readOnly）ので重複しか残らない。⚠️ 「初回測定／前回比」はこのカードにしかないため basic のポータルでは出なくなる。⑤**入力サポートのバナーをカード全体クリックに** `938a771`＝ボタンの入れ子は不正なので中の CTA は span にして見た目だけ残し、ホバーは外側の group から効かせる。**この1件だけ目視未確認**（`/admin/setup` がログイン切れのため。ビルド・tsc・eslint は通過）。⚠️ 作業中に `{条件 && ( … )}` の中に JSX コメントを置いてビルドを2度落とした（root が2つになる）。教訓には記録済みだったが実際に再発した。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-16（その9）：**無認証で叩ける company-scoped API を全件塞いだ**。当初「27件」と報告したが、塞ぐ段でコードを読み直したところ**11件は偽陽性**（`getAdminContext`/`getMemberContext` 経由の認証、`external/brand-ontology` の秘密認証を grep が拾えていなかった）。実測の要対応は25件で、3バッチで全件対応した。①`121e14e` クエリで company_id を受ける7ルート＝`requireCompanyMember()` を `guardCompanyFeature` の前に通す②`febd0c2` リソース ID から company_id を引く15ルート＝`requireResourceCompany()` に切り出して適用③`6d868cd` 連携API 3ルート＝`requireCallerCompany()` でセッションから解決し、クライアント由来の companyId / userId を使わない。**personality/connect は userId をクエリで受けており、他人の ID を渡すだけでその人の管理者権限として扱われていた**（所有者照合も無意味化）。UI 側は既にログインを要求していた（未ログインなら `/admin/login` へ送る）ため壊れるものは無い。検証はいずれも未ログイン401／他社ID 403／正当な呼び出し元200＋画面の目視。⚠️ 途中 dev サーバが `requireCallerCompany is not a function` で 500 を返したが、Turbopack のモジュールキャッシュが古いだけで、再起動で解消（`npm run build` は通っていた）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-16（その8）：**計測のプラン split を入れ替え、あわせて無認証で叩ける AI 生成ルートを塞いだ**。①**split の入れ替え** `8af70bc`＝これまで「basic＝アウター（Premium）／full＝インナー（Enterprise）」だったが、インナーサーベイは自社だけで完結する自己計測なので Premium 側が正しい。外の目線（市場調査を含むアウター）と、その総合値だけが伴走とセットの Enterprise。識別子も実態に合わせて **brandScoreBasic→brandScoreInner** / **brandScoreFull→brandScoreIntegrated** に改名し、`innerSurvey` を premium: true に。BrandScoreView の variant の意味も反転（basic＝インナーのみ／full＝統合＋市場調査）。⚠️ このとき「枠は出して系列データだけ null にする」作りをやめた＝直前に Premium でアウター1本のグラフが見えていた事故の原因で、出す/出さないは必ずカードごと分ける。料金ページ（正）・features・FAQ・`docs/glossary.md` の正準表まで追随済み。⚠️ 指示にあった「部署別ヒートマップ」は載せていない（機能ごと削除済み `48283cd` で実装が無く、書くと存在しない機能の宣伝になる）。⚠️ RLS の migration は作っていない＝「Phase 3b でサーベイ系に入れた enterprise 条件」を探したが**そもそも存在しなかった**。`company_plan_allows` を使うのは announcements / goal_kpis / goal_periods / timeline 系の10ポリシーだけで、サーベイ系は select のみ・プラン条件なし（書き込みは service_role の API 経由）。SELECT を塞がないのは Phase 3b の設計原則（降格後も過去データは閲覧可）どおりで、**仕様として扱う**。②**セキュリティ修正** `63686c4`＝サーベイとクイズの `generate-questions` が、いきなり service_role で company_id を引いてClaude API を呼んでいた。**認証もプラン判定も無く、ID さえ知っていれば未ログインでも他社のブランドデータで外部APIを走らせられた**（実測: 修正前 200 で生成 → 修正後 401）。共通ヘルパー **`requireCompanyMember()`** を `lib/billing/guard.ts` に新設し、両ルートで「認証 → 所属照合 → プラン判定」の順に通す。あわせて実行を `ai_feature_usage` に記録（`survey_question_generation` / `quiz_question_generation`。外部APIを使うのにこの2つだけ記録が無かった）。③**全数調査**＝`app/api` の全 route.ts を機械分類し、**service_role・認証なし・クライアント由来IDを受ける 39件**を特定。うち仕様として公開が12件、**company-scoped の未対応が27件（うち14件は guardCompanyFeature あり＝プランは見るが呼び出し元を見ないので防御にならない）**。④**CTD（premium・デモ）で実機検証**＝サーベイ作成 → AI設問6問生成（CTD固有の設問）→ 配信開始 → 回答画面（5段階リッカート）まで通過。`ai_feature_usage` に `survey_question_generation` が記録されることも確認。オンボーディングは 0/4 の4ステップ表示。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-16（その7）：**入力サポート（オンライン相談）をログイン内で完結させ、依頼をメールで通知するようにした**。①**外部フォームへの遷移を廃止** `5ea9e75`＝セットアップ支援バナーの導線が `/contact?subject=setup-support`（サービスサイト）に出ており、セットアップ中の管理者がアプリの外に放り出されていた。両バナーをその場でダイアログを開くボタンに変更（見た目は不変）。②**依頼を受ける箱** `2dc33be`＝新テーブル `setup_support_requests`（1社1件 pending・RLS は自社 select のみ・書き込みは service_role の API 経由）。プラン変更依頼と同じ作法に揃えた。**カレンダー連携はしない**＝空き時間の照合や二重予約の防止まで自前で持つと、件数が読めないうちから予約システムの難所を抱えることになるため、日程調整は画面の外（人）で行う。③**完了判定を切り出し** `9c2056b`＝`lib/onboarding/status.ts` を新設し、案内カード（`/api/onboarding`）と相談リクエストの両方が同じ判定を通るようにした。相談には**依頼時点のセットアップ進捗をサーバ側で数え直して**控えるので、担当が画面を開く前に「どこで詰まったか」が分かる（クライアントの申告は使わない）。④**スーパー管理に処理キュー** `97c79ea`＝`/superadmin/support-requests` ＋未対応バッジ。バッジの出し分けが3項目になったので三項演算子の連結をパスと件数の表に置き換えた。⑤**日時指定をカレンダーの候補日方式に** `1d3aa79`＝自由記述だと「いつ空いているか」を文章にする手間が先に来て止まるため、Popover + shadcn Calendar で最大3件積む形に。「日程はお任せします」を併設して候補ゼロでも送れる。**API・DB は不変**＝`preferred_slots` は text のままで、送信直前に「第1希望: 8月20日(木) 午後 / …」の1行へ整形する。⚠️ **選択可能範囲の指定でハマった**＝react-day-picker は `{ before, after }` を1オブジェクトで渡すと「2日のあいだ」を指す**区間マッチャ**になり狙いと逆になる（過去日が選べる）。配列で2つのマッチャに分けるのが正。実機で境界確認済み（今日〜3ヶ月後の同日まで可）。⑥**文面から「担当者」を外した** `7452386` `51e83ba`＝この案内はプランを問わず出るが、専任の担当者が付くのは上位プランだけで **Free には付かない**。実態どおり「ID INC. のスタッフ」に直し、プラン変更ダイアログも同様に（体裁＝見出し中央寄せ・ボタン全丸も揃えた）。⑦**依頼が入ったらメールで通知** `cf998dd`＝これまではスーパー管理画面を開いたときのバッジでしか気づけず、開かなければ何日でも放置される作りだった。問い合わせフォームと同じ経路（Resend／`CONTACT_NOTIFICATION_EMAIL`）に相乗りするので **env の追加は不要**（本番にも両方設定済みを確認）。`escapeHtml` が既に5ファイルに複製されていたため7つ目を作らず `lib/mail/superadmin-notify.ts` に共通化（既存5ファイルの統一は別タスク）。実機で2通の着信を確認済み。⑧**スコア推移が Premium に出ていたのを修正** `8787f2d`＝推移は `brandScoreFull`（Enterprise）の機能で料金ページでも Enterprise 欄にしかないのに、`BrandScoreView` の推移カードだけ `isFull` の囲みが漏れていた（系列データを null にしていただけで**枠は出ており、アウター1本のグラフが見えていた**）。同ファイルの他8箇所では効いていたので取りこぼし。⑨**ポータルのお知らせをカード1枚に** `bba0542` `7520e20` `69a6e56` `ae1d4d7`＝1件＝1カードだと2〜3本並んだだけで枠線が増えて細切れに見えるため1枚に集約し、区切りは内側の罫線だけに。タイトル・カテゴリ・時刻を1行に畳み（時刻は右端で縦のラインを揃える）、見出しと遷移リンクもカードの中へ入れてミッションカードと作法を揃えた（ベルは削除・矢印のみ）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-15（その6）：**ポータルの初回表示から往復を1回減らした** `cc49393`。ダッシュボードのデータ取得が Group 1（9本並列）→ 目標クエリ1本 → Group 2（4本並列）の3段になっており、**真ん中の1本のためだけに Supabase への往復が1回増えていた**。この `personal_goals` のクエリが依存しているのは Group 1 で取れる `currentPeriodId` だけなので、組み立てだけ先に済ませて Group 2 に相乗りさせ、3段を2段にした（KPI の絞り込みは目標 ID が要るので Group 2 の後ろへ移動）。⚠️ **実測はできていない**＝ブラウザからユーザーのセッションで投げるクエリのため、ログイン無しの環境では再現できない。改善は構造上のもの（往復3→2）。残る待ち時間の候補として、①管理者は `/api/onboarding` の結果も待ってから描画される（オンボーディング中かどうかでダッシュボードの中身ごと差し替わるため構造上やむを得ない。API 自体は 10〜30ms なので主因ではなさそう）②自分の投稿を `limit` なしで全件取得している（統計の期間フィルタをクライアント側で切り替えるため。数千件規模になると効く）の2点を記録しておく。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：2026-08-15（その5）：**セットアップ案内の一本化とダッシュボードのタブ整理**。①**セットアップの進捗をタブにした** `ddb4e13`＝管理画面はコンパクト版（`OnboardingMiniCard`）、ポータルは全ステップ版（`OnboardingChecklist`）で、同じ進捗の見え方が2種類あった。`OnboardingChecklist` に `surface`（portal/admin）を足して**実装を1つに揃え**、管理画面でも同じカードを出す `98e5f2d`。面ごとの違いは surface だけに閉じ込めた（portal＝「あとで」で閉じられる／管理画面へ出るとき戻り方を仕込む、admin＝閉じられない／戻り方は出さない）。その後カードを常時表示から独立タブ `/admin/setup` に移設 `ddb4e13`。**このタブだけは機能トグルではなくオンボーディングの状態で出し分ける**（全完了で消える）ため、静的な `DASHBOARD_TABS`（company だけで決まる純関数）には混ぜず `DashboardTabs` 側で先頭に足している。完了後に URL 直打ちした人向けの完了面も用意。②**サイドバーの「ダッシュボード」の着地先を進捗タブに** `d8be552`＝未完了の人にとって最初に見る画面はスコアではなく次にやること。完了すれば brand-score に戻る。`useOnboarding` に `userId` を渡してページキャッシュを3箇所（サイドバー・DashboardTabs・setup ページ）で共有（渡さないと遷移のたびに loading を挟み、行き先が一瞬 brand-score に戻る）。③**タブバーの重複を解消** `2cf1ca6`＝同じ JSX が4ページ5箇所に複製されていた（brand-score だけで2回）。`app/admin/components/DashboardTabs.tsx` に集約し 114行削除/11行追加。④**タブ名の整理**＝視聴分析→**ラーニング分析** `8acdd85`／スマート名刺→**スマート名刺分析** `1c89901`（サイドバーの「スマート名刺」＝名刺を作る画面と同名で区別できなかった）／タイムライン分析→**Good Action分析** `59f9b95` `c47e2d2`。**3タブとも「タブ名・パンくず・ページタイトル」の3系統が不揃いだった**（例: タブ=スマート名刺／タイトル=アクセス解析）ので毎回3箇所を揃えている。⑤**「Good Actionタイムライン」→「Good Action投稿」** `e393177`（12箇所）。※ その後ポータルのサイドメニューだけ別セッションが「タイムライン」に短縮（正準表記は Good Action投稿・用語集に短縮形として登録）。⑥**ブランドスコアの空状態が名刺を主役に説明していたのを修正** `8a88a51`＝アウターは市場浸透75%＋デジタル接点25%なのに「名刺閲覧データでアウタースコアを測定できます」と書いており主従が逆だった。市場調査を文言に入れ、入口が無かった「市場調査を取り込む」を追加、CTA を機能トグルで出し分け。スコア推移の注記も「市場調査の取り込み、または名刺の閲覧30件から」に。⑦**名刺の配布導線の誤り** `91f4c47`＝「名刺を配布する」が `/admin/members` を指していたが、QR の一括/個別ダウンロードは `/admin/card-template` にある。**TSエラー0件・console.log を含むファイル17件**。本セッションの12コミットはすべて別セッションが push 済み。以下は前回の記録：2026-08-15（その4）：**表記の統一とカード角丸の統一**（13コミット・**未 push**）。①**カードの角丸を12pxに統一** `a751e04`＝同じ「カード」が2種類の角丸で並んでいた。shadcn の `<Card>`（73ファイルで使用）は `rounded-xl` = 12px、手書きのカードは `rounded-lg` = 8px。手書き側を12pxに寄せた（47ファイル・102箇所）。**対象は globals.css のパディング統一ルールと同じ条件**（`rounded-lg` ＋ `border` ＋ `p-3〜p-8`）で拾い、チップ・ボタン・入力欄などカードでない `rounded-lg` は変えていない。⚠️ **`rounded-xl` は Tailwind 既定値で `--radius` に連動しない**（`sm/md/lg` だけが `var(--radius)` 基準）。`--radius` を変えるとカードだけ取り残される点は未解決＝`xl: calc(var(--radius) + 4px)` を足すかは保留。②**表記の統一**＝「KPI・目標」→「目標・KPI」（11箇所。多数派に合わせた）／管理画面サイドバーから「管理」を外す（目標・KPI／お知らせ／サーベイ。管理画面はどれも管理なので項目ごとに付けても情報が増えない）／ポータルのサイドメニューを「タイムライン」に短縮／ポータルのアップセル見出しを「インナーサーベイ結果を見るには」に。`docs/glossary.md` の正準表と短縮形の表も同時に更新した。③**入力サポートの案内を実機で調整**＝文字サイズ・余白・アイコン（最終的に `Headset`）・ふわっと表示（`animate-in`・0.7秒）。⚠️ **コミットの取り違えを1件やった** `99f2bb0`＝却下されたはずの「注記の削除」が、コマンドの繋ぎ方の誤り（判定の失敗とコミットが別行で `&&` に繋がっていなかった）でそのまま入った。`5ad0fce` で戻し、`8655c38` で改めて依頼どおり削除した。**TSエラー0件・console.log 0箇所・`npm run build` 緑**。**DB 変更なし**。⚠️ **未着手＝Stripe 連携／期限切れの日次 Cron／プラン変更履歴テーブル／入力サポートの予約フォームURL確定／`rounded-xl` の `--radius` 連動**。以下は前回の記録：2026-08-15（その3）：**管理画面にもセットアップ案内／Good Job→Good Action 改称／入力サポートの案内**（32コミット・**未 push**。うち複数は別セッション）。①**管理画面にもポータルと同じセットアップ案内を出した** `98e5f2d` `ddb4e13` `2cf1ca6` `d8be552`＝実装を分けると進捗の見え方が2種類になるので `OnboardingChecklist` を共通化し、面ごとの違いは `surface` プロパティ（portal は「あとで」で閉じられる／admin は閉じられない）だけに閉じ込めた。「セットアップの進捗」はダッシュボードの**先頭タブ**（未完了のときだけ出る）にし、サイドバーの「ダッシュボード」の着地先も進捗タブに変えた。**入口だけ判定が抜けていたのを修正** `1e3d598`＝ポータルの「管理画面」は `/admin` へのリンクで、そこは無条件に `/admin/brand-score` へリダイレクトしていたため、ポータルから移動するたびにブランドスコアのタブに着いて進捗タブがアクティブにならなかった。ポータル側は判定を持っているので `/admin/setup` へ直接送る。（`/admin` 直打ちは従来どおりブランドスコアに着く＝サーバー側で判定を再実装すると「判定は1箇所」が崩れるため見送り）②**「Good Job」→「Good Action」→「Good Action投稿」に改称** `f781146` `e393177` ほか＝事前に **DB に該当データが無いこと**（timeline_posts.category / announcements / philosophy_elements / portal_subtitles）と、コード側が表示文言とコメントだけで識別子・カラム名・FeatureKey（`timeline`）に使われていないことを確認してから置換した。ダッシュボードのタブ名も整理（Good Action分析／スマート名刺分析／ラーニング分析）。`docs/glossary.md` の正準表記も追随し、旧称は「使わない表記」として残した。③**入力サポートの案内を新設** `0635387`＝セットアップで詰まった管理者にオンラインの入力代行を案内する。ポータル左サイドバー下部（コンパクトカード）と管理画面の進捗タブ下（グラデ帯）の2箇所。形が違うだけなので1ファイルに `variant` で持つ。出す条件はオンボーディングと同じで、完了すれば消えるため閉じるボタンは付けない。リンク先は暫定（`/contact?subject=setup-support`）。④**しくみ説明カードの文言・配色を反復調整**。**TSエラー0件・console.log 0箇所・`npm run build` 緑**。**DB 変更なし**。⚠️ **未着手＝Stripe 連携／期限切れの日次 Cron／プラン変更履歴テーブル／入力サポートの予約フォームURL確定**。以下は前回の記録：2026-08-15（その2）：**しくみ説明カードの作り込みとプラン外の穴埋め**（23コミット・**未 push**）。①**しくみ説明カード（`PortalIntroCard`）を追加後、実機を見ながら作り込み**＝管理画面（登録・編集）とポータル（この画面）の2画面対比図。**配色で2回外した**＝(a) `bg-sidebar` を使ったが globals.css の `[data-portal]` が `--sidebar-*` をポータル用の明るい配色に上書きしており、カードはポータル内なので明るいグレーになった (b) `:root` の 18% を直に書いたが、実際の管理画面は `AdminDataProvider` が SidebarProvider へ`220 13% 21%` を渡して**管理画面だけ明るく**しており #2f333d が正だった。**上書きが2段ある**ことを最初に確認していれば防げた。この2つは検証方法の誤りでもあった＝`document.body` 直下に要素を差し込んで測っており、`[data-portal]` の内側で測っていなかった。文言・字送り・並び順は北川さんの指示で反復（リード文は現在地を先に示す形へ、パネルは「登録・編集／管理画面」「この画面／ポータル」の2行に整理、左右を入れ替えてポータルを左＝いま見ている画面を先に、矢印は一方通行から行き来（⇄）へ）。②**プラン外の穴を2件塞いだ**＝アカウントメニューの**名刺プレビューだけロックバッジが無く**、しかも押すとプレビューが開いて使えないことが伝わらなかった。`can(company, "smartCard")` で出し分け、プラン外ではプレビューを開かず `PlanUpsell` をダイアログで出す（専用文言を作らず他画面と同じ面を使う。一般メンバーには `readOnly` で依頼ボタンを出さない）。③**Free の案内から Free で使えない機能の記述を削除**（スマート名刺・「社外に見せる顔」）。④**青のトークン化**＝値が同じなのに直書きしていた2箇所をトークンへ。淡色（`bg-blue-50` 等）は `--ds-app-accent` が hex 変数で不透明度修飾が効かないため素の Tailwind blue のままにする、という運用理由も確認した。**TSエラー0件・console.log 0箇所・`npm run build` 緑・`steps.test.ts` 全ケース pass**。**DB 変更なし**。⚠️ **未着手＝Stripe 連携／期限切れの日次 Cron／プラン変更履歴テーブル**。以下は前回の記録：2026-08-15：**Free 版オンボーディングの再設計＋案内まわりの作り込み**（16コミット・**未 push**）。①**Free を専用ステップに作り直した**＝v1 は standard 向け4ステップからプラン外の②③に鍵をかける引き算で、free では「4つのうち2つ押せない」混在案内になっていた。**プラン別のステップ列**（`getOnboardingConfig(plan)`）に変え、free はブランド情報の入力に絞る。ロック行は廃止し、Standard への案内はカード最下部の1行（`/plan`・鍵アイコン不使用）に集約。AI構築ツールは各ステップの補助リンク「迷ったら: …で下書きを作る」に降格（完了判定には関与しない）。v1 の「free の分母を実行可能数にする」ロジックは撤去。**見出し・リード文・プランバッジの有無・下部の案内文まで config が返す**ので、カード側にプラン分岐は1つも無い。**standard 以上は一切変更なし**（3プランの列・文言・待機ラベルをテストで固定）。②**ステップを管理画面の分類に合わせて6つに**＝「考え方／見え方・聞こえ方」という受け手側の言い方だと、CTA で着いた先の画面名と一致せず迷う。基本情報 → ブランド方針 → パーソナリティ → ビジュアル → バーバル → 招待。**判定に使えないカラムを2件発見**＝`companies.brand_color_primary` は `DEFAULT #000000` で全社 true（使うと初日から完了扱い）、`invite_links` 単独だと直接追加した会社（テックブリッジ）が未完了になる。採用した判定は 基本情報=`industry_category`（ロゴは任意なので外す）／方針=`philosophy_elements`／パーソナリティ=`traits`・`personality_summary`・`archetype` の OR（手入力と診断連携で保存先が違う）／ビジュアル=`brand_visuals`／バーバル=`communication_style`・`brand_terms` の OR（トーンだけ保存しても用語の行は作られない）／招待=OR。③**カード全体クリッカブル型に刷新** `bebea12`＝黒いCTAボタンを6つ並べると全部が同じ重さに見えて「どれから？」で止まる。プログレスシェル（大きな N/total・所要時間の合計）＋次にやる1つの大きなカード（全体が Link）＋残りのコンパクト行、完了は緑リストに畳む。**リストの行は押すと遷移ではなくその場で開く**（`animate-in`・並び順は変えない）。④**Free の案内から Free で使えない機能の記述を削除** `e2e3001`＝スマート名刺は Standard 以上（`smartCard: free = false`）なのに「みんなの名刺ページが動き始めます」を成果に挙げていた。ビジュアルの「社外に見せる顔」も同様（free に社外向けの面が無い）。⑤**ポータルのブランド6画面に、登録を促す空状態を新設** `6f6c04f` `2433ab8`＝「まだ登録されていません」の一行だけでは次に何をすればいいか分からない。登録できるのは管理者だけなので、管理者には該当ページへのボタン、メンバーには「管理者が登録すると表示されます」だけを出す。体裁はアップセル面に揃えた。⑥**管理画面へ来た人にポータルへの戻り方を1回だけ教える** `ceda558`＝戻るリンクはアカウントメニューの中で畳まれていて見えない。セットアップ案内から `/admin` へ送るときにフラグを立て、次の管理画面でアカウント行の真上に吹き出しを出す（localStorage・端末ごと）。⑦**ポータルの役割を説明するカードを新設** `f68d504`＝管理画面（つくる場所）とポータル（見せる場所）の2画面対比図。出す条件はオンボーディングと同じで、個別の「閉じる」は localStorage。⑧**青の統一を点検**＝アプリ面はトークン130箇所／生 blue 58箇所だが、生の53箇所は淡色（`bg-blue-50` 等）で、`--ds-app-accent` が hex 変数のため不透明度修飾が効かず**トークン側に薄い階調を作れない**という構造的な理由がある。値が同じなのに直書きしていた2箇所（`hover:border-blue-700` → `hover:border-ds-app-accent-hover`／`to-blue-600` → `to-ds-app-accent`）をトークンへ `85b0603`。**TSエラー0件・console.log 0箇所・`npm run build` 緑・`steps.test.ts` 全ケース pass**。**DB 変更なし**（判定はすべて既存カラム・テーブル）。⚠️ **16コミットすべて未 push**。⚠️ **未着手＝Stripe 連携／期限切れの日次 Cron／プラン変更履歴テーブル**。以下は前回の記録：2026-08-14（その5）：**初回オンボーディング（管理者向けセットアップリスト）＋ポータルのプラン外非表示**。①**着地先は変えない**＝管理者の初回ログインでリダイレクトはしない（案A・Cは不採用）。未セットアップのあいだだけ、ポータルのダッシュボードの中身を4ステップのやることリストに差し替える `469eb96`。空のカードが並ぶと「導線が分かりにくい画面」ではなく「中身の無いサービス」に見えるため、空状態そのものを案内に置き換える判断。ステップは ①らしさ登録 ②最初のGood Job ③お知らせ1本 ④メンバー招待 の順序固定で、**招待を最後**に置き空のポータルを社員に見せない順路にした。**migration `20260814170000` で `admin_users.onboarding_dismissed_at` を本番適用済み**（会社単位だと管理者が複数いる会社で1人が閉じると全員から消え、localStorage だと別デバイスで復活するため個人に持たせた）。判定は `lib/onboarding/steps.ts` に集約し、ポータルのカードと管理画面ダッシュボードの鏡写しカードが同じ関数を通す（二重実装しない）。完了判定は `GET /api/onboarding` でデータの existence のみ。**④は `invite_links` の発行と「管理者以外のメンバーが居る」の OR**＝テックブリッジは招待リンクを使わず直接追加しており invite_links が0件のため、単独条件だと充実企業に案内が出てしまう。**プラン外のステップは🔒で見せるが進捗の分母から外す**＝free は timeline/announcements が永久に未完了で 4/4 に到達できずカードが消えなくなるため、free は ①④ の2つで完了扱い（n/2 表示）。タイムラインに `?compose=1` を追加（投稿UIがダイアログで URL から開けなかった）。単体テスト `lib/onboarding/steps.test.ts` 全ケース pass。②**「準備完了」通知はセッション内メモリ判定** `226ffdc` `56a69a2`＝未完了→完了に変わった瞬間だけ一度出し、読み込み時点で既に完了している人には出さない。DB にも localStorage にも残さない。**基準値をモジュール変数（`Map<companyId, boolean>`）に置いた**のは、最後の1つを管理画面で終えてポータルに戻る順路が最も多く、フック内 state だと remount で必ず消えて実質発火しないため。会社ごとに持つのは、同じタブで別企業を跨いだときの誤発火を防ぐため。③**プラン外の機能をポータルの一般メンバーには見せない** `a6f58ee`＝管理画面は「隠さずグレーで見せる」ままだが、契約を変えられないメンバーに使えない項目を見せても迷わせるだけ。管理者にはロック付きで見せる。あわせて `PlanUpsell` に `readOnly` を追加し、**`POST /api/plan-change-requests` を管理者のみに制限**（表示を消すのは「見せない」であって「させない」ではないため、UIと同じ `admin_users` を基準にサーバ側でも塞いだ）。④**ポータルの通知バナーを揃え、空の枠の余白を消した** `81f94ba`＝目標・KPIバナーをサーベイ回答バナーと同じ様式・同じ位置へ。中身が null を返す3箇所（サーベイ／理解度テスト／ブランドスコア）のラッパーに `empty:hidden`＝枠だけ残って余白として居座っていた。⑤**Free のメンバー上限の表記を実装に合わせた** `aee0ac9`（料金ページ・FAQ とも5名）。**TSエラー0件・console.log 0箇所（app/配下）・`npm run build` 緑**。ブラウザ確認 A〜D（CTD株式会社=premium空／TAKUMI=free空／テックブリッジ=充実／一般メンバー）すべて問題なし。⚠️ **未着手＝Stripe 連携／期限切れの日次 Cron／プラン変更履歴テーブル**。以下は前回の記録：2026-08-14（その4）：**プラン変更の依頼をアプリ内で受け取る仕組み＋メンバー上限の実効化＋名刺閲覧数の水増し修正**。①**プラン変更リクエスト** `2a81fca`＝アップセル面の CTA が料金ページへのリンクで、読んだあと結局どこから申し込むのか分からず導線が切れていた。決済を自前で持たない以上どのみち人が反映するので、「どの会社がどのプランを希望したか」を構造化して受ける形にした。契約者側はその場でダイアログを開いて4プランから選び伝達事項も添えられる（1社1件・出し直すと差し替え）。スーパー管理に「プラン変更の依頼」ページと未対応件数の通知バッジを新設し、承認するとその場で `companies.plan` を書き換える。入金・契約の確認は画面の外の運用に委ねる旨を確認ダイアログに明記。新テーブル `plan_change_requests`（pending は1社1件の部分ユニーク索引）を本番適用済み。**Enterprise も同じ依頼ボタンに揃えた** `5de1de9`＝ここだけ問い合わせフォームへ送ると依頼が積まれないまま埋もれるため。個別見積である事実はダイアログ側で伝える。②**メンバー上限を実際に効かせた** `ccb0f35`＝`MAX_MEMBERS` は定義もテストもあったのに `getMaxMembers` を呼ぶ場所が無く、全プランで人数が青天井だった。メンバーが増える4経路（個別作成・CSV一括・招待リンクからの自己登録・参加申請の承認）すべてに `checkMemberCapacity` を通す。数えるのは `is_active=true` の人だけ（参加申請中は席を使わない）。**Free は1名→5名に変更** `a5829c6`（1名だと自分ひとりで触るもので終わり、社内で見せ合う体験まで届かないため）。純関数 `fitsWithinMemberLimit` を切り出してテスト追加。③**スマート名刺の閲覧数の水増しを修正**＝TAKUMI（誰にも配っていない会社）で総閲覧数3と出ていた。実データは同一IP・同一UA・28秒間の3回で本人の再読み込み。原因は(a)**5分の重複排除が一度も効いていなかった**（直近アクセスの確認を anon クライアントで投げていたが SELECT ポリシーは authenticated 限定。RLS はエラーではなく0件を返すため毎回INSERT）(b)**ローカル開発と Preview のアクセスが本番の数字に混ざっていた**（`card_views` の216件が localhost 由来）(c)**自社の閲覧を除外していなかった**。(a)(b) を直したうえで、名刺ページの cookie セッションからその会社の members/admin_users なら記録しないようにした。**開発由来269件と TAKUMI の3件を削除済み**（削除前に JSON でバックアップ）。ID INC. は 278→65 件になりアウタースコアのデジタル接点が下がる。④**プラン外ページの不具合3件** `fbec112` `2c70495` `2e23334`＝取得APIが403を返すのにデータ取得が走って「取得に失敗しました」が重なる／CIマニュアル出力だけアップセル面が無く操作面がそのまま出る／ポータルのロックバッジが暗い面用の配色で鍵が地色に溶けて見えない。**TSエラー0件・console.log を含むファイル16件**。以下は前回の記録：2026-08-14（その3）：**用語統一とプラン説明文の見直し、ロックバッジのビルド不具合修正**。①**用語統一** 料金ページ `/plan` をサイト全体の語彙の正と定め、`docs/glossary.md` に「プラン・機能名の正準表記」を追記（プラン名・機能名・サイドメニュー短縮形・反映先の一覧）。features / faq / 各ツールLP / サイドバー / アップセル見出しを正準表記に統一（`866abe2`）。**FAQ の実装との不一致を1件修正**＝Good Jobタイムラインを「Premium」と書いていたが実装は Standard 以上（`entitlements.ts` の `timeline`）。**FAQ の「各ツール 月3回・5ターンまで」は誤り**＝5ターン制限は実在するが**ブランドカラー定義ツールの調整チャットのみ・1セッションあたり・全プラン共通**（`FREE_LIMITS.chatTurnsPerSession`）で、Free の制限でも各ツールの制限でもない。Free の説明から外し別記に。**features に未掲載だった実装済み機能を追加**＝ビデオラーニング／ブランド理解度テスト。ブランドスコアを簡易版(Premium)と統合(Enterprise)の2段に分割し、サポート群を「オプション」から Enterprise 標準内容へ。②**ロックバッジが読めない件の原因はビルド設定だった**（`d4be02e`）＝Tailwind の content が `lib/` を走査しておらず `text-purple-800` が生成されていなかった。content に `lib/` を追加し、暗い面用の `PLAN_TONES_ON_DARK` を新設。**このコミットは HEAD 単体のビルド不良も直している**＝別セッションが `plan-gate.tsx` だけ commit しており、依存する `PLAN_TONES_ON_DARK` が未コミットで TS2305 になっていた。③**プラン説明文の見直し**（`778c00b`）＝Standard から「コンサルの1/10以下」を削除（同ページ下部に根拠つきの比較行があり、カード内で根拠なく繰り返すと値引き主張に見えるため）。Free は「体験」をやめ掲示として形が残ることを打ち出す。プラン変更ダイアログの summary も同じ語彙に統一。**TSエラー0件・console.log 0箇所**。⚠️ **未着手＝Stripe 連携／期限切れの日次 Cron／プラン変更履歴テーブル**。以下は前回の記録：2026-08-14（その2）：**プラン制限の実装が Phase 1〜4 まで完了**。migration 6本を本番適用済み、12コミット。①**Phase 1** companies に plan / plan_started_at / plan_expires_at / is_demo を追加し、既存12社を free 4（うち C&S は実顧客候補）/ standard 2 / premium 5 / enterprise 1 に配置。check 制約は card を含めない（販売終了・コード側の Plan 型にのみ温存）。あわせて approval_status の既定を active→pending に直し、/api/superadmin/create-company に 'active' を明示②**Phase 1.5** スーパー管理の企業一覧にプラン列・デモバッジ・「実顧客/デモ」の内訳、企業詳細にプラン編集UI（service_role の API 経由。課金に直結するので anon＋RLS 直 update は使わない）③**Phase 2** `lib/billing/entitlements.ts` をプラン判定の唯一の正として新設。18機能×5プランを明示的な表で持つ（card は free より上・standard より下で序列に乗らないため）。識別子は buildTools 系。getEffectivePlan は plan_expires_at が過去なら free に落とす遅延評価で、日次 Cron が失敗しても取りこぼさない④**Phase 3a** API Route に51箇所のゲート。403 は `{error:'plan_required', requiredPlan, feature}`、構築ツールの上限超過は 429 `{error:'usage_limit_exceeded'}`。名刺の記録系4本は「閲覧は残し記録だけ止める」方式で 200＋`recorded:false` を返す（配布済みQRを殺さない）⑤**Phase 3b** timeline / announcements / KPI はブラウザから supabase-js で直接書いており requirePlan を差せないため、RLS の WITH CHECK にプラン条件を足した。判定は SQL 関数 `company_plan_allows()` に集約し期限切れも見る⑥**Phase 3c** ブランドスコアを variant: basic/full の条件レンダリングで分割（2ファイルに割ると片方だけ直す事故が起きるため）。basic は総合スコアを名乗らず「アウタースコア」表示⑦**Phase 4** 「隠さずグレーで見せる」方針でロックバッジ（サイドバー14項目）とアップセル面（11画面）、期限7日以内の残日数表示、構築ツールの残り回数バッジ（free/card のみ）。**ついでに既存の穴5件を修正**＝goal_kpis/goal_periods の全ログインユーザー開放（テナント分離漏れ）／personal_goals への他社 company_id 差し込み／管理画面の目標削除が0行で静かに失敗／いいね解除が RLS 拒否で動いていなかった／`.delete()` の0行を成功扱いしていた9箇所。**TSエラー0件・console.log 0件・npm run build 緑・entitlements.test.ts 全ケース pass**。⚠️ **未着手＝Stripe 連携／期限切れの日次 Cron／プラン変更履歴テーブル**（下記「残タスク」参照）。次のマイルストーンは Early Access 開始（LP 実装・参加者への premium＋90日付与）。以下は前回の記録：（本セッション・調査のみ／コード変更なし）追記：**無料登録アカウントにポータル全機能が出ている件の検証**。TAKUMI（`takumi_kitakawa@mac.com`）で ダッシュボードにタイムライン／目標・KPI／ラーニング／サーベイ結果／市場調査が並ぶ、という指摘の裏取り。**結論＝仕様どおりではない**が、原因は既知（「プラン制限の実装」節のとおりプラン分岐が1行も無い）。本番DBで新たに確認した点＝plan カラムだけでなく **plans／subscriptions／billing テーブルも無い**こと、および **Phase 1 までの暫定トグル運用には穴がある**こと（サーベイ結果・市場調査にトグルが無く、管理者バイパスで区分設定も効かない／歯止めは superadmin 承認制のみ）。詳細は「プラン制限の実装」節に追記。**セッション冒頭に見えていたポータルのエラー画面（`app/error.tsx`）は別セッションが `8640944` で修正・push 済み**（サーベイ0件の会社で `innerScore.scores.total` が TypeError。当セッションでも `BrandScorePortalSection` を本命として特定していた）。**コード変更・commit なし**（本ファイルのみ更新）。TSエラー0件・console.log 0箇所（app/配下）・ワークツリーは origin/main と同期済み。以下は前回の記録：2026-08-14 追記：**ブランドスコア画面の高速化＋スーパー管理に最終ログイン列**（`main` に直接コミット・**別セッションが push 済み**・本番 green `8640944`）。①**アウター/インナーの集計APIが遅かったのはデータ量ではなく Supabase への往復回数**だった。outer-score は独立した問い合わせを7回直列に待っており、1往復およそ0.2秒がそのまま積み上がる。回答が39件しかない ID INC. でも 11,760 件のリィツと同じだけ遅かったのがその証拠。2段（会社設定・社員・card_events・brand_page_views・市場調査を同時 → card_views と sample_size を同時）に組み替え、社員数と profile_id 一覧は同条件だったので count 付き1本に統合 `908287b`。②inner-score も「closed が無ければ active」の直列と、回答・設問・参加者の3本直列をやめて2段に。③`calculateMarketScore` は候補調査ごとに段階スコアを引いていたのを `.in()` で1回にまとめた（判定順は従来どおり実施日の新しい順）。④管理画面の「前回記録」はクライアントから直接 `brand_score_snapshots` を引いていたのをやめ、記録一覧API（service_role）の末尾から取る。往復が1回減るうえ、RLS で別会社を見たときだけ空になる食い違いも消える。**実測（ローカル・ウォーム）: outer-score 1.4〜1.6秒 → 0.10〜0.19秒／inner-score ID INC. 1.3〜2.0秒 → 0.21〜0.24秒・リィツ 0.45秒 → 0.29秒。値が変わらないことは両社の JSON（リィツ 総合62.0・市場79・n=220・段階91/67/80/83/72）と SQL の count で確認**。⑤**スーパー管理の企業詳細に「最終ログイン」列を新設** `c90fffa`＝誰が実際に使っているかが画面から分からなかった。`auth.users.last_sign_in_at` は auth スキーマが PostgREST 非公開でクライアントから読めないため、service_role の `GET /api/superadmin/company-logins/[id]` を1本足して `profiles → members → auth.users` とたどる。「まだ招待していない（アカウント未作成）」と「招待済みで未ログイン」は打つ手が違うので、どちらも「—」にせず言葉で分けた。**TSエラー0件・`npm run build` 通過・実画面で DOM 検証済み**（C&S の山田泰子＝2026/08/13 17:21＝DB の 08:21 UTC と一致、ID INC. の members 無し profile が「アカウント未作成」）。**なお本セッションの4コミットは別セッションが push 済みで、その後 `8640944`（サーベイ0件の会社でポータルがエラー画面になる不具合）で追修正されている**。未 push の 27 コミットはすべて別セッションの料金ページ改訂。以下は前回の記録：2026-08-06（その10）追記：**プラン構成の第2〜3弾改訂＝Card 削除／Enterprise 新設／計測の Enterprise 移動**（`main` に 27 コミット・**未 push**）。指示書は Drive の `260716_料金ページ改訂案_v2.md`（Card削除＋Enterprise枠）と `_v3.md`（Free掲示編集＋計測のEnterprise移動）、および `260716_プラン制限実装_指示書_v1.md`（v1.1／v1.2 差分）。①**Brand Card を販売から削除**＝Card は「中身を作る手段（構築・掲示編集＝Standard機能）を含まないまま、配る手段だけを売る」構造で、契約者は空のブランドページを配ることになるため商品として成立していない、という判断②**Enterprise（個別見積）を新設**＝300名超・複数ブランド・人の伴走の受け皿。同時に ¥59,800 を「真ん中」に見せる価格アンカー③**計測グループ（インナーサーベイ／統合スコア／推移記録／ギャップ分析）を Premium → Enterprise へ移動**＝スコアは解釈と打ち手がセットでないと価値にならないため四半期レビューと束ねる。**Premium の description から「数値で追う計測基盤」を除去**（計測が抜けた後に残すと虚偽表示になるため必須対応）④**プラン名から `Brand` 接頭辞を削除**し `Free / Standard / Premium / Enterprise` に統一⑤**features を `― 構築 ― / ― 浸透 ― / ― 発信 ―` の区切りラベルで構造化**し、4枚とも「下位プラン全機能に加えて：」で始まる体裁に揃えた⑥**実装のない「部署別ヒートマップ」の記述を4ファイルから削除**（後述の教訓）⑦FAQ も同期（Card 言及の全削除・ブランドスコアの提供プランを Enterprise に・「想定規模より少人数でも契約可」を追加）。**TSエラー0件・console.log 0箇所（app/配下）・全変更を localhost:3004 の実画面で DOM 検証済み**（区切りラベルがチェックアイコン無しで描画されること、4枚の CTA ボタン上端が揃うこと、Enterprise の空 trigger で枠が出ないこと）。**未決（Phase 1 着手前に要回答）**＝(a) entitlements の識別子を `miniApps` のままにするか `buildTools` に変えるか (b) `card` を DB check 制約から外したままコード側の `Plan` 型にのみ温存する方針でよいか (c) `timeline`/`announcements` の開始プラン（料金ページは Standard〜、指示書 v1.2 は Premium〜で不一致） (d) Premium に載せた「ブランドスコア（簡易版）」の範囲が未定義（実装が1つしかなく簡易版/統合版の区別が存在しない） (e) `ギャップ分析` の表記（画面名は「共感 × 知識 ギャップ」）。以下は同日の記録：（その9）：**ポータルのスコアカードから各調査結果へのリンクを出す**（`feature/survey-report-alignment`・main より 2 コミット先行・**未マージ**・Vercel Preview green `da4e98d`）。①**スコアカードのリンクを props 化** `da4e98d`＝`BrandScoreView` にハードコードしていた「サーベイ管理」「市場調査」リンクを `innerLink` / `outerLink`（`{href,label}` または `null`）で受け取る形にし、**未指定なら従来どおり管理画面のリンク＋`readOnly` で非表示**という既存挙動にフォールバックさせた（管理画面の呼び出し側は無変更）。ポータルからは `/portal/survey`「サーベイ結果」と `/portal/market-survey`「市場調査」を渡す。**区分で見られないページは href ごと渡さない**ので、リンクを踏んで「ご利用の区分では表示されません」に着地することはない②**ポータルのサーベイ選択から「（終了）」を外す** `c7e255b`＝読み手には実施中/終了の別が操作に影響しないため、タイトルだけを出す（`STATUS_LABELS` も削除。一覧の絞り込み条件 `active`/`closed` は不変）。**その6・その7 は PR #9 `521cbc5`／PR #10 `6f5ca0d` で main に入り本番デプロイ green 確認済み**。TSエラー0件・console.log 0箇所（app/配下）。以下は同日の記録：（その7）：**役職区分まわりの仕上げとポータルの読み込み改善**（`feature/survey-report-alignment`・main より 23 コミット先行・**未マージ**・Vercel Preview green）。①**役職区分の「未設定」を廃止し既定を staff に** `05f2291`＝migration `20260806150000_role_category_default_staff.sql` を追加（既存 null を staff にバックフィル→`default 'staff'`＋`NOT NULL`）。**2026-08-06 に MCP で本番適用済み**（当初は Supabase Studio から実行予定としていた）。一覧セレクトと編集フォームから「未設定」オプションを削除し、`memberRoleLabel`／`isStaffRole`／`isPortalPageVisibleForRole` の null フォールバックを staff 扱いに統一②**ポータルのブランドスコアを page-cache 化** `71e6938`＝5本の API（inner-score／outer-score／snapshots／market trend／survey trend）を `Promise.all` で待つ間ずっとスケルトンだったのを、`portal-brand-score-{companyId}` に保存して**再訪時は前回値で即描画→裏で再取得**（stale-while-revalidate）。初回のウォールクロックは不変で、体感が変わるのは2回目以降③**サイドバーの会社ヘッダー**＝区分ラベル追加 `769a9f8` → スローガン除去 `473cc59` → **従業員はスローガン・それ以外は区分ラベル** `b0bb6b1` → 判定を `isStaffRole` に寄せて null でもスローガン側に入るよう修正 `5fd4290`（`=== 'staff'` の直接比較だと、migration 未適用で null の行が else に落ちて「従業員」ラベルが出てしまっていた）④**設定「区分ごとの表示設定」の説明文を改訂** `4d8aa76`→`6754bc4`＝「区分・未設定は常に表示」の記述を削除し1文に統合⑤**メンバー一覧の名前セルを折り返さない** `e2c5cd7`。**判断の記録：管理者バイパス（`isAdmin=true` は区分に関わらず全ページ表示）は現状維持**＝北川さんは admin かつ role=staff のため設定画面で staff を off にしても自分では非表示を体感できない。区分の効きを実機で見るには **admin ではない staff アカウント**でログインする必要がある。**保留：構築ツール（colors/persona/personality/stp）のプラン制限は Phase 0 の調査報告のみ完了・Phase 1 以降は北川さんの回答待ち**（Card プランでの構築ツール可否／未認証利用の扱い／料金ページの「AIミニアプリ」表記を「AI構築ツール」に直すか）。TSエラー0件・console.log 0箇所（app/配下）。以下は同日の記録：**上記その5と関連分を PR #8 で main へ squash マージ・本番デプロイ green 確認済み（`9255602` / Production `state=success`）**。反映内容＝メンバー区分（`profiles.role_category`）＋区分ごとの表示設定（`companies.portal_role_visibility`・管理画面「設定」の区分×ページ表）＋サーベイ結果のポータル表示（`components/brand-score/SurveyResults.tsx` 共通化・`/portal/survey` 一覧選択→フル結果）＋ポータルのKPI/タイムライン/ラーニング/サーベイ出し分け（ダッシュボードのKPIカード `d2243af` 含む）＋タイムライン分析の指標カード1行化 `beb7cef`＋スローガン入れ替え `62e9b3b`＋集計期間変更。**migration 2本（`20260805150000` role_category・`20260805153000` portal_role_visibility）本番適用済み**。判定は `lib/constants/member-roles.ts` の `isPortalPageVisibleForRole` に集約（管理者・区分未設定は常に表示／機能トグルオフは区分に関わらず非表示／既定は目標・KPIとサーベイ結果を従業員に非表示）。**残：管理画面サーベイ詳細ページの `SurveyResults` への差し替え（重複解消）は未実施**。※以下は本マージ前の作業ログ：2026-08-06 追記：**その5「メンバー区分＋区分ごとの表示設定＋サーベイ結果のポータル表示」を `02e49ee` として feature/survey-report-alignment に commit・push**（Vercel Preview green 確認済み `state=success` on `dpl_...`。未マージ）。あわせてダッシュボード周辺の微整として **タイムライン分析の指標カードを1行に収める `beb7cef`** と **ポータルトップの挨拶（ようこそ、○○ さん）を大見出し／スローガンを小さな副題に入れ替え `62e9b3b`** が同ブランチに追加（合計 4 コミット先行）。TSエラー0件・console.log 0箇所（app/配下）。※`app/portal/page.tsx` の KPI 区分ゲート差分だけがまだ working tree に残っており、これは別セッションの成果物のため本セッションでは触っていない）

**2026-09-02 のサマリ（最新2）:** **税表記を「税別」に統一**。`/plan`「¥19,800/月（**税別**）」に対し `/tokusho` が「表示価格は**税込み**です」で、Standard で**月1,980円・年23,760円**の食い違いがあった。**税別を正**と決定（北川さん判断）。`/tokusho` の販売価格を税別表記にし、`/plan` の共通事項の**先頭**に「表示価格はすべて税別」を追加（税表記が Standard/Premium の `priceSuffix` にしか無く、Free と Enterprise は `priceSuffix: null` で**ページ全体としての税表記が存在しなかった**ため。`/tokusho` が「各料金プランページに記載のとおり」と参照する先が弱い）。`/faq` のプラン比較の回答にも1文追加。`priceSuffix: '/月（税別）'` は**2箇所とも残している**（追加であって置換ではない）。`PlanChangeRequestDialog.tsx` は既に税別で正しいので無変更。content-audit の `/tokusho` の必須語に `税別` を追加して再発を見張る。

> ⚠️ **特商法（`/tokusho`）に実態と食い違う記述が3件残っている。文言の承認待ちのため未修正。**
> 1. **支払方法**「クレジットカード（Visa・Master・Amex・JCB）」／**支払時期**「月次（毎月自動更新・自動決済）」 — **決済実装が存在しない**。`package.json` に決済SDKの依存 0件（Stripe/PayJP/PayPal/Square/KOMOJU/GMO/Veritrans/Braintree/Adyen）、Webhook 受け口なし、`plan_expires_at` の自動更新なし。更新経路は `app/api/superadmin/company-plan/route.ts` 経由の**手入力のみ**（`app/superadmin/companies/[id]/page.tsx:390`「Stripe を入れるまでは、ここがプランを変更する唯一の手段です」）
> 2. **キャンセル・解約**「**マイページより**月次契約をいつでも解約できます」 — **マイページに解約 UI が無い**。「解約」の語は `/plan` の共通事項と `/faq` にしか無く、実行導線が存在しない
> 3. **サービス提供時期**「利用登録完了後、直ちにご利用いただけます」 — Free は正しいが、**有償プランは手動反映**なので「直ちに」ではない

> ⚠️ **有償プランの自己申込み導線も無い。** `/plan` の CTA は Free=`/signup` ／ **Standard=`/signup`** ／ Premium=`/contact` ／ Enterprise=`/contact` だが、`app/signup/page.tsx` に `plan` の語は0件で**プラン選択が無い**。Standard の CTA を踏んでも実質 Free 登録にしかならず、有償化は `plan-change-requests` に積んで手動反映する運用。

**2026-09-02 のサマリ（最新）:** **社名表記を登記商号「ID株式会社」に確定**。法人番号 **2011001105974** で確認。カナ社名（アイディ株式会社／アイディー株式会社）は登記商号ではなく、**同名の別法人が複数実在する**（6010701015728 等）ため法人名として使わない。特商法は `ID株式会社` 単独、プライバシーポリシー・利用規約の本文主語も `ID株式会社`、末尾の事業者情報ブロックは `ID株式会社（ID INC.）`（登記商号が先）。マーケティング本文の `ID INC.` は従来どおり。8-31 の「正式名称『アイディ株式会社』に直す」（`9ea43c4`）は**当時の判断であって現在の方針ではない**。あわせて 12日分・17コミットの滞留を push した。

**2026-08-25 のサマリ:** **市場調査スコアの2件の修正＋PWA更新トーストの廃止**。競合の自動抽出が「その他」「あてはまるものはない」を会社として拾い、ベンチマークと順位を汚していたのを除外。段階構成が変わった年どうしを素で引き算すると実態と逆に読める問題に、共通段階での再計算と注記を入れた。「新しいバージョンがあります」のトーストは廃止し、SW を即時有効化に切り替え。**未 push**。

**2026-08-21 のサマリ:** **本番が 504 になった middleware の修正**。matcher が全パスに掛かり、`getUser()`（Supabase Auth への往復）をタイムアウト無しで待っていたため、外部が一度ハングしただけでサイト全体が MIDDLEWARE_INVOCATION_TIMEOUT になっていた。Auth 自体は正常（p99 71ms）で、Vercel edge → Supabase 間のハングと推定。4秒のタイムアウトを付けて失敗しても素通りさせ、matcher はログインが要る経路だけに絞った。

**2026-08-16 のサマリ（その13）:** **スーパー管理の整理**。新規登録の承認を企業一覧の上のアコーディオンへ移設、企業詳細をタブ化、サイドバーに「サイト管理」の見出し。表示語を「企業」から「ブランド」へ統一（アイコンも Sparkles に）。「残存アカウントの削除」は**他社の名刺プロフィールを消せる状態**だったのでスーパー管理へ作り直して移し、溜まっていた孤立アカウント7件を片付けたうえでメニューからは外した。cospark（独自ドメインでない登録）を削除し、デモ判定の誤りも3社訂正。**すべて push 済み**。

**2026-08-15 のサマリ（その6）:** **ポータルの初回表示から Supabase への往復を1回減らした**。データ取得が3段の直列（9本並列 → 1本 → 4本並列）になっており、真ん中の目標クエリ1本のためだけに往復が増えていた。Group 2 に相乗りさせて2段に。実測はできていない（ブラウザのセッションが要るため）ので、体感の確認待ち。

**2026-08-15 のサマリ（その5）:** **セットアップ案内の一本化とダッシュボードのタブ整理**。管理画面とポータルで2種類あったセットアップ案内を `OnboardingChecklist` 1つに統一し（`surface` で面ごとの差だけ切り替え）、独立タブ `/admin/setup` に載せた。未完了のあいだはサイドバーの「ダッシュボード」もそこへ着地する。あわせて4ページ5箇所に複製されていたタブバーを `DashboardTabs` に集約（114行削除）、タブ名を「ラーニング分析／スマート名刺分析／Good Action分析」に整理。ブランドスコアの空状態が名刺を主役に説明していた誤りと、名刺配布リンクの誤りも修正。**すべて push 済み**。

**2026-08-14 のサマリ（その4）:** **プラン変更の依頼をアプリ内で受け取る／メンバー上限の実効化／名刺閲覧数の水増し修正**。アップセル面の CTA を「プラン変更をリクエストする」に変え、スーパー管理で受けて `companies.plan` を書き換える導線を作った（Enterprise 含め受け口は1つ）。定義だけあって一度も呼ばれていなかったメンバー上限を4経路すべてで効かせ、Free は5名にした。名刺の閲覧数は重複排除がRLSで無効化されていた・開発トラフィックが混ざっていた・自社閲覧を数えていた、の3点を修正し、汚れた272件を削除した。**未 push 5コミット**。

**2026-08-14 のサマリ（その1）:** **ブランドスコア画面の高速化＋スーパー管理に最終ログイン列**。集計APIの遅さは行数ではなく Supabase への往復回数だった（outer-score は7回直列）。独立した問い合わせを並列に組み替えて **outer-score 1.4〜1.6秒 → 0.10〜0.19秒／inner-score 最大2.0秒 → 0.21〜0.24秒**。あわせてスーパー管理の企業詳細に従業員の「最終ログイン」列を追加（service_role API 経由で `auth.users` を参照）。**本番 green `8640944`**（本セッションの4コミットを含む。push は別セッションが実施）。

**2026-08-06 のサマリ（その8）:** **ブランドスコアと市場調査をポータルへ／管理者の付与／CSV一括登録**。**PR #9 で main にマージ・本番デプロイ green 確認済み（`521cbc5`・deployment id `5772408830`・state=success・ref 一致）**。①**表示の共通化**＝ブランドスコアを `components/brand-score/BrandScoreView.tsx`、市場調査の結果を `components/brand-score/MarketSurveyResults.tsx` に切り出し、管理画面とポータルで共用。**派生値（総合スコア・5段階の並び・推移グラフの行）もコンポーネント側が持つ**＝呼び出し側で計算すると同じ数字が画面ごとに違う値になる。管理画面は 1,563行→1,023行、市場調査詳細は 940行→535行②**ポータルへの展開**＝`/portal` のダッシュボードにブランドスコア、`/portal/market-survey` を新設。区分ごとの表示設定に `brand_score` / `market_survey` を追加（既定は経営層・管理職に表示）③**管理者の付与・剥奪**＝`admin_users` に行が作られるのは新規登録した本人だけで、各社1人しかおらず抜けると誰も入れない状態だった。アカウント管理に「管理者」列を追加。会社は呼び出した管理者の company_id で確定・自社の members 確認・**最後の1人は外せない**④**CSV一括登録**＝読み込み→行ごとの確認→実行の3段階。解析は純関数（`lib/members/parse-member-csv.ts`・テストあり）で、壊れた行があっても全体を止めず行ごとに ok/error を返す。一度に200件まで。**パスワードは平文でファイルに載る方式**（北川さん判断）なのでダイアログに削除とパスワード変更を促す注意を常時表示⑤**市場調査の「反映する/止める」を廃止**＝実施日で推移に点が打たれるようになり、どれが現行かは実施日から決まる。「実施日が最新で3段階以上スコアが出ている調査」を自動で選ぶ⑥**スコア推移**＝サーベイ終了日・市場調査実施日にも点を打ち、記録日の点と同じ系列につないで1本の線に（塗り丸＝調査日／白丸＝記録日）。集計期間を 6ヶ月/1年/3年/5年/全期間 に変更し既定を1年へ（7日ではログが溜まらずどの会社もほぼ未計測だった。実測で30日=11点／1年=30点）。記録した日の一覧と削除を追加。以下は同日の記録：**外部の市場調査を取り込んでアウタースコアを2本立てにした**＝調査会社のGT集計表（Excel）を取り込み、市場浸透ジャーニー（認知→想起→評価→利用→推奨）としてアウタースコアに反映できるようにした。アウター＝**市場浸透0.75＋デジタル接点0.25**。名刺のアクセスログしか無かった状態（認知率83%の会社のアウターが6.0点だった）を解消。①**GT表パーサ**＝`import-gt-table.ts`。構造的な破綻以外は警告にして取り込みを止めない。実ファイル41ブロック1,608セルを error 0 / warn 0 で取り込み②**自動割り当て**＝取り込み中に5段階へ割り当てて詳細画面へ着地（人手のマッピングは時間的に無理という判断）③**母数下限 n≧50**＝認知者ベースの設問で母数27の会社が順位を歪め、ロイヤリティが「12社中2位」と出ていた（レポートは1位）。除外して調査会社のレポートと一致④**未計測を0点にしない**＝スマート名刺オフ・名刺PV30件未満はデジタル接点を null にして分母から外す⑤**5段階以外の読みどころ5つ**＝市場の期待と自社イメージ／ブランドパーソナリティ／認知経路／事業浸透度／サービス評価。設問コードではなく列見出しと設問文の言い回しで拾う（年度で番号が変わるため）⑥**カード別AI考察**＝`market_surveys.insights`⑦**市場浸透の年次推移**＝スナップショットに転記せず調査の実施日から直接組み立てる。**PR #6 で main にマージ・本番デプロイ green 確認済み（`cf97ff5`）**。以下は前日の記録：⓪**（その4・最新）分析レポートと画面の突き合わせ＋AI考察の導入**＝クライアントの分析レポート（15枚）と画面を照合し、同じデータが「4領域」と「5段階」の2軸で語られていた状態を5段階に一本化。**ダッシュボードのインナースコアも WHY/HOW/WHAT から5段階に変更**し、ダッシュボード→サーベイ詳細で軸が1本に通った。**サーベイ詳細の4カードにAI考察を追加**（`brand_surveys.insights` に保存・未生成時のみ自動生成・再生成ボタン）。生成された数値は全て画面の実測値と突き合わせ捏造ゼロを確認。あわせて記録設定を「自動/任意」の選択＋モーダルに再構成、パンくずの親をリンク化（戻る導線）、部署別スコア表を削除。**`feature/survey-report-alignment` に21コミット・未マージ／未push**。Cowork向けの**レポート修正指示**を Drive `c118_RTM/RTM_ブランド戦略会議/260805_インナーブランディング調査レポート_修正指示_v1.md` に作成済み。以下は同日の記録：①**Googleフォーム回答の取り込み**を新設（複数ファイルを1サーベイにマージ・ドラッグ＆ドロップ・AI不使用の決定論的パース）②**PostgREST 1000行上限バグを修正**（集計が先頭1000行だけの値になっていた既存バグ。毎日3時の cron も影響下）③**ラーニング機能のオンオフ**と**パスワードリセット導線**を追加。ここまで main 反映＋本番デプロイ確認済み（`4bc7d8f`／`7d3eb43`）④**インナーサーベイの評価軸を WHY/HOW/WHAT から5段階（認知→理解→共感→行動→推奨）に統一**し、段階別の詳細と個人単位の累積通過率を実装。**`feature/brand-penetration-journey` に9コミット・未マージ／未push**。survey `3f434cb6`(n=392) で段階スコア5つ・転換率4つ・通過率15マス・部門別・回答者数(SP 243/BO本社 149) すべて Supabase 実測値と一致することを確認済み。以下は前回までの記録：（2026-07-27：2026-08-05：**管理画面全ページのカード間余白を16pxに統一**（縦・横すべてのカード間隔を Tailwind の `4`＝16px に：カード自身の下余白 `mb-6`/`mb-3`→`mb-4`、カードを積むコンテナ `space-y-6`/`space-y-8`→`space-y-4`、カードのグリッド `gap-3`→`gap-4`＋コンテナ `mb-3`/`mb-6`→`mb-4`）。カード**内部**の余白は不変。TSエラー0件・ダッシュボードを実ブラウザで実測しグリッド横16px・カード縦16pxを確認。**admin配下17ファイル・コミット `ca2b9b6`**（`feature/brand-penetration-journey`・未push）。同日その前：新規登録でフリーメール（非企業ドメイン）を拒否する `lib/constants/free-email-domains.ts` を新設し signup/join-company/check-domain で共通化（**main へ push 済み `2211139`**）。以下は前回の記録：2026-07-27：**ブランディング用語wiki（/wiki）を新設・230語を全公開・本番デプロイ済み**（`9a5938d`／`9d1a737`）。**公開の流れ**＝全件 `review` でシード → 基礎・核心23語だけ一時公開してレイアウト検証 → 問題なしを確認し北川判断で**残207語を一括 `published`（計230語・review 0件）**→ push して Vercel デプロイ green（本番 `https://branding.bz/wiki` で index 230語／詳細／カテゴリ全て 200、sitemap 259URL、JSON-LD 発火、canonical 正を実測）。**AI補完176語は未監修のまま公開中**で、Cowork の監修チェックリストを並行で進め修正分だけ後日 UPDATE する運用。GA は root layout の `<GoogleAnalytics gaId="G-CQBED1RNV2" />` が全ルートを包むため `/wiki/*` は自動計測＝追加設定不要。Search Console への sitemap 送信は北川さん側で未実施。以下は実装の詳細：①DB＝`wiki_terms`／`wiki_term_sources`／`wiki_term_quotes`／`wiki_term_relations` の4テーブルを追加（migration `20260726163924`・**本番適用済み**）。公開制御は `wiki_terms.status` 一本で、RLS は `status='published'` のみ public read。関連辺は「両端が公開」のときだけ返す＝未監修用語の存在を関連チップから推測させない。書き込みポリシーは作らず service_role のみ②シード＝`npx tsx scripts/seed-wiki.ts` で **230語／ソース240／引用159／関連653** を投入。**全件 `status='review'`＝公開ページには1件も出ない**。slug で upsert する冪等スクリプトで、すでに published の用語は review に戻さない。`ep_no='?'` の引用は `ep_title`（「第22話-2：…」）から話数を復元して Spotify URL を22件補完（残13件は第34-38話が未配信のため空）。カテゴリ「特化・応用 (ID INC.独自)」は「特化・応用」に正規化し7カテゴリに統一③ページ＝`/wiki`（カテゴリカード7枚＋検索＋カテゴリ/出典絞り込み＋用語グリッド）／`/wiki/[slug]`（一文でいうと→詳しく→関連用語→ID INC.の視点→参考ソース）／`/wiki/category/[slug]`（7カテゴリ全て事前生成）。**マーケLP系 `(site)` グループに配置**しダーク基調・Nav/Footer を既存公開ページと共通化。SSG + ISR 3600s④**データ取得は service_role ではなく anon キー**（`lib/wiki/queries.ts`）＝RLS の published 判定をそのまま公開判定に使い、かつ `SUPABASE_SERVICE_ROLE_KEY` を置かない Preview で generateStaticParams が落ちない（2026-07-16 の API route と同型の事故を予防。`/news` が service_role で済んでいるのは `force-dynamic` だから）⑤SEO＝`DefinedTerm`/`DefinedTermSet`/`Quotation`/`BreadcrumbList` の JSON-LD、sitemap に `/wiki`＋公開済み用語＋カテゴリを追加（未公開のうちは `/wiki` 1本のみ＝監修前をクロールさせない）、robots は既に許可済み⑥ヘッダー（無料ツールの右）とフッター（会社情報列）に導線追加。**TSエラー0件・`npm run build` 緑・localhost:3004 で index/詳細/カテゴリ/関連チップ/Spotify遷移/375px を実機確認**（確認用に `基礎・核心` 23語を一時 published にし、確認後 review に戻して現在 published=0）。**未push**。※以下は前回 2026-07-21（その5）の記録：**ブランド体系の構築度スコア（決定論・非保存）＋到達判定の根を M/V/V 常時に統一**。①「構築完了/構築中」バッジを**「構築度 N（バンド）」**に置換（`09a5ab2`）＝4軸（要素の充足30／接続性30／裏づけ25／ルールの質15）＋未来設計ボーナス+5、配点とバンドしきい値は `lib/brand/build-score.ts` の `WEIGHTS` に集約。接続性・裏づけは既存の共有関数（`buildBrandMapGraph`／`findUnreachableFromPhilosophy`／`backing-targets`）で導出＝チップ・integrity と数字がズレない。バッジクリックで内訳ポップオーバー（軸ごとのミニバー＋改善hint＋該当ステップへの「直しに行く」導線）、企業一覧にも構築度列（7テーブル全社一括取得→メモリ集計）②**到達判定の根を M/V/V 常時に変更**（`d31ce29`）＝旧仕様の mission→vision→value 優先順位フォールバックでは、ミッションがある会社でビジョン/バリューが「理念に届かない」側に落ちる直感に反する挙動だった。共有関数1本の修正でチップと構築度が同時に揃う③構築度バッジを押せるボタン外観に（枠線＋shadow＋chevron＋hover/active/focus-visible）、警告チップ4種のアイコンも 14px・opacity-70 に統一（**実装済み・未コミット**）。**TSエラー0件・build緑・ユニット全pass**。※以下は同日の記録（その4）：**関係語彙の意味制約トリガ導入＋未接続クリックで繋ぎ先AI草案の焦点表示＋孤立ブランドカラー列UI再撤去＋サイドバー用語統一**。①`element_relations` に relation_type の意味制約 DB トリガを追加、`communicatedAs` 廃止、未来設計4種の作成経路整理、裏づけを `evidencedBy` 辺に一本化、出所の構造化（`1b87b5a` 別セッション）②「未接続 N件」チップから要素をクリックするとブランドマップ内でその要素にフォーカスし、繋ぎ先のAI草案を承認/手動入力へ導くフロー実装（`cf64422` 別セッション）③`companies.brand_color_primary/secondary` が 2026-04-06 に一度撤去後復活していた孤立列だと再確認（実データで smart card 参照経路を追跡し `brand_visuals.color_palette.accent_colors[0]` が正本と裏取り）、スーパー管理からUI再撤去（DB列と CIマニュアルPDF 参照は温存, `8f723b6`）④admin ユーザーメニュー「サービス画面」→「ポータル画面」に用語統一（同 `8f723b6`, superadmin/design-system の「サービス画面」語はデザイントークンスコープ用語で別意味のため据置）。**TSエラー0件・全て push 済み**（未コミット2件は別セッション作業）。※以下は同日の記録（その3）：**オントロジーカードの引き算＋「繋がっていない」を1か所に集約**。①AI判定機能を削除（`lib/brand/integrity-ai.ts`／`app/api/superadmin/integrity-ai/route.ts`＝DB書き込みゼロを確認のうえ撤去）②到達可能性の判定を `map-data.ts` の `findUnreachableFromPhilosophy` へ共有化し「理念に届かない 15件」チップを新設（未接続13と重複0を実測で確認）③参考(info)「理念から辿れない要素」26件を廃止④指摘0件なら点検セクションごと非表示⑤補足質問0件時のメッセージ非表示⑥「編集する」パディング16px＋出力テストとの上下入れ替え。**TSエラー0件・build緑**。※以下は同日の記録（その2）：**オントロジーウィザードUI徹底統一＋言葉のルールUI整合＋診断ツール表現ルールライブ表示＋個別 fix**。①ステッパー/ステップ見出し/AIアクションボタン(共通AIButton S)/「編集する」パディング16px/上下余白24px を全ステップで統一（`6529166`〜`552c7fb`）②「編集する」開閉に grid-template-rows アニメ（`c71e459`）③言葉のルールUIをポータルの NG/OK 2カラムに揃え source バッジ追加、scope をUI撤去（DB温存）、AI草案 NG/OK も同カラム化、承認登録失敗 fix（source→manual）（`52405ed`/`1b8bd2f`/`1d58798`/`4f3af9b`/`2c2b475`／docs `4303098`）④パーソナリティ診断ツールに「登録済みの表現ルール」ライブ表示・二重表示解消（`add3089`/`2dc2522`）⑤個別 fix＝◯×問題選択肢IDの文字化け(`33174c5`)／`/admin/members` ダーク化 revert(`0430c1e`)／BrandMap3D 非フルスクリーン背景を白に(`2eb64a7`)。**TSエラー0件・全て push 済み**（未コミット2件は指示待ち）。※以下は同日の記録（その1）：**オントロジーカードのUI刷新（引き算＋3D一本化）＋達成条件フォームのやさしい化**。①**達成条件フォームを非エンジニア向けに刷新**（`e14cd6e`）＝タイプselectを4テンプレカード（数字が目標に届いたら/◯件たまったら/証拠が1つでもあれば/人が見て判断）に、`metric_definitions` 辞書を新設して**指標を名前で選ぶ MetricPicker**（内部キーは自動生成・単位は canonical_unit から自動）、測定値フォームも同ピッカーに統一＝目標と測定値の指標・単位が原理的にズレない。常時プレビュー「こうなったら達成：〜」。あわせて `evaluate.ts` の**「測定値ゼロ」と「測定日なし」の取り違えを修正**（NO_MATCHING_MEASUREMENT / NO_MEASURED_DATE）②**カードをミニマル化**（`4718738`/`4da8fc7`）＝AIレビュー常設・ショートカット3ボタン・現状/構造タブ・提供価値の黄色バナーを撤去し、件数と凡例は「…」メニューへ。既定表示は「ヘッダー→警告チップ→3D→出力テスト→編集する」の5段のみ③**ブランドマップを3Dへ一本化**（`38092b2`/`6d42d31`）＝Three.js不使用のCanvas疑似3D（回転行列＋透視投影＋z順ソート）。2Dの d3-force SVG を廃止し `lib/brand/map-display.ts` に表示定義を集約。獲得目標は上空リング・進捗併記、requires/toBeEvidencedBy は点線。⛶で全画面プレゼンモード（暗背景＋自動回転、Escで閉じ rAF停止）④**出力テストをビフォー/アフター比較ステージに**（`b5ea497`）＝生成前から2パネル常設、右パネルは `lib/brand/output-highlight.ts` でオントロジー由来の語句を決定論ハイライト（AI不使用）⑤**ウィザードを5ステップ化**（`83d6d50`）＝旧ステップ1「基本情報の確認」を削除し、理念・提供価値の未登録は上部の警告チップへ⑥**「編集する」が完了後も開いたままになる不具合を修正**（`f652428`）。**TSエラー0件・build緑・全て本番反映済み**。※以下は前回 2026-07-19 の記録：**ブランドオントロジー「未来設計」を構想からコピーAI注入まで一本通し**。①管理UI(a)(b)(c)＝獲得目標CRUD＋達成条件エディタ／実績への測定値入力／判定・進捗表示（状態バッジ＋進捗バー＋**実証進捗と判定可能率のセット表示**）／人間判断の記録・失効警告・クリア／提供価値の状態遷移と current 昇格（`2931805`・`c753267`）②`desired_evidence_rule_hashes` RPC を本番適用し `fetchCurrentRuleHashes` に結線＝**override の失効判定が実際に機能**（ルール変更→失効→自動評価へフォールバックをデモ企業で実証）③コピーAI **FACT/ASPIRATION 物理分離**（`55b28aa`）＝current の提供価値と測定値を FACT へ、target・transition_candidate の提供価値と未達の獲得目標とビジョンを ASPIRATION へ。ASPIRATION は別セクション注入＋「目指す／これからの形でのみ言及可・断定と数値引用は禁止」を明示、**0件なら注入なしで従来プロンプトと文字列完全一致**④既存バグ修正＝`governance_rules` の `rule_type` 未取得で `bannedTerms` が常に空だった問題（`6361e78`。非実在値 `discouraged_expression` を見ていたのを `banned_word` に是正、純粋関数化＋回帰テスト、実データで空→2件を確認）。**TSエラー0件・build緑・ユニット全pass・検証は全てデモ企業テックブリッジでロールバック運用、実クライアント無変更**。最新コミット `52d65a0`／**11コミット未push**。※以下は前回 2026-07-16 の記録：**SEO土台補修＋3ドメインシナジー実装**の連投。①4ツール（STP/ペルソナ/カラー/パーソナリティ）の無料枠を「月に3回・毎月1日リセット(JST)」に統一（`c719519`）＝ `lib/tools/free-limits.ts` を新設し `MONTHLY_FREE_LIMIT=3`/`getCurrentMonthStartUtcIso`/共通エラー文言/LPバッジ文言を集約、4 sessions/route.ts に月次フィルタ追加、colors は新規に上限チェック実装②4ツールLPに可視FAQ＋FAQPage/BreadcrumbList JSON-LD 追加（`1d44ed2`）＝ `<details>` アコーディオン＋「配列単一ソース」で表示Q&Aと schema を1文字違わず一致担保、Q&A は全て LP表記・コードで裏取り済（無料回数=月3回準拠、colors WCAG/CSS出力/自然言語調整はコード確認、時刻具体化は安全側で回避、aggregateRating は実レビュー無しのため不採用）③**API route の lazy init 化で Preview ビルド復活**（`57c93a2`）＝ `members/create`・`members/register` がモジュールトップレベルで `createClient(URL, SERVICE_ROLE_KEY!)` を呼んで Preview ビルドが collect page data で落ちていたのを `getSupabaseAdmin()` を handler 内で呼ぶ形へ変換、実DBで会員作成/登録が 200+正常INSERT することを確認（テストデータ掃除済み）④**3ドメインシナジー @graph JSON-LD 実装**（`d6ead6f`）＝ `app/layout.tsx` の `<head>` に Organization/SoftwareApplication/WebSite の3ノードを追加、Organization は `parentOrganization`/`publisher` で `https://include.bz/#organization` を親参照、sameAs は include.bz/branding.bz/designnow.design + 公式SNS 6項目で3サイト同一配列に揃え名寄せ担保、SoftwareApplication は有料プラン混在のため offers 省略（0円と偽らない）、WebSite は inLanguage=ja。Footer に DESIGN NOW リンクを追加し include.bz ↔ branding.bz ↔ designnow.design の可視相互リンク3辺完成⑤SEO 引き継ぎ資料 v2 に §11（午後の作業）と §12（route lazy init 完了・`lib/supabase.ts` の潜在リスク）を追記（`2a0d5ed`/`e8c4370`）。TSエラー0件・console.log 0ファイル。最新コミット `d6ead6f`）

---

## 開発状態（ブランチ / マージ / デプロイ）

| 項目 | 状態 |
|------|------|
| 作業ブランチ | **`main`（2026-08-31 その3）** — 本セッションは `4d8e2ee`（資料との食い違い3件）1本。**未 push 12件**（うち5件は別セッションの 2026-08-25 分）。ワークツリーの `CLAUDE.md`（末尾の追記）と `scripts/wiki-seed/` 2件は別セッションの作業なので触っていない。以下は前回の記録：**`main`（2026-08-31 その2）** — 本セッションは `9236cc7`（レイヤー構成の反転修正）1本。CLAUDE.md は引き続き別セッションが末尾を触っているので、`git diff` から自分の hunk だけ `git apply --cached` で staged した。以下は前回の記録：**`main`（2026-08-31）** — 本セッションは `103b575`（営業資料に合わせた表記統一）1本。**未 push 6コミット**＝本セッション1本＋別セッションの2026-08-25分5本。ブランチは切らず main のまま作業（指示どおり）。⚠️ ワークツリーに別セッションの未コミット3件（`scripts/wiki-seed/QUOTE-FILTER-RULES.json`・`terms.json`・CLAUDE.md 末尾の共通ルール追記）が残っている。CLAUDE.md は同一ファイルを両者が触っていたため、`git diff` から自分の hunk だけ切り出して `git apply --cached` で staged した。以下は前回の記録：**`main`（2026-08-25）** — 未 push 4コミット（`5d9f1ce` PWA更新トースト廃止／`e3221de` 受け皿行の除外／`96c92e9` 旧ファイル削除／`9979885` 段階構成の比較）。ワークツリーには別セッションの作業（`CLAUDE.md`・`scripts/wiki-seed/*`）が残っており、こちらのコミットには含めていない。以下は前回の記録：**`main`（2026-08-16 その14）** — `630f71b`（らしさカードの表示スイッチ）まで push 済み・未 push 0件・ワークツリー clean。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-16 その13）** — 本セッションの12コミットはすべて push 済み・未 push 0件。ワークツリー clean。以下は前回の記録：**`main`（2026-08-16 その12）** — 本セッションぶん（パンくず・折り返し・タイムライン・検索の12コミット）は `a9a6ff0` まで push 済み・**デプロイ green 確認済み**。⚠️ ワークツリーに未コミット2件（`app/superadmin/companies/page.tsx` ／ `SignupApprovalSections.tsx`）があるが**別セッションの作業中ファイルなので触っていない**。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-16 その11）** — 本セッションぶんは push 済み。別セッションの `871c719`（新規登録承認のダイアログ化）も本ステータス更新と同時に push された。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-16 その10）** — **未 push 0コミット**（`2e902cd` まで push 済み）。本セッションは新 split の見え方調整5本。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-16 その9）** — **未 push 0コミット**（`6d868cd` まで push 済み）。本セッションは計測 split 入れ替え5本＋セキュリティ修正4本。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-16 その8）** — **未 push 0コミット**（`63686c4` まで push 済み）。本セッションは計測の split 入れ替え5本＋セキュリティ修正1本。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-16 その7）** — **未 push 0コミット**（`ae1d4d7` まで push 済み）。本セッションは入力サポートのログイン内完結・スコア推移のプラン修正・ポータルのお知らせ整理で計21コミット。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-15 その6）** — 未 push 16コミット。うち本セッションは `cc49393`（ポータルの往復削減）1本のみで、残り15本は別セッションの「入力サポート」まわり（ダイアログの体裁・カレンダー方式への変更・依頼メール通知）。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-15 その5）** — 本セッションの12コミットはすべて別セッションが push 済みで origin/main に入っている。未 push の13コミットは別セッションの表記統一・サポートカードまわり。※ 通常どおり main 直接コミット運用。以下は前回の記録：**`main`（2026-08-15 その4）** — 13コミット。カード角丸の統一 `a751e04`（47ファイル）／表記統一 `a643695` `5dc0aaf` `d3fa084` `86297c2` ／入力サポートの見た目調整。※ 通常どおり main 直接コミット運用。以下は前回の記録：| 作業ブランチ | **`main`（2026-08-15 その3）** — 32コミットが**未 push**（複数セッションの作業が混在）。管理画面のセットアップ案内 `98e5f2d`〜`d8be552` ／ Good Action 改称 `f781146` `e393177` ／ 入力サポート `0635387` ／ 着地先の修正 `1e3d598`。※ 通常どおり main 直接コミット運用。以下は前回の記録：| 作業ブランチ | **`main`（2026-08-15 その2）** — しくみ説明カードの作り込みとプラン外の穴埋めで23コミット。`7411273`〜`c06c4c6` がカードの配色・文言・レイアウト、`7c046fd` `2d74763` が名刺プレビューのロック。※ 通常どおり main 直接コミット運用。以下は前回の記録：| 作業ブランチ | **`main`（2026-08-15）** — オンボーディング再設計ほか16コミットが**未 push**（`c415de0` free専用ステップ → `115cfec` 5分割 → `7f6bb97` パーソナリティ独立 → `bebea12` カード全体クリッカブル → `34f8b2a` `1b995e6` 行を押すとその場で開く → `cbd13e5` `055b459` `e2e3001` 体裁と文言 → `6f6c04f` `2433ab8` 空状態 → `e8fa3e7` `7bb0ee5` アップセル面のボタン → `ceda558` 戻り方の吹き出し → `85b0603` 青のトークン化 → `f68d504` しくみ説明カード）。※ 通常どおり main 直接コミット運用。以下は前回の記録：| 作業ブランチ | **`main`（2026-08-14 その5）** — オンボーディング6コミット（`81f94ba` バナー／`a6f58ee` プラン外非表示／`aee0ac9` Free人数表記／`469eb96` セットアップ案内／`226ffdc` 準備完了通知／`56a69a2` 通知の会社別基準値）。push 済み（`9ed341d..56a69a2`）。※ 通常どおり main 直接コミット運用。以下は前回の記録：| 作業ブランチ | **`main`（2026-08-14 その4）** — 未 push 5コミット（`fbec112` プラン外ページのエラートースト／`2c70495` CIマニュアルのアップセル面／`5de1de9` Enterprise も依頼ボタン／`2e23334` ポータルのロックバッジ配色／`37cd848` 別セッションの機能トグル）。ワークツリーには別セッション作業中のファイルが8件あり、こちらのコミットには含めていない。以下は前回の記録：**`main`（2026-08-14 その3）** — 用語統一 `866abe2` ／ ロックバッジ配色＋Tailwind content 修正 `d4be02e` ／ プラン説明文 `778c00b`。このセッション開始時点で **main は origin より16コミット先行**しており（大半は並行セッションの作業）、今回まとめて push。※ 通常どおり main 直接コミット運用。以下は前回の記録：| 作業ブランチ | **`main`（2026-08-14 その2）** — プラン制限の Phase 1〜4 で12コミット。`7e6e0c9` Phase1 → `bd06d81` Phase1.5 → `b69f282` Phase2 → `e47f3ab` Phase3a → `9314c60` Phase3b → `a9b77a1` Phase3c → `f8f6c5d` Phase4、＋セキュリティ修正5本（`fef77de` `3827c65` `24165fe` `17ef232` `8cfd1da`）。※ 通常どおり main 直接コミット運用。以下は前回の記録：| 作業ブランチ | **`main`（2026-08-14 時点）** — 本セッションの4コミット（`772b999` 料金CTA / `6dd2e52` ポータル高速化 / `908287b` 管理画面高速化 / `c90fffa` 最終ログイン列）は **別セッションが push 済みで origin/main に入っている**。**未 push は 27 コミットで、すべて別セッションの料金ページ改訂（その10）**。ワークツリーは clean。以下は前回の記録：**`main`（2026-08-06 その10 時点。料金ページ改訂の 27 コミットが未 push）** — 途中で `feature/survey-report-alignment` から `main` に切り替わっている（PR #10 マージ後）。その10 の作業はすべて `main` に直接コミットしており、**push していないので他セッション・本番には出ていない**。ワークツリーは clean。以下は前回の記録：**`feature/survey-report-alignment`（PR #10 で main にマージ済み・`6f5ca0d`／その後さらに 2 コミット先行・未マージ）** — マージ後もこのブランチで作業を続けている。未マージ分＝その9（サーベイ選択の「（終了）」除去 `c7e255b`／スコアカードから各調査結果へのリンク `da4e98d`）。どちらも Vercel Preview green。以下は前回の記録：**（PR #9 で main にマージ済み・`521cbc5`）** — その8まで反映。⚠️ **PR作成時に CONFLICTING になった**＝#6〜#8 がスカッシュマージされたため同じ内容が別コミットとして main とブランチの両方に存在し、履歴だけがずれていた。main 固有のファイル・内容がゼロであることを確認したうえで `git merge -s ours origin/main` で履歴だけ繋いだ（**merge 前後の tree ハッシュが一致することを確認済み**＝ツリーは1バイトも変わっていない）。以下は前回の記録：**（PR #8 マージ後もそのまま作業継続中）** — その7（役職区分の仕上げ／page-cache／サイドバー表示）まで push 済み・Vercel Preview green。ワークツリーは clean。以下は前回の記録：**（main にマージ済み・PR #6 `cf97ff5`）** — 2026-08-06 の市場調査取り込み／アウター2本立て 36コミット。**GitHub の PR 経由でマージ**（別セッションの未コミット作業が20ファイル分あり、ローカルの checkout を避けたため）。⚠️ **マージ直前に Vercel のプレビューが落ちた**＝`page.tsx` で `SnapshotScheduleCard` に `recordSlot` を渡す変更だけをコミットし、受け取る側のコンポーネントを staging に入れ忘れていた。ローカルの `npm run build` は未コミットの作業ツリーを見ているため通ってしまい気づけない。`63e19d7` で修正しプレビュー green を確認してからマージ。以下は前回の記録：（`fac3803`） — 2026-08-05 その4のレポート整合／AI考察／ダッシュボード5段階化 21コミット＋ステータス更新。`feature/brand-penetration-journey`（浸透ジャーニー／評価軸5段階統一）から派生しており、その9コミット＋余白統一 `ca2b9b6` も**このマージで初めて main に入った**。**ワークツリーを切り替えず `git push origin HEAD:main` で FF マージ**（別セッションの未コミット作業が15ファイル分あったため checkout を避けた）。※通常は `main` 直接コミット運用。ブランチ作成・切替・マージは北川さんの明示指示まで行わない |
| 本番デプロイ | ⚠️ **未 push 12件＝本番未反映**。資料との整合（4区分→3ステップ＋横串／社名／Free の制限明記／アップセル帯／マイクロFB）はすべてローカルで tsc 0件・build 緑・実画面確認まで済んでいるが本番には出ていない。以下は前回の記録：⚠️ **未 push＝本番未反映**。レイヤー構成の反転修正・社名の修正・営業資料との表記統一はいずれもローカルで検証済みだが本番には出ていない。以下は前回の記録：⚠️ **未 push 6コミット＝本番未反映**。営業資料に合わせた表記統一（4区分・/signup 統一・用語wiki 280語・特商法の住所）は、ローカルで tsc 0件・build 緑・localhost:3004 の4ページ目視確認まで済んでいるが**まだ本番には出ていない**（push は明示指示待ち）。以下は前回の記録：⚠️ **2026-08-25 の4コミットは未 push＝本番未反映**。以下は前回の記録：✅ **middleware の 504 修正まで push 済み**（2026-08-21）。以下は前回の記録：✅ **`630f71b` まで push 済み・デプロイ green 確認済み**（ref 一致・`state=success`）。らしさカードの表示スイッチまで反映。以下は前回の記録：✅ **`b9a2527` まで push 済み・デプロイ green 確認済み**（ref 一致・`state=success`。tsc 0件／`npm run build` 緑を push 前に確認）。スーパー管理の整理一式とデザイントークンの統合まで反映。**push 前に消し残しを確認**＝`ds-accent`（`ds-app-accent` を除く）のコード参照は0件。以下は前回の記録：✅ **`a9a6ff0` まで push 済み・デプロイ green 確認済み**（ref 一致・`state=success`）。パンくずの戻り導線・表の折り返し防止・タイムラインの12px統一・ヘッダー検索・フィルタの横スクロールまで反映。以下は前回の記録：✅ **`c0e22b1` まで push 済み・デプロイ green**。カード内カード化・見出し追加・グラフ中央揃え・お知らせの未読フィルタまで反映。以下は前回の記録：✅ **`2e902cd` まで push 済み**。スコア推移の凡例修正・ポータル2カラム・余白統一まで反映。以下は前回の記録：✅ **`6d868cd` まで push 済み**。無認証で叩けた company-scoped API 25件の是正まで反映。以下は前回の記録：✅ **`63686c4` まで push 済み・デプロイ green**。計測の split 入れ替えと AI生成ルートのセキュリティ修正まで本番反映済み。以下は前回の記録：✅ **`ae1d4d7` まで push 済み・デプロイ green**。入力サポートのダイアログ／相談キュー／メール通知／スコア推移のプラン修正／お知らせのカード整理まで本番反映済み。以下は前回の記録：⚠️ **未 push 16コミット＝本番未反映**。ポータルの往復削減と、別セッションの入力サポートまわりはまだ本番に出ていない。以下は前回の記録：**本セッションの12コミットは push 済み**（セットアップ案内の一本化・タブ整理・タブ名変更・空状態の修正）。⚠️ **別セッションの13コミットは未 push＝本番未反映**。以下は前回の記録：⚠️ **未 push 23コミット＝本番未反映**（`origin/main` は23コミット遅れ）。しくみ説明カード（`PortalIntroCard`）と名刺プレビューのロックは、まだ本番では出ていない。**その前のオンボーディング再設計16コミットも同じ塊に含まれる**＝free 専用ステップ・カード全体クリッカブル・空状態・戻り方の吹き出しも本番未反映。`npm run build` 緑・TSエラー0件は確認済みなので、push すれば Vercel は通る見込み。以下は前回の記録：⚠️ **プラン変更リクエスト・メンバー上限・名刺閲覧数の修正は push 済みで本番反映**（`2a81fca` `ccb0f35` `a5829c6` を別セッションが push）。**未 push の5コミットは本番未反映**＝プラン外ページのエラートースト・CIマニュアルのアップセル面・Enterprise の依頼ボタン・ポータルのロックバッジ配色は、まだ本番では直っていない。以下は前回の記録：**プラン制限 Phase 1〜4（`f8f6c5d`）まで push・本番反映**。ブラウザ確認17項目は北川さんが実施済み。**DB 側の migration 6本は push より前に本番適用済み**（カラム追加とポリシー変更が先に入り、コードが後から追いつく順序＝プランを読むコードが無い間も既存挙動は不変だったため安全）。以下は前回の記録：| 本番デプロイ | **`8640944` まで green 確認済み**（Production deployment `5886153336` / `state=success` / 2026-08-13T10:20:53Z）。本セッションのブランドスコア高速化と最終ログイン列はこれに含まれる。⚠️ **料金ページ改訂の 27 コミットは依然 未 push＝本番未反映**。以下は前回の記録：⚠️ **その10（料金ページ改訂 27 コミット）は未 push＝本番未反映**。本番の料金ページは今も Card を含む4プラン（Free/Brand Card/Brand Standard/Brand Premium）のまま。検証は localhost:3004 の実画面で実施済み。以下は前回の記録：**済み・green 確認済み** — 2026-08-06 の PR #10 マージ（`6f5ca0d`・Production `state=success`）まで反映＝その6・その7（役職区分の未設定廃止／page-cache／サイドバー表示／説明文改訂）が本番に入った。**その9（`c7e255b`／`da4e98d`）は Preview green だが main 未マージ＝本番未反映**。以下は前回の記録：2026-08-06 その8（ポータル展開／管理者付与／CSV一括登録）まで反映（`521cbc5`・deployment id `5772408830`・state=success・ref 一致を確認）。以下は従来の記録：市場調査取り込みまで（`cf97ff5`・deployment id `5764435659`・state=success・ref 一致を確認）。以下は従来の記録：2026-08-05 その4まで反映（`fac3803`・deployment id `5758949011`・state=success・ref 一致を確認）。**浸透ジャーニー／評価軸5段階統一／カード間余白16px統一／レポート整合／AI考察がすべて本番に入った**（これまで未pushだった分を含む）。※AI考察は Anthropic API のクレジット残高に依存する。残高切れの間は生成されず、画面は考察なしで表示されエラーはトーストのみ。以下は従来の記録：2026-07-27 の用語wiki（`9d1a737`）まで反映。`https://branding.bz/wiki` を本番で疎通確認（index 230語／詳細／カテゴリ全て 200、sitemap 259URL、DefinedTerm+BreadcrumbList+Quotation の JSON-LD 発火、canonical 正）。以下は従来の記録：2026-07-21（その5）まで反映（Vercel 自動デプロイ・最新 `d31ce29`・deployment success 確認済み）。構築度スコアと到達判定の根 M/V/V 常時まで push 済み。**未コミットの構築度バッジのボタン外観・アイコン統一のみ未反映**。以下は従来の記録：（最新 `8f723b6`）。関係語彙の意味制約トリガと未接続→AI草案焦点フロー・孤立ブランドカラー列UI再撤去・admin用語統一まで push 済み。※前記の「fb5d027以降4コミット未push＋DB CHECK違反リスク」警告は、その3〜その4の間にすべて push 済み・整合復帰済みで既に解消 |
| 2026-08-31 リリース（その3・**未 push**） | **営業資料 v2 との食い違い3件を修正** `4d8e2ee`。①Free カードの注記に「PDF出力・本体への反映は Standard 以上」を明記（`pdfExport`/`portalSync` とも free:false）②アップセル帯の副題をカードと統一（Premium「浸透する」→「構築＋浸透＋発信」／Enterprise「計測＋伴走」→「すべてを、伴走つきで」）③features の発信グループに マイクロフィードバック を追加（実装済みだが一覧から漏れていた）。⚠️ 資料側に残る誤り2件（社名の割れ・p4 の Free 記述）は Cowork の担当 |
| 2026-08-31 リリース（その2・**未 push**） | **レイヤー構成を「構築 → 浸透 → 発信。計測が全部を貫く」に修正** `9236cc7`。同日の `103b575` で入れた逆の構造（計測が独立ステップ・発信が横串）を破棄。①LP の Hero副文・見出しを3語に戻し、柱は3枚のまま。3枚の下に横串の帯を新設（上端の線が3枚を貫く見た目）②features の計測2機能を独立させ、発信のあと・サポートの直前へ。lead は「構築・浸透・発信の3つを、横串で数字にする。」③ドキュメント3件を同内容に。overview には「なぜ計測が横串か」の実装上の根拠まで記載④「4レイヤー構造」の呼称は廃止 |
| 2026-08-31 リリース（**未 push**） | **営業資料を正としたサイト表記の統一** `103b575`。①レイヤー構成を3区分→4区分（構築 → 浸透 → 計測。発信が全部を貫く）＝LP の Hero副文・柱の見出し・柱を4枚に（グリッドは `sm:grid-cols-2 lg:grid-cols-4`。1280px で4列1段・900px で2列2段を実測）②features の計測2機能を浸透グループから独立させ、順を 構築→浸透→計測→発信→サポート に。meta description 2箇所も4区分に③ドキュメント3件（glossary／overview／CLAUDE.md）を同時更新。overview の「計測が横串」を「発信が横串」に改めた④「無料で始める」を /signup に統一（/tools/colors はログイン画面で行き止まりだった）⑤用語wiki 238→280語（本番DB実測）／特商法の所在地に番地を追加 |
| 2026-08-16 リリース（その14・**push済み・本番green**） | **「私たちの『らしさ』」カードの表示スイッチ** `630f71b`。①サイドバー見出しの右にスイッチ（`components/portal/rashisa-card-visibility.tsx` 新設・context で状態共有・Provider は `PortalLayoutClient`）②**既定は非表示**。表示を選んだときだけ localStorage（`portal-rashisa-card-shown`・端末ごと）③開閉は `grid-rows-[0fr]↔[1fr]` ＋ `margin-bottom` ＋ `opacity` を 300ms で補間（常時マウント。条件レンダリングだとスペースが一瞬で詰まる）④スイッチは `-translate-y-[0.5px]`＝和文の字面中心が箱の中心より 0.6px 上に来るぶんの補正（実測 0.6px→0.07px） |
| 2026-08-16 リリース（その12・**push済み・本番green**） | **戻り導線／表の折り返し／タイムラインの余白と検索**。①`1290923` `c3da1a1` `8fea784` パンくずの親から一覧へ戻れるように（スーパー管理は `section` を `{label, href}` へ、ポータルは `parent` を追加）②`c60c8ad` ページ内の「← 戻る」を削除（パンくずと二重）③`2261882` `c8cae9b` `89150bd` 一覧表の折り返しを止める（列名・バッジ＝nowrap／社名＝truncate＋title）④`f52e79c` `1183979` タイムラインのカード内側・カード間を12pxに、いいね/コメントは PC だけ1段階詰め（モバイルは44px維持）⑤`40e83ff` `98a97f5` 検索をヘッダーの虫眼鏡へ（クリックで1つの箱が伸びる／`PortalSearchProvider` ＋ `useRegisterPortalSearch` で画面側から登録）⑥`a9a6ff0` フィルタのバッジを横1列＋横スクロールに（**スクロールバーは非表示＝手がかり無しは保留**）⑦別セッション：スーパー管理の「企業」→「ブランド」`0cc228e` `544e6f4`、アイコンを `Sparkles` に統一 `268e9aa` `8c6e223`、企業詳細のタブ分け `ab8bb78`、承認をアコーディオンへ `dd2f1da`、孤立アカウント整理 `c48d02a` `94a696c`、サイドバー見出し `82540f2` |
| 2026-08-16 リリース（その11・**push済み・本番green**） | **ポータルのスコア表示の仕上げ＋お知らせの未読フィルタ**。①`ac3ae22`→`fd9876d` スコア表示を「カードの中にカード」に（外＝グレー地の器／中＝白カード2枚。らしさカードと同じ入れ子）②`ac0d363` 器に「ブランドスコア」の見出し（下 12px）＋グラフを縦中央（上下 18px）③`c0e22b1` 最新のお知らせを未読のみ3件に（取得は limit 20 に拡大。全件既読ならカードごと消える＝仕様）④別セッション：`bea7cb5` 表示設定アイコンを ToggleLeft に／`871c719` 新規登録の承認をダイアログ化 |
| 2026-08-16 リリース（その10・**push済み**） | **新 split の見え方を実画面で詰めた**。①`2577224` スコア推移の凡例から「総合」「アウター」を除去（`<Line>` を描画したままだと Recharts が凡例だけ出す）②`ad1af6c` ポータルのスコア表示を2カラム（左=インナースコア／右=スコア推移。`readOnly && !isFull` 限定）③`df859d0` 系列1本のときは説明文と凡例を出さない④`2e902cd` ブランドスコアの塊の下余白を mb-8→mb-4（他カードと統一）⑤`938a771` 入力サポートのバナーをカード全体クリックに（中の CTA は span 化。**目視未確認**）⑥`252e4b7` ポータルの Premium で総合スコアカードを出さない（basic では下のインナースコアカードと同じ数字が2回出ていた） |
| 2026-08-16 リリース（その8・**push済み・本番green**） | **計測のプラン split 入れ替え＋AI生成ルートの無認証穴を塞ぐ**。①`b4e477d` entitlements の split を入れ替え（brandScoreBasic→**brandScoreInner**〈premium〉／brandScoreFull→**brandScoreIntegrated**〈enterprise〉／`innerSurvey` を premium へ）②`c4b4979` BrandScoreView の variant を反転し、API ゲートを付け替え（inner-score / surveys/trend / knowledge-gap → Premium、market-surveys 系 → Enterprise）③`de64c4c` 料金ページ・features・FAQ・用語集を追随④`8af70bc` サイドバーとポータルの計測まわりを追随（ロック文言は `minimumPlanFor` から自動生成のため修正不要）⑤`63686c4` **generate-questions 2本の無認証穴を塞ぐ**（`requireCompanyMember()` 新設＋`ai_feature_usage` 記録） |
| 2026-08-16 リリース（その7・**push済み・本番green**） | **入力サポートをログイン内で完結＋依頼のメール通知＋スコア推移のプラン修正**。①`5ea9e75` バナーの導線を外部フォームからダイアログへ（見た目不変）②`2dc33be` `setup_support_requests` 新設（1社1件 pending・RLS 自社 select のみ）③`9c2056b` 完了判定を `lib/onboarding/status.ts` に切り出し、相談に**サーバ側で数え直した進捗**を控える④`97c79ea` スーパー管理 `/superadmin/support-requests` ＋未対応バッジ⑤`1d3aa79` 日時指定を Popover+Calendar の候補日方式（最大3件・「お任せ」併設）。**API・DB は不変**で `preferred_slots` に1行整形して渡す⑥`7452386` `51e83ba` 文面から「担当者」を外す（Free に専任担当は付かない）＋プラン変更ダイアログの体裁も統一⑦`cf998dd` 依頼が入ったらスーパー管理宛にメール（Resend／`CONTACT_NOTIFICATION_EMAIL` に相乗り・env 追加なし・実機で2通着信確認）⑧`8787f2d` **スコア推移カードの `isFull` 囲み漏れを修正**（Premium にアウター1本のグラフが出ていた）⑨`bba0542` `7520e20` `69a6e56` `ae1d4d7` ポータルのお知らせを1枚のカードに集約し、行を1行に畳んで見出しもカード内へ |
| 2026-08-25 リリース（**未 push**） | **市場調査スコアの2件＋PWA更新トースト**。`e3221de` 競合の自動抽出から受け皿行（その他・あてはまるものはない 等）を除外。判定は新設の `lib/brand-score/market-labels.ts` に集約し market-extras と共有／`9979885` trend API に `scored_stages` を追加し、`lib/brand-score/market-trend.ts` で共通段階だけの増減を算出。市場調査一覧に「前回比」＋構成差の注記、`BrandScoreView` の推移グラフにも注記／`5d9f1ce` 更新トーストを廃止し `skipWaiting:true` に。`PWAUpdatePrompt` → `PWARegister` に改名／`96c92e9` 改名時に消し忘れた旧ファイルを削除。 |
| 2026-08-21 リリース（**push済み**） | **middleware の 504 修正**。`lib/supabase/middleware.ts` の `getUser()` に4秒タイムアウト（custom fetch の `AbortSignal.timeout` ＋ `Promise.race`、敗者タイマーは `finally` で `clearTimeout`）を付け、失敗しても `supabaseResponse` を返して素通りさせる。`middleware.ts` の matcher を `/admin` `/superadmin` `/portal` `/mypage` `/api`（cron・card-view・analytics を除く）に限定。障害は 2026-08-21 15:09 JST・`hnd1::knrsh-1787292599032`。Auth 自体は正常で、edge → Supabase 間のハングと推定。 |
| 2026-08-16 リリース（その13・**push済み**） | **スーパー管理の整理**。`dd2f1da` 新規登録の承認を企業一覧の上のアコーディオンへ（サイドバー項目は削除・バッジは企業一覧へ・旧URLは転送）／`c48d02a` 残存アカウントの削除をスーパー管理へ作り直して移設（他社の profiles を消せた穴を塞ぐ）＋`94a696c` 7件を片付けてサイドバーから外す／`ab8bb78` 企業詳細をタブ化（基本情報／ブランドオントロジー）＋タブバー複製の解消／`82540f2` サイドバーに「サイト管理」の見出し／`3093978` メニューの「アカウント」→「メンバー」／`0cc228e` `544e6f4` `268e9aa` `8c6e223` 表示語を「企業」→「ブランド」に統一しアイコンも Sparkles に／`eb10d30` 承認カードの重複した「ドメイン:」表示を削除／`871c719` 却下確認を AlertDialog に／`6253c4c` アカウント管理の「有効」の折り返し修正／`ae3f803` `bea7cb5` 「設定」→「表示設定」＋ToggleLeft／`b9a2527` デザイントークンの重複1件を統合。 |
| 2026-08-15 リリース（その6・**未 push**） | **ポータルの初回表示から往復を1回減らす** `cc49393`。`app/portal/page.tsx` のデータ取得が Group 1（9本並列）→ `personal_goals` 1本 → Group 2（4本並列）の3段だった。真ん中の1本は Group 1 の `currentPeriodId` にしか依存しないので、クエリの組み立てだけ先に済ませて Group 2 に5本目として相乗りさせ、3段→2段にした。KPI の絞り込みは目標 ID が要るため Group 2 の後ろへ移動。**実測なし**（ブラウザのセッションが要るため再現できず）。 |
| 2026-08-15 リリース（その5・**push済み**） | **セットアップ案内の一本化とタブ整理**。①`98e5f2d` `ddb4e13` セットアップ案内を `OnboardingChecklist` に一本化し独立タブ `/admin/setup` へ（`surface` プロパティで portal/admin の差だけ切り替え）。②`d8be552` 未完了のあいだはサイドバーの「ダッシュボード」も進捗タブに着地。③`2cf1ca6` タブバーを `app/admin/components/DashboardTabs.tsx` に集約（4ページ5箇所の複製を解消・114行削除/11行追加）。④`8acdd85` `1c89901` `59f9b95` `c47e2d2` タブ名を ラーニング分析／スマート名刺分析／Good Action分析 に。3系統（タブ・パンくず・ページタイトル）を毎回揃えた。⑤`e393177` 「Good Actionタイムライン」→「Good Action投稿」12箇所。⑥`8a88a51` ブランドスコアの空状態の説明が名刺を主役にしていた誤りを修正＋「市場調査を取り込む」の入口を追加。⑦`91f4c47` 「名刺を配布する」の遷移先を QR のある `/admin/card-template` に。⑧`419ea74` オンボーディング案内文を「Standard プランから」に。 |
| 2026-08-14 リリース（**push済み・本番green**） | **ブランドスコア画面の高速化＋スーパー管理に最終ログイン列**（`main` 直接コミット・`8640944` に含まれる）。①**アウター/インナー集計APIの並列化** `908287b`＝outer-score の7回直列を2段に、inner-score の4回直列を2段に、`calculateMarketScore` の候補ごとのループを `.in()` の1回に。社員数と profile_id 一覧は count 付き1本へ統合。管理画面の「前回記録」はクライアント直読みをやめ記録一覧API（service_role）の末尾から取得（RLS の食い違いも解消）。**実測: outer-score 1.4〜1.6秒 → 0.10〜0.19秒／inner-score ID INC. 1.3〜2.0秒 → 0.21〜0.24秒・リィツ 0.45秒 → 0.29秒。値は不変（リィツ 総合62.0・市場79・n=220）**。②**ポータルの初回表示の段階描画** `6dd2e52`＝5本のfetchを全部投げたうえで inner+outer が返った時点で描画し、推移グラフはスケルトンで待つ。`fetchAllRows` を4ページ並列に（テスト `lib/brand-score/fetch-all-rows.test.ts` 追加）。③**スーパー管理の企業詳細に「最終ログイン」列** `c90fffa`＝新規 `GET /api/superadmin/company-logins/[id]`（service_role・認可は company-view と同方針）で `profiles → members → auth.users` をたどる。「アカウント未作成」「未ログイン」「日時」の3状態を出し分け。④**料金ページのCTAボタンの高さ揃え** `772b999`＝CSS subgrid（`grid-rows-subgrid`＋`row-span-3`）で4枚のカードの行を横断で揃えた。 |
| 2026-08-14 リリース（その4・**一部未 push**） | **プラン変更の依頼／メンバー上限／名刺閲覧数**。①**プラン変更リクエスト** `2a81fca`（push済み）＝新 API `POST/GET /api/plan-change-requests`（契約者・管理者のみ）と `GET/POST /api/superadmin/plan-change-requests`（superadmin）、ダイアログ `components/billing/PlanChangeRequestDialog.tsx`、ページ `/superadmin/plan-requests`、サイドバーの未対応件数バッジ。承認で `companies.plan` を書き換える。②**Enterprise も依頼ボタンに統一** `5de1de9`（未 push）＝未使用だった `contactInstead` prop と Enterprise の自動分岐を削除。③**メンバー上限の実効化** `ccb0f35`＋`a5829c6`（push済み）＝4経路に `checkMemberCapacity`。Free 1名→5名。④**名刺閲覧数の水増し修正**（push済み）＝重複排除を service_role で、本番トラフィックのみ記録、自社閲覧は数えない。⑤**プラン外ページの不具合** `fbec112` `2c70495` `2e23334`（未 push）。 |
| DB マイグレーション（2026-08-15 適用・入力サポート） | ✅ `create_setup_support_requests`(20260815120000) — **本番適用済み**。新規テーブル1本のみで既存テーブルには触れていないため既存挙動は不変。列＝`company_id`／`progress_done`・`progress_total`（依頼時点の進捗）／`preferred_slots`（候補日を1行に整形した文字列。text のまま）／`note`／`requested_by`・`requested_by_email`／`status`（pending/done/cancelled）／`handled_note`・`handled_at`・`handled_by`。1社1件の部分ユニーク索引（status=pending）＋(status, created_at desc) 索引。RLS は自社 select のみで、書き込みは service_role の API を通す。⚠️ 提案時の SQL から `scheduled`/`scheduled_at` を落とした＝日程確定を記録する UI が無いのに列だけ用意すると使われない状態が残るため |
| DB マイグレーション（2026-08-14 適用・プラン変更依頼） | ✅ `create_plan_change_requests`(20260814090000) — **本番適用済み**。新規テーブル1本のみで既存テーブルには触れていないため既存挙動は不変。列＝`company_id`／`current_plan`（依頼時点の実効プランを控える）／`requested_plan`／`note`／`requested_by`／`requested_by_email`／`status`(pending/approved/rejected)／`handled_note`／`handled_at`／`handled_by`。**pending は1社1件**を部分ユニーク索引 `plan_change_requests_one_pending_per_company` で担保（ダミー行で2件目が弾かれることを確認済み）。RLS は自社ぶんの select のみ許可し、書き込みは service_role の API を通す。 |
| DB マイグレーション（2026-08-14 適用・プラン制限） | **6本すべて本番適用済み**。①`add_company_plan`(20260814100000) — companies に plan / plan_started_at / plan_expires_at / is_demo を追加＋既存12社の割り振り＋approval_status の既定を pending に。check 制約は `('free','standard','premium','enterprise')` で **card は含めない**（販売終了。コード側の Plan 型にのみ温存し、復活は SELLABLE_PLANS に1語足すだけにしてある）②`fix_goal_tables_tenant_isolation`(20260814110000) — 🔒 goal_kpis / goal_periods が `USING true / WITH CHECK true` の1本だけで、**ログインさえしていれば他社の目標・KPI を読み書きできた**のを塞ぐ③`personal_goals_company_scope`(20260814120000) — 🔒 自分名義のまま他社の company_id を差し込めた／UPDATE に WITH CHECK が無く付け替えできたのを塞ぐ④`personal_goals_admin_delete`(20260814130000) — 管理画面の「メンバーの目標を削除」が 0 行削除で静かに失敗していたのを直す（子 goal_kpis は FK が既に ON DELETE CASCADE で、参照整合性アクションは子の RLS を迂回するため追加実装は不要と実データで確認）⑤`rls_plan_conditions`(20260814140000) — **プラン条件**。判定関数 `company_plan_allows(company_id, allowed_plans)` を新設し、期限切れは free に落としてから比較する。timeline 3表・announcements は standard 以上、goal_kpis / goal_periods は premium 以上。WITH CHECK だけに足した（USING に足すと解約後に自分の投稿を消せなくなる）。announcements_update は USING しか無く更新後が無検査だったので WITH CHECK も新設⑥`timeline_likes_delete`(20260814150000) — **いいね解除が DELETE ポリシー欠落でずっと動いていなかった**のを直す（0行削除がエラーにならないため気づけなかった） |
| DB マイグレーション（2026-08-06 適用） | ✅ `role_category_default_staff`(20260806150000) — **2026-08-06 に MCP の apply_migration で本番適用済み**。適用前に null だったのは22人（テックブリッジ6／ID INC.4／ナチュラルキッチン3／アーバンクラフト2／TAKUMI・うま屋・Kiitos・リィツ・cospark・CTD・MEGUTAMA 各1）で、全員 staff にバックフィル。適用後は staff 22／manager 2／executive 2、`column_default='staff'::text`・`is_nullable=NO` を確認。**この22人はポータルで目標・KPI／サーベイ結果／ブランドスコア／市場調査が見えなくなった**（既定設定の場合。admin_users にいる管理者はバイパスされるので影響なし）。もともと未設定だった人と元から従業員だった人の区別は付かなくなる（巻き戻しSQLは無い）。以下は適用前の記載： `role_category_default_staff`(20260806150000) — `profiles.role_category` の既存 null を staff にバックフィルし、`default 'staff'`＋`NOT NULL` を付ける。**まだ本番に当てていない**。北川さん側で Supabase Studio の SQL Editor から実行する。適用前の確認は `select role_category, count(*) from profiles group by role_category order by 1 nulls last;`、適用後の確認は `information_schema.columns` で `is_nullable=NO` / `column_default='staff'::text` を見る。**加算ではなく既存列への制約追加**なので、当てる前に対象行数を見ること。※コード側は null でも staff 扱いにフォールバックするため、未適用でも壊れない |
| DB マイグレーション（2026-08-05〜06 適用・市場調査） | **本番適用済み・すべて新規テーブル/加算のため既存挙動は不変**：`create_market_surveys`(20260805192005) — 5テーブル（`market_surveys`／`market_survey_blocks`／`market_survey_cells`／`market_survey_stage_mappings`／`market_survey_stage_scores`）＋RLS ／ `add_outer_market_to_snapshots`(20260805192100) — `brand_score_snapshots` に `outer_market`／`outer_digital`／`outer_market_stages`(jsonb)／`outer_market_survey_id` ／ `add_market_survey_impression`(20260805213000) — `market_surveys.impression_score`(numeric)＋`impression_detail`(jsonb) ／ `add_market_survey_insights`(20260806094500) — `market_surveys.insights`(jsonb)＋`insights_generated_at`(timestamptz) |
| DB マイグレーション（2026-08-04〜05 適用） | **本番適用済み・すべて加算のため既存挙動は不変**：`add_survey_insights`(20260805141726) — `brand_surveys.insights`(jsonb)＋`insights_generated_at`(timestamptz)。カード別のAI考察を保存し、明示的な再生成まで使い回す ／ `add_survey_import_fields`(20260804113753) — `brand_surveys.source`(text NOT NULL DEFAULT 'internal') ／ `respondent_count`(int)。外部調査の取り込みで `survey_participants` が無くても回答率の分子を持てるようにする ／ `add_learning_enabled`(20260805094500) — `companies.learning_enabled`(boolean NOT NULL DEFAULT true)。11社すべて true で適用 ／ `add_inner_stages_to_snapshots`(20260805103000) — `brand_score_snapshots.inner_stages`(jsonb)。浸透段階ごとのインナースコアを保存 ／ `add_respondent_id_to_survey_responses`(20260805103100) — `brand_survey_responses.respondent_id`(uuid)＋既存データ採番＋インデックス。個人単位の通過率算出に使う |
| DB マイグレーション（2026-07-27 適用） | `create_wiki_terms`(20260726163924) — 用語wiki 4テーブル（`wiki_terms`／`wiki_term_sources`／`wiki_term_quotes`／`wiki_term_relations`）＋RLS（published のみ public read）＋インデックス。**新規テーブルの追加のみ＝既存挙動は不変**。ローカル `.sql` は実 version にリネーム済 |
| 2026-08-06 リリース（その10・**未 push**） | **プラン構成の第2〜3弾改訂（料金ページ＋FAQ・公開ページのみ／entitlements は未着手）**。`main` に 27 コミット・**push していない**。①**Card 削除＋Enterprise 新設** `f7b7891`＝`PLANS` から card を落とし末尾に enterprise（個別見積・300名超・メンバー無制限・専任担当）を追加。`ctaStyle: 'secondary'` は Card 専用だったため削除しないと TS2367（到達不能な比較）でビルドが落ちる②**Enterprise の文言調整** `a64baca`／`2d39b8e`＝「個別見積」は金額と同じ 4xl だと字面が重いので `price` が `¥` 始まりかで判定して一段下げる（プランごとのフラグを増やさない）。description は他カードと字数を揃えて 53→48字③**プラン名の統一** `36536fc`＝`Brand Standard`／`Brand Premium` → `Standard`／`Premium`。Free・Enterprise だけ接頭辞なしという不揃いを解消し、アップセルパスの表記とも一致④**v3 の反映** `52f0edc`＝Free に「ブランド掲示 編集＋閲覧」を降ろし、計測グループを Premium から Enterprise へ移動、Premium の subtitle から「＋計測」を除去⑤**features の構造化**（`10fb441`〜`215f338` の10コミット）＝`― 構築 ― / ― 浸透 ― / ― 発信 ―` の区切りで各カードを整理。Free に構築ツール4本の内訳を出し、Standard は「Free全機能に加えて：」で始めて重複列挙を削除、表記を `AI構築ツール（各 月3回まで）` / `AI構築ツール（使用無制限）` と主語を揃えて括弧内だけでプラン差が読めるようにした⑥**タイムライン・お知らせ配信を Premium → Standard へ** `5bc3c65`（⚠️ 指示書 v1.2 の機能マップと不一致・要確定）⑦**Premium に「ブランドスコア（簡易版）」を追加** `d049dbc`（⚠️ 簡易版の範囲が未定義・実装は1つのみ）⑧**Enterprise のサポート欄を人の支援4項目に** `909a7b7`／`e2ce97d`＝クリエイティブサポート／四半期レビュー／市場調査手配／ブランド研修・ワークショップ。**未実装だった「SSO・監査ログ等（対応予定）」「マルチブランド管理（対応予定）」を削除した結果、公開ページから「（対応予定）」の但し書きが全て消えた**⑨**実装のない「部署別ヒートマップ」を削除** `87da540`（plan／faq×2／features／トップの4ファイル） |
| 2026-08-06 リリース（その9・**未マージ**） | **ポータルのスコアカードから各調査結果へ飛べるようにする＋サーベイ選択の表記整理**（`feature/survey-report-alignment`・**Vercel Preview green**・main より 2 コミット先行）①**スコアカードのリンクを props 化** `da4e98d`＝`BrandScoreView` の「サーベイ管理」「市場調査」リンクは `!readOnly` でハードコードされており、ポータルでは何も出せなかった。`innerLink` / `outerLink`（`{href,label}` または `null`）を追加し、**未指定なら従来の管理画面リンク＋`readOnly` で非表示**にフォールバック（管理画面の呼び出し側は無変更）。ポータルは `BrandScorePortalSection` に `surveyHref` / `marketSurveyHref` を渡す形にし、`app/portal/page.tsx` が `isPortalPageVisibleForRole` で `survey` / `market_survey` を判定して**見られる場合だけ href を渡す**（見られない区分にはリンク自体が出ない）。表示位置は管理画面と同じ＝インナーは回答率の下、アウターはサンプル数の下②**サーベイ選択から「（終了）」を外す** `c7e255b`＝読み手には実施中/終了の別が操作に影響しないのでタイトルだけを出す。未使用になった `STATUS_LABELS` も削除（一覧の絞り込み条件 `active`/`closed` は不変） |
| 2026-08-06 リリース（その8・**main反映済み**） | **ブランドスコア/市場調査のポータル展開＋管理者の付与＋CSV一括登録**（PR #9 `521cbc5`・**本番デプロイ green 確認済み**）①**コンポーネント化**＝`components/brand-score/BrandScoreView.tsx`（715行）と `MarketSurveyResults.tsx`（619行）を新設。`SurveyResults.tsx` と同じ読み取り専用・自己完結の作り。`readOnly` で管理画面へのリンクとボタンを出し分ける。**派生値もコンポーネント側が持つ**②**ポータル**＝`/portal` にブランドスコア（`BrandScorePortalSection` が取得を担当・期間は1年固定・印象一致度は出さない）、`/portal/market-survey` を新設（`/portal/survey` と同じ作り）。サイドバー「浸透」に市場調査を追加、パンくずとタイトルを登録（`/portal/survey` のパンくずも未登録だったので同時に追加）③**管理者**＝`GET/POST/DELETE /api/admin/members/admin-role`。アカウント管理に「管理者」列。⚠️ **`admin_users` は RLS で自分の行しか読めない**ため、一覧は必ずサーバー経由（service_role）で取ること。最初クライアントから select して全員 OFF に見えていた④**CSV一括登録**＝`POST /api/members/bulk-create`＋`MemberCsvImportDialog`＋純関数 `lib/members/parse-member-csv.ts`（テスト `parse-member-csv.test.ts`）。見出しのゆらぎを吸収し、氏名/メール/パスワードの空・形式・6文字未満・区分の誤り・ファイル内重複を行ごとに弾く。`.csv` と `.xlsx` に対応⑤**市場調査の status 運用を廃止**＝`calculateMarketScore` が status で絞らず「実施日が最新で3段階以上」を選ぶ。取り込み時の status を draft→active に。一覧のバッジは保存値ではなく状態から導出⑥**スコア推移**＝`GET /api/brand-score/surveys/trend`／`market-surveys/trend` を新設し、調査日の点を記録日と同じ系列に入れる。`computeSurveyScores` を切り出してスナップショットと推移APIで同じ式を使う。`DELETE /api/brand-score/snapshots` で記録日を削除可能に |
| 2026-08-06 リリース（**main反映済み**） | **外部市場調査の取り込み＋アウタースコアの2本立て**（PR #6 `cf97ff5`・**本番デプロイ green 確認済み**）①**アウターの2本立て**＝算出式を `lib/brand-score/outer-metrics.ts` に一本化（API とスナップショットに完全複製されていた）。`OUTER_TRACK_WEIGHTS = { market: 0.75, digital: 0.25 }`。**未計測は0点にしない**＝スマート名刺がオフの会社（名刺ページ自体が非公開でログが溜まらない）と名刺PVが `MIN_CARD_VIEWS_FOR_DIGITAL=30` 未満の会社はデジタル接点を null にして分母から外す。リィツは名刺オフのためアウター＝市場浸透79.0（従来は50.0）②**GT集計表の取り込み**＝`lib/brand-score/import-gt-table.ts`。`PTable####` ブロック・`〜・計` の NET 列・行ごとの base N を読む。構造的な総数の破綻以外は警告にして取り込みを止めない（severity=error だけが commit を止める）。数値回答ブロックは型行をラベル行に使うため専用分岐＋`isPercentCell` で実数行/列の範囲チェックを飛ばす。実ファイル41ブロック1,608セルを error 0 / warn 0③**自動割り当て**＝`market-auto-map.ts`（列見出しの慣習 `認知・計`／`導入・購入経験あり・計`／`ロイヤリティあり・計`／`推奨意向あり・計` と設問文の `【第1想起】` で決定論的に候補を出す）。取り込み中に実行して詳細画面へ着地④**母数下限 `MIN_BENCHMARK_BASE_N=50`**＝「各企業認知者ベース」の設問は認知率が低い会社ほど母数が小さい。n=27 の 81.5%（22人）を n=182 の 78.0% と同列に並べていたためロイヤリティが「12社中2位」と出ていた（レポートは1位）。除外して**調査会社のレポートと5段階の値・競合順位が一致**⑤**5段階以外の読みどころ**＝`market-extras.ts`。市場の期待と自社イメージ（Q5×Q6・選択肢が同一なので順位で突き合わせ・母数が違うため%の引き算はさせない）／ブランドパーソナリティ（SD法の `Aに近い・計`）／認知経路／事業浸透度／サービス評価。**設問コードでは判定しない**（調査会社・年度で番号が変わる）⑥**カード別AI考察**＝`market_surveys.insights`。プロンプトに「想起は純粋想起なので%が構造的に低い」「重視点とイメージは分母が違い順位でしか比べられない」を明記⑦**市場浸透の年次推移**＝`GET /api/brand-score/market-surveys/trend`。スナップショットには転記しない（総合＝インナー×50%＋アウター×50% の合成値なので、市場調査だけ実施日に差し込むと意味が壊れる）。調査の実施日と段階スコアから直接組み立てるので、割り当てを直せば推移も自動で追随⑧**UI**＝アウターの系列色を緑に統一（スコア推移グラフの凡例と対応）／凡例とリンクの**文字は黒**（識別は色印が担う）／ダッシュボードの入口を `/admin/brand-score` に統一（`/admin` と パンくずが `/admin/dashboard`＝タイムライン分析に着いていた）／市場調査カードの日付を日まで出す（月までだと「2026/06 〜 2026/06」になり期間が読めなかった）。**純関数のテスト**＝`import-gt-table`／`market-stage-score`／`market-auto-map`／`market-extras`／`outer-metrics` |
| 2026-08-05 リリース（その2・**未マージ**） | **インナーサーベイの評価軸を5段階に統一＋ブランド浸透ジャーニー**（`feature/brand-penetration-journey`・`3bf9c18`〜`e684629` の9コミット・**未push**）。WHY/HOW/WHAT から **認知→理解→共感→行動（反転点）→推奨** へ評価軸を移行。同じ設問を「何が弱いか（構成要素）」と「どこで止まっているか（浸透段階）」の2軸で見る設計①**`lib/brand-score/funnel-stages.ts` 新設**＝段階マップ（32問構成／30問構成）・`resolveStage`（`reference_data.stage` 上書き → sort_order 対応表 → null）・`calcFunnel`・`calcGroupFunnels`・パターン判定4種・`PASS_THRESHOLD=3.5`。**対応表は定点観測の基準なので変更禁止**（変えると前年比が無意味になる）。単体テスト `funnel-stages.test.ts` で正規化式・重み付け・判定順・5段階が揃わない場合の null を網羅②**総合スコアの算出式を変更**＝`calculate-snapshot.ts` を WHY35%/HOW30%/WHAT35% の加重平均から**全32問の単純平均**へ。移行時点では両者とも 62.0 で一致したが**偶然の一致**であり、過去スナップショットは旧式のまま＝推移グラフは基準が混在する③**段階スコアをスナップショットに保存**＝`inner_stages` jsonb に `{"awareness":59.3,...,"environment":62.0}`。手動API・cron 両方の INSERT に列を追加④**画面**＝総合＋5段階のスコアカード（5段階は1枚のカードに集約・行動は反転点として淡インディゴ＋リング）／**段階別の詳細**（段階ごとに全社バー＋本社=オレンジ・現場=緑の丸印、最下位設問、転換率と「最大の漏れ」バッジ、共感⇄行動間に反転点の破線）／**段階の通過率**（個人単位。各段階平均3.5点以上を通過とし、上流をすべて通過した人だけを数える累積。人数は実カウントで丸め計算しない）／設問別スコアは既定を浸透段階にしタブを 浸透段階/構成要素 の順に。**WHY/HOW/WHAT のマトリクス・役職別スコア・部署別スコアは軸統一と矛盾するため削除**（部門比較は段階別詳細と通過率に統合済み）⑤**検算**＝survey `3f434cb6`(n=392) で段階スコア5つ・転換率4つ・累積通過率15マス・部門別スコア・回答者数(SP 243/BO本社 149) がすべて Supabase 実測値と一致 |
| 2026-08-05 リリース（その4・**未マージ**） | **分析レポートとの軸統一＋カード別AI考察＋記録設定の再構成**（`feature/survey-report-alignment`・`00fa799`〜`baee859` の21コミット・**未push**）①**軸の一本化**＝クライアントの分析レポート（PPTX 15枚）は30問を「4領域（理念浸透/ブランド理解/行動体現/文化・定着）」で切り、画面は5段階で切っていたため、同じデータで最下位が食い違っていた（レポート＝行動体現3.37／画面＝理解57.1）。原因は Q24「自部門の貢献を説明できる」が設問番号の並びでは行動体現の塊に入るが意味は「説明できるか」＝理解であること。**領域軸を画面から削除**し、設問別スコアの軸を「浸透段階（既定）／設問タイプ WHY・HOW・WHAT」の2つに整理。**ダッシュボードのインナースコアも WHY/HOW/WHAT から5段階に変更**（段階が解決できないサーベイのみ従来表示にフォールバック）②**カード別AI考察**＝`app/api/brand-score/surveys/[id]/insights/route.ts` を新設。inner-score の集計結果を要約して渡し「与えられた数値だけを根拠にする」と縛って4カード分の考察を生成、`brand_surveys.insights` に保存。**未生成のときだけ初回表示で1回**走らせ、以降は保存分を表示（毎回APIを叩かない）。作り直しは「AI考察を再生成」ボタン。生成された数値（10.9pt差・19.4pt差・149人中32人など）を全て画面の実測値と突き合わせ**捏造ゼロを確認**③**記録設定の再構成**＝有効/無効トグルを「自動／任意」の選択に変え、自動を選んだときだけ頻度・起点日をモーダルで設定。内部的には `enabled` の true/false に対応するのでスキーマ・Cron は不変④**表記と体裁**＝本社/現場→**BO（本社含む）/SP** に統一・**SPを先に**並べる（母数が大きい側）／最弱段階はオレンジ・最強段階は青（棒は系列色のまま）／サーベイのタイトルをどの状態でも編集可（blur/Enterで即PATCH）／**パンくずの親をリンク化**して戻る導線に（詳細→一覧、ダッシュボード系4タブ→ダッシュボード）／ブランドスコアの操作行を左寄せ・期間を先頭に／**部署別スコア表を削除**（サーベイ詳細のSP/BO別表示に統合済み）⑤**Cowork向けレポート修正指示**を Drive `c118_RTM/RTM_ブランド戦略会議/260805_インナーブランディング調査レポート_修正指示_v1.md` に作成（単位を0〜100へ・4領域→5段階・30問→32問・通過率スライド新設・ドライバー分析とDO NOTは残す） |
| 2026-08-06 リリース（その7・**main反映済み**・PR #9/#10） | **役職区分の仕上げ＋ポータルのスコア読み込み改善**（`feature/survey-report-alignment`・**Vercel Preview green**・main より 23 コミット先行）①**区分の「未設定」を廃止** `05f2291`＝migration `20260806150000_role_category_default_staff.sql`（既存 null → staff バックフィル、`default 'staff'`＋`NOT NULL`）を追加。**本番未適用**。UI 側は一覧セレクト・編集フォームから「未設定」を削除し、初期値を staff に。`memberRoleLabel(null)`→「従業員」／`isStaffRole(null)`→true／`isPortalPageVisibleForRole` は区分未指定を staff として判定するよう変更（従来は「区分未設定なら常に表示」だった）②**ポータルのブランドスコアを page-cache 化** `71e6938`＝`portal-brand-score-{companyId}` に inner/outer/snapshots/2種の推移/前回比をまとめて保存し、再訪時はキャッシュを `useState` の初期値に流し込んで**スケルトンなしで即描画**、裏で5本を再取得して差し替える（stale-while-revalidate）③**サイドバー会社ヘッダーの表示** `769a9f8`／`473cc59`／`b0bb6b1`／`5fd4290`＝区分ラベル追加→スローガン除去→**従業員はスローガン・経営層/管理職は区分ラベル**に整理。判定は `isStaffRole` を使い、migration 未適用で `role_category` が null の行でもスローガン側に入るようにした④**設定の説明文改訂** `4d8aa76`／`6754bc4`＝「区分・未設定のメンバーは常に表示」を削除し、既定が「従業員」であることを明記して1文に統合⑤**メンバー一覧の名前セルを `whitespace-nowrap`** `e2c5cd7`（「山田尊明」が「山田尊／明」と折り返していた）。**管理者バイパスは現状維持**（`isAdmin` は区分に関わらず全ページ表示）＝区分の効きを実機確認するには非 admin の staff アカウントが要る |
| 2026-08-06 リリース（その6・**main反映済み**・PR #9/#10） | **ダッシュボード周辺の微整**（`feature/survey-report-alignment`・**Vercel Preview green・main 未マージ**）①**ポータルトップのスローガンと挨拶の装飾・上下入れ替え** `62e9b3b`＝挨拶（ようこそ、○○ さん）を大見出しに、スローガンを小さな副題として下段に置く。DOM 順を挨拶→スローガンに、className だけを swap ではなく順序も入れ替え（h1 タグは温存）②**タイムライン分析の指標カードを1行に収める** `beb7cef`（別セッション）。**本セッションの staging は `git add -p` で私の hunk のみ選別**し、`app/portal/page.tsx` に混在していた KPI 区分ゲート差分（他セッションの WIP）は working tree に温存 |
| 2026-08-05〜06 リリース（その5・**未マージ**） | **メンバー区分＋区分ごとの表示設定＋サーベイ結果のポータル表示＋ダッシュボード既定タブ＋記録ボタン移動**（`feature/survey-report-alignment`・**`02e49ee` として commit・push 済み・Vercel Preview green 確認**・main 未マージ）①**メンバー区分**＝`profiles.role_category`(executive/manager/staff・migration `20260805150000`・**本番適用済み**)。アカウント管理の一覧インライン選択＋編集フォームで設定。サーベイの role_category と同語彙②**区分ごとの表示設定**＝`companies.portal_role_visibility`(jsonb・migration `20260805153000`・**本番適用済み**)。管理画面「設定」に「区分×ページ」のトグル表を新設し会社ごとに保存。判定は `lib/constants/member-roles.ts` の `isPortalPageVisibleForRole` に集約（管理者・区分未設定は常に表示／機能トグルオフは区分に関わらず非表示）。既定は目標・KPIとサーベイ結果を従業員に非表示。反映先＝ポータルのサイドバー＋各ページ(kpi/timeline/learning/survey)＋ダッシュボードのKPIカード③**サーベイ結果のポータル表示**＝管理画面の結果ビューを `components/brand-score/SurveyResults.tsx`（data/questions/insights を props に取る自己完結・読み取り専用）へ切り出し、`/portal/survey`（一覧から選択→フル結果）で表示。区分で出し分け。**管理画面詳細ページの重複解消（同コンポーネントへの差し替え）は未実施**④**ダッシュボード既定タブ**＝サイドバー「ダッシュボード」の遷移先を `/admin/dashboard`(タイムライン分析)→`/admin/brand-score`(ブランドスコア)に変更⑤**記録ボタン移動**＝ブランドスコアの「スコアを記録」を上部ツールバーから「記録設定」カード内へ（`SnapshotScheduleCard` に `recordSlot` prop 追加）。TSエラー0件・実データで動作確認済み。※`app/portal/page.tsx` は北川さんのスローガンWIPと私のKPI出し分けが混在（コミット時要切り分け） |
| 2026-08-05 リリース（その3・**未マージ**） | **管理画面全ページのカード間余白を16pxに統一** `ca2b9b6`（`feature/brand-penetration-journey`・**未push**）。ページごとに `mb-6`(24px)/`mb-3`(12px)/`space-y-6`/grid `gap-3`(12px) とバラついていたカード**間**の余白を、縦・横すべて16px（Tailwind の `4`）へ統一。①カード自身の下余白 `mb-6`/`mb-3`→`mb-4`②カードを積むコンテナ `<div>`/`<form>` の `space-y-6`/`space-y-8`→`space-y-4`（brand系5ページ＋company 計11）③カードのグリッド `gap-3`→`gap-4`＋コンテナの `mb-3`/`mb-6`→`mb-4`（dashboard/analytics/brand-score 計11）。**カード内部の余白（`CardContent` 内 space-y 等）は不変**。admin配下17ファイル。TSエラー0件・grepで非16px残ゼロ・ダッシュボードを実ブラウザで実測（グリッド横16px・カード縦16px・崩れなし）。※`app/portal/page.tsx` の未コミット変更（スローガン入れ替え）は別作業のため本コミットに含めていない |
| 2026-08-04〜05 リリース（その1・**main反映済み**） | **Googleフォーム回答の取り込み＋ラーニング機能のオンオフ＋パスワードリセット導線**（`4bc7d8f`／`7d3eb43`・**本番デプロイ確認済み**）①**Googleフォーム取り込み**＝`/admin/brand-score/surveys` に「回答を取り込む」を新設。Excel/CSV をドラッグ＆ドロップ（複数可）。職種別にフォームを分けた場合は**複数ファイルを1サーベイにマージ**し、ファイルごとに部署ラベルを付けて部署別比較できる。設問は文言一致で同一視し、片方にしかない設問は追加設問として登録（**設問文は書き換えない**）。回答ラベル→1〜5は決定論的な接頭辞マッチで**AIを使わない**。変換できないラベルが1つでもあれば取り込みを拒否（黙って欠落させない）。失敗時はサーベイごと削除して巻き戻す。取り込みぶん（`source='imported'`）は closed でも削除可能②**1000行上限バグの修正**（既存バグ）＝PostgREST の既定上限により、回答が1000行を超えるサーベイで集計が先頭1000行だけの値になっていた。エラーも警告も出ないため気づけない状態（実例: 243名×30問=7,290行で約1.1pt のズレ）。`lib/brand-score/fetch-all-rows.ts` を新設しページングで全件取得（inner-score／calculate-snapshot／knowledge-gap の3箇所）。**毎日3時のスナップショット cron も影響を受けていた**③**ラーニングのオンオフ**＝`learning_enabled` を feature-toggles に追加。設定画面のトグルでポータル・管理画面の該当メニューとページを非表示に（データは保持）④**パスワードリセット導線**＝`/forgot-password`・`/reset-password` を新設。Site URL が LP（Supabase クライアントを読まない）だったためリンクを踏んでもトップが開くだけだった問題を解消。`resetPasswordForEmail` に `redirectTo` を明示し Site URL に依存しない。メールアドレスの存在有無は画面に出さない（総当たり対策）⑤**その他**＝4ファイルに重複していたダッシュボードのタブ定義を `lib/constants/dashboard-tabs.ts` に集約／役職 `staff` の表示を全画面「従業員」に統一／ダッシュボード系4ページのパンくずを「ダッシュボード › タブ名」形式に統一 |
| 2026-07-27 リリース | **ブランディング用語wiki（/wiki）新設・230語を全公開** `9a5938d`＋`9d1a737`（**本番デプロイ済み**）。`app/(site)/wiki/` に index／詳細／カテゴリ別の3ルート、`components/wiki/` に TermCard・TermDetail・QuoteBlock・RelatedChips・SourceList、`lib/wiki/queries.ts` に anon キーの公開データ取得、`lib/types/wiki.ts` にカテゴリ7件と型、`scripts/seed-wiki.ts` にシード。**段階公開の予定を変更し、北川判断で230語を一括 `published` に**（基礎・核心23語でレイアウト検証 → 問題なしを確認のうえ残207語を UPDATE）。カテゴリ別内訳＝基礎・核心23／構造・管理28／命名・言語18／視覚・デザイン25／顧客・市場26／戦略・マーケ連携54／特化・応用56。**AI補完176語は未監修のまま公開中**＝Cowork 側の `AI補完監修チェックリスト.xlsx` を並行で進め、修正が必要な用語だけ後日 terms.json 更新＋個別 UPDATE で直す運用 |
| DB マイグレーション（2026-07-21 適用） | `metric_definitions`(20260721005131) — 会社ごとの指標辞書（metric_key/display_name/canonical_unit・UNIQUE(company_id,metric_key)・RLS superadmin_all＋select）。**加算のみで既存挙動は不変** |
| DB マイグレーション（2026-07-18〜19 適用） | **本番適用済み・全て加算で既定値のため既存挙動は不変**：`desired_evidence`(20260718133238) ／ `proof_point_measurements`(141811) ／ `desired_evidence_evaluations`＋失効スナップショットトリガ(141841) ／ `value_propositions.lifecycle_state`(141907) ／ `element_relations` relation_type +4種(141920) ／ `desired_evidence_rule_hashes` RPC(20260719025247)。ローカル `.sql` は実 version にリネーム済 |
| DB マイグレーション（2026-07-16 適用） | **DDLなし**（アプリ層のみ）。※2026-06-30 適用分（既記載）: `blocked_competitor_domains` / `companies.approval_status` / `companies.competitor_flag`。※2026-07-14 その5分: `companies.representative_profile TEXT` も引き続き有効 |
| 2026-07-21 リリース（その4） | **関係語彙の意味制約＋未接続→AI草案焦点フロー＋孤立ブランドカラー列UI再撤去＋admin用語統一**①**関係語彙の整理と意味制約DBトリガ** `1b87b5a`（別セッション）＝`element_relations` の relation_type に意味制約 DB トリガを追加、`communicatedAs` を廃止し未来設計4種の作成経路を整理、裏づけは `evidencedBy` 辺に一本化、出所を構造化②**未接続クリック→AI草案の焦点表示** `cf64422`（別セッション）＝「未接続 N件」チップから要素をクリックするとブランドマップ内で該当要素をフォーカスし、繋ぎ先のAI草案を承認/手動入力へ導くフローを実装③**孤立ブランドカラー列UI再撤去** `8f723b6`（本セッション）＝`companies.brand_color_primary/secondary` は 2026-04-06 に「AI・表示側の参照を brand_visuals へ移行」の際にスーパー管理から一度撤去されたが復活していた孤立列。実データで smart card の参照経路を追跡（`/card/[slug]/page.tsx:187-193` は `visuals?.color_palette.accent_colors[0].hex` を読み、ID INC. は `#06B6D4` で `companies.brand_color_secondary=#0055ff` は未参照と確認）してから UI のみ再撤去。DB列と唯一の残存参照 CIマニュアルPDF (`lib/ci-manual/data-fetcher.ts`) は温存④**admin ユーザーメニュー用語統一** 同 `8f723b6`＝サイドバー内「サービス画面」→「ポータル画面」に変更。superadmin/design-system の「サービス画面」語はデザイントークンスコープ用語（LP系 vs shadcn系）で別意味のため触らず |
| 2026-07-21 リリース（その2） | **オントロジーウィザードUIの徹底統一＋言葉のルールUI整合＋診断ツール表現ルールライブ表示＋個別 fix**①**ステッパー/ステップUI 統一**（`6529166`/`3bbb120`/`16febed`/`e6e64d0`/`1ca05a5`/`2b1210c`/`552c7fb`/`05e06fc`）＝ステッパー線の先頭丸からのはみ出し修正／「AI草案を生成」をステップ見出し行の右端へ移動／全ステップのAIアクションを共通 `AIButton(S)` に統一／ステップ見出しをツール側と同じ体裁に統一＋「Step N:」表記＋ステップパネル囲い解除／「編集する」展開部パディング16px統一／ステッパー上下余白 16→24px②**「編集する」開閉アニメ**（`c71e459`）＝grid-template-rows 0fr↔1fr で高さアニメーション③**言葉のルールUI整合＋AI草案 fix**（`52405ed`/`1b8bd2f`/`1d58798`/`4f3af9b`/`2c2b475`）＝ポータル表示に合わせて NG/OK 2カラムボックス化＋sourceバッジ追加／NG/OK例をカード全幅に拡張／scope 列は UI 撤去（DB列・機構は温存）／AI草案の NG/OK例も登録済み一覧と同じ2カラムボックスに／AI草案の承認登録が失敗する不具合を修正（source を manual に戻す）＋docs更新 (`4303098`)＝表現ルール追加生成（診断ベース）を設計済みバックログとして記録④**パーソナリティ診断ツールに表現ルールライブ表示**（`add3089`/`2dc2522`）＝診断ツールに「登録済みの表現ルール」をライブ表示／表現ルールを1セクションに統合（診断由来の二重表示を解消）⑤**個別 fix**（本セッション: `33174c5`/`0430c1e`/`2eb64a7`）＝理解度テスト管理の◯×問題で選択肢IDの `'true'/'false'` が `uppercase` 描画で幅からはみ出し「TRUE しい」等の文字化けを起こしていた問題を修正（`single_choice` 時のみ id バッジ表示）／2026-07-15 に `/admin/members` だけダーク化したものを他 admin ページとの一貫性のため revert（`fe9e4ac` の revert）／superadmin ブランドマップ埋め込み表示の背景を `#f8fafc` → 白 `#ffffff` に統一（フルスクリーンのダークは維持） |
| 2026-07-16 リリース | **SEO土台補修＋3ドメインシナジー実装の連投**①**4ツールの無料枠を「月に3回・毎月1日リセット(JST)」に統一** `c719519`＝新規 `lib/tools/free-limits.ts` に `MONTHLY_FREE_LIMIT=3` / `getCurrentMonthStartUtcIso(now?)` / 共通エラー文言 `MONTHLY_LIMIT_REACHED_MESSAGE`（「翌月1日 0:00（日本時間）にリセットされます」）/ LPバッジ文言 `FREE_TIER_BADGE_LABEL`（「無料で月に3回まで利用可能」）を集約。4つの sessions/route.ts（stp/persona/personality/colors）に `.gte('updated_at', 月初JST)` フィルタを追加、colors は上限チェックが未実装だったため新規に追加（`in_progress` チェック後、新規作成前に月次カウント→403）。4LP の CTA バッジを共通定数参照に統一。JST 月初境界テスト4ケース通過。実DB影響評価: 切替により新規ブロックされる既存ユーザーは実質ゼロ②**4ツールLPに可視FAQ＋構造化データ追加** `1d44ed2`＝各LPの CTA 直前に `<details>` アコーディオンで4問Q&A、`FAQPage` + `BreadcrumbList` JSON-LD を出力（既存 `SoftwareApplication` は維持）。**表示Q&Aと FAQPage schema は「配列単一ソース(`FAQ_ITEMS`)」で構造的に完全一致担保**（Node 検証で4LP×4問=16ペアの1文字一致を実測確認）。BreadcrumbList は「ホーム→各ツール名」の2階層で schema のみ（画面表示なし）。Q&A 内容は全て LP表記・コードで裏取り済（無料回数=`FREE_TIER_BADGE_LABEL`準拠、colors の WCAG/CSS出力/自然言語調整は HIGHLIGHTS+コード確認、時刻具体化は安全側で回避、aggregateRating は実レビュー無しのため不採用）③**API route の lazy init 化で Preview ビルド復活** `57c93a2`＝`app/api/members/create/route.ts` と `app/api/members/register/route.ts` がモジュールトップレベルで `createClient(URL, SERVICE_ROLE_KEY!)` を呼んでいたため、`SUPABASE_SERVICE_ROLE_KEY` を Preview に置かない方針下で Preview ビルドが `Collecting page data` フェーズで落ちていた。既存の `getSupabaseAdmin()` を handler 内で呼ぶ lazy init に変換（ロジック・レスポンス一切不変）。他 createClient を使う route 24本は helper 関数内呼び出しで既に安全（触っていない）。**実DB で会員作成・登録が 200+ 正常INSERTすることを確認**（テックブリッジ企業借用でテストデータ作成 → 検証 → 直後に auth.users/profiles/members/invite_links を削除して痕跡ゼロ）④**3ドメインシナジー用 @graph JSON-LD 実装** `d6ead6f`＝`app/layout.tsx` の `<head>` に Organization/SoftwareApplication/WebSite の3ノードを追加。Organization は `@id=https://branding.bz/#organization`、`parentOrganization`/`publisher` で `https://include.bz/#organization` を親参照、`logo=https://branding.bz/logo.svg`、`sameAs` は `[include.bz, branding.bz, designnow.design, facebook.com/include.bz, instagram.com/include.bz/, twitter.com/include_bz]` の6項目で include.bz/designnow.design と同一配列（名寄せ担保）。SoftwareApplication は有料プラン混在のため `offers` を意図的に省略（有料SaaS本体を 0円と偽らない方針）、description に「無料ツールも公開」を明記。WebSite は `inLanguage=ja`。**Footer の会社情報カラムに `https://designnow.design/` (DESIGN NOW) リンクを追加** し include.bz ↔ branding.bz ↔ designnow.design の可視相互リンクが3辺揃った。全13公開ページで3ノード発火を実測確認⑤**SEO 引き継ぎ資料 v2 に午後作業を追記** `2a0d5ed`/`e8c4370`＝§11（GA4 発火確認・4LP FAQ 実装・**★重要**: Google が2023-08以降 FAQ リッチリザルトを政府/医療限定にしたため一般企業の FAQPage schema はリッチリザルト表示されない知見・被リンク設計確定版：include.bz Tips 5記事から各ツールLPへ1リンク差す表）＋§12（route lazy init 完了、`lib/supabase.ts` module-top-level `createBrowserClient` の潜在リスク：現状は env が入っているので実害ゼロだが env オペミス時に再発する構造） |
| 2026-07-15 リリース（その2） | **公開LP＋認証周辺の大規模統一**①**フッターCTA強化型へ刷新** `4a87dec`＝ポータルCTA・SNS・会社リンクを1コンポーネント化し `components/Footer.tsx` に集約（`components/lp/Footer.tsx` は削除、全公開ページで同一）②**ページ遷移フェードイン＋#アンカースムーズスクロール＋FinalCta統一** `02938f2`＝`components/lp/PageTransition.tsx` を `(site)/(marketing) layout.tsx` に挿入し `key={pathname}` で毎遷移 0.45s fade-in／`html { scroll-behavior:smooth }` + `[id] { scroll-margin-top:5rem }`（固定ヘッダ64px+余白）／FinalCta の文面・見出しサイズを features 下部に統一③**フッター機能列のリンク切れ修正** `513d6e9`→`4902ab2`＝一旦全部 `/features` に集約→`GlowCard` に `id` prop を追加し `/features` 各カードに `feature-brand/timeline/kpi/score/card` の id 付与→フッターを `/features#feature-xxx` に戻す（実測: スクロール後 top=80px＝5rem オフセット効き済み）④**LP共通コンポーネント化** `0e160e4`＝(a)`components/lp/tools.ts`（構築ツール4種の href/label/icon/color/desc をSSOT）(b)`components/lp/ToolCard.tsx`（グラデーション付きカード本体）(c)`components/lp/FinalCta.tsx`（青ラジアル装飾カード末尾CTA、default title/lead/primary＋secondary/note で features とトップを共有／注意書き「※業種業態によってはご利用をお断りする場合があります」をデフォルト表示）(d)`app/(site)/features/page.tsx` の構築セクションにツールカード追加＋「ブランド掲示」を浸透セクション先頭へ移動＋ツール説明文を2行に拡充(e)規約・プライバシー・特商の3ページヒーローを `PageHero` に統一し「最終更新日」を children で受ける形に統一（features/news と font/color/margin 完全一致・実測で18px/white60/mt-24 一致確認）⑤**規約本文 pt-6 統一＋FAQアコーディオンアニメ** `1a6d795`＝3ページ本文セクションに `pt-6` を追加（padding-top 24px 実測一致）／FAQ をネイティブ `<details>` から `components/lp/FaqItem.tsx`（`useState`＋grid-rows[0fr→1fr] トリック）に置換し 300ms ease-out で滑らかに開閉（実測: grid-template-rows 0px→111px, transition-duration 0.3s, cubic-bezier(0,0,0.2,1)）⑥**/admin/members ダーク化** `fe9e4ac`＝layout.tsx で親レイアウトの padding を負マージンで打ち消し、`bg-[#08080a] text-white` でページ全域を染める／Card/Table/Skeleton/Input/バッジ/amber リクエスト box を公開LP同等のトーン(border-white/10, bg-white/[0.03], emerald/rose)に統一。**サイドバー・AdminHeader は他 admin ページと共有のため未変更**（部分適用）⑦**認証ゲート/スプラッシュを共通化＝11ページを `/admin/login` に統一** `55d3a4c`＝新規 `components/admin/GateShell.tsx`（リキッドグラス+Specular+BottomLogo、cardStyle は `/admin/login` と完全一致、primary/secondary ボタン対応）＋新規 `components/admin/AuthSplash.tsx`（白背景+text-black/40 の中間画面）。AdminDataProvider / PortalDataProvider / SuperAdminShell のゲート画面3種（承認待ち・権限なし・ローディング）を GateShell + AuthSplash に統一／OAuthコールバック2ページ（`/portal/auth/callback`, `/admin/login/callback`）＋後方互換リダイレクト5ページ（`/portal/login`, `/tools/{stp,colors,persona,personality}/auth`）を AuthSplash に統一し白フラッシュを解消⑧**404/500/loading をブランドデザインで実装** `a044e29`＝`app/not-found.tsx`（リキッドグラスカードで「🔍 ページが見つかりません」）／`app/error.tsx`（GateShell で「⚠️ エラーが発生しました」＋再試行/トップへ）／`app/loading.tsx`（AuthSplash）。Next.js デフォルトの英語 404 画面を置換⑨**ゲート画面の絵文字を lucide アイコンに置換** `9e7362e`＝🚫→`ShieldAlert`（PortalDataProvider/SuperAdminShell と統一）／⏳→`Hourglass`／🔍→`SearchX`／⚠️→`AlertTriangle`。全て size=48・GateShell 側の text-white/70 を継承 |
| 2026-07-15 リリース（その1） | **スーパー管理サイドバーの整備**①**承認待ちの通知バッジ** `3e12092`＝`companies.approval_status='pending'` の件数をサイドバー「新規登録の承認」に赤バッジ表示（0件なら非表示・100件以上は`99+`）。初回/ページ遷移時に再取得＋承認/却下直後は `signup-requests-changed` カスタムイベントで即時反映。`rounded-full` は**globals.cssの本文14px底上げ対象外**にする意図も兼ねる②**バッジを項目の上下中央へ** `ad5b019`＝shadcn `SidebarMenuBadge` 既定の `top-1.5`(上寄せ)で中心が4px上にずれていた。同じ peer variant キーで上書きし `top-1/2 + -translate-y-1/2`（実測: 153px→157px＝テキスト中心と一致）③**アクティブ/ホバー背景を背景色に馴染ませる** `27d0923`＝背景を無彩色(`0 0% 10%`)に変えた際にアクセントが紺(`213 45% 35%`)のまま取り残され明度+25pt・色相ありで浮いていた→通常管理画面と同じ「背景と同色相・明度+8pt」の関係に合わせ `0 0% 18%` に（border も無彩色化）。識別は白文字＋`font-semibold` が担う（コントラスト13.6:1）④**ロゴをbzマークに差し替え** `47971b8`→`8525291`→`53e0c2e`＝ShieldCheck＋琥珀色背景を廃止し、北川さん提供の元画像を `public/logo-mark.png` として採用（Desktop の `Group 8963.png` 1024x1024 を `cmp` でバイト一致確認。一旦作った再現SVGは削除＝二重管理を残さない）⑤**ロゴ外線** `066e549`→`8964aba`→`ea9b6ad`→`cf45508`＝`border-gray-300`→`500`→`600`→`700`(#374151) と段階調整。下限の目安＝`gray-800`(#1f2937)は画像背景 #222222 と同化し外線の意味が消える⑥**サイドバートグルのサイズ統一** `3353dfd`＝スーパー管理だけ既定 `h-7 w-7`(28px/アイコン16px)のままで、通常管理画面・ポータルの `size-11 [&_svg]:size-6`(44px/24px)と不揃いだった→同一指定に統一（CLAUDE.mdのタップ領域44px基準にも合致）⑦**ファビコン・PWAアイコンを提供のbzマーク画像に差し替え** `68ce83b`＝`public/logo-mark.png`（濃グレー背景#222222＋白マーク）を**正本**にし、`scripts/gen-pwa-icons.mjs` をベクターパス描画（白背景＋濃マーク）から**画像リサイズ方式**へ書き換え。出力＝`icon-192`/`icon-512`/`icon-maskable-512`/`apple-icon`(180)＋**新たに favicon も生成対象に**（`app/icon.svg` 削除→`app/icon.png`(512) へ置換、`app/favicon.ico` を64pxで再生成）。ImageMagick不在のため .ico は **PNG-in-ICO をNodeで自作**（sipsで可読・ICOヘッダ type=1/count=1 を検証）。**2026-06-07 の白黒反転（`85b04b1`＝濃背景＋白マーク→白背景＋濃マーク）を再反転して元の濃背景＋白マークへ戻した形**。素材が全面塗りのため purpose:any も角丸なしの正方形になる（iOS/Androidは自動マスクのため実害なし）。**iOSはホーム画面に再追加しないと更新されない** |
| 2026-06-30 リリース（未記載だったので追記） | **新規owner登録を superadmin 全件承認制に＋競合ドメイン警告** `8449c72`（設計書: `Documents/Claude/ID_bzサービス開発/260630_新規owner登録_競合ドメイン承認制_設計_v1.md`）。目的＝**ID INC. の競合（同業ブランディング会社）が branding.bz を不用意に覗きに来るのを防ぐ**（§4「同業者対策」の残タスクだった承認制ゲートの実装）。①`/api/signup` が企業を `approval_status='pending'`・members を `status='pending'`/`is_active=false` で作成（**行は作る＝パターンA**）。自動ログインを廃止し signup step4「承認待ち画面」へ②登録メールドメインを `blocked_competitor_domains` と照合し `competitor_flag`。**一致しても自動ブロックせず**承認キューで⚠警告（誤検知対策・フリーメールは判定不可のため人手判断）③superadmin へ承認依頼メール（競合一致は件名に⚠）④`AdminDataProvider` が `approval_status='pending'` の企業を「承認待ち」画面でブロック（superadminは対象外）⑤`/superadmin/signup-requests` に承認/却下UI＋API。**承認**=company active＋members有効化＋本人へ承認メール、**却下**=本人へ却下メール後に members/profiles/admin_users/companies/auth user を削除。同ページ下部で競合ドメインの手動CRUD（RLSで直接）。**設計の肝**＝承認待ちでも members 行を作るので oauth-gate（下記）が孤児と誤判定して削除しない |
| 2026-06-21 リリース（未記載だったので追記） | **Googleログインを既存メンバー専用化＋新規登録の開発者通知** `29ef82f`。「Googleで続ける」は `signInWithOAuth` が `auth.users` を自動作成するが、新規登録(`/api/signup`)と違い members を作らないため、**members無しで詰まる＋メールアドレスを占有して再登録が衝突**していた（実際に孤児アカウント2件を発見・削除）。`/portal/auth/callback` が `POST /api/portal/oauth-gate` を呼び、members（status問わず）と admin_users がどちらも無い孤児なら service_role で `auth.users` ごと削除→signOut→`/portal/auth?error=not_registered`。**pending member / admin は削除しない**。併せて `/api/signup` 成功時に開発者へ通知メール（env `SIGNUP_NOTIFICATION_EMAIL`、未設定なら `CONTACT_NOTIFICATION_EMAIL`）。**方針A**（Google＝既存者専用・新規はメール登録）を採用 |
| 2026-07-14 リリース（その5） | **代表者プロフィール機能＋ポータル微整**①**代表者プロフィール新設** `853fb28`＝`companies.representative_profile TEXT` を追加、管理画面 基本情報の代表者名の下に `AutoResizeTextarea`「プロフィール（任意・改行可）」、ポータル `/portal/about` は当初 事業内容の上に別ブロックで表示 → `c43bfb4` で「代表者」行の名前直下に段落として統合（whitespace-pre-wrap で改行反映、空なら非表示）②**180字超はもっと読むトグル** `a1e4ce8`＝先頭180文字＋「…」表示、「もっと読む/閉じる」で切替③**開閉に高さアニメーション** `a8d58b2`＝サブコンポーネント `RepresentativeCell` を新設、`useRef`＋`useEffect` で `scrollHeight` を実測し `height: 0 → scrollHeight → auto` の3段階遷移で確実に animate（300ms ease-out、初回マウントは `didMount` ref で発火抑止）④**ポータル「私たちについて」を接し方の下へ** `685f160`＝「私たちの『らしさ』」グループ内の最下段（順: 考え方→感じられ方→見え方・聞こえ方→接し方→私たちについて）⑤**会社名主/副の言語トグル追従** `591ac8c`＝ポータル `/portal/about` のヘッダ会社名表示を `companies.name_display_lang` に連動（`ja`: H1=日本語表記/下段=英語表記, `en`: H1=英語表記/下段=日本語表記）。両モードで実データ切替→表示確認済、現在は日本語モードに戻し |
| 2026-07-14 リリース（その4） | **ポータルナビ再編＋基本情報フォームの精整**①**ポータル「私たちについて」を「私たちの『らしさ』」グループの先頭（考え方の上）へ移動** `23684bb`＝ユーザーメニュー内の項目は削除。事実情報（会社名・設立・代表者・事業内容）→らしさ（考え方/感じられ方/見え方・聞こえ方/接し方）の順で読める導線に②**ポータル『私たちについて』事業内容カードのミニマル化** `72200e8`＝左端の青バー（`bg-ds-app-accent`）と通し番号 01/02.. を削除、単純な枠だけのカードに③**基本情報「概要」見出し・説明文を削除** `28f95bf`＝h2「概要」と説明文「ポータルの『私たちについて』ページに表示されます（任意）」を除去。設立/代表者フィールドは残置④**「ウェブサイトURL」→「ウェブサイト」に短縮** `fe5eb6e`（表示側ポータルは「公式サイト」のまま。用語統一は保留）⑤**各セクション間の余白を20→32pxに統一** `1417689`＝全7セクション（ロゴ/企業名/設立/代表者/業種/ウェブサイト/事業内容）の `mb-5→mb-8`、実測で全gap=32px⑥**設立/代表者を1カラム独立セクションに分割** `13a7e03`＝旧 `grid-cols-1 sm:grid-cols-3` 内2セルから、各 `mb-8` の独立ブロックに（他セクションと同じ32pxリズム）⑦**設立/代表者の小ラベルをh2装飾に統一** `df15f3b`＝旧 `<p className="text-[11px] text-gray-500 mb-1.5">` → `<h2 className="text-xs font-bold mb-3">`。7見出し全て H2/14px/700/mb-12px/同色⑧**代表者の入力幅を日本語表記と同じに** `21fef26`＝`sm:grid-cols-2` の1カラム分でラップ（実測 328.5px＝日本語表記と一致） |
| 2026-07-14 リリース（その3） | **ポータル「私たちについて」新設＋会社情報フォーム再編**①**ポータル会社概要ページ新設**＝`/portal/about` を追加（会社名 日/英・ロゴ・設立/代表者/業種/公式サイト・事業内容を表示。サイドバーナビ/パンくず/タイトル登録, `0b77a14`）→「私たちの『らしさ』」の下へ移動 `cc7da98` →サイドバーヘッダー（会社名ブロック）のリンク先を `/portal/about` に `f8f1b18` →最終的にユーザーメニュー（マイプロフィールの上）へ移動しメインナビの独立項目は廃止 `74a91a6` ②**名称/アイコン統一**＝「会社について」→「私たちについて」に統一（会社限定でなく中立に、管理画面セクションも「概要」に, `f36a63a`）／アイコンを Building2→Users に `2fb6f04`（プレースホルダー修正 `c172d5d`／スローガン表示を削除 `52fc9f7`）③**事業内容の移動**＝編集を「ブランド方針」→「基本情報」へ、ポータル表示を「考え方」→「私たちについて」へ移動 `d66838b`。共通コンポ `components/shared/BusinessContentEditor.tsx` を新設。データは `philosophy_elements` の `element_type='service'` 行、表示順は `brand_guidelines.business_content_sort`。guidelines の `syncPhilosophyElements` は service を触らない（value/action_guideline のみ同期）ため service 行と衝突しない④**基本情報「設立」を年/月ドロップダウン化** `d015429`＝沿革（`2011年5月` 文字列保持）と共通ヘルパー `lib/year-month.ts`（`parseYearMonth`/`formatYearMonth`/`YEAR_OPTIONS`）に切り出し、沿革も import 差し替え⑤**基本情報から「競合企業・サービス」欄を削除** `f1d5f92`（UIのみ。`companies.competitors` 列と shared-profile ツール連携、`/api/admin/competitors/suggest` は温存。competitors を load/save から除去）⑥**基本情報から「所在地(address)」欄を削除** `f3b6061`（UIのみ。列は残置）⑦**基本情報の並び順入れ替え** `4344e3d`＝概要（設立/代表者）を業種の上へ。新しい並び: ロゴ→企業名→概要→業種→ウェブサイトURL→事業内容⑧**会社情報の競合カラードット** `9594609`＝未指定は #888888 でフォールバック、空値のドットは非表示 |
| 2026-07-13 リリース（その2） | **フォーム機能拡張（スローガン説明文・沿革入力改善・画像並べ替え/複数UP・企業名トグル）＋ポータル微調整**①**スローガン説明文**＝管理画面ブランド方針のスローガン下に「説明文（任意）」を追加、ポータル「考え方」でスローガン直下に本文canonicalで表示（`brand_guidelines.slogan_description`, コミット `62c8846`）②**沿革入力の刷新**（`app/admin/brand/guidelines/page.tsx`）＝年をフリーテキストから「年ドロップダウン＋月ドロップダウン（任意）」に（year フィールドに "2011年"/"2011年5月" の整形文字列で保持＝DB/ポータル不変・既存データ互換, `f551a1a`/月幅圧縮 `59b1bb9`）／各行をドラッグ並べ替え可能に（SortableHistoryItem, `0d8e1ca`）／出来事欄を複数行入力（AutoResizeTextarea, `bb8e2b3`）＋ポータル沿革の出来事に `whitespace-pre-wrap` で改行反映（`31db652`）③**ポータル「考え方」余白**＝行動指針・事業内容の項目間を 8px→12px（space-y-3, `0b14443`/`e17fef3`）④**ポータル「見え方」ロゴ基本形**＝`PortalImageCard` に `fit="width"` を追加し画像を幅フィット表示（左右レターボックス余白解消, `efc9f66`）＋角丸なし・画像間 gap-2→gap-4（16px）（`75df6c5`）⑤**管理画面ビジュアルの画像操作**＝ロゴ基本形画像をドラッグ並べ替え（`4eb8882`）／ロゴガイドラインは「登録順/カスタム」トグルを廃止し常時ドラッグ並べ替えに統一（`0fbd9b7`）＋セクション自体もドラッグ並べ替え（SortableSection, `65bce2e`）／画像アップロードを複数選択対応（ロゴ基本形・各セクション・参考画像の3スポットに `multiple`＋順次アップロード、10枚上限は残り枠まで, `1017df4`）⑥**会社情報の企業名を日本語/英語＋表示トグルに一本化**（`app/admin/company/page.tsx`, `2a3fb1c`→`a1ab1fd`）＝単独の「企業名またはブランド名」入力を廃止し「日本語表記」「英語表記」＋表示トグル（日本語/英語, 既定=日本語）に再構成。保存時に `name` を選択表記へ同期（選択側が空なら他方→従来nameでフォールバック）→スマート名刺・サイドバー・ポータル等は既存の `name` 参照のまま表示が切替（30+ファイル不変）。旧単一 name は初回読込で ASCII→英語表記/他→日本語表記へ移行。読み方は一旦追加後に撤去（`name_reading` カラムは残置・無害）。**実データ設定**: ID INC. の日本語表記を「アイディ株式会社」に設定＋トグル日本語で保存済（表示が「アイディ株式会社」に。英語へ戻すはトグル英語で保存） |
| 2026-07-13 リリース（その1） | **管理画面ブランド基盤の編集UI再構成＋本文/ラベル装飾の全面統一**（コミット `3e72a14`・32ファイル）①**カード整理**＝ブランド方針/ビジュアル/バーバル/パーソナリティ/ブランド戦略の各ページでカードのマージ・分割・並べ替え。セクション間余白を32px（`space-y-8`/`mb-8`）に統一。バーバルは「コミュニケーションスタイル＋表現ルール」を1枚に統合、パーソナリティは「概要＋特性」を統合、ブランド戦略は「ターゲット概要＋主なターゲット＋ターゲット適合マップ」統合・「自社の立ち位置＋ポジショニングマップ」統合＋カード順を整理②**ラベル装飾統一**＝ビジュアル（カラーカテゴリ/フォント）・バーバル・ブランド戦略（軸ラベル/プロット項目/プレビュー）の小見出しを共通ラベル `FieldSubLabel`（text-[11px] text-gray-500）に統一③**ブランド戦略の編集機能追加**＝(a)自社の強み/競合の特徴をポジショニングマップの各プロット項目カード内で編集可能化（自社=`companies.strengths` / 競合=`competitors_analysis[].traits`、名称が会社名と一致する項目を自社判別）。読み取り専用だった「自社の強み」「競合分析」カードは廃止 (b)自社の立ち位置を管理画面で編集可能化（ターゲット名/ステートメント/なぜなら、`brand_stance_statements` を brand_personas row0 に保存）④軸選定の根拠を軸ラベルの下へ移動＋枠（bg-gray-50 rounded p-3）を撤去、「各企業の配置根拠を見る」detailsを撤去⑤ビジュアルの「ロゴの基本形…」補足をロゴ基本形タイトル直下へ移動⑥サイドバートグルのアイコンサイズをポータルに合わせる（`size-11 [&_svg]:size-6`）⑦ポータル・構築ツール・共有コンポーネント（TargetDeepDive/PositioningMapAndStance/PersonalityRadar/BrandPersonalityCard等）の本文を canonical（`text-base text-foreground/80 leading-relaxed`）に統一、セカンダリラベル/余白/カード装飾を微調整 |
| 2026-07-11 リリース | **STP UI整理: Step3/4/5 のマップ・ターゲット表示を統一**①**Step4（ポジショニング）**＝「自社・競合の一覧」カードを廃止しマップに完全統合。要素追加/選択はマップ上で完結（右上に "競合他社を追加" ピルボタン）、点クリックでフローティングパネル（位置スライダー＋AI根拠）→「詳しく編集」でモーダル（名前/色/traits/削除）。「AIで再生成」を「ポジショニング」見出し右へ移動、説明文は「このポジショニングをもとに」を削り「各ターゲットに対して自社が何者として…」だけを残す。ポジショニングマップ見出し右にAIボタン、要素追加ボタンはマップ内右上に。軸選定オーバーレイとマップ本体の余白を80→96pxに調整②**Step3（ターゲティング）**＝「ターゲット市場候補」と「ターゲット適合マップ」を1枚のCardに統合、罫線なしで連結。適合マップのスライダーカード内に軸選び方セレクターを内包、"軸を確定/変更可" ボタンの状態表示化（従来 "軸を確定/ロック解除"→現在の状態を示す "軸を固定中/軸を変更可"）、二重表示の軸固定中バッジを削除。グレーセグメントに `animate-pulse` を追加（クリック促進）。「軸の選び方」行をカードとマップの間へ、AIで提案生成ボタンとの左右関係を入れ替え。「購買決定要因」のラベルを「ターゲットは何を重視して選ぶか」に変更。メインターゲットカード右上の AIで提案生成 ボタンをタイトル行の縦中央に整列（top-3→top-4）③**Step5（確認・出力）**＝戦略整合性スコアパネルを削除（未使用の checkConsistency import と useMemo も掃除）。「自社の強み」「競合分析」を "ターゲット" セクションから "ポジショニング" カード内（マップの下）へ移動。共通コンポ `PositioningMapAndStance` に `belowMap` slot を追加。自社の強み本文を白カード（rounded-md border p-3）で囲み、競合分析カードと意匠統一。競合分析にマップと同じ色丸（18px h/w、白ボーダー2px、opacity 0.85）＋社名テキストも同色に④**全ツール横断の見出し余白統一**＝共通コンポ `FieldHeading` に mt-8 デフォルトを追加（+ 全ページ最初の h2 のみ `mt-0` で打ち消し）。h2＋AIButton のフレックス行ではボタン(28px)との items-center 整列オフセット分だけ mt を減らして（mt-7=28px or mt-3=12px + オフセット4px）合計32pxを維持。ToolStep1BasicInfo.tsx の "現状の顧客層" と "競合企業・サービス" セクション、colors Step1 の "既存のブランドカラーがある"、STP Step3 "ターゲット適合マップ"、STP Step4 "ポジショニングマップ" で修正⑤**メイン/サブターゲットカード意匠統一**＝STP Step3/Step4/Step5・portal/strategy・admin/brand/strategy・共通 PositioningMapAndStance/TargetSegmentCards の**6ファイル**でメインカード・サブカードとも境界線を `border-2` に統一⑥**Step5「ターゲット別立ち位置」カード内のターゲット名文字を 18px（text-lg）に統一**。TagInput の内側パディングを 8px に戻し（一時 16px 検討→revert）、Step4のAI推奨ラベル「軸ロック中」表示を削除⑦**STP Step3で「自社の強み」を Step4 に完全移動**（AI生成・必須制約もStep4側へ）＋Step3の「競合分析（任意）」を Step4「自社・競合の一覧」に統合。既存セッションの `session_data` から `positioning.items[].traits` が抜けた分を、`companies.strengths`/`companies.competitors_analysis` から Step4 マウント時に**空欄のみ**自動バックフィル |
| 2026-07-10 リリース | **STP分析ツール T/P表示のコンポーネント化＋ポータル/Step4との共通化**（コミット `e797452`）①`components/shared/` に `TargetSegmentCards`・`TargetDeepDive`・`TargetFitMapPreview`・`PositioningMapAndStance` を新設し STP Step5・Step4・`portal/strategy` の3箇所で共通利用②ポータル `portal/strategy` にターゲット深掘り情報を新規表示③STP Step5 のセグメンテーション（S）セクションと外枠カードを削除しシンプル化④STP Step4 で自社の立ち位置カードを先頭配置、軸選定根拠テキスト削除⑤STP Step3 の `pr-24` をメインカードのみに限定⑥`SurveyBanner.tsx` の見出し文字色をアクセントカラーに⑦segmentation は本体（brand_personas/companies）への同期廃止。**修正コミット** `e797452` で `PersonaCarousel` の `title` prop 未コミット分を追加し本番ビルドエラーを解消 |
| 2026-07-09 リリース | **ペルソナビルダー UI 微調整＋丸型ピル/タグ 14px 統一**①ペルソナビルダー Step4（ジャーニー）＝「感情カーブ（優先度の注釈）」見出しを 18px、ペルソナ名アコーディオンを 16px、「タッチポイント候補プール」上の区切り罫線を削除し余白を mt-5+pt-5(40px) → mt-8(32px) に整理。加えて Step2（ペルソナ生成）の各ペルソナ見出し「ペルソナN」を 18px に（コミット `520f2f1`）②**システム全体**＝`globals.css` の「本文最低14px」一括ルールの除外から `.rounded-full` を撤去し、丸型ピル/タグ（期待印象タグ・各ツールのタグチップ等）を本文と同じ 14px に統一（アバターは text-2xl 等で対象外・ボタン/タブ/Badge/ステッパーは除外維持）。※ personality の Step4 診断結果表示は一旦入れて revert 済み（元の「実行専用ステップ」仕様に戻す） |
| 2026-07-02 リリース | **カラー定義ツールの UX 全面改修＋ブランドステージ削除**（コミット `f1c31f3`）①**Step1（基本情報）**＝補足テキストを見出し直下へ移動＋文言リライト、業種セレクトの背景を白に統一②**Step2（イメージ入力）**＝「追加の質問」を常時表示化、キーワード選択エリアを白カードで括り「ブランドイメージを表すキーワード（3〜5つ）」見出し追加、カテゴリラベルを `FieldSubLabel` で統一、キーワードチップのパディングを 10x12、選択済みリストを独立コンポーネント（`SelectedKeywordList`）に切り出して「選択済み＋追加の質問」を1枚の白カードに統合、ムードボードのラベルを「前のボード/次のボード」に③**Step3（AI提案）**＝見出し「提案パレット + 件数」追加、「AIで再提案」を Sサイズで同一行右端に、パレットカード全体クリックで選択（`role="button"`＋キーボード対応、タブとボタンは `stopPropagation` で保護）、「提案理由」「プレビュー」を `FieldSubLabel` に統一、区切り罫線＋16px間隔で整列、タブUIをスライドインジケーターでアニメーション化＋文字を太字に④**Step5（確定・出力）**＝STP/ペルソナと同じ `ToolConnectActions` パターンに再構成、スティッキーフッターに PDF ダウンロードを移動、CSS 変数コピーはゴーストボタンで温存、連携済み時のグリーンサクセスボックス追加⑤**ブランドステージのコード全消し**＝実効がプロンプト1行のみで機能への影響が薄いため、色ツール入力/バリデーション/state/保存、`generate` プロンプト、`competitors/suggest` プロンプト＋SELECT列、管理画面の編集UI/型/定数/保存の**全参照を削除**。`companies.brand_stage` カラムは後回し（アプリからの参照は消滅済み） |
| 前回リリース（2026-06-27〜28） | STP分析ツールの一連の改修①段階1の決定論化（temperature=0・軸両端ラベル）②ターゲット適合マップを C案（推奨即生成＋遅延切替＋キャッシュ）へ③死蔵データ表示④根拠データを本体へ⑤ConnectModal 累積バグ修正 |
| 前々回リリース（2026-06-24） | ①管理画面ペルソナ保存を id保持sync 化②ペルソナTier1パラメータを離散カラム化＋編集可能に③未使用voice／孤立フィールド撤去④Step4名称を「ジャーニー／タッチポイント」に統一 |
| 未コミットWIP | **2026-08-25 時点：3件（すべて別セッション作業）** — `CLAUDE.md`／`scripts/wiki-seed/QUOTE-FILTER-RULES.json`／`scripts/wiki-seed/terms.json`。**TSエラー0件・console.log を含むファイル18件**。以下は前回の記録：**2026-08-16（その13）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-16（その11）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-16（その10）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-16（その9）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-16（その8）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-16（その7）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-15（その6）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-15（その5）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル17件**。以下は前回の記録：**2026-08-14（その5）時点：0件**（ワークツリー clean・`origin/main` と同期）。**TSエラー0件・console.log 0箇所（app/配下）**。以下は前回の記録：| 未コミットWIP | **2026-08-14（その4）時点：8件（すべて別セッション作業）** — `app/(site)/plan/page.tsx`（本セッションの Free 5名表記もここに同居）／`app/api/plan-change-requests/route.ts`（管理者のみに絞るガード追加）／`app/portal/` 配下6件。**TSエラー0件・console.log を含むファイル16件**。以下は前回の記録：**2026-08-14（その2）時点：なし**（ワークツリー clean）。**TSエラー0件・console.log 0件（app/配下）・npm run build 緑・entitlements.test.ts 全ケース pass**。以下は前回の記録：| 未コミットWIP | **2026-08-14 時点：なし**（ワークツリー clean）。**TSエラー0件・console.log を含むファイル 15件（app/・lib/・components/ 合計）**。以下は前回の記録：**2026-08-06（その10）時点：なし**（ワークツリー clean）。ただし **`main` に 27 コミットが未 push** で残っている＝コミット済みだが未共有。以下は前回の記録：**2026-08-06（その9）時点：`lib/brand-score/funnel-stages.ts` 1件**（別セッション作業・本セッションでは触っていない）。内容＝`StageScore` に生の平均 `avg`(1〜5) を追加し、`stageScoreOf` を `stageStatsOf` に置き換えて平均と0〜100スコアを同じ計算から返すようにするもの。**丸めた 0〜100 から平均を逆算すると小数第2位がずれる**（実例: BO の推奨は生の平均 3.1252＝3.13 だが 53.1 から逆算すると 3.12）という、既出の「丸めた値を再集計すると誤差が拡大する」教訓と同型の対処。以下は前回の記録：**2026-08-06（その7）時点：なし**（ワークツリー clean・stash は `stash@{0}` の古い別件のみ）。※ セッション途中で別セッションの CSV 一括登録 WIP により `app/admin/members/page.tsx(615)` で `Upload` の未 import による TS エラーが出ていたが、`7e8f9fa`「CSVでアカウントを一括登録できるようにする」の commit で解消済み。以下は前回の記録：**2026-08-06（夕方）時点：`app/portal/page.tsx` の KPI 区分ゲート差分1件のみ残存**（別セッションの WIP。挨拶・KPI カード条件の `kpiEnabled` を `kpiVisible = kpiEnabled && isPortalPageVisibleForRole(company,'kpi',...)` に差し替え）。※上記の その5 セット（14ファイル＋新規6件）は `02e49ee` で commit・push 済み・Vercel Preview green 確認。以下は前回の記録：**2026-08-06 時点で14ファイル＋新規6件（すべて別セッション作業・本セッションでは触っていない）**: メンバー区分（`lib/constants/member-roles.ts`＋migration `20260805150000`）、ポータルの区分別表示制御（migration `20260805153000`）、ポータルのサーベイ結果（`app/portal/survey/`＋`components/brand-score/SurveyResults.tsx`）、admin/portal 各所。**互いに参照し合っているだけで main のビルドには影響しない**ことを確認済み。※`SnapshotScheduleCard` の `recordSlot` は本セッションのコミット漏れだったため `63e19d7` で回収済み。以下は前回の記録：**2026-08-05 時点で15ファイル＋新規6件**: ポータルのサーベイ回答画面（`app/portal/survey/`）、役職カテゴリ（`lib/constants/member-roles.ts`＋migration `20260805150000_add_member_role_category`）、ポータルの役職別表示制御（migration `20260805153000_add_portal_role_visibility`）、`components/brand-score/`、admin/portal 各所。※別セッションが `SnapshotScheduleCard` に `recordSlot` prop を追加中（記録ボタンをカード内に入れる作業）。以下は前回の記録：(a) `app/admin/brand/visuals/page.tsx`, (b) `lib/page-cache.ts`。※前記録のフルスクリーンアニメと3amberボタン強化は別セッションが `c697373 ui(superadmin): プレゼンモードの開閉にフェード＋スケールのアニメーションを追加` として先に本流へ取り込んで消化済み。stash 0件 |

---

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| プロダクト名 | branding.bz |
| 運営 | ID INC.（アイディー株式会社、CEO: 北川巧） |
| カテゴリ | 中小企業向けブランディングSaaS |
| コア機能 | ブランド構築→浸透→発信の一貫支援 |
| 本番URL | https://branding.bz |
| デプロイ | Vercel自動デプロイ（git push） |

### Supabase
- Project URL: https://wfabdmfgngjtihhlrrpk.supabase.co
- RLS: **全テーブル有効**（旧「無効」記載は実態と相違）。ポリシーはテーブルごとに異なり多くが緩い（authenticated=true 等）ため本番前に精緻化が必要。読み書きは service_role の API Route が基本。新規テーブル（learning_*）は**ポリシー無し=deny-all**でservice_roleのみ
- Storage: avatars, logos, brand-assets, timeline-images

---

## 2. 現在のフェーズ

**Phase 2 進行中 — タイムライン・KPI・Brand Score**

### 開発フェーズ履歴
- ✅ Phase 0: スマート名刺プロトタイプ
- ✅ Phase 1: ブランド掲示＋スマート名刺＋管理画面＋ポータル
- 🔄 Phase 2（現在）: タイムライン、KPI、Brand Score（サーベイ・マイクロフィードバック・ダッシュボード）
- ⬜ Phase 3: ミニアプリ群（STP・カラー定義・ペルソナビルダーは完了）

### 3レイヤー構造
1. **構築**（ミニアプリ群）— 理念・コピー・カラー・ペルソナをAIで策定。無料〜フリーミアム
2. **浸透**（branding.bz本体）— ブランド掲示・Good Jobタイムライン・KPI・学習。月額サブスク
3. **発信**（スマート名刺）— QRコードから個人プロフィール＋企業ブランド表示

---

## 3. 完了済み機能

### 🆕 初回オンボーディング（管理者向けセットアップリスト）（2026-08-14・**本番反映済み**）

> **2026-08-15 に Free 版を再設計（v2・未 push）。** 以下の v1 の記述は standard 以上に当てはまる。
> free は専用の6ステップ（基本情報 → ブランド方針 → パーソナリティ → ビジュアル → バーバル → 招待）で、
> ロック行を持たず、Standard への案内はカード最下部の1行のみ。
> 見せ方も刷新（プログレスシェル＋次にやる1つの大きなカード＋残りのコンパクト行）。
> ステップ列・文言・プランバッジの有無は `getOnboardingConfig(plan)` が返し、UI にプラン分岐は書かない。

> コミット: `469eb96` `226ffdc` `56a69a2` ／ migration: `20260814170000_add_onboarding_dismissed_at.sql`（本番適用済み）
> 検討の経緯: 管理者の初回ログイン着地について案A（未セットアップなら /admin へリダイレクト）・案B（着地は変えずダッシュボードを案内に差し替え）・案C（常に /admin）を比較し、**案B を採用**

**方針**: 着地先は変えない（全員 `/portal` のまま）。未セットアップのあいだだけ、ポータルのダッシュボードの中身を4ステップのやることリストに差し替える。
空のカードが並ぶ画面は「導線が分かりにくい」ではなく「中身の無いサービス」に見えるため、空状態そのものを案内に置き換えた。

| 項目 | 内容 |
|---|---|
| 対象 | 管理者のみ（`admin_users` に自社行がある人）。メンバーには出さない |
| ステップ | ①らしさ登録 ②最初のGood Job ③お知らせ1本 ④メンバー招待（順序固定・**招待を最後**に置き、空のポータルを社員に見せない順路） |
| 完了判定 | `GET /api/onboarding` でデータの existence のみ。程度判定は入れない |
| ④の判定 | `invite_links` の発行 **OR** 管理者以外のメンバーが1名以上。招待リンクを使わず直接追加した会社（テックブリッジ）を未完了にしないため |
| プラン外 | 🔒で見せるが**進捗の分母から外す**。free は timeline/announcements が永久に未完了で 4/4 に到達できずカードが消えなくなるため、free は①④の2つで完了扱い |
| 逃げ道 | 「あとで」→ `admin_users.onboarding_dismissed_at`。ポータルの案内だけ消え、**管理画面の鏡写しカードは 4/4 完了まで残る** |
| 完了通知 | 未完了→完了に変わった瞬間だけ一度。**セッション内メモリのみ**（`Map<companyId, boolean>`・DB/localStorage 不使用） |
| 判定の置き場 | `lib/onboarding/steps.ts` 1箇所。ポータルのカードと管理画面の鏡写しカードが同じ関数を通す |

**併せて実装**: タイムラインの `?compose=1`（投稿UIがダイアログで URL から開けなかった）／単体テスト `lib/onboarding/steps.test.ts`。

**検証**: CTD株式会社（premium・空＝0/4）／TAKUMI（free・空＝0/2＋②③ロック）／テックブリッジ（充実＝カード非表示）／一般メンバー（非表示）のブラウザ確認すべて問題なし。

---

### 🆕 ブランディング用語wiki `/wiki`（2026-07-27 公開 / 2026-08-18 詳細定義を拡充・**本番公開済み・230語全公開**）

> コミット: `9a5938d` ／ migration: `20260726163924_create_wiki_terms.sql`（本番適用済み）
> 仕様書: `scripts/wiki-seed/PHASE2-IMPLEMENTATION.md` ／ シードデータ: `scripts/wiki-seed/terms.json`

**データ**: 230語 / 参考ソース240 / ポッドキャスト引用151 / 関連辺653。**全件 `status='published'`**（2026-07-27 全公開）。

**詳細定義（`long_def`）**: 2026-08-18 に平均108字→**670字**（627〜856字）へ拡充。全230語が「### 基本的な意味」で始まる Markdown で、見出しは4〜5本（基本的な意味／なぜ重要か／実務での使い方／中小企業での実践／よくある誤解／構成要素／具体例）。更新は `npx tsx scripts/update-wiki-longdef.ts`（`--dry-run` で件数と文字数を事前確認できる）。**`seed-wiki.ts` は使わないこと** — 子テーブルを作り直すため、本番で削除済みの引用8件が復活する。

**公開の仕組み**: 公開判定は `wiki_terms.status='published'` の1本だけ。RLS がそれを強制し、アプリ側は anon キーで読む（`lib/wiki/queries.ts`）ので、コード側で条件を書き忘れても未公開の用語は漏れない。sitemap も published だけを列挙する。

**個別の公開/非公開の切り替え**（監修で修正が必要になった用語を一時的に下げる等）:
```sql
update wiki_terms set status='review',     updated_at=now() where slug = '<slug>';  -- 非公開に戻す
update wiki_terms set status='published',  updated_at=now() where slug = '<slug>';  -- 再公開
```
反映は ISR（3600s）で最大1時間。即時反映が要るときは Vercel の再デプロイ（または該当パスの revalidate）を打つ。

**ルート**
| URL | 内容 | レンダリング |
|---|---|---|
| `/wiki` | カテゴリカード7枚（件数付き・クリックで絞込）＋検索＋カテゴリ/出典セレクト＋用語グリッド | Static + ISR 3600s |
| `/wiki/[slug]` | 用語名+英訳+カテゴリ → 一文でいうと → 詳しく → 関連用語 → ID INC.の視点（北川引用+Spotify） → 参考ソース | SSG（generateStaticParams）+ ISR |
| `/wiki/category/[slug]` | カテゴリ別一覧＋他カテゴリ回遊。7件を事前生成 | SSG + ISR |

**SEO**: `DefinedTermSet`（index）／`DefinedTerm`＋`BreadcrumbList`（詳細）／`Quotation`（引用ブロック・表示と同一データから生成）。canonical・og は全ルートで自ページURL。

**計測**: `app/layout.tsx` の `<GoogleAnalytics gaId="G-CQBED1RNV2" />` が root layout にあり全ルートを包むため `/wiki/*` は自動計測。**wiki 固有の追加設定は不要**。

**未決・残**: ⓪**`terms.json` が Cowork 側で再生成されるたびに、削除済みの引用8件（氏名を含む名乗り・雑談5／年末挨拶1／番組定型1／回タイトル読み上げ1）が復活する。** 2026-08-18 に再除去したが、生成側に除外ルール（名乗り・エンディング定型・回タイトル読み上げのみ・話者名ラベル付き雑談）を入れないと再発する①**AI補完176語が未監修のまま公開中**（北川判断・文量が5倍になったぶん誤りがあった場合の影響も大きい）→ Cowork の `AI補完監修チェックリスト.xlsx` を並行で進め、修正が必要な用語だけ terms.json 更新＋個別 UPDATE ②Search Console への sitemap.xml 送信（北川さんのアカウント操作・未実施） ③`reading`（よみがな）はカラムのみ・未投入 → 50音インデックスは後段フェーズ ④ポッドキャスト第34-38話公開時の Spotify URL 追加（13引用が「配信準備中」表示） ⑤`/wiki/podcast/[ep]`（エピソード→用語）は未実装 ⑥`view_count` はカラムのみでトラッキング未実装。

**別タスク（wiki 起因ではない既存課題）**: `notFound()` を通る全ページが **HTTP 200 を返す soft-404**。本番の `/news/zzz` でも同様（`curl -o /dev/null -w '%{http_code}' https://branding.bz/news/zzz` → 200。未マッチのパス `/totally-unknown-path` は正しく404）。wiki は230語すべて published のため該当URLはほぼ無く、緊急度は低い。

### 🆕 新規登録ゲート（Googleログイン孤児ガード＋新規owner承認制＋競合ドメイン警告）（2026-06-21／06-30・本番デプロイ済み）

> コミット: `29ef82f`（oauth-gate）/ `8449c72`（承認制＋競合ドメイン）/ `3e12092`（承認バッジ）
> 設計書: `Documents/Claude/ID_bzサービス開発/260630_新規owner登録_競合ドメイン承認制_設計_v1.md`
> 検証手順: `Documents/Claude/ID_bzサービス開発/260630_branding-bz_デプロイ後検証手順_v1.md`

**新規アカウントの3経路（承認の有無）**
1. **既存企業へ参加**（ドメイン一致→企業選択）= **承認制**（その企業の管理者が `/admin/members` で承認）。`/api/signup/join-company` が members を `status='pending'`/`is_active=false` で作成
2. **新規企業を登録**（ドメイン不一致 or 「別企業として新規登録」）= **superadmin 全件承認制**（2026-06-30〜）。`/api/signup` が company を `approval_status='pending'` で作成
3. **Googleログイン** = **既存メンバー専用**（oauth-gate が孤児を削除）

**① Googleログイン孤児ガード**（`/api/portal/oauth-gate`）: `/portal/auth/callback` がログイン確定後に呼ぶ。members（status問わず）と admin_users がどちらも無い「孤児」なら service_role で `auth.users` ごと削除→signOut→`/portal/auth?error=not_registered`。**pending member / admin は削除しない**（＝承認待ちを消さない）。方針A＝Googleは既存者専用・新規はメール登録

**② 新規owner登録の superadmin 承認制**: `companies.approval_status`（'pending'\|'active'\|'rejected'）／`members` は `status='pending'`/`is_active=false`。**承認待ちでも行は作る（パターンA）**＝oauth-gate の誤削除を防ぐ設計。`AdminDataProvider` が pending 企業を「承認待ち」画面でブロック（superadminは対象外）。signup は自動ログインを廃止し step4「承認待ち画面」へ

**③ 競合ドメイン警告**: `blocked_competitor_domains`（手動メンテ・RLS superadminのみ）と登録メールドメインを照合し `companies.competitor_flag`。**自動ブロックはせず承認キューで⚠警告**（誤検知対策。フリーメールは判定不可＝人手判断に委ねる。判定はv1では完全一致）

**④ 承認UI** `/superadmin/signup-requests`: 一覧（競合は⚠赤バッジ）＋承認/却下＋競合ドメインの手動CRUD。**承認**=company active＋members有効化＋本人へ承認メール／**却下**=本人へ却下メール後に members/profiles/admin_users/companies/auth user を削除。サイドバー「新規登録の承認」に**承認待ち件数の赤バッジ**

**⑤ メール通知**: 承認依頼（superadmin宛・競合一致は件名⚠）／承認・却下（本人宛）。宛先 env `SIGNUP_NOTIFICATION_EMAIL`（未設定なら `CONTACT_NOTIFICATION_EMAIL`＝現状 kitakawa@include.bz）。Resend・noreply@branding.bz

**運用判断（2026-06-30 確定）**: 競合=ID INC.の競合（全社共通の単一ブロックリスト）／ブロックリストは手動メンテ専用テーブル／一致時は承認キュー＋警告（即ブロックしない）／新規owner登録は競合か否かに関わらず全件承認制

### 🆕 カラー定義ツール 2026-07-02 改修まとめ（本番デプロイ済み）

> コミット: `f1c31f3` `feat(colors): カラー定義ツールのUX改善＋ブランドステージ削除`

**UX 改修（Step1〜5）**
- Step1（基本情報）: 補足テキストを見出し直下へ移動＋文言リライト、業種セレクト背景を白に統一
- Step2（イメージ入力）:
  - 「追加の質問」を常時表示化（3つ以上選択時のトグル撤去）
  - キーワード選択エリアを白カード内に配置＋見出し「ブランドイメージを表すキーワード（3〜5つ）」追加
  - カテゴリラベルを `FieldSubLabel`（`text-[11px] text-gray-500 mb-1 block`）に統一
  - キーワードチップのパディングを 10x12（`py-2.5 px-3`）
  - 選択済みリストを独立コンポーネント（`SelectedKeywordList`）に切り出し、親（`Step2ImageInput`）で「選択済み＋追加の質問」を1枚の外側白カードに統合＋間に細い罫線
  - ムードボードのボタンラベルを「前の質問/次の質問」→「前のボード/次のボード」
- Step3（AI提案）:
  - カード内に見出し「提案パレット + 件数」追加、「AIで再提案」を Sサイズ（`sm`）で同一行右端に移動
  - **パレットカード全体クリックで案選択**（`role="button"`＋キーボード対応＋`aria-pressed`）。プレビュータブと「この案を選ぶ」ボタンは `stopPropagation` で保護
  - 「提案理由」「プレビュー」を `FieldSubLabel` に統一、区切り罫線＋前後 16px 間隔で整列
  - **タブUIをスライドインジケーターでアニメーション化**（`translateX` + 300ms ease-out）＋文字を太字に
- Step5（確定・出力）:
  - STP/ペルソナと同じ `ToolConnectActions` 共通コンポーネントで「branding.bz への連携」カードに統一
  - スティッキーフッターの右側に「PDFをダウンロード」を移動（左：調整に戻る）
  - CSS 変数コピーはゴーストボタン（`variant="ghost" size="sm"`）で温存
  - 連携済み時：グリーンサクセスボックスを追加（persona と同じ）

**ブランドステージのコード全消し（DB カラムは後回し）**
実効がプロンプト1行のみで機能への影響が弱いため、コード側の全参照を削除:
- `Step1BasicInfo.tsx`: 入力欄・必須バリデーション・state・プリフィル・保存ペイロード・本体同期＋未使用 import（Select 系・BrandStage 型）
- `Step5Export.tsx`: 本体書き戻しペイロードから `brand_stage` を除去
- `api/tools/colors/generate/route.ts`: プロンプトの `- ステージ:` 1行を削除
- `api/admin/competitors/suggest/route.ts`: SELECT 列＋競合提案プロンプトから削除
- `admin/company/page.tsx`: 編集UI・`BRAND_STAGES` 定数・型・SELECT 列・mapping・保存を削除
- **温存**: `shared-profile` の同期プラミング（不活性）／`lib/types/color-tool.ts` の `BrandStage` 型／`companies.brand_stage` カラム本体（DROPは別途SQL＋実行確認で）

### 🆕 STP分析ツール 2026-06-27〜28 改修まとめ（本番デプロイ済み）

> コミット: `bb7f270` / `db92b55` / `4decc2a` / `c05ac80` / `a8bb706` / `d81404f` / `5f16906` / `2fb3ff3`

1. **AI生成の安定化**
   - `temperature=0` で決定論化（適合マップ／ポジショニング／ブランドスタンスの提案）
   - 軸候補を Step2 セグメンテーションの切り口に限定
   - 切り口に `axis_endpoints`（軸両端ラベル）を生成し、適合マップ軸に対称表現で採用
   - `axis_type='ordinal'` のみを軸候補に限定（カテゴリ型は禁止）

2. **C案実装（推奨即生成＋遅延切替）**
   - AI推奨候補（戦略×分散）のみ即時生成
   - 「他の軸も試す」ドロップダウンで 強み×分散・分散×分散 を遅延生成＋キャッシュ
   - 生成済み候補は **⚡即切替**、未生成は **⏱再生成** を識別表示
   - キャッシュ・選択中ストラテジーを session（`targeting.target_fit_map_cache` / `_selected_strategy`）に永続化

3. **キャッシュ永続化バグの完全解消（4層）**
   - 保存側: 空 `{}` 経由の書き込み防止・debounce を待たず即保存・初回マウントの空セーブをスキップ
   - 復元側: `useState` lazy initializer で props から復元
   - effect発火: `lastTargetsRef` でターゲット実値を追跡（配列参照の変化に騙されない）
   - UI: ステップ進捗ローダー（`StepProgressLoader`／`StepProgressPanel`）

4. **死蔵カラム表示UI追加（ステップ①）**
   - 管理画面 `/admin/brand/strategy`: ターゲット適合マップ（軸根拠付き）＋自社の立ち位置×N本カード
   - 社員ポータル `/portal/strategy`: 自社の立ち位置×N本カード（適合マップは社員向け非表示）
   - 既存 `TargetFitMapStatic` を流用・データ無ければ非表示の条件レンダー

5. **STP根拠データの本体保存・表示（ステップ②）**
   - `companies` テーブル拡張: `strengths(text)` / `competitors_analysis(jsonb)`（migration `20260628050011_*`・本番適用済み）
   - 連携API改修で5データを保存:
     - `targeting.buying_factors` → `brand_personas.decision_factors`
     - `targeting.strengths` → `companies.strengths`
     - `targeting.competitors_analysis` → `companies.competitors_analysis`
     - `positioning.axis_rationale` ＋ `items[].reasoning/confidence` → `positioning_map_data` に埋め込み
   - 管理画面: 自社の強み・競合分析・ポジショニング根拠（軸選定の根拠＋各企業の配置根拠＋確信度バッジ）を表示
   - ポータル: 自社の強みのみ表示
   - `ConnectModal`: 上書き確認の**累積処理パターン**（`accumulatedConfirm`）で新規5項目同時連携時の 409 ループバグを解消

6. **オントロジー統合の方針決定（やらない）**
   - 「自社の立ち位置」≠ Value Proposition と概念整理
   - STPの立ち位置は「ポジショニングマップへの分析メモ・考察」として位置づけ
   - `value_propositions` テーブルへの自動投入はしない（手動入力 or 将来の別ツール）

### 🆕 コピーAI（MVP・feature/superadmin-company-view・main未反映）
- **7段階クリエイティブ・パイプライン**（診断→インサイト→切り口→生成→批評→リライト）。一発生成を禁止。
- **尖り度マトリクス**（`lib/copy/role-matrix.ts`）: `copy_role`（hero_h1=狂犬100% / section_heading=70% / body_copy=40% / cta=0%）で態度表明・陳腐句ブロック・評価軸を動的切替。
- **INTENT/FACT/RULES 3層分離**（`lib/copy/ontology-blocks.ts`）: 理念・バリュー=引用禁止（意味だけ翻訳）／proof_points=引用可／governance_rules=禁則。コピペ・平均値化を構造で防止。
- **批評（インスペクター）**（`lib/copy/inspector.ts`/`metrics.ts`/`score.ts`）: 二値チェックリスト＋引用（点数はTS合成）、クリシェ密度・継承重複(containment)はコード計算、処方箋のみ返す（リライト本文は生成器へ再パス）。生成=`claude-sonnet-4-6`／批評=`claude-opus-4-8`。craft_score低×brand_fit高=赤旗→自動リライト送還（最大2回）。
- **人間ゲート**: インサイトは pain_points 等へ接地（source_ref必須・捏造破棄）。クライアントは id のみ送信・サーバ再取得。
- **ワークベンチUI**: `/superadmin/companies/[id]/copy`（superadmin専用）。
- DB（本番適用済み）: `copy_projects`/`copy_insights`/`copy_angles`/`copy_drafts`/`copy_quality_reviews`（RLS superadmin_all＋member_select）。
- 実証: before/after で「30%捏造→実データ42%引用」、退屈なbody_copy(craft46)→赤旗→自動リライト(craft88)、医療コンプラ禁則を狂犬モードでも遵守。

### 🆕 ペルソナビルダー連続改修（feature/superadmin-company-view・main未反映）
- discrete pain_points 写像（連携が `goals.pain_points→brand_personas.pain_points`、`primary_goals→needs` を書く）。
- セグメント型粒度に統一（役割呼称・年齢層・短い体言止め課題）。年収/家族/学歴を削除、口癖・1日の過ごし方は任意。
- マルチペルソナ化（`session_data.personas[]`・connectはN件sync・冪等）＋ターゲット別グルーピング（`target_name` を `persona_data` に格納）。
- ジャーニー: maxTokens 8000＋堅牢パーサ、Step4任意化。一度撤去→北川さん判断で復元（5ステップ・`journey_map_data` 列は残置）。

### 🆕 ブランドオントロジー カードUI刷新（引き算＋3D一本化・2026-07-21・本番反映済み）
- **カード構成を5段に固定**：`ヘッダー（タイトル＋状態バッジ＋⛶＋…）` → `警告チップ` → `3Dマップ` → `出力テスト` → `編集する（折りたたみ）`。既定で見せるのは要点だけにし、件数・凡例は「…」メニュー、俯瞰は⛶に畳んだ。
- **警告チップ**（条件表示・行動導線つき）：`未接続 N件 → 繋ぎに行く`（ポップオーバーで未接続要素を一覧・行クリックで該当ステップへ）／`理念が未登録`・`提供価値が未登録`（未登録時のみ）／`裏づけ M/M`／`くい違い`（発生時のみ）。
- **ビューアは3Dに一本化**：`BrandMap3D.tsx`（Three.js不使用・Canvas疑似3D＝回転行列＋透視投影＋z順ソート・依存追加なし）。同心リングの決定論レイアウト（中心=ミッション／内周=理念／中=事業／中外=提供価値・ペルソナ／外周=実績・ルール／**上空=獲得目標**）、`requires`・`toBeEvidencedBy` は点線、獲得目標は進捗を併記。ドラッグ回転・ホイール/ピンチズーム・ホバー強調＋左下説明バー・クリックで詳細パネル連動・自動フィットの固定初期構図。**⛶で全画面プレゼンモード**（暗背景＋自動回転オン・Escで閉じ rAF 停止）。2Dの d3-force SVG は廃止（`lib/brand/map-data.ts` の島判定は温存）。
- **出力テストを比較ステージ化**：生成前から2パネル常設（左=オントロジーなし／右=あり）。右パネルは `lib/brand/output-highlight.ts` で**オントロジー由来の語句を決定論ハイライト**（AI不使用・長い順に非重複）。注記2行は ⓘ ツールチップへ。
- **ウィザードを5ステップ化**：旧ステップ1「基本情報の確認」を削除（実績→言葉のルール→未来設計→関係性→補足質問）。前提の未登録はチップへ移管。撤去したUI：AIレビュー常設／ショートカット3ボタン／現状・構造タブ／提供価値の黄色バナー（**`MapReviewPanel` と map-review API はレポート出力用に残置**）。
- **達成条件フォームをやさしく**：4テンプレカード＋常時プレビュー「こうなったら達成：〜」。`metric_definitions`（指標辞書）を新設し、目標・測定値の双方を **MetricPicker（名前で選ぶ）** に統一＝内部キー・単位が自動で一致。
- 追加コンポーネント: `BrandMap3D` / `MetricPicker`。追加lib: `lib/brand/map-display.ts`（色・ラベル・関係の言い回し）・`lib/brand/output-highlight.ts`（ハイライト純関数）。
- コミット：`e14cd6e`(やさしいフォーム) / `38092b2`・`4718738`・`6d42d31`(3D一本化) / `4da8fc7`(最終構成) / `f652428`(折りたたみ修正) / `b5ea497`(出力テスト) / `83d6d50`(5ステップ化)。**全て本番反映済み**。

### 🆕 ブランドオントロジー未来設計（現在→理想→道のり）：設計＋DB基盤＋ドメイン層＋UI＋コピーAI注入まで実装完了（2026-07-19）
- **設計7本** `docs/260718_ブランドオントロジー_*.md`（**C案統合設計_v1 が正**・判定ロジック詳細/DB基盤マイグレ設計を含む）。方針：Proof Point＝**事実限定**／**Desired Evidence（獲得目標の証拠）を独立概念**／**state と progress を分離**／**indeterminate 導入**（データ不足と未達を混同しない）／**重み付き実証進捗＋判定可能率**（セット表示必須）／VPは `target→transition_candidate→current`（管理者昇格）／**FACT・ASPIRATION分離**で反捏造を維持。
- **DB基盤5本 本番適用済み**（`apply_migration` 実行・ローカル `.sql` は実versionにリネーム済）：`desired_evidence`(20260718133238) ／ `proof_point_measurements`(141811) ／ `desired_evidence_evaluations`＋失効スナップショット＋bumpトリガ(141841) ／ `value_propositions.lifecycle_state`(141907) ／ `element_relations` relation_type +4種〔aspiresTo/requires/toBeEvidencedBy/verifies〕(141920)。**全て加算・既定値で挙動不変**。
- **ドメイン層**：`lib/brand/future-design/`（rule検証・評価エンジン・人間判断の失効判定・重み付き進捗・読取専用IO）＋ `lib/brand/integrity.ts` に §10 未来設計チェック（獲得計画なし／判定条件未設定／昇格レビュー待ち／単位・指標不一致／測定値なし判定不能／override要再確認 ほか）。**ユニット計20 pass・tsc/build緑・DB無変更**。
- **§11 管理UI**（企業詳細・オントロジー構築ウィザードのステップ4「未来設計」）：獲得目標CRUD＋達成条件エディタ（boolean/count/aggregate/manual・保存前に `validateRule`）／実績への**測定値入力**（指標キー・値・単位・測定日・対象範囲・出典）／判定と進捗の表示（状態バッジ 達成・一部・未達・判定不能＋進捗バー＋理由文、セクション上部に**実証進捗と判定可能率をセット表示**・獲得目標0件は §14.6 で「未設定」）／**人間判断**パネル（記録・**失効中の警告と再確認導線**・自動評価に戻す）／**提供価値の状態遷移と current 昇格**（昇格者・日時を記録）。`element_relations` は `desired_evidence` を端点に選べるよう拡張（aspiresTo/requires/toBeEvidencedBy/verifies）。
- **§14.2 rule_hash 算出RPC 本番適用済み**（`desired_evidence_rule_hashes` / version 20260719025247・SECURITY DEFINER・service_role のみ）→ `fetchCurrentRuleHashes` を実RPCに結線し、**override の失効判定が実際に機能**（ルール変更で失効→自動評価へフォールバック）。
- **§9 コピーAI FACT/ASPIRATION 分離注入**：FACT＝実績＋測定値＋`lifecycle_state='current'` の提供価値（null は current 扱い）／ASPIRATION＝`target`・`transition_candidate` の提供価値＋**met 以外**の獲得目標＋ビジョン。ASPIRATION は system プロンプトに FACT と別セクションで注入し「目指す／これからの形でのみ言及可・事実として断定や数値引用は禁止・引用可の事実は FACT のみ」を明示。獲得目標は met でも引用対象にしない（§14.5・引用できるのは紐づく実績）。**ASPIRATION 0件なら注入なし＝従来プロンプトと文字列完全一致**。あわせて `governance_rules.rule_type` 未取得で禁止語チェック（`bannedTerms`）が常に空だった既存バグを修正。
- コミット：`6fe76f1`(docs) / `21d4961`(M1) / `2f71a93`(M2-5) / `6b23da1`(engine) / `34a3f94`(integrity) / `8260da1`(status) / `2931805`(UI a+b) / `c753267`(人間判断・VP昇格・RPC結線) / `55b28aa`(コピーAI FACT/ASPIRATION) / `6361e78`(禁止語fix)。**約11コミットが未push**。
- 検証：tsc / build 緑。ユニット（判定エンジン20＋FACT/ASPIRATION分離）全pass。デモ企業テックブリッジのみでロールバック検証（人間判断の記録→ルール変更で失効→自動評価フォールバック→クリアで復帰／状態と進捗の整合CHECK違反はAPIとDBの双方で拒否／VP昇格で promoted_by・promoted_at 記録）。**実クライアント（リィツ／ID INC.）は無変更・テスト行は全削除**。
- 残：**ID INC. での実機E2E**（実データでの一連の操作確認）。
- **運用注意**：このリポは `supabase db push` **不可**（ローカル/リモートの version ドリフト）。マイグレは **apply_migration＋ローカル `.sql` を実versionにリネーム**する運用。


- スマート名刺 (/card/[slug]) — プロフィール＋企業ブランド＋MVV＋マイクロフィードバック
- 管理画面 (/admin) — 企業情報・メンバー・ブランドガイドライン・お知らせ・名刺テンプレート
- ポータル (/portal) — メンバー向けブランド掲示・タイムライン・KPI・サーベイ回答
- Brand Score (/admin/brand-score) — インナー＋アウター＋マイクロフィードバック
- STP分析ツール (/tools/stp) — 5ステップAI提案＋PDF出力＋branding.bz連携＋自動保存インジケーター。**再設計後の最終形**：Step2 セグメンテーション（A/B/C/D 切り口バッジ・規模感・重視点・axis_endpoints 両端ラベル）／Step3 ターゲティング（購買決定要因・自社の強み・競合分析の入力＋**ターゲット適合マップ C案**＝推奨即生成・他軸遅延生成・キャッシュ即切替）／Step4 ドラッグ操作型ポジショニング（軸選定の根拠・配置根拠・確信度をAI生成）／Step5 ConnectModal（項目別チェック＋上書き確認の累積処理）。段階1の提案は temperature=0 で決定論化。**連携データの本体反映**：適合マップ・自社の立ち位置・自社の強み・競合分析・購買決定要因・ポジショニング根拠まで `brand_personas`/`companies` に保存し、管理画面（全件）／ポータル（強み・立ち位置のみ）に読み取り表示
- カラー定義ツール (/tools/colors) — 5ステップAIパレット＋PDF出力＋branding.bz連携＋自動保存インジケーター
- ペルソナビルダー (/tools/persona) — 5ステップAIペルソナ＋ジャーニーマップ＋連携＋自動保存インジケーター
- スーパー管理画面 (/superadmin) — 企業管理・ニュース管理・企業削除（カスケード）・管理者メール/名前表示
- マーケティングページ — LP・料金・FAQ・お問い合わせ・お知らせ
- ドメイン認証による企業マッチング — メールドメインで既存企業を検索・承認制参加フロー
- サインアップ画面刷新 (/signup) — グラスモーフィズムUI・4ステップフロー（アカウント→ドメインチェック→企業情報→個人情報）
- 管理画面 参加リクエスト承認/拒否UI (/admin/members)
- 認証システム抜本改修 — @supabase/ssr全面移行（cookieベース）・middleware.ts でセッション自動更新・Provider 2層分離（AppAuthProvider + AdminData/PortalDataProvider）
- メンバー削除/孤立アカウント復旧 — Service Role経由でmembers/profiles/auth.users連鎖削除、残存アカウント削除UI
- マイページ振り分けルート (/mypage) — サーバー側で admin/superadmin/member を判定して即リダイレクト
- グローバルヘッダー — ログイン状態で「ログイン」⇔「マイページ」を自動切替
- 参加リクエスト通知メール — Resend経由で (a) 申請受付時に管理者全員へ通知、(b) 承認時に申請者本人へログインURL付きで通知
- メンバー一覧UI改善 — `/admin/members` 一覧から `status='pending'` を除外し、参加リクエストセクションと重複表示しないように
- 機能オン/オフトグル (/admin/settings) — 配列駆動（`lib/constants/feature-toggles.ts` が唯一の定義源）で「Good Jobタイムライン・目標/KPI管理・スマート名刺」を企業単位で表示制御。PATCH API（service_role + company_idガード）。ポータル/管理画面の出し分け、スマート名刺は公開ページ /card/[slug] を門番ガード。データは削除せず再オンで復活
- コンセプトビジュアル複数化＋スライドショー (ブランド方針) — admin で複数アップロード＋ドラッグ並べ替え（最大10枚）、portal でスライドショー表示（自動送り＋前後ボタン＋ドット、枠高さ固定・object-cover）。DB: `brand_guidelines.concept_visuals(jsonb配列)` 追加、レガシー `concept_visual_url` は先頭画像と同期しCIマニュアル表紙互換を維持（2026-06-03）
- 企業作成フロー修正（ポータルログイン不可の解消） — `create-company`/`signup` が admin_users だけでなく **profiles＋members** を作成するよう修正（ポータルは members 行で判定するため旧実装ではログイン不可だった）。既存ロックアウト5社（リィツメディカル/MEGUTAMA/CTD/あいうえお/テスト）を本番DBへバックフィル（2026-06-03）
- スーパー管理「新規企業を登録」モーダル整理 — 表示に使われない孤立カラム（slogan/MVV/ブランドカラー）の入力UIを削除（企業名＋Web＋管理者のみ。ブランド情報は作成後に各編集ページで設定）（2026-06-03）
- FAB共通コンポーネント化 — `components/ui/fab.tsx`（`Fab`/`FabButton`）を新設し、ポータル/管理/スーパー管理に散在していた右下FAB約19ファイルを統一（bottom-8/z-50/h-12 に統一）（2026-06-03）
- ポータルメニュー再構成「私たちの『らしさ』」 — 視点ワード構成（考え方／感じられ方／見え方・聞こえ方／接し方）。「見え方・聞こえ方」は展開式サブメニュー（ビジュアル／バーバル）、values/termsは配下に内包（ルート生存）。各ブランド掲示ページ見出しを「視点ワード ｜ 名詞」の二段表記に（管理画面は名詞のまま据え置き）（2026-06-03）
- タイムライン投稿UIのFAB＋モーダル化 — 常時表示の投稿カードを廃し右下FAB→モーダル。説明文はモーダル内 DialogDescription へ移設（2026-06-03）
- ブランドパーソナリティ独立編集ページ (/admin/brand/personality) — verbal からトーンオブボイス編集（状態・取得・`brand_personalities` 保存）を分離。サイドバーはブランド戦略の下（ビジュアルの上）。ポータルの personality 独立表示と対（2026-06-03）
- MVV「コピー＋説明文」分離表示 — `lib/brand-mvv.ts`（`splitBrandCopy`/`combineBrandCopy`、空行 `\n\n` 区切り）で mission/vision をキャッチコピー（大太字）と説明文（通常）に分離描画。表示6箇所（portal/guidelines・ダッシュボード・KPI参照・スマート名刺mission/vision・CIマニュアルPDF）に適用。編集フォーム（/admin/brand/guidelines）もコピー欄/説明文欄の2入力に分離（DBは mission/vision 各1カラムのまま）（2026-06-03）
- ブランド方針ページ UI ポリッシュ (/portal/guidelines) — ①ブランドストーリーを300字超で「もっと見る」折りたたみ（`ExpandableText`）②沿革タイムラインにドット連結の縦ライン（角丸クリップのレール）③行動指針・事業内容の左バーを「私たちの『らしさ』」カード同装飾（`absolute w-1` バー＋`overflow-hidden`角丸クリップ、青）に統一（2026-06-03）
- 全画面 UI 微調整 — ①H2セクション見出しを `text-sm`→`text-xs`（全ページ123箇所）②セクションカードの section 間余白を `space-y-6`（24px）に統一（portal表示3カード＋visuals/strategy＋admin編集3カード、最上段H2は不変）③サイドメニューの「らしさ」アイコンをダッシュボードに統一（感じられ方=Smile、接し方=Target）④ダッシュボード見出しを「私たちの『らしさ』」に変更＋Sparklesアイコン削除⑤バリュー説明欄を複数行入力化（登録順・カスタム両タブ）⑥ロゴ未登録時はサイドバー（admin/portal）のアイコン枠を非表示（2026-06-03）
- ブランド基盤の項目オーナーシップ再編（管理＋ポータル）（2026-06-03）
  - **行動指針**: brand_personas（先頭ペルソナ相乗り）→ **brand_guidelines.action_guidelines** へ移設（カラム追加＋データ移行）。編集はブランド方針。読み取り側（戦略/ポータルブランド方針/タイムラインのカテゴリ/管理ダッシュボード/CIマニュアル）も新カラム参照に更新
  - **特性(traits)**: ブランド方針 → ブランドパーソナリティへ移動（brand_guidelines.traits のまま、編集UIのみ移動）
  - **トーンオブボイス**: ブランドパーソナリティ → バーバルへ移動（brand_personalities.tone_of_voice のまま、管理・ポータル両方）
  - **パーソナリティ概要**: `brand_guidelines.personality_summary`(新規カラム) を新設。管理「ブランドパーソナリティ」で編集→ポータル「感じられ方」のレーダー下に表示
  - 管理サイドバー並び: ブランド方針→パーソナリティ→ビジュアル→バーバル→ブランド戦略→CIマニュアル出力
  - 各ブランド編集ページの「ポータルサブタイトル」入力欄を撤去（保存ロジックは残置＝既存値は保持）
- ブランドパーソナリティ拡充＋トーンオブボイス整理（2026-06-03）
  - 特性スコアを **1〜10 → 1〜5（5段階）** に統一（編集 max=5／レーダー domain=[0,5]／「/5」表記／CIマニュアルのバー幅 `score*20%`）
  - 特性を **3項目（カテゴリー=name / コピー=新規copyフィールド / 説明文=description）** に分離。jsonb のためマイグレーション不要、旧データは description の最初の改行で自動分割（`lib/brand-mvv.ts` の `resolveTraitCopy`）。ポータルは「カテゴリー(小)＋コピー(大)＋説明文」、編集は縦積みカード
  - **トーンオブボイス**: ポータル表示ラベルを「トーン＆マナー」→「トーンオブボイス」に改称。コピー＋説明文に分離（`splitToneOfVoice`＝空行区切り。**空行なしは全文を説明文**扱いで splitBrandCopy とは逆）。`combineBrandCopy` で結合し `tone_of_voice` 1列のまま（DB無変更）。管理(verbal)も2入力、CIマニュアル(verbal.tsx)も分割
  - ポータルダッシュボード4カード説明文を調整: トーンオブボイスを「感じられ方=ブランドの人格・キャラクター」から「見え方・聞こえ方=…（トーンオブボイス・用語ルール含む）」へ移動
- 入力プレースホルダーの固有名詞除去（システム全体）（2026-06-03） — placeholder に実在の社名/人名/クライアントのスローガン・MVV・特性コピーを使わないよう全件修正（リィツメディカル等6件を架空の汎用例に置換、`ueda@example.com`→`member@example.com` 等）。**CLAUDE.md コーディング規約に恒久ルール追記**（再発防止）
- ポータル ブランド掲示の4象限再配置＋ダッシュボード概観カード（2026-06-03・本番デプロイ済み） — 各ページ表示を「らしさ」4象限に再マッピング（DB無変更・表示レイヤーのみ）：考え方/guidelines＝MVV・バリュー・**提供価値（brand_values＋companies.provided_values、空なら非表示）**・行動指針・沿革・事業内容／感じられ方=新規 **/portal/personality**（人格traits＋トーンオブボイス＝旧verbalから移管。サイドバー「感じられ方」を verbal→personality に確定、breadcrumb/dynamic-title 登録）／見え方・聞こえ方=visuals＋verbal（用語ルール）／接し方=strategy（行動指針は guidelines へ移管済み）。**ダッシュボード `/portal` トップに4象限概観カード**（内部=紫violet/外部=緑emerald、Compass/Smile/Eye/Target、各ページへ遷移）。管理画面（/admin）は無変更
- ビデオラーニング機能 (/admin/learning・/portal/learning)（2026-06-03・本番デプロイ済み） — YouTube動画で社内学習（**浸透**レイヤー）。管理者が動画を登録/編集/削除/公開トグル/dnd並べ替え＋視聴分析（動画別カード=視聴人数/完了率/平均進捗/総再生回数＋メンバー×動画マトリクス、recharts）、メンバーはポータルで視聴し進捗（再生/到達度/完了）が記録される。視聴トラッキングは YouTube IFrame Player API（最初のPLAYINGでセッションINSERT→15秒間引きPATCH＋PAUSED/ENDED即送信＋離脱時keepalive、90%/ENDEDで完了）。DB: `learning_videos`/`learning_video_views`（1セッション=1行）。`lib/youtube.ts`(extractVideoId/getThumbnailUrl)・`components/learning/YouTubePlayer.tsx`。**登録＝即公開**（ダイアログ初期値=公開）
- スマート名刺カバー写真＝コンセプトビジュアル＋事業内容の縦線統一 (/card/[slug])（2026-06-03・本番デプロイ済み、commit 1b37c27） — ①名刺ヘッダーのカバー写真を「個人カバー(profile.cover_image_url) → 企業のコンセプトビジュアル(`brand_guidelines.concept_visuals[0]`／空なら `concept_visual_url`) → ブランドカラー無地」の優先順で適用（DB無変更・表示レイヤーのみ）②事業内容の縦線を border-l-2（角丸沿い2px）からポータル同装飾の `absolute w-1` 全高バー＋`overflow-hidden`角丸クリップ＋`pl-5` に統一。**色はブランドアクセント維持**（ポータルは固定blue-600）
- ロゴ基本形画像（ビジュアル）（2026-06-03・本番デプロイ済み、commit f748f80） — ビジュアル編集のロゴコンセプト上に「ロゴ基本形」を新設。**複数枚＋キャプション**対応（ロゴガイドラインと同様）。DB: `brand_visuals.logo_images(jsonb配列 {url,caption})`。ポータル「見え方」では**枠なし表示＋クリックで拡大**（既存の `setModalImage` 拡大ダイアログ再利用）＋キャプション。**保存不具合2点を修正**: ①logo_images カラム追加後の PostgREST スキーマキャッシュ未reloadで保存が PGRST204 失敗 → reload実行 ②保存成功時にページキャッシュ未更新で他ページ往復すると消える → 保存後に `setPageCache` で最新反映
- 「見え方・聞こえ方」タブ名＆アイコン統一（2026-06-03・本番デプロイ済み、commit d1d7150） — ポータルのブランド表現タブ（`BrandExpressionTabs`）を「ビジュアル/バーバル」→**「見え方/聞こえ方」**に改称。見え方タブのアイコンを Palette→**Eye**（サイドメニューと統一）。管理サイドバーの「ビジュアル」アイコンも Palette→Eye に変更
- ポータル「見え方・聞こえ方」をサブメニュー廃止→タブ切替化＋ダッシュボード調整（2026-06-03・本番デプロイ済み、commit d1d7150/444db65） — ①サイドメニューの「見え方・聞こえ方」を展開式サブメニュー（ビジュアル/バーバル）から**単一リンク**（/portal/visuals）に変更。ビジュアル/バーバルの切替は **`BrandExpressionTabs`（ページ上部タブ）** に移管（visuals/verbal 両ページ先頭に配置）②ダッシュボード4象限カードの説明文調整（考え方=「…行動指針・**ストーリー**」に変更＝沿革・事業内容を省略、ページ表示は残置）③**ダッシュボード4カードを等高化**（grid に `auto-rows-fr`＋外側Linkに `h-full`。グリッド行→Link→Card の高さチェーンを通し、説明文の行数差で高さがバラつく問題を解消）
- タイムライン系ラベルを「分析」表記に統一（2026-06-03・本番デプロイ済み、commit 4614522） — ポータルダッシュボードの「あなたのブランドコミット」→「**あなたのタイムライン分析**」、管理ダッシュボードの共通タブ「タイムライン投稿」→「**タイムライン分析**」（dashboard/analytics/brand-score の3画面で共有するタブ定義をすべて更新）
- ポータル ラーニング動画グリッドをPC時3カラム化（2026-06-03・本番デプロイ済み、commit 79e88be） — `/portal/learning` の動画グリッドを `sm:grid-cols-2` → `lg:grid-cols-3`（モバイル1列/タブレット2列/PC3列）。本体＋ローディングスケルトン両方
- Disk IO 最適化（Supabase「Disk IO Budget枯渇」通知への対応）（2026-06-04・本番DB適用＋デプロイ済み、commit 0729186） — 診断: 認証(auth)系テーブルへの大量seq scanが主因（RLSが `auth.uid()` を行ごと再評価）。対応: ①不足FKインデックス**29本**追加（advisor `unindexed_foreign_keys` → 残0）②RLSの `auth.uid()/jwt()` を `(select …)` でラップ**40ポリシー**（advisor `auth_rls_initplan` → 残0、**アクセス制御ロジックは不変**・認証ユーザーで閲覧可を検証済み）③ラーニング視聴進捗のPATCH間隔 15秒→30秒（書き込み半減）。SQLは `supabase/migrations/2026...disk_io_*.sql` に記録。残課題: `multiple_permissive_policies`(31)整理、必要なら compute add-on アップグレード（有料・要ユーザー操作）
- 管理メニュー整理＋画像拡大の閉じるボタン修正（2026-06-04・本番デプロイ済み、commit a4c5c8e） — ①管理サイドバーの「設定」を独立項目→ユーザーメニュー（アバターのドロップダウン）内「サービス画面」の上に移動 ②ポータル画像拡大ダイアログ（`/portal/visuals` の `setModalImage` ライトボックス）の閉じるボタンが画像外に浮く問題を修正：既定の DialogContent クローズボタンを `[&>button]:hidden` で隠し、画像を `relative w-fit` で包んで右上角に固定したカスタム×（黒半透明丸＋白X）に置換
- ロゴ基本形=複数枚＋キャプション＋画像カード共通化（2026-06-03・本番デプロイ済み、commit 79e88be） — `brand_visuals.logo_images` を `string[]`→`{url,caption}[]` に変更（キャプション対応・旧文字列配列は読込時に自動変換）。管理は `CaptionedImageCard`（サムネ＋削除確認＋キャプション、dragHandle任意）、ポータルは `PortalImageCard`（クリック拡大＋キャプション）を新設し、ロゴ基本形・ロゴガイドライン・（管理は）ビジュアルガイドラインで共用。ポータルのロゴ基本形は**2カラム**＋画像一回り大きく（h-220）＋ロゴが切れない `object-contain`
- ブランド戦略「ターゲット概要」と「主なターゲット」の分離（2026-06-03・本番デプロイ済み、commit 79e88be） — 管理に「**ターゲット概要**」テキストエリアを新設（`brand_personas[0].target` を直接編集。旧：セグメントから箇条書き自動生成して上書き、を廃止）。「主なターゲット」＝`companies.target_segments` と**別フィールドで独立管理**。ポータル戦略は **概要文＋主なターゲット（名称＋説明カード）の両方**を表示（管理入力がポータルに出ない不整合・概要文が消える不具合を解消）。各保存後 `setPageCache` 更新
- ブランド理解度テスト (/admin/brand-score/quizzes, /portal/quiz/[id])（2026-06-04・DB本番適用済み migration 20260603000000／**本番デプロイ済み** commit ca0eae9＋AI設問の質改善 17c314f） — 記名式クイズ・AI設問生成（ブランドデータが正解キー、関係/理由型＋conceptタグ、カラーは名称/役割で出題しHEX禁止）・k匿名集計・共感×知識ギャップ分析・本人結果（解説付き学習）・生成ダイアログUI改善
- 未使用コード・依存の一掃（2026-06-04・本番デプロイ済み、commit a2f98d6） — `knip` で検出した死んだファイル9個（旧Sidebar/PortalHeaderContext/PortalStyles/StepPlaceholder×2/ProgressBar/OuterScoreSection/ui-alert/ui-checkbox）、未使用依存3個（next-themes/@radix-ui/react-checkbox/pg）、完全未使用export（color-utils 4関数・portal-subtitles・page-cache.clearPageCache・AdminStyles・pdf-styles.A4）、本番デバッグログ `console.log/debug/info` 122行を削除。`console.error/warn` は維持。tsc/eslint/next build 全パス
- コードレビュー指摘の修正（2026-06-04・本番デプロイ済み、commit 01aab2f） — xhigh recall レビューで検出した6件を修正：①AppAuth/Admin/PortalDataProvider のログアウト・ユーザー切替時に企業/プロフィール/権限 state をリセットし**前ユーザーのデータ残存（同タブA→Bログイン時の漏れ）を防止** ②AppAuthProvider の signOut を useCallback・context value を useMemo 化（Admin/Portal の contextValue も）で**不要な再レンダー解消**（user?.id 依存最適化を実効化） ③理解度テスト生成数入力を 0〜20 にクランプしサーバー normalizeCount と一致（表示と実生成の乖離解消） ④timeline スケルトンのカテゴリピルを実描画と同条件 `categories.length>1` に整合 ⑤ci-manual/data-fetcher の console.log 削除で残った空 if ブロック整理
- 提供価値を「考え方｜ブランド方針」→「接し方｜ブランド戦略」へ移動（案A：編集統合）（2026-06-04・本番デプロイ済み、commit e228f12） — 提供価値（Value Proposition）は顧客起点の概念のため、顧客との関係を扱う「接し方」へ移設。**表示**：ポータル4象限カード文言を 考え方=「MVV・バリュー・行動指針・ストーリー」／接し方=「顧客ターゲット・ペルソナ・ポジショニング・提供価値」に変更。`/portal/guidelines`（考え方）から提供価値の取得・統合・表示を**除去**、`/portal/strategy`（接し方）に提供価値（`brand_values`＋`companies.provided_values`、空なら非表示）の統合表示を**追加**。**編集**：`/admin/brand/strategy`（ブランド戦略）に提供価値（`brand_values`）編集セクションを統合（旧 `/admin/brand/values` の全削除→全INSERT保存ロジックを移植）、`/admin/brand/values` 単独ページは `/admin/brand/strategy` へ**リダイレクトで吸収**。**データテーブルは `brand_values` のまま（DBスキーマ変更なし）**。`companies.provided_values`（ID INC. 1社のレガシー text[]）の brand_values 統合・廃止、テーブル名 `brand_values` リネーム、`/portal/values` 孤立ページ整理は別途（将来）
- PWAステップ④ Web Push 通知（お知らせ公開時に配信）（2026-06-09・本番デプロイ済み、commit 8fd6717／DB適用済み migration 20260609015938／VAPID環境変数 設定済み） — お知らせ公開時に、通知をオンにした企業メンバーの端末へプッシュ通知を送る。①DB: `push_subscriptions`（user/company/endpoint/keys・RLS本人のみ・配信はservice_role）②`web-push` 導入＋VAPID鍵（env: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`、Vercel Production設定済み）③`app/sw.ts` に `push`/`notificationclick`（通知タップで該当URLへ）④API `/api/push/{subscribe,unsubscribe,announce}`（cookie認証→service_role、announceは企業adminを検証、無効購読404/410を自動削除）⑤`components/pwa/PushToggle`（通知オン/オフ・iOS非対応時は案内）を**マイプロフィール**に設置⑥`AnnouncementCreateDialog` 公開成功時に `/api/push/announce` をfire-and-forget。**iOS制約: ホーム画面追加PWA(standalone)＋iOS16.4以上＋ユーザー許可が必要**。お知らせ編集からの公開（下書き→公開）への送信は未対応（次バッチ候補）
- モバイル本文段落の引き上げ（C案）＋ヘッダーアイコン統一＋PWAアイコン白黒反転（2026-06-07・本番デプロイ済み、commit 532e6b6/85b04b1） — ①**C案（本文だけ・スマホ16px/PC14px）**: ポータル全体の本文段落（説明文・ストーリー・ミッション本文等、`leading-[1.8/1.9]`/`leading-relaxed`/`whitespace-pre-*` を伴う読ませる段落）に `text-base sm:text-sm` を付与（16ファイル37段落）。見出し/ラベル/リンク/ボタン/バッジ/エラーバナーは対象外。コードモッド `scripts/codemod-mobile-body.mjs`（ローカルのみ・未コミット）で機械適用→origin/main基点worktreeで再実行しデプロイ。実測: 375px=16px / 664px=14px（`sm:`640px切替）。**過去の「モバイルroot17px底上げ(de6656c)」は案A(4d21680)で撤去済み**で、本件がその代替（本文のみ・モバイル限定）②**ヘッダーアイコン統一**: `PortalLayoutClient` のサイドバートグル(16px)/ベル(20px)の不揃いを **22px・枠40px(size-10)** に統一（全ポータル画面共通）③**PWAアプリアイコン白黒反転**(commit 85b04b1): 濃灰背景＋白マーク→**白背景＋濃マーク#1a1a1a**（icon-192/512・maskable・apple-icon再生成、`gen-pwa-icons.mjs` の BG/FG入替）。favicon(`app/icon.svg`)は据え置き。**iOSはホーム画面再追加でアイコン更新**。 ※**2026-07-15 `68ce83b` で再反転＝濃背景#222222＋白マークへ戻し、favicon も同マークに統一**（提供画像 `public/logo-mark.png` を正本に。この③の状態は現在は無効）
- 認証前ページUI引き上げ（ログイン/サインアップ/管理ログイン）（2026-06-07・本番デプロイ済み、commit 3bca687） — グラスモーフィズム系の認証前3画面（`/portal/auth`明・`/admin/login`暗・`/signup`多段）を一回り大きく。ロゴ32→40px、サブテキスト/ラベル `text-xs→text-base(16px)`、入力欄 `h-10→h-12(48px)`＋`text-base md:text-base`（PC含め16px固定＝**iOS入力時の自動ズーム防止**）、Google/ログイン等ボタン `h-11→h-14(56px)`・主ボタン`text-lg(18px)`・Google SVG `h-5→h-6`、区切り/リンク補足 `text-xs→text-sm`。共通`Button`は未使用（生button）、共通`Input`はclassNameでスコープしadmin等のフォームに波及なし（`components/ui/input.tsx`無変更）。375px実測: 入力16px/overflow0/iosZoomSafe true。ツール認証(`/tools/*/auth`)は対象外（次バッチ候補）
- ポータルUI 案A ダッシュボード基本サイズ引き上げ（2026-06-07・本番デプロイ済み、commit 4d21680） — include.bz調査（root16px固定＋コンポーネントを最初から14-16px/見出し16-18pxで設計、ズーム・root底上げ無し）を踏まえ方針転換。**前ターンの暫定 root 17px 底上げ（globals.css）を撤去**し root を16pxに戻したうえで、**ダッシュボード(/portal トップ)1画面のみ**コンポーネント基本サイズをPC含め引き上げ。セクション見出しeyebrow×5（お知らせ/ミッション/らしさ/目標KPI/タイムライン分析）と4象限カードのサブラベル/説明を `text-xs(12px)→text-sm(14px)`、4象限アイコン `size-10/icon20→size-11/icon24`、矢印16→18、サーベイ/理解度テストバナー（dashboard専用）の見出し14→16・補足12→14・ボタン32→**44px(h-11)**。大見出し（スローガン36px/ミッションコピー24px/4象限タイトル18px）は据え置き＝間延び回避。検証: 型0・/portal 200・横オーバーフロー無し想定。**残: 実機(PC/モバイル)で適量確認→OKならポータル他画面(guidelines/strategy/visuals/personality/timeline/kpi等)とヘッダー共通部(PortalLayoutClientのトグル/ベル)へ横展開（次バッチ）**
- モバイルUI底上げ第一弾 ルートフォントサイズ（2026-06-07・本番デプロイ済み、commit de6656c） — PWA実機の「全体が一回り小さい」問題の対応。診断で原因は**ページスケールではなくデザイン上のモバイルサイズ感**と確定（viewport は `device-width/initial-scale=1` で正常・横オーバーフロー無し・375pxで `scrollWidth==innerWidth` を実測確認）。対応として `app/globals.css` に **`@media (max-width:639.98px){ html{font-size:17px} }`**（16→17px・+6.25%）を追加。PCのコンパクト設計（見出し`text-xs`等）は維持しつつ、sm未満のみ rem基準のTailwind値（文字・余白・`h-*`）が一律約6%拡大。marketing崩れ無し（リセット不要）。前ターンの診断で一時追加した `viewportFit:'cover'` は revert 済み（safe-area対応とセットの別バッチへ）。**第二弾候補（実機判断後）**: モバイル時の px固定 lucideアイコン（`size={N}` 約98件）の底上げ、タップターゲット<44px（`h-9≈38px`/`h-10≈42.5px`）の調整、`viewport-fit:cover`+`safe-area`のPWA没入対応
- PWAステップ③ 更新通知UI＋ChunkLoadError対策（2026-06-07・本番デプロイ済み、commit 1ead410） — SWを `skipWaiting:true→false`（新SWを待機）＋ `register:false`（自動登録オフ）に変更し、`components/pwa/PWAUpdatePrompt.tsx` で `@serwist/window` 手動登録→`waiting` 検知で sonner トースト「新しいバージョンがあります」→「更新」クリックで `messageSkipWaiting()`→`controlling` でそのタブのみリロード。**ChunkLoadError 自動リカバリ**（sessionStorageで1セッション1回・無限ループ防止）も同梱、devは早期return。layoutに配置。`skipWaiting:false` が ChunkLoadError 対策の本体（旧タブは更新するまで旧チャンク供給）。authBypass先頭順序・clientsClaim・fallbacks は不変＝認証バイパス回帰なし。CLAUDE.mdに skipWaiting:false が正である旨を追記。検証: webpackビルド成功・sw.jsにSKIP_WAITING待機処理＋認証バイパス確認・@serwist/window手動登録バンドル確認・型0。**残: 実機で更新フロー（waiting→トースト→更新→activated）・複数タブChunkLoadError無し・回帰(A→B非漏洩)の確認。スコープ外=Web Push/バックグラウンド同期/オフライン書込**
- PWAステップ② Service Worker導入（serwist）（2026-06-07・本番デプロイ済み、commit 6f1a9f4） — `@serwist/next`＋`serwist` でSW導入し、(a)Android Chromeのインストール要件充足（fetch handler）(b)静的資産キャッシュで再訪高速化(c)オフライン汎用フォールバック。**最重要のキャッシュ安全設計**: `app/sw.ts` で認証配下（/portal /admin /superadmin /api）を `NetworkOnly` で **defaultCache より先回りマッチ**＝認証HTML/RSC/APIは常にネット直行でキャッシュ汚染ゼロ（A→Bログイン時の前ユーザーデータ露出をディスク永続レベルで構造的に防止）。残りは `defaultCache`（静的=CacheFirst/SWR、公開ページ=NetworkFirst）。`app/offline/page.tsx`＝認証情報を含まない静的フォールバック。skipWaiting/clientsClaim/navigationPreload 有効。`public/sw.js` は gitignore（ビルド生成物）。**Next.js16のTurbopackと@serwist/next(webpack注入)が衝突するため、本番ビルドのみserwist適用＋`build`を`next build --webpack`に変更（devはTurbopack維持・SWはdev無効）**。middleware matcherに sw.js/manifest.webmanifest/offline 除外、tsconfigに webworker/typings/exclude 追加。検証: webpackビルド成功・sw.js生成・認証バイパス確認・/offline描画・型0。**残: 実機でSW登録/認証NetworkOnly/A→B非漏洩/オフライン/Androidインストールの確認。スコープ外=更新通知UI(ステップ③)・Web Push**
- PWAステップ① manifest＋アイコン＋メタタグ（2026-06-07・本番デプロイ済み、commit e92960c） — スマホ「ホーム画面に追加」でスタンドアロン（アプリ風）起動を可能化。①`app/manifest.ts`（name/short_name、`display:standalone`、theme/background=#fff、ja/business、アイコン3種、**`start_url:/portal`**＝インストール版はポータルダッシュボード起点／未ログインは /portal がクライアント側で /portal/auth へ誘導。commit 24decc3）②アプリアイコンをブランドマーク（logo.svgの幾何学シンボル・フォント非依存）で生成＝`public/icons/icon-192/512.png`(any・角丸)＋`icon-maskable-512.png`(maskable・全面塗り)＋`app/apple-icon.png`(180・iOS)③`app/layout.tsx` に `viewport`(device-width＋theme-color)・`appleWebApp`(capable/title/status-bar)・`applicationName`・旧iOS(<16.4)互換の `apple-mobile-web-app-capable`④生成スクリプト `scripts/gen-pwa-icons.mjs`(sharp)。**iOSはスタンドアロン起動可。Android自動インストールプロンプトとオフライン動作はSW必須＝ステップ②で対応予定**
- マイページ振り分け修正＋LP「無料で始める」導線（2026-06-07・本番デプロイ済み、commit e92960c） — ①`/mypage` を「スーパー管理者のみ /superadmin/companies、一般管理者・メンバーは /portal」に変更（従来は一般管理者が /admin/dashboard に着地。管理画面へはポータルのサイドメニューから遷移可）②LPヒーロー「無料で始める」のリンクを `/contact`→`/signup` に変更（下部CTA「お問い合わせ」は /contact 据え置き）
- スケルトンローディングを実構造に整合（全16ページ）（2026-06-04・本番デプロイ済み、commit 3d987d3） — ローディング時のスケルトンが実際の描画（段組数・カード/セクション数・レイアウト種別）と乖離していた問題を全ページ点検し修正。**ポータル**: dashboard=4象限を誤 `grid-cols-3`→`grid-cols-2`＋KPIバナー/統計整合、guidelines=4セクション構成、visuals=タイトル→タブ＋ガイドライン4枚目追加、strategy=移設済み行動指針カード除去、kpi=進捗バー＋3カード、timeline=廃止した常時投稿フォーム除去→フィルタ/検索行＋投稿カード。**管理**: analytics=サマリ4→3＋最終2列、brand/{guidelines,personality,values,verbal,visuals}=カード数・項目整合、brand/strategy=タイトル＋タブ＋3カード→2カード、company=タイトル除去＋ロゴ＋5項目、members-portal=タイトル除去。**スーパー管理**: companies=ヘッダーのタイトル＋ボタン除去（新規登録は右下FAB）＋先頭列を画像→テキスト。stale なタイトル/タブ要素を整理、型エラー0。**理解度テスト等の未完成分は持ち込まず、別worktree経由でスケルトン16ファイルのみ main に載せて本番デプロイ**
- ビデオラーニング カテゴリー>テーマ階層化（2026-06-08・本番デプロイ済み、commit 64546fa） — 動画を「**カテゴリー（大分類）> テーマ（学習レベル）> 動画**」の2階層で整理。DB: `learning_categories`/`learning_themes` 新設＋`learning_videos.theme_id`（RLS有効ポリシー0／**テーマ削除→動画は theme_id=NULL で生存**=SET NULL、**カテゴリ削除→テーマCASCADE**）。API（service_role）: categories/themes の CRUD＋reorder、`/api/learning/structure`（カテゴリ>テーマ>動画ネスト＋テーマ配下 **video_count 自動算出**、`?published=true`でポータル用=公開のみ＋自分の進捗）。`videos` POST/PATCH に `theme_id` 対応（**既存 `/api/learning/videos` GET 応答形は不変**）。管理 `/admin/learning`＝「動画（カテゴリー>テーマでグルーピング＋未分類）」「カテゴリー・テーマ」の2タブ＋動画ダイアログにカテゴリー→テーマ2段選択。ポータル `/portal/learning`＝階層表示＋テーマ「○本」バッジ、未分類は末尾「その他」。1動画=1テーマ（複数所属は将来）。既存 `learning_videos.category`(text) はレガシー残置（未使用）。視聴トラッキング/進捗/視聴分析(`/admin/analytics/learning`)は不変
- STP分析 Step5 をパーソナリティ診断と同パターンに刷新（2026-06-11・本番デプロイ済み、commit 61c823b） — ①**連携モーダル**: 単純なAlertDialog→項目別チェックボックス＋プレビュー＋上書き確認の `ConnectModal`（パーソナリティ診断と同UI）。3項目（セグメンテーション/ターゲティング/ポジショニング）を個別ON/OFF可、各項目の既存値があれば「⚠ 既存の○○を上書きします」警告→確定時にAlertDialogで再確認。プレフライトGET `/api/tools/stp/connect?sessionId=&companyId=` で既存値有無を判定 ②**ターゲット戦略の概要文 AI生成**: 新API `/api/tools/stp/suggest-target-summary`（メイン+サブ+セグメンテーション+購買決定要因+自社強みを踏まえ Claude が180〜260字で要約）。Step5表示時に未生成なら自動生成、`session_data.targeting.target_summary` に保存、Sparklesアイコン付きカードで表示＋「再生成」リンク ③**末尾アクション共通化**: 「branding.bz への連携」Card＋「最初からやり直す」ghost を `components/shared/ToolConnectActions.tsx` に切り出し、superadmin デザインシステム「サービス画面 > コンポーネント」カタログにも `tool-connect-actions` として登録 ④**連携API のデータ突き合わせ修正**: 旧コードは `brand_personas[0].target ← targeting.target_description`（短文）のみで `companies.target_segments` を更新せず、`/admin/brand/strategy` の「ターゲット概要」「主なターゲット」に反映されなかった。新コードは `brand_personas[0].target ← target_summary || target_description`（AI生成長文を優先）、`companies.target_segments ← buildTargetSegments(targeting, segmentation)`（メイン+サブを `{name, description}` 配列で書き込み、サブの description は segmentation から引き当て）、プレフライト/上書き確認も `companies.target_segments` の既存有無を判定するよう拡張 ⑤**PDF/連携ボタン配置**: フッターの sticky bar に「戻る | PDFをダウンロード」を配置、上方の独立カードに「連携する項目を選ぶ」（パーソナリティ診断と同レイアウト）

- **ブランド理念オントロジー実装（要素ID化＋型付き関係グラフ＋整合性チェック）**（2026-06-09〜10・本番デプロイ済み） — ブランド体系を「自由文/jsonb」から「ID付き要素＋関係グラフ」へ正規化し、AI参照・整合性点検を可能にした。**全段階 dual-run（新テーブル稼働→読取り切替→編集切替→デプロイ→旧列DROP）を厳守**。
  - **理念要素のID化 `philosophy_elements`**: mission/vision/value/action_guideline/**service（事業内容）** を 1行=1要素 のテーブルへ正規化（旧 `brand_guidelines` の mission/vision(text)・values/action_guidelines/business_content(jsonb) を撤去）。表示（card/portal/guidelines/ci-manual）・AI（targets/competitors/quiz設問/brand-data）・編集（`/admin/brand/guidelines` を行差分CRUDへ）・`tools/shared-profile`（読み書き同期）を全て新テーブルへ切替。旧列は退避テーブル `archive_brand_guidelines_*`（RLS有効・ポリシー無し=service_role限定）へバックアップ後 DROP。取得は `lib/brand/philosophy.ts`（`fetchPhilosophy`）に集約
  - **型付き関係グラフ `element_relations`**: 5種（philosophy_element/value_proposition/proof_point/governance_rule/persona）をポリモーフィック端点(kind+id)で結ぶ関係（guides/evidencedBy/promisedTo/communicatedAs/constrainedBy/conflictsWith）。端点存在＋同一company を SECURITY DEFINER トリガで担保（自己参照/重複はDB制約）。superadmin 企業詳細にオーサリングUI、AI草案生成6ルートへ関係要約を注入（`lib/brand/relations.ts`）。テックブリッジに実関係5件を投入し before/after でAI出力反映を実証
  - **整合性チェック**: 決定論（`lib/brand/integrity.ts`）。superadmin 企業詳細のオントロジーカード末尾に表示時自動実行（**読み取り専用・自動修正なし**）。結果は `ProfilingSection` の補足質問生成でも利用。**指摘0件のときは何も描画しない**（`return null`＋ラッパ `empty:hidden`）<br>※ **AI判定は 2026-07-21 に廃止**（`lib/brand/integrity-ai.ts` / `app/api/superadmin/integrity-ai/route.ts` を削除）。governance_rules を Claude が実テキスト評価して修正案を出す機能だったが、結果は表示のみでオントロジーのデータに反映されず（DB書き込みゼロ）運用に使われていなかったため。復活は履歴から戻す<br>※ **「理念から辿れない要素」の findings も 2026-07-21 に廃止**。判定関数は `lib/brand/map-data.ts` の `findUnreachableFromPhilosophy` に残り、カード上部のチップが使う（下記）
- **デザインシステム管理機能（デザイントークンDB管理＋実測ビューア）**（2026-06-11・本番デプロイ済み、commit ee66a4f/f60c701/fde3b26/7dbcf63） — include-bz から移植し、`/superadmin/design-system`（スーパー管理者専用）でサービス全体の色をパレット管理。**①基盤色までDB化**: `design_tokens`（LP用 `--ds-*` ＋ shadcn基盤 `--primary`/`--foreground`/`--border` 等のHSL成分 ＋ アプリ青 `--ds-app-*`、計52トークン）を `getDesignTokensCss()`→`app/layout.tsx` の `<style id="design-tokens">` で :root 注入、`/api/revalidate` でタグ無効化。基盤色を変えると管理/ポータル/ツール全画面が一括追従（seed=現行値の透過コピーで**見た目不変**）。履歴 `design_token_history`＋ロールバック。RLSは superadmin_all＋SELECT公開（LP SSRがanon読み）。**②ハードコード青の全置換**: `blue-500/600/700` の text/bg/border/ring 165件＋recharts/SVG/inlineの青hex 8件を `--ds-app-*` へ（色1:1一致＝blue-600=accent/700=hover/500=soft）。淡色背景 `bg-blue-50` 等・PDF・ブランドカラーデータは据え置き。**③2階層タブ＋実測ビューア**: 上位＝ウェブサイト(LP)/サービス画面(アプリ)の下線型タブ、下位＝カラーパレット/タイポ/スペーシング/コンポーネント/レイアウト/レスポンシブ/ドキュメントの7タブ。タイポ等は対象ページ（website=公開LP群／service=管理ダッシュボード等）を不可視iframeで**実測**（ハードコードの転記表を持たない）。コンポーネントは実体描画＋使用色をトークン逆引き。**④ドキュメントタブ（design.md）**: 自動サマリー（DBトークン/実測タイポ・スペーシング/コンポーネント/@media）＋手書きメモ（`design_docs` テーブル・scope別・RLS superadmin）を結合し**コピー/.mdダウンロード**。編集UIは hex/rgba/HSL成分のマルチフォーマット対応（`hsl-color.ts` で双方向変換）。DB migration: 20260611130000/140000/150000。残: 淡色背景の青・gray系623件のトークン化は将来
- **モバイルUX基準 v1.0 策定＋全画面サイジング是正（バッチ進行中）**（2026-06-16・本番デプロイ済み） — HIG(44pt)/Material(48dp)/WCAG AA(4.5:1)/iOS入力16px を `CLAUDE.md`「モバイルUX基準（確定版v1.0）」に恒久化（タップ44px/入力16px/コントラスト4.5:1/常用12px未満廃止/見出し二段階）。是正バッチ: ①フォーム系（ラベル・見出し`text-xs→text-sm`／入力`h-10→h-11`）`2563cba` ②検索入力`h-8 text-xs→h-11 16px`・フィルタ/期間ピル`text-xs→text-sm`・FABラベル/アイコン拡大`ff9cae7` ③FAB高さ`h-12→h-14`（fab.tsx・浮遊ボタンの例外XL）`4d8a179` ④§6 `--muted-foreground 45.1%→40%`（globals.css・全画面の薄グレー可読性）＋基準恒久化`235a48f` ⑤§3 タップ領域44px化＝いいね/コメント`min-h-11`(横並び維持・glyph20px)／…メニュー・コメント送信・KPI編集削除・目標編集`size-7/8/9→size-11`／サイドバー項目`h-10→h-11`／ヘッダーbell・トグル`size-10→size-11`／コメント入力`h-9→h-11`(16px)／画像削除`size-8→size-10` `f7b35dd`。**glyphは20-24px維持しヒット領域(padding/min-h)で44px確保**。残: batch3(メタ12px未満廃止)・batch4(カード/ダイアログtitle16-18px)・phase2(認証/名刺/管理)
- **Web Push 通知 ブロック時の再許可案内UX**（2026-06-16・本番デプロイ済み、commit 4268e68） — 一度「許可しない」を選ぶと `Notification.requestPermission()` が再ダイアログを出さず行き止まりだった問題を解消。`components/pwa/PushToggle` が `permission==='denied'` を検知し、iPhone/PC それぞれの設定からの再許可手順を画面内に案内（旧・赤エラーの置換）。default（ダイアログ閉じ）と denied を区別
- **管理サイドバー再編＋メニュー整理**（2026-06-24・本番デプロイ済み、commit 827a9a6/33e65a6） — ①「ブランド基本情報」→「**基本情報**」改称（サイドバー/動的タイトル/レイアウト/パンくず4箇所統一）しユーザーメニュー（アバターのドロップダウン）内へ移動 ②「アカウント管理」→「**アカウント**」に改称しユーザーメニューへ移動 ③ダッシュボードのタブで「タイムライン分析」が `timeline_enabled=false` 企業に出ない不具合を修正（brand-score と揃えて常時表示）④ラーニングの「視聴分析」タブを**ダッシュボードのタブへ移設**（新ルート `/admin/analytics/learning`）⑤サイドバーに**「構築」グループ**（STP分析/ペルソナビルダー/ブランドカラー定義＝各ツールの `/tools/*/app` へ）と**「浸透」グループ**（サーベイ管理/理解度テスト/ラーニング）を新設＝3レイヤー構造に整合 ⑥業種マスタに大分類「**コンサルティング**」追加（中分類: 経営・戦略/人事・組織/IT・DX/財務・会計/ブランド・マーケティング/その他）⑦行動指針の説明文を改行可（AutoResizeTextarea）＋D&D並べ替え対応
- **ペルソナビルダー ↔ 管理画面ペルソナの呼応（5ステップ維持版）**（2026-06-25・Stage1 commit 022164d は push済／Stage2'＋Step5レイアウトは実装済み・未コミット） — Persona Builder ツールの出力構造を管理画面 `/admin/brand/strategy` のペルソナ入力に揃えた。**Stage1**: GoalsData の「課題・悩み(challenges)」を「課題・ペインポイント(pain_points)」1欄に統合（管理画面が1欄のため。旧challengesは normalize で pain_points へ移送＝後方互換）、「主な目標(primary_goals)」を「ニーズ」表記（管理画面 needs と一致）。suggest-goals/suggest-journey の参照も pain_points に一本化。**Stage2'**: Step2に「説明(description)」欄を追加しAI生成（1〜2文の状況説明）→ description列へ（**copyAIが personaBlock の `状況:` として読む生きた入力**＝従来の「職業・規模」自動連結より太る）。connect/persona-mapping の description写像を「入力値優先・空なら連結フォールバック」に。Tier2（性別・役職・勤務先規模・媒体・性格特性・購買動機）は「詳細設定」アコーディオンに退避。Step2/Step3/Step5を管理画面の項目順（名称→年齢層・職業→説明→ニーズ→課題・ペインポイント→意思決定要因→購買障壁→ブランドへの期待）に。Step5は色分けタグの2カラム要約をやめ管理画面と同じ縦一列の読み取りビューに作り替え。**方針判断**: 当初「案3＝1枚統合カード（必然的に4ステップ）」で着手したが、北川さんが「ステップを減らしたくない」→**5ステップ維持・統合せず**に確定（統合カード Step2Persona.tsx は破棄、Step2/3は別ステップのまま管理画面レイアウトに揃える）。**追加（2026-06-26）**: ①Step2の「詳細設定」アコーディオンを廃し Tier2 を常時表示に ②Step5に **Step4Journey を `readOnly` で読み取り埋め込み**（見出し/説明/ペルソナ一覧カード/編集UI/フッターを隠した実ビュー＝感情カーブ＋タッチポイント候補プール）③**PDF出力にジャーニー設計を反映**（`PersonaPdfDocument`/export route）④連携ダイアログに **「ジャーニー設計は連携対象外＝branding.bz未反映・PDFのみ反映」** の注記。⑤**ポータル「接し方」**のペルソナ表示を改善（`PersonaCarousel`＝2.5枚スライダー／顔アイコン表示／`brand_expectations` 表示／課題チップ色をビルダーと統一(orange)＋ラベル「課題・ペインポイント」／ニーズ・課題は3件＋「もっと見る」展開アニメ／年齢層・職業を改行＋`min-h`でニーズ位置揃え／職業フォールバック説明の重複抑制）。**判断メモ**: セグメント説明文をペルソナ「説明」欄へ転化する案は、セグメント=集団の括り／説明=個人の背景で性質が違うため**不採用**。**残**: 実機E2E（使い捨て企業でconnect→管理画面反映：説明＝入力文章が入る／課題・ペイン統合が効く）。
- **同業者対策：利用規約に競合排除条項＋運用ポリシー＋テストデータ整理**（2026-06-24・本番デプロイ済み、commit 7d2df63／DB削除済み） — 同業者（ブランディング・デザイン関連事業者）の利用を防ぐ。①利用規約 `/terms` に「**第4条（同業者の利用制限）**」新設（第3条 利用登録の不承認事由・第12条 登録抹消事由にも該当を追記、条番号繰り下げ・最終更新日更新）②運用ルールを `docs/competitor-screening-policy.md` に策定（審査基準クロ/グレー/シロ・北川一次審査1営業日・**ブランディング系は一律クロ**・却下文面・遡及審査手順）③**既存12社を遡及棚卸し**＝実在の外部登録に同業者なし（branding系3社は example.com のデモ/シード、ID INC.は運営）。要確認は atelier Kiitos（業種未確認）④**テスト3社をDB削除**（テスト株式会社/株式会社あいうえお/トヨタファイナンス＝著名社名を個人gmailで登録）＋auth ユーザー4件。CASCADE/NO ACTION のFK構造を確認しアトミックなトランザクションで削除（CTD＝北川さんのdots.bz、atelier Kiitos は残置）。**残: ~~承認制ゲート~~ ✅2026-06-30実装済（`8449c72`・§開発状態「2026-06-30 リリース」参照＝新規owner登録の superadmin 全件承認制＋競合ドメイン一致で⚠警告）／AI同業判定の実装は別タスク（未着手）**

- **ブランド体系の構築度スコア（決定論・非保存）＋到達判定の根を M/V/V 常時に統一**（2026-07-21・本番デプロイ済み、commit `09a5ab2`/`d31ce29`） — 「構築完了/構築中」の二値バッジを **「構築度 N（バンド）」** に置き換え、内訳と改善導線まで出す。**①スコア（`lib/brand/build-score.ts`・新規）**: 4軸＝要素の充足30／接続性30（未接続率15＋理念からの到達率12＋一枚岩3）／裏づけ25／ルールの質15、＋未来設計ボーナス+5（**未実施でも減点しない**）。バンドは〜40構築中／〜70基盤あり／〜90充実／90超構築完了。**配点・しきい値は WEIGHTS 定数に集約**して調整可。**AI不使用・DB書き込みなし・表示時に算出**（実証進捗と同じ原則）。接続性・裏づけは既存の共有関数（`buildBrandMapGraph`／`findUnreachableFromPhilosophy`／`backing-targets`）で導出＝チップ・integrity と数字が構造的にズレない。**②UI**: バッジクリックで内訳ポップオーバー（4軸ミニバー＋「何をすると上がるか」hint＋接続性→Step4／裏づけ→Step1／ルール→Step2の「直しに行く」導線、ボーナスは別行で控えめ）。**「構造的完成度であってブランドの良し悪しではない」旨を注記**。企業一覧にも構築度列（会社ごとにクエリせず7テーブルを全社一括取得しメモリ集計・0点は「未着手」）。**③採点の欠陥を実データ検証で1件検出・修正**: 理念が1件も無い会社は到達判定がスキップされ「到達12点」が無条件に付いていた（2要素の会社が14点）→ `hasRoots` を入力に追加し根なしは到達0点＋専用hintへ（MEGUTAMA 14→2）。**④到達判定の根を M/V/V 常時に**（`d31ce29`）: 旧仕様は mission→vision→value の優先順位フォールバックで、ミッションがある会社ではビジョン/バリューが「理念に届かない」側に落ちる直感に反する挙動だった。**共有関数 `findUnreachableFromPhilosophy` 1本の修正でチップと構築度が同時に揃う**ことを確認。ID INC. 実測（同一データで根定義だけ切替）＝理念に届かないチップ 5→2件・接続性 20→21/30。**⑤実データ**: ID INC. 85充実／リィツ76充実／テックブリッジ51基盤あり(+3)／要素ゼロ4社は0・未着手。ユニット（`build-score.test.ts`）で空企業・満点・裏づけ欠落・未来設計±・島減点・バンド境界・根定義を固定
- **オントロジーの構造是正（語彙・意味制約・裏づけ一本化・出所構造化）**（2026-07-21・実装済み／DBマイグレーション3本適用済み） — 「オントロジーとして意味があるか」の忖度なし見直しで見つかった構造問題を一括是正した。**①語彙の整理**: 定義10種のうち実使用3種だった関係語彙を実態に合わせた。communicatedAs は廃止（現行の要素6種に「表現物」にあたる種が無くレンジを定義できない。既存0行・UI/AI/DBすべてで作成不可に）。**未来設計4種（aspiresTo/requires/toBeEvidencedBy/verifies）は読み取り側だけあって作成手段が無かった**（KIND_ORDER に獲得目標が無くステータス過去記録の「端点に選べるよう拡張」は実装漏れ）→ 関係フォームに獲得目標を追加し実機で verifies 作成→削除を確認。promisedTo/conflictsWith はスキャンプロンプトに明示的な検討指示を追加＋「約束されていないペルソナ」info チェック新設（integrity.ts）。**②意味制約（ドメイン/レンジ）**: (relation_type, source_kind, target_kind) の許可組をDBトリガ `validate_element_relation_semantics`（20260721163054）で強制。コード側の正は `elements-catalog.ts` の `RELATION_RULES`（トリガと同内容を保つ契約・`relation-rules.test.ts` で回帰）。関係フォームは種別を選ぶと起点/対象セレクトが許可種別だけに絞られる。validateCandidates も同基準で候補を破棄。**あわせて relation-scan の実バグ2件修正**＝プロンプトが「6種のみ」と言いつつ全10種を列挙し4種の説明が undefined／AIが aspiresTo 等を提案し得た（当日の焦点スキャンで実際に混入を確認）。**③裏づけの一本化**: proof_points.value_proposition_id（FK）と evidencedBy 辺の二重表現を辺に統一（20260721163027 でFK3行→辺移行・FK全null化）。ProofPointsSection の裏づけ対象セレクトはUX不変のまま永続化先を辺に変更（syncBacking・関係グラフ側で手作りした他の辺は温存）。読み側は従来から union 済みのため無変更で動く。**FK列は残置＝コードデプロイ後にDROP予定**。**④出所の構造化**: note の「AI提案:」文字列ハックを廃止し、source('manual'/'ai_scan')・ai_confidence・created_by 列を新設（20260721163009・36行バックフィル済み）。一覧に「AI提案」バッジ表示。**検証**: 全46行がトリガ適合（BEFORE UPDATE 全行通過）・無効組/communicatedAs の拒否をSQLで実証・tsc/build/ユニット全pass・テックブリッジで作成→削除ロールバック・リィツの裏づけ3件が辺から正常表示・ID INC. でバッジ表示確認。**⑤残（バックログ）**: philosophy_elements の service（事業内容・20行）分離は影響範囲が製品全面（表示/AI/ツール同期）に及ぶため未実施＝要計画。FK列DROP・deprecated読み替え（map-data の proofFks 引数等）の掃除もデプロイ後
- **オントロジーカードの引き算＋「繋がっていない」を1か所に集約**（2026-07-21・実装済み） — 点検結果が Step5 の下に数千px積み上がり「補足質問の一部」に見えていた状態を解消した。①**AI判定機能を削除**＝`lib/brand/integrity-ai.ts` と `app/api/superadmin/integrity-ai/route.ts` を削除、`IntegrityCheckSection` から実行ボタン・結果表示・修正案コピーを撤去。**削除の根拠は「DB書き込みが1件も無い（insert/update/upsert/delete ゼロ）＝オントロジーのデータに一切影響しない」ことをコードで確認**②**「理念に届かない N件」チップを新設**（未接続チップと提供価値未登録チップの間）＝到達可能性の判定を `lib/brand/map-data.ts` の **`findUnreachableFromPhilosophy` に切り出して共有**し、`integrity.ts` のインライン実装を廃止。チップは「線が1本も無いもの＝未接続」を除いた残り（＝島）だけを出すので**2つのチップの件数が重ならない**（ID INC. 実測: 未接続13＝理念6+表現ルール4+ペルソナ3／島15／重複0）③参考(info)の「理念から辿れない要素」26件を廃止（チップが同じ情報＋クリックで該当ステップへ飛ぶ導線を持つため）④**指摘0件のときは点検セクションごと非表示**（「オントロジー構築完了🎉」と緑ボックスが二重になっていた）⑤補足質問が0件かつ保留0件のときのメッセージを非表示（保留がある場合は呼び戻す導線があるので残す）⑥「編集する」ヘッダのパディングを16pxに統一、「編集する」と「この体系で、言葉はどう変わる？」の上下を入れ替え。**注意した点**: 25−13＝12 は誤り（25はペルソナ除外・13は含むため二重に引く）。実データで突き合わせて15と確定した

---

## 4. 残タスク

### 🆕 営業資料 v2 との突き合わせ（**未 push**・2026-08-31 その3）

- [x] ~~**Free で PDF出力・掲示反映ができないのに料金ページに書いていなかった**~~ ✅ Free カードの注記に明記
- [x] ~~**アップセル帯の副題がカードと食い違っていた**~~ ✅ カード側に統一
- [x] ~~**マイクロフィードバックが機能一覧から漏れていた**~~ ✅ features の発信グループに追加
- [x] ~~**資料側の社名の割れ**（Cowork）~~ ✅ p15・p16 とも「ID INC.（アイディ株式会社）」に統一（2026-08-31・Cowork）
- [x] ~~**資料 p4 の Free 記述の誤り**（Cowork）~~ ✅ フッターを「Freeプランで各ツール月3回まで、生成結果を画面で確認できます。／PDF出力とブランド掲示への反映は Standard 以上。回数も無制限になります。」に差し替え、冒頭の副文も「Standard 以上なら、PDF出力と本体への反映まで一本道です。」に修正（2026-08-31・Cowork）。**あくまで実装に寄せた記述**なので、下の商品判断で Free に開放するなら資料も戻すことになる
- [x] ~~**p4 と同じ誤りが p13・p14 にも残っていた**~~ ✅ **p4 を直しただけでは足りなかった**。実ファイルを再走査してp13 STEP1「無料で利用開始」直下の「回答内容は、そのまま企業情報として自動反映されます」と、p14 Q&A「入力する情報が多くて大変では？」の「出力はそのままブランド掲示に反映されます」を発見。3枚とも「Standard 以上で」と明記して修正済み（Cowork）。⚠️ **同じ主張が何枚に散っているかは、1枚直した時点で全文検索して確かめる**
- [x] ~~**資料 p8 の機能一覧にマイクロフィードバックが無かった**~~ ✅ サイトに追加した結果、資料の発信グループだけ2件で取り残されていたので3件に揃えた（Cowork）。アイコンもサイトと同じ MessageSquareHeart。3項目目で2行になりサポート群と重なったため、サポートのブロックを 5.98in→6.42in に下げて解消済み
- [ ] **Free の PDF出力・本体連携をどちらに寄せるか（商品判断・未決）** — 現状は「実装どおり Standard 以上」で資料・サイトとも表記を統一済みなので、**期待が割れている状態はいったん解消**している。⚠️ **Free に開放する方向に決まった場合、戻すのは4箇所**＝(a) `lib/billing/entitlements.ts` の `pdfExport` / `portalSync` を `free: true` に (b) 営業資料 p4（冒頭とフッターの2箇所）・p13 STEP1・p14 Q&A (c) 料金ページ Free カードの注記「生成結果は画面で確認できます。PDF出力・本体への反映は Standard 以上」 (d) FAQ「無料で使えますか？」の「PDF出力や本体連携をご利用の場合は、Standard以上のプランが必要です」。**4箇所を同時に直さないとまた割れる**
- [ ] **導入フローの「審査」がサイトだけ無い** — 資料 p12 は STEP2 に審査を明記、サイトのFAQは「最短即日・3ステップ」で審査に触れない。**北川さんの指示で保留**（サイト・資料とも未着手）
- [ ] **push（本番反映）** — 明示指示待ち

### 🆕 レイヤー構成の反転修正（**未 push**・2026-08-31 その2）

- [x] ~~**レイヤー構成が逆だった**~~ ✅ 「構築 → 浸透 → 発信。計測が、その全部を貫く」に統一。LP・features・ドキュメント3件
- [x] ~~**LP に横串を示すブロックが無かった**~~ ✅ 3カードの下に幅いっぱいの帯を新設（グリッドと左端・幅が一致、すき間20px）
- [x] ~~**LPの柱セクションが目視未確認だった**~~ ✅ Cowork 側（Claude in Chrome）で localhost:3004 を開いて確認済み。柱3枚が横一列で揃い、計測の帯は左右端がグリッドと一致・カード間と同じ間隔・上端に3枚を貫く細い線あり。/features のグループ順（構築→浸透→発信→計測→サポート）と計測の lead も想定どおり
- [ ] **push（本番反映）** — 明示指示待ち。tsc 0件・build 緑・4ページ確認まで完了済み

### 🆕 営業資料との整合（**未 push**・2026-08-31）

- [x] ~~**レイヤー構成が3区分でサイトと資料が食い違っていた**~~ ✅ 4区分（構築／浸透／計測／発信）に。LP・features・ドキュメント3件を同時更新
- [x] ~~**「無料で始める」が /tools/colors でログイン画面の行き止まりだった**~~ ✅ /signup に統一
- [x] ~~**用語wiki の語数が 238語 表記**~~ ✅ 280語（本番DB実測）
- [x] ~~**特商法の所在地に番地が無かった**~~ ✅ 神奈川県川崎市高津区久本3-6-7-303
- [ ] **push（本番反映）** — 明示指示待ち。tsc 0件・build 緑・4ページ目視確認まで完了済み
- [x] ~~**社名の割れ**~~ ✅ 正式名称は **「ID INC.（アイディ株式会社）」**（長音符なし）と確定。サイトの5箇所（特商法・利用規約×2・プライバシーポリシー×2）を修正済み。⚠️ **営業資料の「ID 株式会社」は誤りなので資料側の修正が必要**（Cowork 側の作業）
- [ ] **審査フローの事前開示**（保留）／**FAQ「導入にどのくらい時間がかかりますか？」**（現状維持）— 今回の対象外
- [ ] **LP の柱セクションで「発信が横串」であることを表現するか** — 4枚を横並びにしただけなので、資料の代表見出し「構築 → 浸透 → 計測。発信が、その全部を貫く」のうち**横串のニュアンスは画面に出ていない**。見出しか小見出しで補うかは未依頼のため触っていない

### 🆕 「私たちの『らしさ』」カードの表示スイッチ（**push済み・本番green**・2026-08-16 その14）

- [x] ~~**ダッシュボードのショートカットカードを畳めるようにする**~~ ✅ サイドバー見出しの右にスイッチ（既定は非表示・localStorage で端末ごとに記憶）
- [x] ~~**切り替えでスペースが一瞬で詰まる／広がる**~~ ✅ `grid-rows-[0fr]↔[1fr]`＋余白＋不透明度を 300ms で補間
- [x] ~~**スイッチと見出しの上下中心がずれて見える**~~ ✅ `-translate-y-[0.5px]`（箱の中心は元から一致・和文の字面中心とのズレ 0.6px を補正）
- [ ] **スイッチはダッシュボード以外の画面でも押せる** — 効果はダッシュボードにしか出ないので、切り替えた結果がその場で見えない。ダッシュボード表示中だけ出す（`useRegisterPortalSearch` と同じくページ側から登録させる）案があるが、**北川さんの判断待ち**

### 🆕 seed migration に消し残した design_tokens の行（2026-08-16 その13の付随）

- [ ] **`--ds-accent-primary` の INSERT が `supabase/migrations/20260611130000_create_design_tokens.sql:131` に残っている** — `b9a2527` で本番DBの行とコード参照は消したが、**migration から作り直すと復活する**。適用済み migration は書き換えない原則なので、消すなら削除の migration を1本足すことになる。**実害は無い**（コードから参照が無く、スーパー管理のデザインシステム画面に孤児の行が出るだけ）ため、足すかどうかは保留

### 🆕 戻り導線／表の折り返し／タイムラインの余白と検索（**push済み・本番green**・2026-08-16 その12）

- [x] ~~**詳細画面から一覧へ戻る導線がパンくずに無かった**~~ ✅ スーパー管理は `section` を `{label, href}` に、ポータルは `parent` を追加
- [x] ~~**パンくずとページ内「← 戻る」が二重にあった**~~ ✅ ページ内リンクを削除
- [x] ~~**一覧表の列名・バッジ・社名が折り返して行の高さが揃わなかった**~~ ✅ 固定文言は `whitespace-nowrap`／可変長の社名は `truncate` ＋ `title`
- [x] ~~**タイムラインの投稿カードの内側とカード間が16pxだった**~~ ✅ 12pxに（方向指定 `px-3 py-3` で `globals.css` の統一ルールを回避）
- [x] ~~**いいね/コメントの行が高かった**~~ ✅ PC だけ1段階詰め（モバイルは44pxを維持）
- [x] ~~**検索欄が投稿の上に常時居座っていた**~~ ✅ ヘッダーの虫眼鏡に移しクリックで展開（context で画面側から登録）
- [x] ~~**フィルタのバッジが折り返して投稿を下へ押し出していた**~~ ✅ 横1列＋横スクロールに
- [ ] **フィルタの横スクロールに気づく手がかりが無い** — スクロールバーを隠しているため、マウス利用者は Shift+ホイールでしか動かせない。右端のフェードか矢印ボタンを足すかは**北川さんの判断待ち**
- [ ] **入力サポートのバナーのカード全体クリックが目視未確認**（その10から継続） — **CTD（0/4）でログインして `/admin/setup`** を開く必要がある
- [ ] **同じパンくずの戻り導線が未適用の詳細画面** — `/portal/learning/[id]`／`/portal/quiz/[id]`／`/portal/survey/[id]`。お知らせと同じ形にできる（未依頼）

### 🆕 ポータルのスコア表示の仕上げ／お知らせの未読フィルタ（**push済み**・2026-08-16 その11）

- [x] ~~**インナースコアとスコア推移が別々のカードで並んでいた**~~ ✅ 「カードの中にカード」に（外＝グレー地の器／中＝白カード2枚）
- [x] ~~**器に見出しが無かった**~~ ✅ 「ブランドスコア」を追加（下の余白 12px・列間は 16px のまま）
- [x] ~~**グラフが上に寄って下に余白が余っていた**~~ ✅ 見出し下の残り高さの中央に（上下 18px）
- [x] ~~**読んだお知らせがダッシュボードに残り続けていた**~~ ✅ 未読のみ3件に（取得は limit 20）
- [ ] **入力サポートのバナーのカード全体クリックが目視未確認**（継続） — このバナーはオンボーディング未完了のあいだしか出ないため、セットアップ済みの ID INC. では確認できない。**CTD（0/4）でログインして `/admin/setup`** を開く必要がある

### 🆕 新 split の見え方調整（**push済み**・2026-08-16 その10）

- [x] ~~**スコア推移の凡例に「総合」「アウター」が残っていた**~~ ✅ `<Line>` ごと `isFull` で出し分け（系列データを null にするだけでは凡例が出る）
- [x] ~~**ポータルのスコア表示が縦に間延びしていた**~~ ✅ 2カラム化（左=インナースコア／右=スコア推移・`readOnly && !isFull` 限定）
- [x] ~~**系列1本なのに凡例と「塗りつぶした点／白い点」の説明が出ていた**~~ ✅ `isFull` のときだけ出す
- [x] ~~**ブランドスコアの塊だけ下余白が32pxだった**~~ ✅ 他カードと同じ16pxに
- [x] ~~**ポータルの Premium で同じスコアが2回出ていた**~~ ✅ 総合スコアカードを basic では出さない
- [ ] **「初回測定／前回比」が basic のポータルで出なくなった** — 総合スコアカードにしか無かった表示。必要ならインナースコアカードへ移す
- [ ] **入力サポートのバナーのカード全体クリックが目視未確認** — `/admin/setup` がログイン切れで確認できていない。ビルド・tsc・eslint は通過済み。次にログインしたとき押下してダイアログが開くことを確認する ⚠️ このバナーはオンボーディング未完了のあいだしか出ないので、確認には CTD（0/4）でのログインが要る

### ✅ 無認証で叩けた company-scoped API — **全件対応済み**（2026-08-16 その9）

**⚠️ 当初「27件」と記録したが誤り。** grep が `auth.getUser` の文字列しか見ておらず、
それをラップしたヘルパー（`getAdminContext` / `getMemberContext`）や秘密認証を
「無認証」と誤判定していた。コードを読んで実測した正しい内訳は以下。

| 区分 | 件数 | 判断 |
|---|---|---|
| 仕様として公開（匿名回答・受験・匿名ツール・計測ビーコン・サインアップ） | 8 | ⭕ そのまま |
| **偽陽性（実は認証済み）** | **11** | ⭕ learning系4／クイズ受験系4／quizzes participants・results／`external/brand-ontology`（秘密認証） |
| **要対応 → 対応済み** | **25** | ✅ 下記3バッチで全件塞いだ |

- [x] ~~**クエリで company_id を受ける7ルート**~~ ✅ `121e14e` `requireCompanyMember()` を `guardCompanyFeature` の前に通す
- [x] ~~**リソース ID から company_id を引く15ルート**~~ ✅ `febd0c2` `requireResourceCompany()`（リソースを引く→所属照合）
- [x] ~~**連携API 3ルートが company_id / user_id をクライアントから受けていた**~~ ✅ `6d868cd` `requireCallerCompany()` でセッションから解決。`personality` は userId を受けており、他人の ID を渡すだけでその人の管理者権限になっていた
- [x] ~~**AI設問生成2ルートが完全に無認証**~~ ✅ `63686c4`

検証はいずれも3点セット（未ログイン401／他社ID 403／正当な呼び出し元200）＋画面の目視。

### 🆕 計測のプラン split 入れ替え（**push済み・本番green**・2026-08-16 その8）

- [x] ~~**インナーサーベイが Enterprise 限定だった**~~ ✅ Premium へ（自社だけで完結する自己計測なので）
- [x] ~~**識別子が実態とズレていた**~~ ✅ `brandScoreBasic→brandScoreInner` / `brandScoreFull→brandScoreIntegrated`
- [x] ~~**BrandScoreView の basic がアウター表示だった**~~ ✅ インナー表示へ反転。アウター・市場浸透・総合はカードごと非表示
- [x] ~~**料金ページ・features・FAQ・用語集が旧 split のまま**~~ ✅ すべて追随
- [ ] **「部署別ヒートマップ」は料金ページに載せていない** — 機能ごと削除済み（`48283cd`）で実装が無い。載せるなら先に機能を作る必要がある
- [ ] **既存 Premium 4社（ID INC. / CTD / MEGUTAMA / リィツ）がインナーサーベイを使えるようになった** — 値付けの前提が変わるので、営業・請求側の認識合わせが要るかもしれない


- ✅【対応済み 2026-06-25・方針①採用】管理画面のペルソナ入力項目が Persona Builder の出力に追いついていない（不足フィールド多数）（2026-06-24 調査 → 2026-06-25 実装） — **対応**: 方針①（管理画面に不足フィールドを追加＋ビルダーを管理画面レイアウトに呼応）で実装。①Tier1（意思決定要因/購買障壁/ブランドへの期待）を離散カラム化＋管理画面で編集可（commit 891f854・本番反映済）②ビルダーを管理画面に呼応＝課題・悩み＋ペインを「課題・ペインポイント」1欄に統合（Stage1 022164d）、主な目標→ニーズ表記、説明欄追加でcopyAIの`状況:`入力を強化、Tier2は詳細設定アコーディオン、Step2/3/5を管理画面の項目順に（Stage2'・実装済み未コミット）。**5ステップ維持**（1枚統合カード＝4ステップ化は北川さん判断で不採用）。**残**: 実機E2E（使い捨て企業でconnect→brand_personas→管理画面反映の通し確認）。 — **当初状況**: 管理画面 `/admin/brand/strategy` のペルソナ編集は discrete 6項目のみ（`PersonaItem` ＝ name / age_range(年齢層) / occupation(職業) / description(説明) / needs(ニーズ) / pain_points(課題・ペインポイント)）。一方 Persona Builder ツールの最終出力（`app/tools/persona/app/[sessionId]/components/persona-types.ts` の `Demographics` ＋ `GoalsData` ＋ `JourneyMap`）はもっと多い。
  - **ツール出力にあって管理画面に入力欄が無いフィールド**: 勤務先規模(company_size) / 居住地(location) / 性格特性(personality_traits) / 主な目標(primary_goals) / 課題・悩み(challenges ※pain_points とは別) / 意思決定要因(decision_factors) / 購買動機(buying_motivation) / 購買障壁(buying_barriers) / ブランド期待(brand_expectations) / 成功定義(success_definition) / 性別(gender) / 役職(company_role) / 趣味(hobbies) / 利用メディア(media_channels) / 日常(daily_routine) / 口癖(quote) / カスタマージャーニー(journey_map)。
  - **重要**: これらは連携 `/api/tools/persona/connect` で `brand_personas` の **`persona_data`(jsonb) ＋ `journey_map_data`** に**保存はされている**が、管理画面UIが**表示も編集もしていない**＝ツールで作ったリッチな内容が branding.bz 側では見えない／欠落して見える。discrete列への写像は `lib/tools/persona-mapping.ts`（needs / pain_points / age_range / occupation / description）。
  - **要対応（判断ポイント）**: ①管理画面のペルソナ編集に不足フィールドを追加し `persona_data` を表示・編集可能にする か ②連携で来たリッチ情報は「読み取り専用表示」に留める か。まず①②どちらの方針かを北川さんに確認 → 実装。
  - **関連ファイル**: 管理=`app/admin/brand/strategy/page.tsx`（`PersonaItem` 型・fetch・handleSubmit）／ ツール型=`app/tools/persona/app/[sessionId]/components/persona-types.ts`／ 連携=`app/api/tools/persona/connect/route.ts` ＋ `lib/tools/persona-mapping.ts`／ DB=`brand_personas`（discrete列 ＋ `persona_data` jsonb ＋ `journey_map_data`）。

### ✅ プラン制限の実装（Phase 1〜4 完了・**本番反映済み**・2026-08-14）

> 指示書: Drive `260716_プラン制限実装_指示書_v1.md`（v1.4 まで）
> 料金ページ側: `260716_料金ページ改訂案_v2.md`（Card削除＋Enterprise）／`_v3.md`（Free掲示編集＋計測のEnterprise移動）

**何が完成したか**: プランは DB の `companies.plan` に持ち、判定は `lib/billing/entitlements.ts` 一箇所。
API・RLS・UI の三層で止める。UI でボタンを隠すだけでは URL 直叩きで通るため、必ずサーバー側に置いた。

| 層 | 実装 |
|---|---|
| DB | プラン4カラム＋12社の配置。migration 6本適用済み |
| 判定 | `entitlements.ts`（18機能×5プラン）。`getEffectivePlan` の遅延評価で期限切れは free に落ちる |
| API | 51箇所のゲート。403 `{plan_required, requiredPlan, feature}` ／ 429 `{usage_limit_exceeded}` |
| RLS | 10ポリシーにプラン条件（`company_plan_allows()`・期限切れ判定込み）。ブラウザ直書き経路を塞ぐ |
| 運用 | superadmin でプラン・期限・デモ区分を編集（service_role の API 経由） |
| UI | ロックバッジ14項目・アップセル面11画面・期限予告・構築ツールの残り回数 |

**ゲート全体表（何がどのプランで止まるか）**

| 機能 | free | standard | premium | enterprise |
|---|---|---|---|---|
| 構築ツール（colors/persona/personality/stp） | ✓ 各月3回 | ✓ 無制限 | ✓ | ✓ |
| ブランド掲示 閲覧／編集 | ✓ | ✓ | ✓ | ✓ |
| PDF出力／本体連携／CIマニュアル | 403 | ✓ | ✓ | ✓ |
| スマート名刺／閲覧解析／マイクロFB | 記録停止 | ✓ | ✓ | ✓ |
| タイムライン／お知らせ＋Web Push | 403＋RLS | ✓ | ✓ | ✓ |
| ビデオラーニング／理解度テスト／KPI | 403 | 403 | ✓ | ✓ |
| ブランドスコア（簡易版・アウターのみ） | 403 | 403 | ✓ | ✓ |
| インナーサーベイ／ブランドスコア完全版／市場調査 | 403 | 403 | 403 | ✓ |
| メンバー上限 | 5 | 50 | 300 | 無制限 |

※ `card` は販売終了。DB の check 制約には含めず、コード側の `Plan` 型にのみ温存している（Early Access 終了時の
ダウングレード受け皿として復活させる可能性があるため。復活は `SELLABLE_PLANS` に1語足すだけ）。
※ 名刺の記録系4本は「閲覧は残し記録だけ止める」方式で、403 ではなく 200＋`recorded:false` を返す。
配布済みの QR を殺さないための判断。

**併せて塞いだ既存の穴（プラン制限とは独立）**

1. 🔒 `goal_kpis` / `goal_periods` が全ログインユーザーに開いていた（マルチテナント分離漏れ）
2. 🔒 `personal_goals` に他社の company_id を差し込めた／更新で付け替えられた
3. 管理画面の「メンバーの目標を削除」が 0 行削除で静かに失敗していた
4. いいね解除が DELETE ポリシー欠落でずっと動いていなかった
5. `.delete()` の 0 行を成功扱いしていた9箇所（RLS で弾かれても成功トーストが出る）

**⚠️ 未着手（Early Access 開始までに要判断）**

- [ ] **Stripe 連携** — 決済・サブスクリプション。EA 期間中（90日）は不要だが、終了までに必要。
      現状プランを変える手段は superadmin の編集UIだけ
- [ ] **期限切れの日次 Cron** — `plan_expires_at` を過ぎた企業を free に落とすバッチ。
      **機能面は `getEffectivePlan` の遅延評価で守られている**ので急がない。
      必要なのは「契約プランの表示と実態を揃える」データ整合の観点のみ
- [ ] **プラン変更履歴テーブル** — 現状 `companies.plan` を上書きしており、
      いつ誰がどのプランへ変えたかが残らない（API の console.info のみ）。
      課金と突き合わせる段階で必要になる。`company_plan_history`（company_id / from / to /
      changed_by / changed_at / reason）を想定

**次のマイルストーン**: Early Access 開始（LP 実装・参加者への `plan='premium'` ＋ `plan_expires_at`=登録+90日 の付与運用）。
EA 専用の分岐はコードに作っていない＝実効プラン判定がそのまま働き、期限が来れば自動で free に落ちる。

### 🆕 入力サポートのログイン内完結／メール通知／スコア推移のプラン修正（**push済み・本番green**・2026-08-16 その7）

- [x] ~~**入力サポートの申し込みが外部の問い合わせフォームに出ていた**~~ ✅ ログイン内のダイアログで完結（`/contact?subject=setup-support` への遷移を廃止）
- [x] ~~**「いつ空いているか」を文章にする手間で止まる**~~ ✅ Popover + Calendar で候補日を最大3件積む方式に。「日程はお任せします」も併設
- [x] ~~**依頼に気づけるのがスーパー管理画面のバッジだけ**~~ ✅ 依頼が入ったらメールで通知（入力サポート・プラン変更の両方）
- [x] ~~**相談を受けても「どこで詰まったか」が分からない**~~ ✅ 依頼時点のセットアップ進捗をサーバ側で数え直して控える
- [x] ~~**プランを問わず出る画面で「担当者」と書いていた**~~ ✅ Free に専任担当は付かないので「ID INC. のスタッフ」に。プラン変更ダイアログも同様に
- [x] ~~**Premium にスコア推移グラフが出ていた**~~ ✅ `BrandScoreView` の推移カードだけ `isFull` の囲みが漏れていた
- [x] ~~**ポータルのお知らせが1件＝1カードで細切れに見える**~~ ✅ 1枚のカードに集約し、行を1行に畳んで見出しもカード内へ
- [ ] **`brandScoreFull` の他の要素にも `isFull` の囲み漏れが無いか未確認** — 料金ページの Enterprise 欄は「統合ブランドスコア／スコア推移の自動記録／ギャップ分析／市場調査手配」の4つ。今回直したのは推移のみで、**ギャップ分析は未確認**
- [ ] **`escapeHtml` が5ファイルに重複したまま** — 今回は7つ目を作らず `lib/mail/superadmin-notify.ts` に共通化したが、既存の contact / signup / join-company / superadmin-signup-requests / members-join-requests は手つかず。別タスクとして起票済み
- [ ] **スーパー管理 `/superadmin/support-requests` の画面を目視できていない** — 検証セッションが superadmin 権限を持たないため。API のゲート（401/403）とビルドは確認済み

### 🆕 市場調査スコアの精度／PWA更新トースト（**未 push**・2026-08-25）

- [x] ~~**「その他」「あてはまるものはない」を競合として拾っていた**~~ ✅ 受け皿行を除外（`benchmark.competitorMax` と順位の母数が汚れていた）
- [x] ~~**段階構成が変わった年を素で引き算していた**~~ ✅ 共通段階で `computeMarketScore` を掛け直し、構成差は注記で出す
- [x] ~~**「新しいバージョンがあります」のトーストが邪魔**~~ ✅ 廃止し SW を即時有効化に
- [ ] **push していない** — 本番はまだ受け皿行を競合に含んだままで、ベンチマークと順位がずれている
- [ ] **既存の割り当てには遡って効かない** — 除外が効くのは自動割り当ての提案時。すでに `market_survey_stage_scores` に保存済みの調査は、割り当てをやり直さないと競合セットが古いまま
- [ ] **`portal/market-survey` に推移が無い** — 単一調査の結果ビューのみ。ポータルで推移を見るのはダッシュボード（`BrandScoreView`）
- [ ] **前回比は整数まるめ** — `computeMarketScore` が `clamp`（`Math.round`）を通す既存挙動。小数で見たい場合は別途
- [ ] **PWA の切り替えは1回だけ旧SWを経由する** — 既存ユーザーは次回アクセス時に古いSWからの入れ替えが1度起きる。その回だけトーストが出る可能性がある

### 🆕 middleware の 504（**push済み**・2026-08-21）

- [x] ~~**外部通信の1回のハングでサイト全体が 504 になる**~~ ✅ `getUser()` に4秒タイムアウト＋失敗時は素通り
- [x] ~~**公開ページまで middleware を通していた**~~ ✅ matcher をログインが要る経路だけに絞った
- [x] ~~**タイムアウト値の根拠が無い**~~ ✅ 実測 p99 71ms / max 161ms に対して約25倍。数値はコード内コメントに記載
- [ ] **`[middleware] セッション更新をスキップ` の監視** — 本番ログにこれが出るなら 4 秒が短すぎるか別の問題。出ないことを継続的に見る
- [ ] **原因側は未解明のまま** — Auth は正常でリクエストが Supabase に到達した形跡も無い。edge → Supabase 間のハングと推定しているだけで、再発するかは分からない
- [ ] **除外した API の判断は静的な確認どまり** — cron・card-view・analytics が cookie セッションに依存していないことはコードで確認したが、本番トラフィックでの検証はしていない
- [ ] **`middleware` ファイル規約が deprecated** — ビルド時に「Please use "proxy" instead」の警告が出ている（今回の修正とは別件・既存）

### 🆕 スーパー管理の整理（**push済み**・2026-08-16 その13）

- [x] ~~**「残存アカウントの削除」で他社の名刺プロフィールを消せた**~~ ✅ スーパー管理へ移設し、対象を孤立 auth ユーザーだけに閉じた（profiles には触れない）
- [x] ~~**承認待ちが別ページで、企業を見にきたときに気づけない**~~ ✅ 企業一覧の上のアコーディオンへ
- [x] ~~**企業詳細が縦に長く、性格の違うものが混在**~~ ✅ 基本情報／ブランドオントロジーのタブに
- [x] ~~**「企業」と「ブランド」で同じものを2つの名前で呼んでいた**~~ ✅ 表示語とアイコンを統一
- [x] ~~**却下確認がブラウザの confirm**~~ ✅ AlertDialog に（誰にメールが行くかも確認画面に出す）
- [ ] **孤立アカウント画面は「取得失敗」と「0件」が同じ見た目** — 実際に7件あるのに「ありません」と出た事象があり、原因を特定できていない（作成直後で dev サーバー未コンパイルの可能性）。取得に失敗したらそう表示する作りにしたい
- [ ] **cospark の削除を本人に知らせていない** — 承認済みアカウントのため却下フローを通っておらず通知経路が無い。次にログインしようとした時点で入れなくなる
- [ ] **デモ判定は手作業のまま** — 3社の is_demo を手で訂正した。登録経路から自動で決まる仕組みは無い

### 🆕 ポータルの表示速度（**push済み・本番green**・2026-08-15 その6）

- [x] ~~**ポータルの初回表示でスケルトンが長く出る**~~ ✅ データ取得の3段直列を2段に（往復を1回削減）
- [ ] **改善幅を実測できていない** — ブラウザからユーザーのセッションで投げるクエリのため、ログイン無しでは再現できない。北川さんの体感待ち。変わらなければ Network タブで最遅リクエストを特定するところから
- [ ] **管理者は `/api/onboarding` も待ってから描画される** — オンボーディング中かどうかでダッシュボードの中身ごと差し替わるため構造上やむを得ない。API 自体は 10〜30ms なので主因ではなさそう
- [ ] **自分の投稿を `limit` なしで全件取得している** — 統計の期間フィルタをクライアント側で切り替えるための作り。今の規模では問題ないが、数千件になると効いてくる

### 🆕 セットアップ案内の一本化／タブ整理（**push済み**・2026-08-15 その5）

- [x] ~~**管理画面とポータルでセットアップ案内の見え方が2種類ある**~~ ✅ `OnboardingChecklist` に一本化（`surface` で面ごとの差だけ切り替え）
- [x] ~~**進捗カードがブランドスコアのページ内にあり、タブを切り替えると消える**~~ ✅ 独立タブ `/admin/setup` に移設
- [x] ~~**タブバーが4ページ5箇所に複製されている**~~ ✅ `DashboardTabs` に集約（114行削除）
- [x] ~~**タブ名がサイドバー項目と同名で区別できない／機能名と一致しない**~~ ✅ ラーニング分析・スマート名刺分析・Good Action分析
- [x] ~~**ブランドスコアの空状態が名刺を主役に説明していた**~~ ✅ 市場調査を主役にし、入口も追加
- [x] ~~**「名刺を配布する」の遷移先に QR が無い**~~ ✅ `/admin/card-template` へ
- [ ] **`OnboardingMiniCard` が未使用のまま残っている** — `DashboardTabs` が `OnboardingChecklist` に切り替わったので参照ゼロ。別セッションがオンボーディングを触っている間は消さずに置いてある
- [ ] **`/admin` を直接開くと `/admin/brand-score` に飛ぶ** — サイドバー経由はセットアップ未完了なら `/admin/setup` に着地するが、`app/admin/page.tsx` のサーバー側リダイレクトは brand-score のまま。ログイン直後にここを通る導線があると未完了でもスコア画面に着く
- [ ] **「タブ名・パンくず・ページタイトル」が3系統に分かれている** — 今回3タブ連続で不揃いが見つかった（例: タブ=スマート名刺／タイトル=アクセス解析）。1箇所にまとめないと今後もずれる
- [ ] **オンボーディング案内文の「タイムライン」表記** — `lib/onboarding/steps.ts` の「タイムラインやお知らせで日常に広げる」は正準表記だと「Good Action投稿」。未対応

### 🆕 プラン変更の依頼／メンバー上限／名刺閲覧数（2026-08-14 その4）

- [x] ~~**アップセル面の CTA が料金ページへのリンクで導線が切れる**~~ ✅ その場で依頼できるダイアログ＋スーパー管理の処理キューを新設（`2a81fca`・本番反映済み）
- [x] ~~**Enterprise だけ問い合わせフォームに飛んで依頼が積まれない**~~ ✅ 受け口を1つに統一（`5de1de9`・**未 push**）
- [x] ~~**メンバー上限が定義だけで一度も効いていない**~~ ✅ 4経路すべてに `checkMemberCapacity`（`ccb0f35`・本番反映済み）。Free は5名（`a5829c6`）
- [x] ~~**名刺の閲覧数が水増しされる**~~ ✅ 重複排除・開発トラフィック・自社閲覧の3点を修正し、汚れた272件を削除（本番反映済み）
- [x] ~~**プラン外ページで「取得に失敗しました」が出る／CIマニュアルだけ操作面が出る／ポータルの鍵が見えない**~~ ✅ 修正（`fbec112` `2c70495` `2e23334`・**未 push**）
- [ ] **プラン変更依頼のメール通知が無い** — 画面内のバッジのみ。スーパー管理を開かないと気づけないので、依頼が滞留しうる。新規登録の承認は Resend でメールを送っているので同じ仕組みを足せる
- [ ] **承認/却下の結果が依頼者に伝わらない** — `handled_note` は社内メモとして持っているが、依頼者への通知経路が無い。プランが変わったことは画面で分かるが、却下されたことは分からない
- [ ] **既存の超過会社を戻す導線が無い** — プランを下げて上限を超えた会社は、既存メンバーがそのまま残る（追加だけ止まる）。誰を外すかは人が決める話なので自動では削らない方針だが、超過している事実を管理画面に出していない
- [ ] **料金ページの Free 5名表記が未コミット** — `app/(site)/plan/page.tsx` は別セッションが編集中のため、こちらではコミットしていない（想定規模〜5名／メンバー上限5名／サポート メール／1人あたり ¥0、Standard は 5〜50名）
- [ ] **Standard の「1人あたり目安 ¥396〜1,980」が下限5名と合わない** — 5名で計算すると ¥3,960。表記を直すか「10名以上での目安」と但し書きするか未決
- [ ] **Free と Standard のサポートがどちらも「メール」で差にならない** — 差をつけるなら Standard を「メール（優先対応）」等にする

### 🆕 表示速度／スーパー管理の最終ログイン（**push済み・本番green**・2026-08-14）

- [x] ~~**ポータルと管理画面のブランドスコアの表示が遅い**~~ ✅ 往復回数を減らして解消（outer-score 1.4〜1.6秒 → 0.10〜0.19秒／inner-score 最大2.0秒 → 0.21〜0.24秒）
- [x] ~~**誰が実際にログインしているか画面から分からない**~~ ✅ スーパー管理の企業詳細に「最終ログイン」列を追加
- [ ] **最終ログインは1人ずつ `getUserById` を呼んでいる** — 現状の規模（1社あたり最大6名）では問題ないが、数百名規模の企業が出たら `listUsers` でまとめて引く形に変える。該当箇所にコメント済み（`app/api/superadmin/company-logins/[id]/route.ts`）
- [ ] **同じ「最終ログイン」を管理画面のメンバー一覧にも出すか未検討** — 今回はスーパー管理のみ。会社の管理者が自社メンバーの利用状況を見たい場面はありそうだが、指示が無いので広げていない
- [ ] **inner-score をさらに速くする余地（保留）** — 回答の集計を SQL 側の RPC に寄せれば 0.1 秒程度まで落ちる見込み。現状 0.2〜0.3 秒で体感上の問題は無いため着手していない

### 🆕 ポータル展開／管理者／CSV一括登録（**main反映済み**・2026-08-06 その8）

- [x] ~~**ブランチを main にマージして本番デプロイ**~~ ✅ PR #9（`521cbc5`）・deployment success 確認済み
- [ ] **ポータルの表示を実機で確認していない** — `/portal` のブランドスコアと `/portal/market-survey` は会員ログインが要るため Claude 側から見られていない。**管理職区分のアカウントで崩れがないか要確認**
- [ ] **CSVのパスワードが平文** — 北川さん判断で「CSVにパスワード列」方式を採用。ファイルが共有フォルダに残るリスクは実装で消せない。後から「各自にパスワード設定メールを送る」方式へ切り替え可能（`/reset-password` は実装済みで、CSVからパスワード列を外すだけ）
- [ ] **市場調査を「過年度」にする導線が無い** — 反映のオン/オフを廃止した結果、手動の逃げ道は `archived` だけになったが UI から設定できない（API はある）。必要なら一覧の「…」メニューに追加する
- [ ] **cron のスナップショットが30日固定** — 画面の集計期間を 6ヶ月〜全期間にしたが、自動記録は `calculateSnapshot(supabase, company_id, 30)` のまま。**画面と推移グラフが食い違う**。直すには `brand_score_schedules` に集計期間の列を足す
- [ ] **管理者バイパスは現状維持** — `isAdmin=true` は区分に関わらず全ページ表示。区分の効きを実機で見るには admin ではない staff アカウントが要る

### 🆕 市場調査の取り込み／アウター2本立て（**main反映済み**・2026-08-06）

- [x] ~~**ブランチを main にマージして本番デプロイ**~~ ✅ 2026-08-06 に PR #6 でマージ（`cf97ff5`）・deployment success 確認済み
- [ ] **2025年のGT表を取り込む** — 年次推移は2件目から線になる。2025年の調査（`ID : Client B/c118_株式会社リィツメディカル/01_ブランディング/05_ブランド認知度調査/`）を取り込めばその場で2点になる。**5段階の対応表は定点観測の基準なので変えないこと**
- [ ] **想起の競合に「その他」「あてはまるものはない」が混ざる** — 純粋想起は自由回答なので、受け皿の選択肢まで競合として並んでいる。競合トップ13.6%は「その他」で会社ではなく、実質の競合トップは日本アルコン11.8%。「41社中1位」の41も実際の会社は39社
- [ ] **市場調査の実施日を後から編集できない** — 取り込みダイアログでしか入力できず、未入力だと推移の横軸が取り込み日になる（画面には `(取込日)` と出る）。2026年分は API 経由で 6/12〜6/18 を投入済み
- [ ] **`/mapping`（指標の割り当て）への導線が無い** — 「指標の割り当て」ボタンを「AI考察を再生成」に置き換えたため URL 直打ちのみ。自動割り当てが外れたときに人が直す唯一の手段なので、タイトル横の「…」メニューに戻すか要判断
- [ ] **属性別（開業医/勤務医・エリア）は GT表に無い** — BD1〜BD11 は表側の定義だけで実データは `03_cross_pct` 側。エリア差は大きい（認知：中京・甲信越100% / 北海道・東北・北陸64%）ので、取り込むなら別ファイルの対応が要る
- [ ] **印象一致度をスコアに使うか未決** — `market_surveys.impression_score`（0/20/40/60/80/100）は保存しているが画面にも算出にも出していない。使うなら刻みの粗さを解消する必要がある

### 🆕 レポート整合／AI考察（`feature/survey-report-alignment`・**main反映済み**・2026-08-05 その4）

- [x] ~~**ブランチを main にマージして本番デプロイ**~~ ✅ 2026-08-05 に FF マージ（`fac3803`）・deployment success 確認済み
- [ ] **Cron の同日重複ガードが無い** — 手動記録（`POST /api/brand-score/snapshots`）は同日2回目を409で弾くが、Cron（`/api/cron/brand-score-snapshot`）は無条件 INSERT。手動記録した日に予定日が重なると同じ `snapshot_date` の行が2本入る
- [ ] **Cron の集計期間が30日固定** — `calculateSnapshot(supabase, company_id, 30)` のハードコード。画面で90日を選んでいても自動記録は常に30日で計算される
- [ ] **`calcNextSnapshotDate` の月末ズレ** — 月を単純加算するため起点日が31日だと 1/31→3/3 のように飛び、以降ずっとずれ続ける（`schedule-utils.ts`）。頻度に「任意（Nヶ月ごと）」を足す場合は同時に直すこと
- [x] ~~**アウタースコアに外部調査の取り込み口が無い**~~ ✅ 2026-08-06 に市場調査の取り込みを実装（PR #6）。アウター＝市場浸透0.75＋デジタル接点0.25。印象一致度は「市場の期待と自社イメージ」として画面に出したが**スコアには算入していない**（5項目中いくつ当たったかの分数で、周りの0〜100スコアと単位が違い、1項目の増減で20点飛ぶため）
- [ ] **Cowork のレポート修正を確認する** — 指示書は Drive `c118_RTM/RTM_ブランド戦略会議/260805_インナーブランディング調査レポート_修正指示_v1.md`。スライド10「全60セル（30問×2群）中の最低値」は32問構成での再計算が必要

### 🆕 インナーサーベイ 5段階統一（`feature/brand-penetration-journey`・**main反映済み**）

- [x] ~~**設問別スコアの軸整理**~~ ✅ 2026-08-05 その4で「浸透段階／設問タイプ WHY・HOW・WHAT」の2軸に整理し、領域軸を削除
- [x] ~~**ダッシュボードとサーベイ詳細の軸の不一致**~~ ✅ 2026-08-05 その4でダッシュボードのインナースコアも5段階に変更
- [x] ~~**ブランチを main にマージして本番デプロイ**~~ ✅ 2026-08-05 に `feature/survey-report-alignment` ごと FF マージ（`fac3803`）・deployment success 確認済み
- [ ] **総合スコアの基準混在をどう扱うか決める** — 全32問の単純平均に変更したため、過去スナップショット（WHY35%/HOW30%/WHAT35%）と基準が混在する。移行時点は 62.0 で一致したが偶然。推移グラフに注記を出すか、過去分を再計算するか未決
- [ ] **社内配信サーベイの回答者識別** — 既存2サーベイ（「第1回ブランドサーベイ」90行・「第1回 ブランド浸透度調査」39行）は全回答行の `submitted_at` が同一で、回答者を区別する情報が存在しない。`respondent_id` を採番しても1人に潰れる。**社内配信フロー（`/portal/survey/[id]` → respond API）が提出時刻を同一値で入れていないか要調査**。今後の回答も同じ問題を抱える可能性
- [ ] **現場版を単独取り込みした場合の Q24 上書き運用を決める** — 文言が「名刺交換で強みを伝える」になるため `reference_data.stage='advocacy'` の上書きが必要だが、UIからの設定導線は未実装（DB直接更新の前提）
- [ ] **パターン判定「内向き型」の向きを確定** — 指示書は「推奨 − 行動 ≥ 5」だったが、定義（実践はしているが外に渡せていない）に沿う「行動 − 推奨 ≥ 5」で実装した。今回のデータでは差が出ないが来年ズレる
### 🆕 ブランドオントロジー（未来設計＋カードUI刷新・本番反映済み）

- [x] ~~**push（＝共有・本番デプロイ）**~~ ✅ 2026-07-19 に未来設計11コミット、2026-07-21 にカード刷新一式を push・Vercel Ready。コードとDBが揃った状態。
- [ ] **実機E2E（最重要の残タスク）** — superadmin でログインし、テックブリッジ（デモ）で一周してから ID INC. へ。
  - 未来設計：獲得目標を作る → 実績に測定値を入れる → `verifies` で繋ぐ → 進捗が出る → 提供価値を `target` にする → コピーAIで「目指す」として出る（事実として断定されない）。
  - カードUI：5段構成になっているか／「…」から件数・凡例／未接続チップ→一覧→該当ステップ遷移／「編集する」の畳み・展開／3Dの初期構図で全要素が見えるか・回転/ズーム/ホバー説明バー/クリック詳細／⛶全画面の開閉（リーク有無）／出力テストの2パネルとハイライト。
  - ※**一部は画面操作で確認済み**（2026-07-21）＝テックブリッジで未接続チップ行クリック→Step4焦点パネル→AI提案5件→1件承認で未接続26→24→削除で復帰、関係フォームの種別セレクト9種（communicatedAs 不在）と verifies の端点絞り込み、ID INC. の「AI提案」バッジ18件。**未確認は未来設計の一周（獲得目標→測定値→verifies→進捗→target昇格→コピーAI）と3Dの操作性**。Claude 側のローカルプレビューはセッションが失効すると `/superadmin` に到達できず（パスワード入力は不可）、その間の担保は tsc/build・ユニット・SQL（ロールバック付き）・Nodeからのロジック実行まで。特に**3Dの自動フィット係数**（`VIEW_W*0.40` / `VIEW_H*0.38`）と**ハイライトの効き具合**（`MIN_LEN=4`）は実物を見ないと詰められない。
- [ ] **`philosophy_elements` の `service`（事業内容）分離**（要計画・単独タスク推奨） — 事業内容が理念の器に同居（全社20行・理念要素の27%）。到達判定の根選定・3D配置・プロンプトのすべてが `service` を special-case しており、分類学として濁っている。影響が表示/AI/ツール同期の製品全面に及ぶため、オントロジー構造是正（2026-07-21）では意図的に見送った。
- [ ] **`proof_points.value_proposition_id` 列の DROP** — 裏づけは `evidencedBy` 辺に一本化済み（20260721163027）で全行 null・書き込み経路ゼロ。新コードの本番稼働を確認後に DROP。あわせて `map-data.ts` の `proofFks` 引数など deprecated 読み替えの掃除。
- [ ] **オントロジーの「使わせる」フェーズ（ロジックより優先度が高い）** — 構造の器は整ったが、`promisedTo` 0行（ペルソナが全社で未接続＝約束の相手が体系に無い）・`conflictsWith` 0行（矛盾チェックが構造的に空振り）・未来設計の実運用ゼロ・グラフが実質2社のみ。焦点パネル／構築度スコア／「約束されていないペルソナ」チェックは全てこの穴を埋めるための装置なので、**リィツかテックブリッジで実際にグラフを埋める運用を1周回して、装置が人を動かすか検証する**のが次の一手。
- [ ] **`RELATION_RULES` の粒度**（実害が出てから対応で可） — 現在は kind 単位（`requires` は「理念→獲得目標」）。意味的には「**ビジョン**→獲得目標」が正しく、行動指針→獲得目標も通る。element_type 単位まで絞るにはトリガが理念テーブルを引く必要があり一段重い。
- [ ] コピーAI `body_copy` の maxTokens 上限調整（スモークで末尾が切れた）。
- [ ] **未使用になった依存・関数の整理**（要判断）：`d3-force` / `@types/d3-force` はアプリ参照ゼロ（3D一本化で不要に）。`lib/brand/map-data.ts` の `concentricLayout` も未使用（`buildBrandMapGraph` は現役）。`package.json` を触るため未実施。
- [ ] **superadmin から他社の理念・提供価値を編集する導線**（要判断）：`/admin/brand/*` は `useAuth()` で自社を解決するため、他社を見ている superadmin 画面からリンクすると自分の会社が開いてしまう。現状チップは「登録先の案内」に留めている。

### 🆕 コピーAI／ペルソナビルダー（feature ブランチ・main未反映）

#### ジャーニーUI差し替え（2026-06-20・実装済み・未コミット）
- [x] ~~ジャーニーUI方針：タッチポイント中心化（感情カーブは優先度の注釈に格下げ）~~ ✅
- [x] ~~マルチペルソナ化：`session_data.personas[i].journey_map` 構造へ＋後方互換移送＋connect全ペルソナ書込~~ ✅
- [x] ~~Step4Journey 全ペルソナ集約ビュー：感情グラフ重ね描き／TPプール集約／優先度＝カバー人数×ネガ感情～~ ✅
- [x] ~~フィルタchip：「全員 ⇔ 単体ペルソナ」が感情グラフ・TPプール・Accordionの3箇所に連動~~ ✅
- [x] ~~suggest-journey: 「短い共通ラベル」プロンプトに変更（cov集約が効くように）~~ ✅
- [x] ~~「感情：すべて/ネガのみ」フィルタ削除（相対優先度Highと重複のため）~~ ✅
- [ ] **Lv2タッチポイント分類体系**（`lib/persona/touchpoint-vocabulary.ts` 新設・12カテゴリ・90ラベル前後・**brand_creative カテゴリでID INC.発注機会の間接導線を組み込む**）— 指示書作成済み（`outputs/260620_ペルソナビルダー_Lv2_タッチポイント分類体系_指示書.md`）→ Claude Code に渡す
- [x] ~~**Step2「デモグラフィック」→「ペルソナ生成」改名**~~ ✅（ステッパー＝基本情報/ペルソナ生成/課題・購買行動/ジャーニー設計/確認・出力。2026-06-25 時点で反映済み）
- [x] ~~**管理画面ペルソナとの呼応**~~ ✅（2026-06-25・§3「ペルソナビルダー↔管理画面ペルソナの呼応」参照。Stage1 push済／Stage2'＋Step5は未コミット。5ステップ維持で確定）

#### 元の残タスク
- [ ] 実データでフルパイプライン一周（ID INC.でペルソナ作成→連携→コピーAI）を実施。
- [ ] main 反映・本番デプロイの判断（§開発状態）。`lib/claude-api.ts` モデルhotfixの main 反映も保留中。
- [ ] コピーAI: richなvoice注入（`persona_data` 口癖等を生成器へ）／body_copyトークン上限調整／クライアント画面への開放。
- [ ] リィツメディカル【B】3件（取引施設数・対応スピード実測値・医師の声）を次回打合せで確認→管理画面から追加。


### 🔴 Phase 2（進行中）
- [ ] タイムライン機能の完成
- [ ] KPI管理機能の完成
- [ ] Brand Scoreダッシュボードの改善

### 🟢 UI/導線（フォローアップ）
- [x] ~~ブランドパーソナリティの人格(traits)編集を `/admin/brand/personality` に集約~~ ✅ traits をパーソナリティへ、トーンをバーバルへ移動して整理（2026-06-03）
- [x] ~~ポータルメニュー「感じられ方」のリンク先を /portal/personality（独立ページ）へ確定する~~ ✅ サイドバーを /portal/verbal → **/portal/personality** に変更＋アクティブ判定追加、breadcrumb/dynamic-title に personality 登録、ダッシュボードに「らしさ」4象限概観カード設置。本番デプロイ済み（2026-06-03）

### 🟣 モバイルUX是正（基準v1.0準拠・1バッチずつ verify→deploy）
- [x] ~~batch1 基準確定＋グレー濃色化~~ ✅（235a48f）
- [x] ~~batch2 §3タップ領域44px~~ ✅（f7b35dd）
- [ ] batch3 §1メタ文字の12px未満廃止（`text-[10/11px]→text-xs`・日時14px）。survey5段階caption/KPI密集ウィジェットは幅制約ありで個別確認。例外据え置き＝通知数バッジ・アバター頭文字
- [ ] batch4 カード/ダイアログの title 16-18px＋KPIセットアップ入力16px化
- [ ] phase2 認証画面・スマート名刺(/card)・管理(/admin)を同基準で監査・是正

### 🟠 本番準備
- [ ] RLSポリシー設定（全テーブル）
- [ ] Stripe決済連携
- [x] ~~認証フロー整備~~ ✅ ドメイン認証・承認制参加・サインアップUI刷新・ログイン専用化完了
- [x] ~~認証システム抜本改修~~ ✅ @supabase/ssr 移行・cookieベース・Provider統合（2026-06-02）
- [ ] `flowType: 'pkce'` への変更（別フェーズ。既存セッション全無効化リスクあり）
- [ ] middleware.ts → proxy.ts へリネーム（Next.js 16 deprecation 警告対応）
- [ ] **4ツール無料上限のテスト対応恒久化**（旧: STP無料上限のテスト対応。2026-07-16 に「月に3回」で4ツール統一済み。案①: `@include.bz` 等のホワイトリスト分岐を `lib/tools/free-limits.ts` の判定に追加／案②: `MONTHLY_FREE_LIMIT` を env 化してテスト環境のみ大きい値）。未実装。現状はDB手動リセットで都度対応
- [ ] **`robots.ts` の disallow 非対称を是正**（2026-07-16 発見・優先度低）: 他3ツール（stp/persona/colors）は `/tools/{name}/app/` と `/tools/{name}/auth/` を Disallow しているのに **personality だけ両方漏れ**。SEO 実害はほぼ無い（認証必須ページなので実質インデックスされにくい）が統一のため `app/robots.ts` に2行追加する
- [ ] **`lib/supabase.ts` の module-top-level lazy 化**（2026-07-16 発見・優先度低・潜在リスク）: `export const supabase = createClient()` が モジュール評価時に `createBrowserClient(URL!, ANON_KEY!)` を即時実行。`NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` が未設定な環境で `import { supabase } from '@/lib/supabase'` する module を評価すると落ちる。**現状は Preview に両 env が入っているので実害ゼロ**。他プロジェクト流用時や env オペミス時に再発するので `export function getSupabase()` パターンへの lazy 化を検討（ただし広範に import されているため影響範囲大＝大工事）

### 🟡 Phase 3 残り
- [ ] 理念策定ツール（/tools/philosophy）
- [ ] コピーライティングツール（/tools/copy）

### 🔵 保留
- [ ] **【バックログ・設計済み】管理画面からの表現ルール追加生成（パーソナリティ診断ベース）**
  - **内容**: `brand_personalities`（診断結果）を入力に `tone_rule` を追加生成する API＋UI。診断ツールを一度通した企業が、あとから表現ルールだけ増やせるようにする。
  - **前提（調査済み）**: 診断ツールの連携（`/api/tools/personality/connect`）は既に `governance_rules` へ直接書いている（`rule_type='tone_rule'` / `scope='global'` / `source='personality_diagnosis'`）。ポータルの「表現ルール」もスーパー管理のステップ2も同じテーブルを見ているので、**「スーパー管理で参照する」仕組みは新規に作る必要がない**。
  - **確定済みの設計判断**:
    - 置き場所 = `/admin/brand/personality`
    - 診断未実施の企業では生成ボタンを出さず「先にパーソナリティ診断を」と案内する
    - `source='admin_generated'` を使う。**診断connectは `source='personality_diagnosis'` を全削除→再INSERTする置換方式**のため、同じ source にすると再診断で追加分が消える
    - 企業adminは `governance_rules` にRLSで直接書き込めない（`select` と `superadmin_all` のみ）ため、service_role＋`admin_users` 判定の専用APIが必須
    - 生成は候補提示 → 1件ずつ承認（AI草案パネルと同じ作法。いきなり本登録しない）
    - 役割分担: `draft-extraction`＝禁止語・主張ルール（事業データから）／本機能＝トーンルール（人格から）
  - **着手条件**: 実企業（またはID INC.）が診断→連携を実運用し、ルール追加の実需要が見えたとき。
- [ ] **`governance_rules.scope` は注入絞り込み未接続**（`getGuardrails` に scope 絞り込み機構はあるが呼び出し元が誰も `scopes` を渡しておらず、全ルールが常時注入＝安全側）。設定しても効かないためUIからは撤去済み（DB列・API・機構は温存、新規は `'global'`）。ルールが数十件規模になり文脈別の出し分けが必要になったら、**注入漏れリスク**を設計した上で「接続する／列を廃止する」を判断する
- [ ] RAG（pgvector）によるAIブランドアドバイザー
- [ ] 理解度スコアを `brand_score_snapshots` / スコア推移グラフへ統合（知識も時系列で追う）
- [ ] generate-questions の出題バリエーション微調整（短文ビジョンのTF逐語化・否定形の単調さ）
- [ ] 理解度テストのリマインドメール、再受験対応

---

## 5. 技術メモ・教訓

| 日付 | 内容 |
|------|------|
| 2026-08-31 | **「サイトの正」は1画面のなかでも割れる** — 料金ページのアップセル帯だけプラン副題が古いまま取り残され、同じページの上下で Premium の説明が食い違っていた。**同じ事実を2箇所に書いた時点で、片方だけ直す事故は必ず起きる。**プラン名や区分を変えたら、そのページ内を語で grep して全部拾う |
| 2026-08-31 | **資料と実装が食い違ったら、まず実装を読んで正を決める** — 資料は「Free で PDF出力・掲示反映まで一本道」と書いていたが、`entitlements.ts` は `pdfExport`/`portalSync` とも `free:false` で API も実際に止めていた。**どちらが正しいかは条文ではなくコードが決める。**そのうえで「資料に寄せるか実装に寄せるか」は商品判断として人に返す |
| 2026-08-31 | **実装済みなのに機能一覧に無い機能がある** — マイクロフィードバックは名刺ページで収集し管理画面で集計まで動いているのに、`/features` に載っていなかった（FAQ には言及あり）。**機能を足したときに一覧へ登録する導線が無い**ので、営業資料との突き合わせのような外部視点でしか見つからない |
| 2026-08-31 | **LP（`app/(site)/page.tsx`）は JS からスクロール・寸法計測ができない** — `scrollIntoView` / `window.scrollTo` が効かず、`document.scrollingElement.scrollHeight` がビューポート高と同値で返る。**マウスホイールのスクロールは正常に効く**ので、このページの目視確認はホイール操作で行うこと（プレビューの自動スクロール＋スクリーンショットに頼らない） |
| 2026-08-31 | **「横串」を4枚目のカードで表現しない** — 計測を3つのステップと同じ形のカードにすると、並びの4番目＝次のステップに見える。**形が意味を語ってしまう**ので、横串は「3枚の下に幅いっぱいの帯」で表す。帯の上端に3枚を貫く線を引くと、文章を読まなくても関係が伝わる |
| 2026-08-31 | **構造の指示は根拠ごと書き残す** — 同じ日に「計測が横串」→「発信が横串」→「計測が横串」と2度反転した。結論だけ書いてあると次に触る人がまた迷う。**なぜそうなのかを実装の事実で残す**（インナースコア＝浸透の測定／アウタースコア＝発信の測定／理解度テストは構築のブランドデータを正解キーにする＝計測は3つを参照しないと成立しない）。docs/branding-bz-overview.md にこの根拠を書いた |
| 2026-08-31 | **数え方が揺れる呼称を正準表記にしない** — 「3レイヤー構造」「4レイヤー構造」と行き来していた。3ステップ＋横串1つという形なので**どちらの数字も間違いになりうる**。数を含まない「レイヤー構造」に改め、glossary から「旧称は使わない」の断定も外した |
| 2026-08-31 | **区分を増やすときは「どの説明文がどの区分の話か」まで動かす** — レイヤーを3→4に増やす作業で、見出しだけ足して本文を放置すると「浸透」の説明に「スコア化」が残り、新設した「計測」と中身が重なって区分の意味が消える。**箱を足す前に、既存の本文を1文ずつどの箱の話か仕分ける。** 今回は LP の body 1文と features の機能2件を移動させた |
| 2026-08-31 | **画面の区分を変えたら meta description も直す** — features を4グループにしたのに meta は「構築・浸透・発信を支える機能」のままだった。指示書には無かったが、説明文だけ古い区分で残ると同じ不整合が検索結果側で再発する。**表示と一緒に、そのページを説明しているテキストも探す** |
| 2026-08-31 | **`md:grid-cols-4` は使わない（1枚が狭くなりすぎる）** — 3枚→4枚に増やすとき `md:grid-cols-3` を単純に4にすると、md（768px）で1枚あたり約170pxになり本文が読めない。**`sm:grid-cols-2 lg:grid-cols-4` で中間幅は2列に畳む**。1280px＝4列273px・900px＝2列416px を実測して確認した |
| 2026-08-31 | **同じファイルを別セッションが触っているときは hunk 単位で staged する** — CLAUDE.md の冒頭を直したかったが、末尾に別セッションの未コミット追記があった。ファイル単位で `git add` すると相手の作業まで巻き込む。`git add -p` は対話式で使えないので、**`git diff` の該当 hunk だけをファイルに切り出して `git apply --cached`** する |
| 2026-08-25 | **集計表の「受け皿」はコード付きの行として実在する** — GT表の `q1_AC` に「その他 13.6%」「あてはまるものはない 11.8%」が他社と同じ形式で並んでいた。行の形だけ見ると会社と区別が付かず、競合として拾ってベンチマークの最大値と順位の母数を汚していた。**外部由来の表を機械的に読むときは「値が入っている＝中身がある」と見なさない**。ラベルで受け皿を落とす判定が要る。 |
| 2026-08-25 | **平均で作った指標は、分母が変わると実態が動かなくても数字が動く** — 市場浸透スコアは「スコアが出ている段階の平均」。推奨(20点)の設問が翌年なくなっただけで 52→60 に上がり、「浸透が進んだ」と読めてしまう。**年をまたいで比べる指標は、比較のたびに共通部分だけで計算し直す**。構成が違うこと自体も画面に出して、黙って別物を比べない。 |
| 2026-08-25 | **UI を消すときは、それが唯一の操作手段になっていないか確かめる** — PWA の更新トーストを消すだけだと、`skipWaiting:false` のまま新SWを有効化する手段が無くなり、全タブを閉じるまで古い資産が配られ続ける。見た目を消す変更でも、裏の状態機械のどこに繋がっているかを見る。 |
| 2026-08-21 | **全リクエストの前段に置いた外部通信は、その1回のハングをサイト全体の障害に変える** — middleware が全パスに掛かり、その中で Supabase Auth への往復をタイムアウト無しで待っていた。**Auth 自体は正常（p99 71ms）だったのに**、edge → Supabase 間が一度ハングしただけでログイン不要の LP まで 504 になった。外部が「落ちる」必要すらなく、1回詰まれば足りる。**前段（middleware・共通レイアウト）で外部を呼ぶときは、①本当に全パスで要るのか ②失敗・遅延したときに素通りできるか、を必ず決めておく**。セッション更新のように「できたら嬉しい」処理は、落ちてもページを返すのが正しい。 |
| 2026-08-21 | **Promise を放置してもリクエストは死なない** — タイムアウトを `Promise.race` だけで作ると、待つのはやめられるが通信自体は生き続け、Edge の実行時間を食い続ける。接続を本当に打ち切るには `AbortSignal.timeout` を fetch に渡す必要がある。両方掛けておくと、abort が効かない経路（DNS で止まる等）も race 側で受け止められる。なお **race の敗者に置いた setTimeout も同じ**で、勝敗が決まったら `clearTimeout` しないとタイマーが残る。 |
| 2026-08-16 | **「どこかの会社の管理者か」は認可ではない** — 残存アカウント削除の API は、呼び出し元が管理者であることだけを確かめ、削除対象がその会社のものかを見ていなかった。しかも `profiles.delete().eq('email', email)` に会社の絞り込みが無く、手前の「members に居ないこと」チェックは members を持たない profiles を素通りする。結果、他社の名刺プロフィールを消せた。**company スコープのデータを触る API は「誰か」ではなく「その会社のものか」を確かめる**。 |
| 2026-08-16 | **同じ値のトークンでも、役割が違うなら統合しない** — 色を統合したいと言われて洗い出すと同値が11組あったが、まとめてよいのは1組だけだった。shadcn の `--background`/`--card`/`--popover` が同じ白なのは「いまたまたま一致している」だけで、まとめるとカードだけ色を変えられなくなる。**値の一致は統合の理由にならない。役割が同じかどうかで判断する**。 |
| 2026-08-16 | **内部状態の復旧手段を、原因の分からない人の画面に置かない** — 「『既に登録されています』エラーが出る時に使用」という削除ボタンを契約者の管理画面に出していた。壊れているのはこちらの状態で、直すのもこちら。さらに、片付けたあとは常設メニューから外して画面だけ残す、という置き方もある（発生源が塞がっていれば、常時見えている必要はない）。 |
| 2026-08-16 | **「ずれている」の指摘は、直す前に測る** — スイッチと見出しの上下中心がずれて見えると言われたが、実測すると**箱の中心は 0.3px しか違わなかった**。原因は和文の行ボックスが下側に余白を多く取ることで、**字面の中心が箱の中心より 0.6px 上**に来ること。目分量で「1px 下げる」と逆方向に外していた。`canvas.measureText` の `actualBoundingBoxAscent/Descent` と `Range.getBoundingClientRect()` を突き合わせれば字面中心は出せる。**見た目の指摘ほど、数字にしてから触る** |
| 2026-08-16 | **開閉する領域は条件レンダリングにしない** — `{cond && <div/>}` だと閉じた瞬間にスペースが詰まってページが飛ぶ。**常時マウントしたまま `grid-rows-[0fr]↔[1fr]` で高さを動かす**（中身は `min-h-0 overflow-hidden` の箱に入れる）。⚠️ `max-height` での代用は、閉じ切るまでの速さが中身の量で変わるので使わない。margin も一緒に transition に乗せないと、隙間だけ先に消える |
| 2026-08-16 | **ヘッダーとページの間は context で繋ぐ（`components/portal/portal-search.tsx`）** — ヘッダーに検索を置くと「どの画面で・どんな placeholder で出すか」をヘッダー側が知る必要がある。ここにページ名の分岐を持つと画面が増えるたびに触ることになるので、**検索したい画面が `useRegisterPortalSearch(placeholder)` で名乗り出る**形にした（`PortalSearchProvider` が受け、query を返す）。ヘッダー側は登録があるかどうかしか見ない。パンくず（`lib/portal-breadcrumb.ts`）の `parent` も同じ考え方で、**マップ1箇所に足せば導線が出る** |
| 2026-08-16 | **`p-3` を書いても16pxになる（`globals.css` のカード統一ルール）** — 「角丸＋枠＋`p-3`〜`p-8`」に当てはまる要素はパディングが16pxへ統一される。タイムラインのカードを12pxにしたくて `p-3` に変えても見た目が変わらず、計測して初めて気づいた。**方向指定（`px-3 py-3`）が既定の逃げ道**で、ルール側のコメントにそう書いてある。**効かない CSS に出会ったら、まず `globals.css` の統一ルールを疑う**（角丸12px統一も同じ条件で拾っている） |
| 2026-08-16 | **戻り導線は「パンくず」か「ページ内リンク」のどちらか一方にする** — お知らせ詳細にパンくずの親リンクを足したら、ページ内の「← 戻る」と同じ働きの導線が2つ並んだ。どちらが正か迷わせるだけなので後者を削除した。**新しい導線を足すときは、既に同じ役割のものが無いか画面全体を見てから足す** |
| 2026-08-16 | **パンくずのラベルは遷移先の画面名に合わせる** — お知らせ詳細の親を「お知らせ」と書くとカテゴリ名にも読め、押した先が一覧だと分からない。「お知らせ一覧」に直した。**リンクのラベルは、押した先の画面のタイトルと一致させる** |
| 2026-08-16 | **表の折り返し対策は「固定文言」と「可変長」で分ける** — 列名・バッジは中身が決まっているので `whitespace-nowrap` で足りるが、社名のように伸び続けるものを nowrap にすると表が横に広がって別の問題になる。可変長は `truncate` ＋ `title` 属性（全文はホバー）にする |
| 2026-08-16 | **「1枚にまとめて」は2通りに読める** — カード2枚を1つにする依頼で、最初は「枠を1本にして中を罫線で割る」形を作ったが、狙いは「カードの中にカード」だった。**同じ言葉で違う絵になるときは、既存画面の中から手本を探して合わせる**（今回はポータルの「私たちの『らしさ』」＝グレー地の器＋白カードが正解だった）。作り直しは1往復で済んだが、先に手本を探していれば0往復だった |
| 2026-08-16 | **`{条件 && ( … )}` の中に JSX コメントを置くとビルドが落ちる（再発）** — 説明コメントを `{/* … */}` のまま条件式の括弧の中に置くと root が2つになり「Expected '</', got 'ident'」で落ちる。**教訓として記録済みだったのに同じセッション内で2度繰り返した。** 条件式に説明を足したくなったら、括弧の外（`{条件 && (` の前）に置くか、既にあるコメントブロックに書き足す |
| 2026-08-16 | **Recharts は系列にデータが無くても凡例を出す** — プラン別に系列を絞るとき、`dataKey` の値を null にするだけでは `<Line>` が定義されている限り凡例に名前が並ぶ。「使えないはずの系列名が見える」状態になるので、**`<Line>` ごと条件で出し分ける**。同じ「枠だけ残る」問題をカード単位では直したのに、グラフの系列側で見落とした＝**一度見つけた失敗パターンは、同じ画面の別レイヤーにも無いか探す** |
| 2026-08-16 | **監査も実測で裏取りするまで信じない** — 「無認証ルート27件」と報告したが、実際に塞ぐ段になって11件が偽陽性と判明した。grep は文字列しか見ておらず、`auth.getUser` をラップしたヘルパー（`getAdminContext` / `getMemberContext`）や秘密認証（`BRAND_ONTOLOGY_API_SECRET` の fail-closed）を「認証なし」と誤判定していた。**ルートの安全性は grep ではなくコードを読んで判定する。** 件数を報告する前に、少なくとも数件は開いて中身を確かめる |
| 2026-08-16 | **ゲートは認証の上にしか乗らない** — プラン制限で51箇所にゲートを貼ったが、`generate-questions` は**認証レイヤーそのものが無く**、ゲートを貼る土台が無かった。プラン判定を足しても、誰が呼んでいるか分からないルートでは意味をなさない。**「機能を絞る」作業の前に「本人か」を確かめる層があるかを見る**。今回は51箇所を数えて満足し、認証の有無を数えていなかった |
| 2026-08-16 | **`guardCompanyFeature` はプランを見るが、呼び出し元が誰かを見ない** — `guardCompanyFeature(companyId, feature)` は「その会社のプランで使えるか」しか判定しない。`company_id` をクライアントから受けるルートでは、他社の ID を渡されるとそのまま通る。実際 company-scoped の27件中14件がこの状態で、「ガードしてあるように見えて素通り」だった。**`requireCompanyMember()` を先に通してから** `guardCompanyFeature` を呼ぶこと |
| 2026-08-16 | **プラン設計は「どちらが自社だけで完結するか」で切る** — 計測の split を「basic＝アウター／full＝インナー」で切っていたが、インナーサーベイは自社の社員に配るだけで完結する自己計測、アウター（市場調査）は外部の手配と解釈が要る。**伴走とセットで売るべきなのは後者**なので、Premium＝インナー／Enterprise＝統合＋市場調査に入れ替えた。機能の重さではなく「顧客だけで完結するか」で線を引く |
| 2026-08-16 | **プランを問わず出る画面に、上位プランでしか成り立たない言葉を書かない** — 入力サポートの案内はプランに関係なく出るのに、文面が「担当者がサポートします」だった。専任の担当者が付くのは上位プランだけで **Free には付かない**＝付かない人にも約束したことになる。実態どおり「ID INC. のスタッフ」に直し、同じ性質のプラン変更ダイアログも揃えた。**再発しないよう、両ファイルの先頭コメントに理由ごと残した**（「文面に『担当者』を使わない」）。表記だけ直すと次に触る人が同じ言葉に戻す |
| 2026-08-16 | **プラン分岐は「データを空にする」ではなく「枠ごと出さない」** — スコア推移が Premium に出ていた原因は、`variant=basic` のときに系列データ（total/inner）を null にしていただけで**カード自体は描画していた**こと。結果アウター1本のグラフという中途半端な状態で、Enterprise 限定のはずの機能の枠が見えていた。同じファイルの他8箇所では `isFull` が効いていたので**単なる取りこぼし**。データを削る対処は「見た目は残る」ので、機能の出し分けには使わない |
| 2026-08-16 | **react-day-picker の `{ before, after }` は「範囲外」ではなく「範囲内」** — 過去日と3ヶ月より先を落とすつもりで `disabled={{ before: today, after: today+3mo }}` と1オブジェクトで渡すと、**2日の“あいだ”を指す区間マッチャ**として解釈され狙いと逆になる（過去日が選べてしまう）。正しくは `disabled={[{ before: … }, { after: … }]}` と2つのマッチャを配列で渡す。境界は実機で確認するのが確実（今日〜3ヶ月後の同日まで可・その翌日から不可） |
| 2026-08-16 | **`{条件 && ( … )}` の中にコメントとJSXを並べると root が2つになる** — 説明コメントを `{/* … */}` のまま条件式の括弧の中に置いたら「Expected '</', got 'ident'」でビルドが落ちた。JSX コメントも式の子として数えられるため。**コメントは条件式の外か、要素の内側に置く** |
| 2026-08-15 | **並列の塊のあいだに1本だけ挟まった await は、それだけで1往復** — ポータルのデータ取得は「9本並列 → 1本 → 4本並列」で、真ん中の1本（`personal_goals`）のためにブラウザ→Supabase の往復が丸ごと1回増えていた。その1本が依存していたのは前段の結果1つだけで、後段と同時に投げられた。**並列グループを分ける前に「この await は後ろのグループに混ぜられないか」を見る**。管理画面の集計APIで同じ形を直したのと同じ話。 |
| 2026-08-15 | **同じ情報を2つの実装で見せていると、片方だけ育って食い違う** — セットアップ案内は管理画面がコンパクト版、ポータルが全ステップ版と別実装で、進捗の見え方が2種類あった。1つのコンポーネントに寄せ、面ごとの違い（閉じられるか・戻り方を案内するか）は `surface` プロパティ1つに閉じ込めた。**「同じものを2画面で見せたい」ときは、コンポーネントを分けるのではなく差分をプロパティにする**。 |
| 2026-08-15 | **表示名が「タブ・パンくず・ページタイトル」の3系統に分かれていて、必ずどれかがずれる** — タブ名を3つ変えたが、3回とも3系統が不揃いだった（タブ=スマート名刺／パンくず=スマート名刺／タイトル=アクセス解析、など）。1画面の名前が3ファイルに散っている構造そのものが原因で、**名前を変えるたびに3箇所を grep する運用**でしのいでいる。いずれ1つの定義から3系統を引く形にしたい。 |
| 2026-08-15 | **画面の説明文は、実装の重みと合っているかを確かめる** — ブランドスコアの空状態が「名刺閲覧データでアウタースコアを測定できます」と書いていたが、実際のアウターは市場浸透75%＋デジタル接点25%で、名刺は条件次第で寄与0%（card_enabled オフ／期間内PV30件未満）。案内どおり名刺を配ってもスコアが動かず「壊れている」と受け取られる。**重みを持つ機能の説明文は、重みの定義（ここでは `OUTER_TRACK_WEIGHTS`）と突き合わせる**。 |
| 2026-08-15 | **「◯◯する」という導線は、その動詞ができるページに送る** — 「名刺を配布する」が `/admin/members`（アカウント管理）を指していた。配布に使う QR の一括/個別ダウンロードは `/admin/card-template` にあり、遷移先では配布できなかった。ラベルの動詞と遷移先の機能が一致しているかは、リンクを足すときに毎回確かめる。 |
| 2026-08-15 | **`rounded-xl` はデザイントークンに連動していない** — `tailwind.config.ts` は `lg: var(--radius)` / `md: calc(--radius - 2px)` / `sm: calc(--radius - 4px)` を定義しているが、**`xl` は Tailwind 既定の 12px 固定**。shadcn の `<Card>` が `rounded-xl` なので、カードだけが `--radius` の外側にある。**トークンで揃えたつもりでも、使っているクラスがトークン由来か確かめる。** |
| 2026-08-15 | **却下された変更をコミットしてしまった** — ユーザーが中断した編集のあと、`python3 ...`（判定つき）と `git commit` を**別行**に書いたため、判定が失敗してもコミットだけが実行された。結果、コミットメッセージと中身が食い違う履歴（`99f2bb0`）が残った。**中断・失敗でコミットまで到達しないよう、判定とコミットは同じ `&&` チェーンに繋ぐ。** |
| 2026-08-15 | **小さい文字は globals.css が 14px に底上げしている。ただし列挙漏れがある** — `:is(.text-xs, .text-[9px], .text-[10px], .text-[11px], .text-[12px], .text-[13px], .text-[0.8rem])` を 14px に上げるルールがあり、`text-[12px]` と書いても **14px になる**（button・タブ・svg 配下だけ除外）。一方 **`text-[10.5px]` や `text-[11.5px]` は列挙に無いので素通りする**ため、0.5刻みを使った箇所だけ本当に小さく出る。実際サポートカードで「説明文（12px指定→14px）が見出し（11.5px→11.5px）より大きい」という逆転が起きた。**サイズを指定する前に、その値がこのルールの対象かを確かめる。** どうしても12pxにしたい場合は inline style で上書きする（クラスでは勝てない）。 |
| 2026-08-15 | **出し分けを1箇所に足しても、入口が別なら直らない** — セットアップ中の着地先を「進捗タブ」にする改修で、サイドバーとタブバーには出し分けを入れたが、**ポータルからの入口（`/admin` への静的リダイレクト）だけが素通り**していた。同じ判断を使う場所が3つあるとき、2つ直して満足すると残り1つが必ず表に出る。**「その状態を使う場所」を先に数え上げてから直す。** |
| 2026-08-15 | **CSS 変数は「どこで使うか」で値が変わる。同じクラスでも場所が違えば別の色になる** — ポータルに置いたカードで管理画面と同じ色を出そうとして2回外した。`--sidebar-background` は **3段**で定義されている＝`:root`（18%）／`[data-portal]`（明るいグレー・globals.css）／`AdminDataProvider` が SidebarProvider へ渡すインライン（21%・**これが実際の管理画面**）。`bg-sidebar` を使うと2段目を、`:root` の値を直書きすると1段目を拾い、どちらも実物と一致しない。**トークンを別の文脈へ持ち出すときは、上書きが何段あるかを先に数える。** |
| 2026-08-15 | **クラスの効きを試すなら、実際に置く場所の内側で測る** — 上の件で `document.body` 直下に要素を差し込んで`getComputedStyle` を見ており、「合っている」と誤って報告した。実際のカードは `[data-portal]` の内側にあり、そこでは別の値になる。同様に、ソースに `hover:` 付きでしか無いクラスを裸の名前で試すと「生成されていない」と誤診する（`hover:border-ds-app-accent-hover` で実際に起きた）。**検証用の要素は、本番と同じ祖先・同じ形で置く。** |
| 2026-08-15 | **「既定値が入っているカラム」は完了判定に使えない** — オンボーディングの「ビジュアル登録済み」を `companies.brand_color_primary is not null` で見ようとしたが、この列は `DEFAULT #000000` で**全社が最初から true**。実データで空の会社（TAKUMI/CTD）まで true になって初めて気づいた。同じ形で `invite_links` 単独も危うい＝招待リンクを使わず直接メンバーを追加した会社（テックブリッジ）が未完了になる。**判定に使う前に、空の会社と充実した会社の両方で実データを引いて、真偽が割れることを確かめる。** |
| 2026-08-15 | **ds系トークンに不透明度修飾（`/10`）は効かない** — `--ds-app-accent` は hex 値の CSS 変数なので、`bg-ds-app-accent/10` は**透明**になる（computed style で確認）。淡い階調が要るときは素の `bg-blue-50` を使う。アプリ面の生 blue 58箇所のうち53箇所が淡色なのはこの構造的な理由で、統一漏れではない。逆に**値が同じ中間色の直書き**（`border-blue-700` = `--ds-app-accent-hover`）はトークンに寄せる。なお生成されるのは `hover:` 付きのクラスだけなので、裸のクラス名で効きを試すと「効かない」と誤診する。 |
| 2026-08-14 | **「セッション内でだけ覚える」をフック内 state で書くと、ほぼ発火しない** — セットアップ完了の通知を「未完了→完了に変わった瞬間」に出す実装で、基準値をフック内の state/ref に置くと**一度も鳴らない**。最後のステップを終える動線（お知らせ＝`/admin/announcements`、招待＝`/admin/members-portal`、投稿＝`/portal/timeline`）はいずれもページ遷移を跨ぐため、戻ってきたときには新しいフックが「初回観測＝既に完了」と判断して黙るから。**モジュール変数に置くと remount を跨いで覚えていられ、リロードで消える**＝「セッション内・メモリのみ」という要件を満たしたまま実際に鳴る。ただし**グローバル1値にすると別企業を跨いだときに誤発火する**ので `Map<companyId, boolean>` にする。「メモリのみ」の置き場所は state だけではない、という話。 |
| 2026-08-14 | **「定義してテストも書いた」と「実際に呼ばれている」は別** — `MAX_MEMBERS` は entitlements に定義され `getMaxMembers` のテストまであったのに、呼ぶ場所がテスト以外に1つも無く、全プランで人数が青天井だった。料金ページには「50名」「300名」と書いてあるのに効いていない状態。**上限・制限のたぐいを足したら、grep して呼び出し側があるかを必ず確かめる**。テストが通っていることは実装が使われている証拠にならない。 |
| 2026-08-14 | **RLS で保護されたテーブルを anon で読むと、重複排除のような「無ければ書く」判定が常に通ってしまう（3回目）** — 名刺の閲覧記録は5分の重複排除を持っていたが、直近アクセスの確認を anon クライアントで投げており、SELECT ポリシーが authenticated 限定だったため常に0件＝「直近アクセス無し」と判定してリロードのたびに記録していた。エラーが出ないので誰も気づかない。**RLS 下の読み取りは「0件」と「読めない」を区別できない。判定に使う読み取りは service_role で行い、読み取り失敗はログに残す**。 |
| 2026-08-14 | **ローカル開発と Preview は本番の Supabase を見ている＝開発中の操作が顧客の数字になる** — `card_views` の216件が localhost 由来だった。アクセスログ・利用回数など「実際に使われた回数」を記録する API は、`VERCEL_ENV`/`NODE_ENV` で本番だけ記録するようにする。記録した数字がスコアの材料になっている場合、汚れはスコアごと嘘になる。 |
| 2026-08-14 | **画面はプランで分岐しているのに、データ取得だけがプランを見ていないと無関係なエラーが出る** — プラン外の会社でサーベイ管理を開くと、アップセル面の上に「サーベイ一覧の取得に失敗しました」が重なっていた。取得の useEffect が `companyId` だけを見て走り、403 を受けて必ず失敗するため。**ゲートは描画と取得の両方に置く**。なお `company` は `companyId` より後に届くので、両方揃うまで待ってから判定しないと「プラン外」と誤判定して永久に取得しなくなる。 |
| 2026-08-14 | **配色は「置く面の明暗」とセットで持つ** — ロックバッジは暗い面用の配色（`text-green-200` 等）を面に関係なく使っており、明るいポータルのサイドバーでは鍵が地色に溶けて「枠だけあってアイコンが無い」ように見えた。描画はされている。同じ部品を明暗どちらの面にも置くなら、`tone` のようなプロパティで明示的に切り替える。 |
| 2026-08-14 | **Tailwind の content グロブに `lib/` が無く、`lib/` にしか無いクラスは CSS に生成されていなかった** — サイドバーのプランバッジが「薄い紫地に薄い文字」で読めなかった原因は配色ではなくビルド設定。配色定数 `PLAN_TONES` は `lib/billing/plan-display.ts` にあるが、content は `app/` と `components/` だけを走査していたため `text-purple-800` が生成されず、文字色がサイドバーの明るい文字色を継承していた。**他プランのバッジが読めていたのは `text-green-800` / `text-amber-800` がたまたま `app/` 側でも使われていたからで、偶然に助けられていた。**クラス名を定数として持つファイルは必ず content に含める。`lib/brand/rule-display.ts` の遵守レベル配色も同じ理由で効いていなかった。 |
| 2026-08-14 | **並行セッションが「参照する側」だけコミットして HEAD が壊れる** — `plan-gate.tsx` は別セッションが commit したが、そこが import する `PLAN_TONES_ON_DARK` はこちらの未コミット作業ツリーにしか無く、**HEAD 単体では TS2305 でビルドが落ちる状態**だった。ローカルは作業ツリーを見るので `tsc` も dev サーバも通ってしまい気づけない。**同じ repo を複数セッションで触る日は、push 前に `git stash` して HEAD 単体で `tsc` を通す。** （既出の「ローカル build は未コミット作業ツリーを見るので通ってしまう」と同じ穴。今回は他セッション由来という新しい入口だった） |
| 2026-08-14 | **RLS で書き込みを止めると、UI は「成功した」と嘘をつく** — supabase-js の `.delete()` / `.update()` は `.select()` を繋がない限り影響行を返さず、**RLS で 0 行になっても `error` は null**。そのため管理画面の「メンバーの目標を削除」は削除できていないのに成功トーストを出していたし、いいね解除は DELETE ポリシーが無いまま何年も動いていなかった（0行がエラーにならないので誰も気づけない）。プランゲートを RLS に載せる前に、`.select('id')` で影響行を受け取り 0 件を「権限がありません」として扱う形に9箇所を統一した。**書き込み系に RLS 条件を足すときは、UI 側が 0 行を検出できるかを先に確認する** |
| 2026-08-14 | **同じ判定を2箇所に書くと、片方だけ直す日が必ず来る** — プラン判定はアプリ（`entitlements.ts`）と RLS（`company_plan_allows()`）の両方に必要だった。ブラウザから supabase-js で直接書くテーブルには API ゲートを差せないため。二重実装は避けられないので、①RLS 側は10ポリシーに式を撒かず SQL 関数1本に集約 ②`entitlements.ts` の該当 feature に「RLS 側にも同条件あり（migration 名）」の相互参照コメントを置く、の2点で「両方直す」を思い出せるようにした。同じ理由で `BrandScoreView` は basic/full の2ファイルに割らず `variant` の条件レンダリングにしている |
| 2026-08-14 | **権限は安全側（free）に、課金事故は顧客側に倒す** — `getEffectivePlan` は company が無い・未知のプラン名なら free を返す（安全側）が、`plan_expires_at` が壊れた文字列のときだけは契約プランのまま返す。値の不備で有償顧客の機能を止めるのは事故だから。**「安全側」は一律ではなく、誰が損をするかで向きを決める** |
| 2026-08-14 | **指示を実装を読まずに名前で決めると、経路ごと塞いでしまう** — 指示書は `/api/portal/oauth-gate` を「standard 以上」としていたが、実体は**どの企業にも紐づかない孤児 auth ユーザーを削除する**経路だった。プランで止めると free 企業のメンバーが Google ログインできなくなる。同様に `shared-profile` は GET（構築ツールのプリフィル）と PATCH（本体への書き戻し）で意味が違うため、PATCH だけを止めた。**ゲートを貼る前にそのルートが何をしているかを読む** |
| 2026-08-14 | **【その後解消】この「4本しかない」状態は `37cd848` で9本に揃えた**（announcements / survey / market_survey / quiz / ci_manual を追加）。サイドバーの出し分けも href の直書き分岐をやめ、項目側の `toggleKey` に持たせた＝項目が増えるたびに分岐を書き足す形だったのが抜けの原因だったため。 |
| 2026-08-14 | **「とりあえずトグルで切って凌ぐ」と決める前に、そのトグルが何を覆っているかを数える** — プラン制限（Phase 1〜3）が入るまでの暫定策として「Free 企業は機能トグルをオフにする」が浮かぶが、トグルは `timeline`／`kpi`／`card`／`learning` の4本しかなく、**サーベイ結果と市場調査は対象外**（`survey_enabled` は未実装）。さらに `isPortalPageVisibleForRole` の `if (isAdmin) return true` で管理者には区分設定も効かない。**暫定策の適用範囲を確認せずに「運用でカバーできる」と判断すると、塞げていない穴を塞いだつもりになる。** 現に効いているのは承認制だけ、という事実まで降りて初めて安全度が分かる。 |
| 2026-08-14 | **「遅い」の原因はデータ量ではなく往復回数のことがある** — ブランドスコアの表示が遅く、11,760行のリィツが重いのだと思い込みかけた。ところが回答が39件しかない ID INC. でも同じだけ遅い。集計APIが独立した問い合わせを1本ずつ順番に待っており、Supabase への1往復およそ0.2秒が単純に積み上がっていた（outer-score は7回直列＝約1.5秒）。**行数を疑う前に、直列に await している問い合わせの数を数える**。データ量が違う会社どうしで同じだけ遅ければ、それは往復回数の問題。 |
| 2026-08-14 | **`{score: null, message}` のような「データが無いときの別形」は、使う側すべてでガードが要る** — inner-score API はサーベイ0件の会社に `scores` キーの無い応答を返す。管理画面とポータルのサーベイ結果画面はガード済みだったが、新設した `BrandScorePortalSection` だけ漏れていて、`innerScore.scores.total` で TypeError → エラー画面になった（TAKUMI 等8社が該当）。**既存画面が同じAPIをどう受けているかを見てから新しい呼び出し側を書く**。型が `any` 相当だと TS も止めてくれない。 |
| 2026-08-14 | **auth スキーマは PostgREST に公開されていないので、最終ログインはクライアントから読めない** — `auth.users.last_sign_in_at` を `supabase.from(...)` で引こうとしても届かない。service_role の `auth.admin.getUserById` を使う API を立てる必要がある。また `profiles`（名刺の従業員）には `auth_id` が無く、`members` が `profile_id ↔ auth_id` を繋いでいる。**「従業員のログイン状況」は profiles 単体では出せない**という前提を先に確かめる。 |
| 2026-08-06 | **機能を消しても、その機能を売っている文章は消えない** — 「部署別ヒートマップ」は `48283cd refactor: ブランドスコアから部署別スコアの表を削除` で実装が無くなっていたのに、料金ページ・FAQ・機能ページ・トップの4ファイルに記述が残り、**実装のない機能を売り続けていた**。しかも本セッション序盤の Phase 0 調査（削除前）では実在を確認していたため、その記憶のまま「実装済み」と説明してしまい、北川さんの「そんな機能実装してる？」で初めて露見した。**UI コンポーネントを削除する PR では、その機能名で公開ページを grep する**（`grep -rn "<機能名>" app/\(site\)`）。同種の危険は「ギャップ分析」にもあり、画面上の名前は「共感 × 知識 ギャップ」で料金ページの表記と一致していない |
| 2026-08-06 | **プラン構成を変えたら、機能名だけでなく「その機能を前提にした説明文」も虚偽になる** — 計測を Premium → Enterprise へ移した際、features の行を移すだけでは足りず、Premium の description「社員が学び、理解し、体現する仕組みと、**それを数値で追う計測基盤**」が残ると計測を含まないプランで計測を謳うことになる。同じ理由で FAQ の「ブランドスコアは Premium プランでご利用いただけます」も虚偽になる。**プランから機能を抜くときは、features 配列だけでなく description・subtitle・FAQ・トップ・機能ページを横断で洗う**。今回は北川さんが指示書で「description 書き換えは必須」と明示していたため取りこぼさずに済んだ |
| 2026-08-06 | **`readOnly` のような「見せる/見せない」の真偽値は、行き先まで一緒に消してしまう** — `BrandScoreView` は管理画面とポータルで共有しているが、リンクを `!readOnly &&` で囲んで href を直書きしていたため、ポータルに**別の行き先のリンクを出す**という選択肢が構造上取れなかった。`innerLink` / `outerLink` を `{href,label} | null` の props にし、未指定なら従来挙動へフォールバックさせて解決。**共有コンポーネントで「出す/出さない」だけを持たせると、後から「別のものを出す」が足せない**。リンク先の判断（区分で見られるページか）は呼び出し側に持たせるのが正しく、そうしないとコンポーネントが認可の知識を持ち始める |
| 2026-08-06 | **「既定値を決めた」ことと「既存行に既定値が入った」ことは別** — 区分の既定を staff にすると決め、UI とヘルパーを直して push したが、migration を当てていないので既存行は `role_category = null` のまま。サイドバーの分岐を `roleCategory === 'staff'` と書いたため、null が else に落ちて「従業員」ラベルが出続けた（`memberRoleLabel(null)` のフォールバックを従業員にしていたので、一見それらしく見えて原因が分かりにくい）。**列の既定値に依存する分岐は、SQL を当てるまで null が来る前提で書く**。判定は `isStaffRole`（`role == null || role === 'staff'`）のような**ヘルパー1本に寄せて、直接比較を散らさない** |
| 2026-08-06 | **「管理者は常に表示」の抜け道があると、出し分けの設定を自分では検証できない** — 区分ごとの表示設定で従業員を off にしても、北川さん（admin かつ role=staff）には全ページ見えたまま。`isPortalPageVisibleForRole` の先頭に `if (isAdmin) return true` があるため。仕様としては維持と判断したが、**この手のバイパスを入れると、設定を作った本人が効きを実機確認できなくなる**。検証には非 admin のアカウントが要る、と分かる場所（設定画面の注記）に書いておく |
| 2026-08-06 | **RLS で保護されたテーブルをクライアントから読むと、エラーも出ずに空が返る（同じ穴に2回落ちた）** — `admin_users` の SELECT ポリシーは `auth.uid() = auth_id`（自分の1行だけ）。クライアントから `select().eq('company_id', …)` しても他人の行は返らず、管理者列が**全員 OFF に見えていた**。同じ日に `brand_score_snapshots` でも「前回記録日 —」で同じことが起きており、そちらは `catch` で握り潰していたため気づけなかった。**RLS のあるテーブルを一覧表示する用途で読むときは、必ず service_role の API を通す。** クライアントから読んでよいのは「自分のデータ」だけ |
| 2026-08-06 | **スカッシュマージを続けると、同じ内容なのに履歴がずれて競合になる** — #6〜#8 を squash merge した結果、main とブランチに同じ内容が別コミットとして存在し、PR #9 が CONFLICTING になった。中身の衝突ではないので、`main 固有のファイル・内容がゼロ`であることを確認したうえで `git merge -s ours origin/main` で履歴だけ繋いだ（**merge 前後の tree ハッシュ一致を確認**）。`-s ours` は相手側の変更を黙って捨てるので、**「相手に固有の内容が無い」ことを確かめずに使わない**。長い feature ブランチを使い回すなら、squash ではなく通常マージにするか、マージのたびにブランチを切り直すほうが安全 |
| 2026-08-06 | **導出できるものを人に入力させない** — 市場調査の「反映する/止める」は、どの調査が現行かを人に切り替えさせる手作業の帳簿だった。実施日が入っていれば「最新で3段階以上スコアが出ているもの」で決まる。同様に、一覧のバッジも保存値ではなく状態から出す形に変えた。**状態から決まるものを状態として持つと、来年の調査を取り込んだ瞬間に自動で入れ替わる** |
| 2026-08-06 | **同じ数字を2箇所で計算しない（コンポーネント化の判断）** — ブランドスコアを管理画面とポータルで共用するとき、総合スコア・5段階の並び・推移グラフの行という派生値をどちら側に置くか迷ったが、**コンポーネント側に持たせた**。呼び出し側で計算すると、片方だけ直したときに同じサーベイが画面ごとに違う点数になる。インナースコアの算出も `computeSurveyScores` に切り出してスナップショットと推移APIで共通化した |
| 2026-08-06 | **ローカルの `npm run build` は「未コミットの作業ツリー」を見ているので、コミット漏れを検出できない** — `page.tsx` で `SnapshotScheduleCard` に `recordSlot` を渡す変更だけをコミットし、受け取る側のコンポーネントを staging に入れ忘れた。作業ツリーには両方あるためローカルビルドは通り続け、**Vercel が committed tree でビルドして初めて型エラーになった**。マージ直前に気づけたのは PR の check を見たため。`-A` を使わず対象ファイルを明示指定する運用（別セッションの作業を巻き込まないため）では、**props を足したら渡す側と受ける側の両方を staging に入れたか確認する**。あるいは push 後に CI のグリーンを待ってからマージする |
| 2026-08-06 | **「未計測」と「0点」を混ぜると、測っていないことが低評価として集計される** — アウタースコアで、名刺のPVが3件しかない会社の「関心度0点・遷移率0点」がそのまま加重平均に入り、市場浸透79点の会社のアウターが50点になっていた。スマート名刺をオフにしている会社は名刺ページ自体が非公開なので、アクセスが無いのは当たり前。**測れないものは null にして分母から外す**（5段階の `absent`／`unmapped` を分けているのと同じ考え方）。同じ理由で、GT表のパーサも読めない値は 0 ではなく null にしている |
| 2026-08-06 | **「各企業認知者ベース」の設問は、母数の小さい会社が順位を壊す** — ブランド認知度調査のロイヤリティ設問で、n=27 の 81.5%（22人）を n=182 の 78.0% と同じ土俵で並べたため「12社中2位」と出た。調査会社のレポートは1位。認知率が低い会社ほど分母が小さくなるので、**%だけを並べると無名の会社が上位に来る**。調査票が N数一覧のページをわざわざ置くのはこのため。`MIN_BENCHMARK_BASE_N=50` で切ったところレポートと一致した。母数が読めないセルは判断材料が無いので残す |
| 2026-08-06 | **設問の種類が違う数字を、同じ物差しで比べさせない** — 純粋想起（何も見せずに社名を書いてもらう）は第1想起なら全社の合計が最大100%にしかならず、41の回答に分散すれば平均は2.1%になる。助成想起の認知83%と並べると「想起が低い」と読めるが、低さは市場の評価ではなく割り算の構造。段階ごとに変換式を変え（想起は10%で50点・30%で100点）、AI考察のプロンプトにも明記した。**同じ画面に%とスコアが並ぶときは、どちらで比べるのかを本文に書く** |
| 2026-08-06 | **点数にできるからといって点数にしない** — 市場が重視する上位5項目のうち自社イメージでも上位に入る数を「印象一致度」として0〜100で出したが、実体は5分の3＝60。同じ画面の 79.0／91.0 とは単位が違い、1項目の増減で20点飛ぶ。**定点観測に使えない数字を、使える数字の隣に置かない**。順位の対比表だけを残し、点数は表示から外した（DBには保存してある） |
| 2026-08-06 | **系列色は印に、文字は黒に** — アウターを緑にした流れで凡例やリンクの文字まで系列色にしたところ、青と緑の文字が並んで系列名そのものが読みにくくなった。識別は左の色印が担うので、**文字は本文色のまま**にする。同様に、SD法の対義語を右端に置くとバーの空白が「対義語側」に見えるが、実際その大半は「どちらともいえない」（革新的な22.0%の残りは、古くさい7.1%と中立70.9%）。**引き算で読めてしまう置き方をしない** |
| 2026-08-05 | **同じデータの「切り方」が2つあると、矛盾していないのに矛盾して見える** — クライアントのレポートは30問を設問番号の並びで4領域に切り「行動体現3.37が最低」、画面は意味で5段階に切り「理解57.1が最低」。**どちらも正しく、原因は Q24「自部門の貢献を説明できる」が並び順では行動体現の塊に入るが意味は理解であること**。数字が合っているかを検算しても発見できず、両方の集計を並べて初めて分かった。**同じ指標を2箇所で見せるなら切り方も揃える**か、揃えられないなら食い違いの理由を書いておく。今回は画面を正本として軸を5段階に一本化し、レポート側の修正指示を別途作成した |
| 2026-08-05 | **AIに数値を語らせるときは「与えられた数値だけを使う」と縛り、出力を全件検算する** — サーベイ詳細のAI考察は、集計済みの値だけを JSON で渡し、プロンプトで新しい数値を作らないよう明示した。生成後に 10.9pt差・15.6pt差・19.4pt差・149人中32人・Q24 2.89 の全てを画面の実測値と突き合わせ、捏造ゼロを確認。**検算しない限り「もっともらしい数字」は混入し得る**前提で運用する。あわせて生成結果は DB に保存して未生成時のみ自動生成＝表示のたびにAPIを叩かない |
| 2026-08-05 | **「クリア」「達成」のような達成感のある語は、単なるしきい値判定に使わない** — 通過率の説明を「そこまでの段階を全部クリアした人の割合」と書いたところ、北川さんから「できちゃってるってことでしょ？」と指摘。実際は各段階の平均が3.5点以上かという線引きにすぎず、3.5は5点満点では「まあまあ」。**認知51.8%が「半分の人はもう認知の課題を終えた」と誤読される**。「平均3.5点以上だった人の割合」と事実だけを書く形に直した |
| 2026-08-05 | **PostgREST の行数上限は「エラーにならない」ので最も危険な部類のバグ** — `.select()` に上限指定が無いと既定1000行で黙って切られ、集計が「先頭1000行だけの値」になる。**件数もエラーも返らない**ため、画面には一見それらしい数字（実例: 33件）が出て気づけない。既存サーベイが6人×15問=90行だったため2年近く顕在化せず、243名×30問=7,290行の取り込みで初めて露見（約1.1pt のズレ・毎日3時の cron も影響下）。**集計クエリは全件取得を明示する**（`lib/brand-score/fetch-all-rows.ts`）。「1000件」という数字を見たら上限を疑う |
| 2026-08-05 | **丸めた値を再集計すると誤差が拡大する** — API が設問ごとの平均を小数2桁に丸めて返し、画面側がその値で加重平均を取っていたため、設問1問だけのマスで実測と 0.1 ずれた。0〜100換算は25倍なので**丸め誤差 ±0.005 が ±0.125 に増幅**する。設問数の多い段階スコアは誤差が相殺されて一致したため、発見が遅れた。**再集計される可能性がある値は表示用に丸めず、丸めは描画時に行う** |
| 2026-08-05 | **「1人=1レコード」の手がかりが無いテーブルで人数を数えると静かに間違う** — `brand_survey_responses` は匿名設計で回答者IDを持たず、人数を「行数 ÷ 設問数」で推定していた。職種別にフォームを分けた取り込み（BO版30問／SP版30問→マージ後32問）では各群が全設問に答えないため、実数 243/149 が 228/140 になっていた。**その群が実際に回答した設問の種類数で割る**のが正しい。ただし1つの役職群が複数の設問セットにまたがると、この推定自体が成立しない（368 vs 392）。**人数を主役にする指標を作るなら `respondent_id` を持たせる**（暫定で `submitted_at` をキーにしたが、同一秒の複数提出で1人に潰れる。実際に既存2サーベイは全行同一タイムスタンプで回答者を区別する情報が存在しなかった） |
| 2026-08-05 | **リンクの受け皿が無いと、認証は成功しているのに何も起きない** — Supabase の Site URL がマーケLP（Supabase クライアントを読み込まない）だったため、パスワードリセット／マジックリンクのトークンが誰にも拾われず捨てられ、トップページが開くだけだった。**リンク自体は正常**で、supabase-js は「クライアントが生成されたときに URL のハッシュを読む」ので、クライアントの無いページに飛ばすと無言で失敗する。`resetPasswordForEmail` の `redirectTo` を明示して Site URL 設定に依存させないのが確実 |
| 2026-08-05 | **クライアント描画のページは `curl` で検証できない** — デプロイ反映の確認で `/admin/login` を curl したが文言が0件で「未反映」と誤判定しかけた。実際は `checkingSession` 中スプラッシュを返す設計で、HTMLソースに本文が含まれないだけだった。同様に「POSTが405を返す＝ルートが存在する」も誤りで、**存在しないパスでも405が返る**。**反映確認は実ブラウザで実物を見る** |
| 2026-07-21 | **未コミットのまま長時間持つと並行セッションの `git reset` で消える** — 3Dビューの実装一式（新規ファイル含む）が別セッションの `reset: moving to HEAD` で消滅し、作り直した。git に入っていない作業は復旧できない。**並行作業がある前提で、区切りごとに即コミットする**（以降この運用に切り替え、実際に守れている） |
| 2026-07-21 | **「初回の値で確定」させると非同期の後追いで壊れる** — 「編集する」の開閉を最初の `onStatusChange` で latch していたが、`inspection` は後から確定するため初回は必ず未完了。結果、完了しても開いたままだった。**非同期で確定する値に依存する初期化は、確定するまで追従させる**（ユーザー操作があればそちらを優先） |
| 2026-07-21 | **番号で並ぶUIは番号ハードコードが必ず腐る** — ウィザードにステップを1つ挿入/削除するたび、`gotoStep(4)`・`activeStep === 5`・`step <= 5`・案内文の「ステップ5」が一斉にズレた（実際にクイックアクションが別ステップを指すバグが発生）。**`STEPS` から `LAST_STEP` を導出し、他所のマッピングも定数経由にする** |
| 2026-07-21 | **ID を人に見せない（見せるなら「内部ID」と明示）** — 達成条件と測定値で `metric_key` を手入力させていたため、目標と測定値でキー・単位がズレると黙って「判定不能」になっていた。**辞書テーブル（`metric_definitions`）を置いて名前で選ばせ、キーと単位は定義から自動で埋める**と、不一致が原理的に起きなくなる |
| 2026-07-21 | **「データ不足」と「条件を満たさない」を混同しない** — aggregate 判定で、一致する測定値が0件でも `latest` なら `NO_MEASURED_DATE`（測定日がない）を返していた。原因の違う2つが同じ文言になると利用者は直しようがない。**母集団を先に作り、空なら NO_MATCHING_MEASUREMENT、母集団はあるが日付が無い場合だけ NO_MEASURED_DATE** |
| 2026-07-19 | **`select` に列を足し忘れると絞り込みが黙って空になる** — `governance_rules` の select に `rule_type` が無いのに `r.rule_type` でフィルタしていたため `bannedTerms` が常に `[]`（禁止語チェックが無言で無効化）。エラーにならないので気付けない。**フィルタに使う列は必ず select に含める＋抽出は純粋関数に切り出してテストを書く**。合わせて実在値の確認も必須だった（`discouraged_expression` は存在せず、正しくは `banned_word` / `claim_rule` / `compliance_rule` / `tone_rule`） |
| 2026-07-19 | **ハッシュはDB側で算出する** — 人間判断の override 失効判定に使う `rule_hash` は `achievement_rule::text` の sha256 を DB（トリガ＋RPC）で計算する。アプリ側で JSON を直列化すると**キー順や空白の差でハッシュがぶれて誤失効**する。RPC は SECURITY DEFINER＋service_role のみに絞る |
| 2026-07-19 | **テストの substring 一致は文言衝突に注意** — 「current VP は ASPIRATION に入らない」の assert が、注記文言「移行候補・まだ**現在の約束**ではない」に引っかかって偽陽性で落ちた。プロダクト側の文言を変えて解消。`includes()` で否定を検証するときは、他の行に同じ語が現れないか確認する |
| 2026-07-19 | **プロンプト改修は「0件なら従来と完全一致」をテストで固定する** — ASPIRATION ブロックは素材0件なら注入せず、`buildCopySystemPrompt` の出力が従来と**文字列完全一致**することを assert。既存企業への影響ゼロを構造的に担保できる |
| — | supabaseクライアントのauth設定にlock:false必須（LockManagerタイムアウト回避） |
| — | QRコードは1000x1000px高解像度対応 |
| — | provided_valuesはPostgreSQL配列型（text[]） |
| 2026-03-31 | ドメイン認証: companies.email_domain カラム追加、フリーメール除外リスト（gmail, yahoo等）で企業ドメインのみマッチ |
| 2026-03-31 | 承認制参加: members.status='pending'→管理者承認で'active'。拒否時はmember+profile+auth user削除 |
| 2026-03-31 | /portal/auth はログイン専用化、新規登録は全て /signup 経由に統一（Google OAuth含む） |
| 2026-03-31 | UIラベル統一: 「企業名またはブランド名」を全画面（signup, admin, STP, colors, persona）で使用 |
| 2026-04-06 | companies.mvv / brand_color_primary / brand_color_secondary / brand_story のAI参照を brand_guidelines / brand_visuals へ移行。スーパー管理画面からUI削除 |
| 2026-04-06 | スーパー管理画面: 企業カスケード削除API、管理者テーブルにメール・名前表示 |
| 2026-04-06 | ミニアプリ全3種にAutoSaveIndicator追加（Google Docs風、saving→saved→fade out） |
| 2026-04-06 | STP Step5: TargetingDisplay / PositioningDisplay にtitle・showIcon propsで表示カスタマイズ（ポータル/管理画面に影響なし） |
| 2026-06-02 | STP無料プラン上限: `app/api/tools/stp/sessions/route.ts` の `FREE_LIMIT = 3`。`status='completed'` のセッション数でカウント（作成のみ・in_progressは対象外）。管理者/特定ドメインのバイパス分岐は無し（admin_users参照はcompany_id紐付け用のみ） |
| 2026-06-02 | テスト対応: kitakawa@include.bz の完了済みセッション3件を `archived` に退避しカウントを0にリセット（個別データ操作。制限ロジック自体は全ユーザーで有効のまま）。本アカウントは今後さらに3件completeすると再ロックする |
| 2026-06-02 | 参加リクエスト通知: Resend (`noreply@branding.bz`) を `/api/signup/join-company` と `/api/members/join-requests` (approve分岐) に組み込み。管理者宛は `admin_users.auth_id` → `supabaseAdmin.auth.admin.getUserById()` でメール解決。`RESEND_API_KEY` 未設定時はスキップ、送信失敗時も本処理は成功扱い（contact APIと同パターン） |
| 2026-06-02 | members status フィルタ: `/admin/members` 一覧クエリは `is_active` で「有効/無効」を表示するため status フィルタを掛けていなかった。pending メンバーを除外する際 PostgreSQL の `!= 'pending'` は NULL を除外してしまい既存レコードが消えるリスクがあるため、SELECT に `status` を含めて **クライアント側で `.filter(m => m.status !== 'pending')`** する方式を採用 |
| 2026-06-02 | **認証「読み込み中」固まりの根本原因＝自前lock実装の単一障害点**。@supabase/ssr へ移行し cookie ベース＋middleware.ts の getUser() で期限切れトークン自動リフレッシュ。lock 上書きは廃止（旧「lock:false必須」メモは無効化） |
| 2026-06-02 | AuthProvider 6種（admin/portal/superadmin/tools/stp/unified）を AppAuthProvider（セッション層）+ AdminData/PortalDataProvider（データ層）に統合。旧 hook（useAuth/usePortalAuth等）は alias で互換維持し既存88箇所は無変更。診断レポート: `docs/auth-diagnosis-report.md` |
| 2026-06-02 | **@supabase/realtime-js@2.106.2 は phoenix サブモジュール欠落バグ**で middleware が Module not found→全ページ500。package.json overrides で 2.105.4 に固定して回避 |
| 2026-06-02 | Header.tsx の isLoggedIn 分岐は SSR/CSR 差で Hydration mismatch → mounted フラグで初期表示をSSRと一致させてからCSRで切替 |
| 2026-06-02 | マイページ遷移を高速化: クライアントで getUser+admin_users を待たず /mypage Server Component で cookie 判定し 307 即リダイレクト |
| 2026-06-02 | ログイン/登録ページ（admin/login, portal/auth, signup）のロゴリンクは絶対URLでなく相対パス `/` に。プレビュー環境のlocalhost外ブロック回避＋全環境で正しく動作 |
| 2026-06-02 | メンバー削除は Service Role API（/api/members/[id]）で timeline投稿/いいね/コメント→members→profiles→auth.users をFK順に連鎖削除。孤立auth.users復旧用に /api/members/cleanup-orphan と管理画面UIを追加 |
| 2026-06-03 | **機能トグルは配列駆動**。`lib/constants/feature-toggles.ts` の `FEATURE_TOGGLES` が唯一の定義源（key=companiesのbooleanカラム名）。設定ページは配列をmap、PATCH API（/api/admin/settings, service_role, company_idガード）は `FEATURE_TOGGLE_COLUMNS` で許可カラム判定、両Providerの companies select も同配列をspread展開。→ 新機能は **companiesにカラム追加＋配列に1行** で全箇所自動対応。判定は全箇所 `isFeatureEnabled(company, key)`（`!== false` 方式＝null/未追加でも有効扱い） |
| 2026-06-03 | 出し分け方針: タイムライン/KPIは社内に閉じるためサイドバー項目＋各ページ＋ポータルトップのウィジェット＋管理ダッシュボードのタブを非表示。**スマート名刺は公開ページ /card/[slug] を門番ガード**（認証外のサーバーComponentで `companies(*)` joinの `card_enabled` を見て「非公開」表示。slug発行ロジックとデータは不変、再オンで一斉復活） |
| 2026-06-03 | `card_enabled` は **profiles と companies の両テーブルに存在**。profiles.card_enabled=個人ごとの名刺公開フラグ（/card の既存 `.eq('card_enabled', true)` フィルタ）、companies.card_enabled=今回追加の会社単位トグル。**別物**。公開ページは両方を独立に評価 |
| 2026-06-03 | `/admin/analytics`（アクセス解析）にアウター指標は同居していない（純粋な名刺閲覧解析）。`OuterScoreSection.tsx` はフォルダにあるが **どこからもimportされていない孤立ファイル**。アウタースコアは `/admin/brand-score` 側で `/api/analytics/outer-score` 経由表示（名刺トグルでは触らない） |
| 2026-06-03 | **コンセプトビジュアルは新カラム `concept_visuals(jsonb配列)` とレガシー `concept_visual_url(単一URL)` の二重持ち**。保存時に `concept_visuals[0]` を `concept_visual_url` へ必ず同期（CIマニュアル表紙 `lib/ci-manual/sections/cover.tsx` とseedが単一URLを参照するため）。読込は新カラム優先・空ならレガシーを1枚として配列化。portalスライドショーは `app/portal/guidelines/ConceptVisualSlideshow.tsx`（カルーセルlib未導入のため自前実装、枠高さは先頭画像で固定しobject-cover敷き詰め）。アップロード先は `brand-assets` バケット `{companyId}/concept-visuals/` |
| 2026-06-03 | **Supabase MCP `apply_migration` でカラム追加してもPostgRESTのスキーマキャッシュが自動リロードされない**ことがある（このプロジェクトはDDL用イベントトリガー未設置の疑い）。新カラムを含むPATCH/INSERTが `PGRST204 column not found in schema cache` で失敗 → 保存できない症状。対処は `NOTIFY pgrst, 'reload schema';` を execute_sql で実行。dev/prod は同一Supabaseプロジェクトなので一度直せば両方解消 |
| 2026-06-03 | **ポータルのアクセス権は `members` 行（auth_id + is_active=true）で判定**（`PortalDataProvider`）。管理画面は別系統で `admin_users` 判定。企業＋初期ユーザーを作る処理は admin_users だけでなく **profiles＋members も必ず作る**（手本: `app/api/members/create/route.ts`）。members 無し＝管理画面は入れるがポータルは「アクセス権限がありません」 |
| 2026-06-03 | **brand_* 行は企業作成時に作られず、各編集ページ初回保存で upsert される**。管理ブランドページ（guidelines/verbal/visuals）の `brand_*` 取得は `.single()` でなく **`.maybeSingle()`**（新規企業は0件で `.single()` だと PGRST116→「データの取得に失敗しました」）。配列取得（`.order()`）の values/personas/terms は0件でも問題なし |
| 2026-06-03 | **ブランド表示の真の参照元は `brand_guidelines`/`brand_visuals`**。`companies.slogan/mvv/brand_color_*` は表示にほぼ未使用の孤立カラム（mvv/secondaryはデッド、primaryはCIマニュアルのフォールバックのみ）。新規企業モーダルから当該入力UIを削除済み |
| 2026-06-03 | **ポータルの slogan 漏洩バグ**: ダッシュボード見出し/サイドバーは `PortalDataProvider.slogan`（=brand_guidelines.slogan）を表示。行が無い企業で `setSlogan` を呼ばないと前に見た企業の値が残る。→ 無条件 set ＋ fetch 開始時に null リセットで解消。slogan 空のときダッシュボード大見出しは非表示 |
| 2026-06-03 | **右下FABは共通コンポーネント `components/ui/fab.tsx`（Fab/FabButton）を使う**。生の `<div className="fixed bottom-...">` は新規に書かない。variant=primary(黒)/secondary(白枠)、icon/type(submit)/form/disabled を受ける。ローディング切替は呼び出し側で icon と children を出し分け |
| 2026-06-03 | **mission/vision は「コピー＋空行＋説明文」を1カラムに保存**。表示側は `lib/brand-mvv.ts` の `splitBrandCopy()`（最初の空行 `/\n\s*\n/` で2分割、空行なしは全文コピー扱い）で出し分け、編集側は `combineBrandCopy()` で再結合。DBスキーマ・編集の運用（1テキスト）は不変。説明文の表示は `whitespace-pre-line` で改行保持（card/kpi にも付与済み） |
| 2026-06-03 | **左端カラーバー装飾は「らしさ」カード方式**＝`relative overflow-hidden rounded-*` の上に `absolute left-0 top-0 bottom-0 w-1 bg-*` を重ね、カードの角丸でクリップして丸端に見せる（`border-l-2` の直線とは別物）。行動指針・事業内容もこの方式（青）に統一。内側は `pl-5` でバーと本文の重なり回避 |
| 2026-06-03 | **セクション見出しH2は `text-xs font-bold ... tracking-wide`**（旧 text-sm から1段階縮小、全123箇所）。複数H2を縦に積むセクションカードは `CardContent` の `space-y-6`（24px）で section 間を空け、`space-y` 仕様で最上段H2のみ余白据え置き。マーケLP/ツールの大見出し（text-xl/2xl/4xl）は対象外 |
| 2026-06-03 | **管理サイドバーの実体は `app/admin/components/AppSidebar.tsx`**（`Sidebar.tsx` は未使用の旧版）。ポータルは `app/portal/components/PortalSidebar.tsx`。各ページ見出しはパンくず方式（`lib/admin-breadcrumb.ts` / `lib/portal-breadcrumb.ts`、マップで pathname→{section,title}）に集約済みで本体h1は無し |
| 2026-06-03 | **ブランド基盤の項目オーナーシップ（最終形）**: 行動指針=`brand_guidelines.action_guidelines`（編集=ブランド方針）／特性traits=`brand_guidelines.traits`（編集=ブランドパーソナリティ）／パーソナリティ概要=`brand_guidelines.personality_summary`（編集=ブランドパーソナリティ・ポータル感じられ方のレーダー下に表示）／トーンオブボイス=`brand_personalities.tone_of_voice`（編集=バーバル・ポータルもバーバルに表示）。**brand_guidelines は複数画面が同一行を触るため必ず PATCH（部分更新）で。行が無ければ INSERT** |
| 2026-06-03 | **行動指針の移設**: 旧 `brand_personas` 先頭ペルソナ行の `action_guidelines` 相乗り → `brand_guidelines.action_guidelines` カラムへ移行（apply_migration＋データ移行＋NOTIFY reload）。ブランド戦略はペルソナを delete→insert で作り直すため、行動指針を相乗りさせると保存のたびに消えるリスクがあった。これを解消。読み取り側はポータルブランド方針/タイムラインのカテゴリ/管理ダッシュボード/CIマニュアル。`brand_personas.action_guidelines` カラムはレガシー残置（無害） |
| 2026-06-03 | **「保存したのに他ページ往復で消える」の2大原因**: ①**カラム追加/削除のDDL後に `notify pgrst,'reload schema'` を忘れる**と書き込みが PGRST204 で静かに失敗（ロゴ基本形 logo_images で再発）→ apply_migration の直後に必ず reload。②**管理編集ページは `page-cache`（モジュール内Map）でSPA往復時の再fetchをスキップ**する設計のため、**保存成功時に `setPageCache` で最新値を更新しないと往復で保存前の表示に戻る**。visuals は対応済み。他のブランド編集ページも同パターンの潜在リスクあり（保存後 setPageCache 未実施） |
| 2026-06-03 | **ビデオラーニング**: `app/api/learning` 配下は全て service_role。認証は cookie `getUser()` で 管理者=admin_users / メンバー=members(is_active).profile_id・company_id を解決（共通ヘルパー `lib/learning/auth.ts`、ブラウザは @supabase/ssr cookie のため Route Handler で getUser 可）。`views` は1セッション=1行をINSERT→PATCH、進捗は既存値とMAXで巻き戻り防止・completedは一度trueなら維持。duration確定もviews PATCHに同梱（videos PATCHは管理者専用のまま）。集計は analytics ルートのJSで動画別＋メンバー別マトリクスを生成。プレイヤー(`YouTubePlayer.tsx`)はYT.Playerが渡したDOMをiframeで置換するため、React管理ノードを直接渡さず手動生成した子ノードを渡す（アンマウント時 NotFoundError 回避）。新規2テーブルはRLS有効・ポリシー無し（service_roleのみ）。MVPは全社利用可（プラン制限は Stripe 実装後） |
| 2026-06-04 | 理解度テストは**サーベイ（共感）と別物**＝正誤のある知識テスト。サーベイ匿名回答テーブルと混ぜず専用4テーブル（記名 profile_id 持ち）で分離 |
| 2026-06-04 | k匿名は**API側で担保**：results に個人行を返さない／participants にスコアを載せない／部署・役職・設問別は n≥3／overall・company_average も n<3 で抑制（UI隠蔽ではなくデータを出さない） |
| 2026-06-04 | 受験用 `take` は `correct_option_id`/`explanation` を SELECT せず除外（提出前の正解リーク防止）。本人確認は `getMemberContext`/`getAdminContext`（セッション）、クライアント `profileId` 不使用 |
| 2026-06-04 | 採点は**単純正答率**（カテゴリ加重なし）。ギャップ分析は共感（サーベイ）×知識（テスト）を WHY/HOW で対比 |
| 2026-06-04 | AI設問生成：汚染ブランドデータ（別クライアントの混入）をAIが無視して整合データのみで作問＝「捏造禁止」ルールが機能。配信前の管理者レビュー必須 |
| 2026-06-04 | 複数セッション並行時のコミットは**パス明示 stage**（`git add -A` 不使用）で他作業の巻き込みを防止 |
| 2026-06-04 | 死んだコード検出は **`npx knip`** が決定版（未使用ファイル/export/依存をまとめて検出）。ただし「未使用export」でも**内部使用**しているケース（例: `getContrastRatio` は `calculateAccessibilityScore` が内部利用、`PORTAL_PAGE_KEYS` は型生成に利用）は消すと壊れる → 削除前に必ず内部参照を grep 確認 |
| 2026-06-04 | **同期/バックアップツール（iCloud/Time Machine等）が git 操作を巻き戻す**：`git rm` で削除したファイルが**未追跡で物理復活**（mtimeが元のまま）し、削除済み export を import してビルドを壊す。さらに `.next/` 内に **「○○ 2.ts」形式の重複ファイル**を生成し `tsc` が `Duplicate identifier` で落ちる。対処: `find . -name "* 2.*" -delete`＋復活ファイル再削除。恒久対策は同期対象から repo を除外 |
| 2026-06-04 | **console.log 一括削除の落とし穴**：行ベース削除は `console.log('msg', {` のような**複数行ログの1行目だけ消してオブジェクト本体を孤立**させ構文崩壊する。括弧バランスを追う方式で削除し、必ず `tsc` で検証。`console.error/warn` は残す |
| 2026-06-04 | **データProvider（Admin/Portal）はログアウト・ユーザー切替で app固有 state を必ずリセット**。AppAuthProvider は signOut が SPA `router.replace`（全リロードしない）＋同一idで user 参照を保持するため、リセットしないと**同タブで別ユーザーがログインした瞬間に前ユーザーの企業/権限/プロフィールが一瞬露出**する。`if(!user)` 分岐と fetch 開始時の両方でリセット |
| 2026-06-04 | **Context value は useMemo / 関数は useCallback で参照安定化**。`value={{...}}` を毎レンダー新規生成すると、user の参照を保っても全 consumer が再レンダーし「user?.id 依存で再取得を抑える」最適化が相殺される。AppAuth/Admin/Portal の3 Provider で対応 |
| 2026-06-04 | **生成数クランプは client/server 二重で一致**：サーバー `normalizeCount`（floor・0〜20・NaN/負はfallback4）に対し client の `<input type=number max=20>` は**タイピング/ペーストを制限しない**ため、onChange でも同ロジックでクランプしないと「合計N問」表示と実生成数が乖離する |
| 2026-06-08 | **並列セッション環境では未コミットの作業が消える**：複数Claudeセッションが同一repoで作業中、誰かの `git reset`/`checkout`/ブランチ操作で**自分の未コミット変更（新規ファイル含む）が working tree から消失**する（ビデオラーニング階層化の実装が2回消えた）。対策＝**作業はこまめに main へ commit**（push は `merge-base --is-ancestor` で fast-forward 確認後）。DDL（マイグレーション）は git 管理外なので DB には残るが、コードが消えると不整合になる |
| 2026-06-08 | **Turbopack `.next` キャッシュ破損**で dev が `Module not found: next-dev-turbopack.js` / `corrupted database` / `Unable to open static sorted file *.sst`：dev 起動中に git reset/checkout でファイルを大量に入れ替えると Turbopack 永続キャッシュ(`.sst`)が不整合化する。対処＝**dev停止→`rm -rf .next`→`npm run dev` 再起動**（ソース不変・生成物のみ削除で必ず直る）。本番(Vercel)は毎回クリーンビルドのため無関係 |
| 2026-06-09 | **カラースキームを「役割で1色」に統合**（commit 781a8db）。ニュートラル=gray（slate/neutral廃止）/ アクセント・情報=blue（sky/indigo廃止）/ success=green（emerald廃止）/ warning=amber（yellow廃止）/ error=red。**アクセントは橙#FF6A00を一度全面適用→ユーザー判断でblueへ巻き戻し**（橙トークン・config・globalsは完全撤去済み）。**重要な落とし穴**＝スコア/ランクのティア配色（green→blue→amber→orange→red）・感情スケール（red→orange→gray→green→emerald）・KPIカテゴリは「段階/項目の区別」用の**Layer 3 多色で、意味色統合の対象外**。一律 sed で潰すとティアが同色化して壊れる→必ず文脈確認。規約は `branding-bz/CLAUDE.md` カラー章に明文化 |
| 2026-06-09 | **並列セッションの再確認**：色作業中に別経路で `PortalSidebar.tsx`/`PortalLayoutClient.tsx`/`visuals/page.tsx`＋新規 `lib/brand/integrity.ts` 等が working tree に出現。コミットは `git add -u`＋無関係分を `git restore --staged` で除外し**自分の12ファイルのみ**を commit。`git add -A` は他セッションの作業を巻き込むので使わない |
| 2026-06-10 | **理念オントロジーの正本は `philosophy_elements`**（mission/vision=各社1行singleton、value/action_guideline/service=複数行）。`brand_guidelines` の mission/vision/values/action_guidelines/business_content は DROP 済み（退避テーブルにバックアップ）。表示・AI・編集は全て `lib/brand/philosophy.ts`（`fetchPhilosophy`）経由。編集は行差分CRUD（id一致でUPDATE・新規INSERT・消えた行DELETE）。values_sort/business_content_sort（表示順設定）は brand_guidelines に残置 |
| 2026-06-10 | **element_relations はポリモーフィック端点(kind+id)**。直接FK（proof_points.value_proposition_id等）は残し、跨ぐ関係のみ本テーブル。端点検証は SECURITY DEFINER トリガ（存在＋同一company）。トリガ関数は EXECUTE を anon/authenticated から剥奪（トリガは権限無しでも発火する）。`lib/brand/elements-catalog.ts` が5種を `{kind,id,label}` で返す共通取得 |
| 2026-06-10 | **マイグレは必ずローカル.sql先行→適用**（`branding-bz/CLAUDE.md` 恒久ルール化）。MCP `apply_migration` 使用時も同一SQLを `supabase/migrations/<version>_<name>.sql` に保存し同コミットに含める（version はリモート `schema_migrations` 記録値に合わせる）。破壊的変更（DROP）は退避テーブルへ事前バックアップ。旧 iCloud フォルダ撤去→**正本は `~/dev/branding-bz`＋GitHub** |
| 2026-06-10 | **AI判定の誤検知/ハルシネーション対策**: 1社1回のClaude呼び出し（ルート毎に呼ばない＝コスト/レート対策）、NG/OK例をfew-shot、「明確な違反のみ報告」指示、返却の quoted_text が原文に実在・rule_id/target_ref が実在することをコード側で検証し通らないものは破棄。プロンプトで `target_ref` と label を**別行**に（同一行だとAIがlabelをrefに混入させ全dropするバグ）。1チェック≒3,000〜6,000 tokens（sonnet） |
| 2026-06-16 | **モバイルUX基準v1.0が判定基準**（`CLAUDE.md`「モバイルUX基準（確定版v1.0）」）。タップ44px（**glyphは20-24px維持し `padding`/`min-h-11` でヒット領域だけ44px確保**）・入力16px（iOSズーム防止）・コントラスト4.5:1・常用テキスト12px未満廃止・見出し二段階（タイトル16-18px/eyebrow14px）。是正は **token→共通コンポーネント→codemod→個別** の順、1バッチずつ実機/プレビュー確認後にデプロイ |
| 2026-06-16 | **`--muted-foreground` を 45.1%→40%**（globals.css・白地で約4.7:1→約5.7:1）で日時/メタ/薄ラベルを全画面底上げ。これ以上薄いグレーを小文字に当てない。**FABは例外的に `h-14`(56px)**（fab.tsx・浮遊アクションボタン。インライン大は `h-12` のまま） |
| 2026-06-16 | **作業フォルダ移転**: branding-bz 正本は **`~/dev/branding-bz`＋GitHub**。旧 `~/Documents/.../ID_bzサービス開発/branding-bz`(iCloud) は凍結＝新規作業しない。.md知識はリポ内（CLAUDE.md @import）、成果物はGoogle Drive、`.env.local` は各自手元。STATUSは `docs/BRD-PROJECT-STATUS.md`（repo内）へ移設済み。プレビューは作業ルートが ~/dev のセッションで行う |
| 2026-06-18 | **コピーAI設計の核**: ①事実は機械・語りはAI（craft採点はLLMでなくコードで合成）②生成器≠批評器（生成sonnet／批評opusの別モデル）。INTENT(引用禁止)/FACT(引用可)/RULES(禁則) を物理分離してプロンプト注入。クリシェ密度・継承重複(containment)・数値捏造は決定論計算、Tension/Stance/藁人形チェックのみLLM。批評は処方箋のみ返し本文は書かせない（先祖返り防止） |
| 2026-06-18 | **コピーAI DB**: `copy_projects/insights/angles/drafts/quality_reviews`（RLS superadmin_all＋member_select）。人間ゲートはクライアントから id のみ受け、本文はサーバ再取得（改ざん防止）。インサイトは pain_points 等へ source_ref 接地必須・接地しない候補はコード破棄 |
| 2026-06-18 | **ペルソナ粒度はセグメント型(役割アーキタイプ)に統一**＝オントロジー(discrete pain_points)に揃えるため。suggest-goals の課題は短い体言止め。rich profile(口癖等)は voice 素材として temporarily 任意保持。connect は company のペルソナを sync(上書き)するため既存ペルソナのある company でうかつに連携しない |
| 2026-06-18 | **JSON生成の途中切れ対策**: suggest 系で maxTokens 不足だと長文出力が切れて `JSON.parse` 失敗。maxTokens 引き上げ＋「完結した最後のオブジェクトまでで配列を閉じる」救済パーサで復旧。継承重複は対称Jaccardでなく containment(|A∩B|/|A|) で測る（短いコピペを大コーパスで希釈しないため） |
| 2026-06-20 | **ジャーニーは孤立データ**: 生成時は guardrails＋relations を注入する(入力はオントロジー参照)が、`journey_map_data` の出力は下流(コピーAI/オントロジー)から読まれない。コピーAIの起点は pain_points。ジャーニーUIシンプル化は3案提示中 |
| 2026-06-20 | **ジャーニーUIをタッチポイント中心に再定義**：ジャーニー＝「ブランド施策を当てるタッチポイントを洗い出す道具」。感情カーブは「優先度の注釈」に格下げ。`Persona.journey_map` でマルチペルソナ化、UIは「全ペルソナ集約ビュー」（重ね描き感情グラフ＋集約TPプール＋単体絞り込みフィルタ）。優先度＝`カバー人数 × ネガティブ感情` で算出（`calcPriority` 関数で1箇所集中・調整容易）。集約キーは `(stage_idx, normalize(tp_name))` |
| 2026-06-20 | **タッチポイント表記の集約バグ→共通ラベル化で対処**：suggest-journey が「具体的・固有名詞（括弧でキーワード補足）」で出していたためペルソナ間で文字列が一致せず cov=1 ばかりで優先度が全Lowに張り付いた。プロンプトを「短い共通ラベルで、同じ接点は必ず同じ表記」「長い括弧付き固有表記禁止」に変更して解消（実生成で 3人カバー15行・2人カバー4行に）|
| 2026-06-20 | **感情フィルタ削除（重複機能の整理）**：相対優先度に変えた後は「ネガのみ」と「優先度High」が同じものを別の言い方で再フィルタする状態に。`emotionFilter` state＋UI＋適用ロジックを削除。残るのは「表示ペルソナ（全員/各ペルソナ）」と並び替え（優先度/ステージ/カバー人数）＋「全ペルソナ接触のみ」フィルタ |
| 2026-06-20 | **タッチポイント語彙の偏り対策＝Lv2分類体系**：suggest-journeyの代表ラベルが17件のデジタル偏重だったため、AIの選択肢自体がオンライン中心になっていた。`lib/persona/touchpoint-vocabulary.ts` で12カテゴリ・90ラベル前後（オンライン検索/オウンド/ペイド/SNS/アーンド／イベント／人的対面・紹介／紙媒体／CS／物理体験／検討購入／**ブランドクリエイティブ表現**）を体系化。業態タグ（saas/b2b/b2c/manufacturing/medical/local_smb等）で `★主要` マークを付与。`brand_creative` カテゴリ（ロゴ/CI/空間/サイン/ムービー/パッケージ/印刷物）は **ID INC.のクリエイティブ領域を網羅して間接的セールス導線を組み込む** マーケ意図あり |
| 2026-06-21 | **公開サイト ダークデザイン全面移行＋制作事例ショーケース刷新（本番デプロイ）**：旧LP(`/lp`)を `/` 系（`app/(site)`）へ昇格、旧デザインは `/classic` 退避。トップの「Made with」風ショーケースを7列パターン化＋読み込みごとにランダム並び替え。各カードを実クライアントの実事例に差し替え（画像は `public/marketing/images/showcase/showcase-NN.jpg`、PNG→JPG q80最適化。業種ラベルは `lib/constants/industries.ts` の業種マスタに準拠）。一部カードはカスタマーボイス化（櫻井運輸/テックブリッジ/ritz medical）。無料ツール4ページ（STP/ペルソナ/カラー/パーソナリティ）をダーク化＋ファーストビューのキャッチを「AIでブランディングを加速させる。」に統一。`/portal/auth` も新デザインに調整 |
| 2026-06-23 | **AIボタンの文言・配置ルールを策定し4ツール統一**（`docs/260622_AIボタン文言ルール_v1.md`）。文体「AIで{動作}」（動作語＝STP/カラー=提案→「提案生成」・ペルソナ=生成・パーソナリティ=診断）、ローディング「{動作}中…」、配置＝見出し直下・左寄せ（下部ナビ内に置かない／二重配置しない）。見出し下リード余白は全ステップ16px(`mb-4`)に統一 |
| 2026-06-23 | **STP Step4 ポジショニングをドラッグ操作型に改修**（`InteractivePositioningMap`新設：Pointer Events・`touch-action:none`・ヒット領域22px・`setPointerCapture`・viewBox 5:3全幅・1カラム＝リスト→詳細スライダー→チャート）。表示専用 `PositioningMap`・`PositioningData`・APIは不変。**ペルソナ Step4（ジャーニー設計）は 表示フィルタ＋感情カーブ＋タッチポイント候補プールを1カードに統合**（感情グラフ＝ステージナビ＋ペルソナ別詳細アコーディオン、ペルソナ選択は上部フィルタに集約）。4ツールのヘッダーロゴを LP と同じ `/logo.svg`(top -2px) に統一（本番デプロイ済み） |
| 2026-06-23 | **git運用の教訓**：`git add <file> && git commit` は**インデックス全体**をコミットするため、並列セッションがステージ済みの変更（旧 `Step2Demographics`/`Step3Goals` の削除・再作成）を**巻き込んで誤コミット**した。対策＝**`git commit -m "…" -- <path>` のパス指定コミット**で自分の変更だけを確定する（`git add -A` 同様に巻き込み注意） |
| 2026-06-25 | **ペルソナbuilder↔管理画面の呼応＝Tier整理**: 管理画面ペルソナフォームが正＝Tier1（discrete: needs/pain_points/decision_factors/buying_barriers/brand_expectations）＋本人基本（name/age_range/occupation/description）。ビルダーのTier2（性別・役職・勤務先規模・媒体・性格特性・購買動機）は `persona_data` 止まりで管理画面非表示＝「詳細設定」アコーディオンへ。**description はオントロジー(graph)の端点ではないが copyAI が personaBlock の `状況:` として読む生きた入力**（`lib/copy/ontology-blocks.ts`／`insights.ts`／personality診断も参照）＝消さず、自動連結より入力文章を優先。孤立フィールド（居住地/趣味/成功定義/1日の過ごし方/口癖）は consumer 無し＝撤去。**「案3＝1枚統合カード」は Step2+3 を1枚にする＝必然的に4ステップ化するため、ステップ数を保ちたい要件では統合せず各ステップを管理画面レイアウトに揃える（5ステップ維持）方が合う** |
| 2026-06-26 | **ジャーニーの扱い＋ポータル接し方の表示改善**: ジャーニー設計は branding.bz 連携の対象外（`brand_personas.journey_map_data` には書くが管理/ポータルで表示しない）＝**PDFのみ反映**と決定。Step5に `Step4Journey` を `readOnly` で埋め込み（編集UI/AI生成/フッター/見出し/ペルソナ一覧カードを隠した実ビュー）、PDFテンプレにジャーニー節を追加、連携ダイアログに注記。ポータル「接し方」＝`PersonaCarousel`(2.5枚 scroll-snap)・顔アイコン・brand_expectations・課題色をビルダーと統一(orange)・ニーズ/課題3件＋もっと見る(grid 0fr→1frアニメ)。**判断**: セグメント説明文をペルソナ「説明」へ転化する案は性質が違う（集団の括り≠個人の背景）ため不採用。**運用教訓**: 提案でも懸念は実装前に率直に指摘する（黙って実装しない）。**git**: 並行セッションが `components/*PositioningMap.tsx` を編集中＝パス明示コミットで巻き込み回避 |
| 2026-06-26 | **STP Step2セグメンテーションのUI改善**: AIが出力済みなのに画面に出ていなかった `size_hint`/`priorities` を表面化（Step3ターゲット選定の判断材料の取りこぼし解消）。切り口に **A/B/C/D 番号バッジ**（色分け・5つ目以降グレー）、各セグメントに **規模感バッジ**（大=緑/中=黄/小=灰、クリックで循環）、**「重視すること」入力欄**、**2列グリッド**化＋削除ボタンをカード右上に。型・API・プロンプトは不変。**運用**: 並行セッションが同フォルダで大規模リファクタを実時間編集中だったため、本Step2のみをパス指定コミット(`697ecfd`)＆push＝検証済みの自分の変更だけを安全に出荷（リファクタ確定後に別途出荷） |
| 2026-06-27 | **並列セッション環境ではローカル `tsc` が本番ビルドと一致しない**: 別チャットがローカルにのみ存在する編集（例：`StepProgressLoader` に新 export を追加）を入れた状態だと、自分の `npx tsc --noEmit` は**ローカルの未コミットファイルを参照して通る**が、Vercel は origin のコミット済みツリーでビルドするため `has no exported member` で落ちる。**対策**＝push 前に「自分が import している先（例: `components/stp/StepProgressLoader.tsx` の `StepProgressPanel` export）がコミット済みか」を確認する。インポート依存が未コミットなら、その export だけ追加コミットで先に出す（実際 `StepProgressPanel` の追加コミットでデプロイ復旧）。`git status`／`git diff origin/main -- <dep>` で依存の同期状態を見る、または `next build` でローカル本番ビルドを通してから push するのが安全 |
| 2026-06-27 | **React useEffect の deps は「配列の参照」で発火する罠**: `setSubTargets(prev => prev.filter(...))` は中身が同じでも**毎回新しい配列**を返すため、それを依存に持つ autosave/再生成 effect が無限に再発火し、適合マップが「リロードのたびに再生成」される事故になった。**対策2点**＝①フィルタ結果が同一なら**同じ参照を返す**（`filtered.length===prev.length ? prev : filtered`）②effect の発火判定は配列参照ではなく**実値を正規化した文字列を ref に保持して比較**（`lastTargetsRef` 方式：`${mainTarget}|${[...subTargets].sort().join('|')}` を前回値と比較し、変化時のみ再生成）。マウント直後は初回フラグで skip、生成中は busy ref で autosave を抑止。さらにハードリロードは unmount cleanup が走らないため、デバウンス保存ではなく**生成完了時に即時保存**する |
| 2026-06-28 | **上書き確認の累積処理パターン**: サーバーが複数項目の上書き確認を `409 needsConfirm` で**1項目ずつ順番に**返す設計だと、クライアントが「今回確認した項目だけ」を送ると前ラウンドの確認が毎回失われ、サーバーが先頭項目で再び 409 → **無限ループ**になる。**対策**＝確認済み overwrite フラグを `accumulatedConfirm` state に**累積**し、各ラウンドで `{...accumulatedConfirm, 今回分:true}` の `newConfirm` を作って**直接** `executeConnect(newConfirm)` に渡す（state 更新の遅延に依存しない）。リセットは**成功時・キャンセル時・新規開始時**の3箇所。`ConnectModal.tsx` で適用 |
| 2026-06-28 | **STP「死蔵データ／届かない情報」の双方向是正**: 監査で①保存されるが表示されないカラム（`target_fit_map_data`・`brand_stance_statements`）と②ユーザー入力/AI生成だが連携で1ビットも本体に届かないデータ（`buying_factors`・`strengths`・`competitors_analysis`・`axis_rationale`・items の `reasoning`/`confidence`）を特定。**ステップ①**＝①を `/admin`・`/portal` に読み取り表示（DB変更なし、既存カラムを読むだけ）。**ステップ②**＝`companies` に `strengths(text)`/`competitors_analysis(jsonb)` を追加し、`connect/route.ts` の保存マッピングを拡張（購買決定要因→`brand_personas.decision_factors`、強み・競合分析→`companies`、軸/配置根拠→`positioning_map_data` に埋め込み）、管理画面は全件・ポータルは強みのみ表示。**教訓**＝連携APIは「ツールで作った値の出口」なので、新フィールドを足したら connect の保存マッピングと表示側の両方を同時に追わないと死蔵カラムが生まれる |
| 2026-06-30 | **UI統一のまとめ出荷（本番デプロイ・commit f4617a5）**: ①各ツールStep1の基本情報フォームを共通コンポーネント `components/shared/ToolStep1BasicInfo.tsx` に集約（STP/ペルソナは薄いラッパー化。`showCompetitors`/`targetLabel`/`targetLead`/`nextLabel` で出し分け。ペルソナは競合欄を非表示＝companies.competitors を空で潰さないガード付き。AI提案の suggest-targets は「STP専用でなく共用」扱い）。②カラー定義・パーソナリティの**フッター次へ/戻るをL仕様**(`h-14 px-6 text-base font-bold`)に統一（STP/ペルソナと同段。全16箇所）。③**プルアップ(ユーザー)メニューをポータル基準に3画面統一**（管理・スーパー管理を `h-11 px-3 gap-2 text-base font-medium` + content `p-2`）。④スーパー管理サイドバー背景を黒寄りダークグレー、管理画面サイドバーをワントーン調整、`SUPER ADMIN` ヘッダーバッジ削除。⑤ポータル: ミッションカード背景にコンセプトビジュアル(白80%スクリム＋blur)、らしさ4象限の左縦バー撤去、考え方ページの節間余白32px・沿革レイアウト・投稿カード余白(10/12px)・カード間隔(8px)・罫線撤去、ペルソナカルーセルのページネーションを見出し横へ。⑥`PersonalityTraitList` タイトル・ガイドライン見出しを18px、カラー入力を正方形(スウォッチ余白除去)＋全丸。**呼称確定**: フッター次へ系(`h-14 px-6 text-base`)を「Lサイズ」と正式命名（S/M は AIButton）。**git運用**: 並行セッションの未コミット変更（news系/`package.json`/`PalettePreview`）と混在する作業ツリーから、**自分の17ファイル＋新規1件のみをパス指定コミット**し他者WIPを巻き込まず出荷。実機はローカルプレビュー(3004)で各変更を都度検証、tsc はプロジェクト全体0エラー |
| 2026-06-21 | **「Googleで続ける」は auth.users だけ作って members を作らない**＝新規登録(`/api/signup`)と経路が別。放置すると①members無しで [[portal-access]] ゲートに弾かれ**ユーザーが詰まる**②そのメールアドレスを占有して**後の正規登録が「既に登録されています」で衝突**、の二重の害。対策＝コールバックで孤児判定→`auth.users` ごと削除（`/api/portal/oauth-gate`）。**判定は members を status で絞らない**のが要点（`status='pending'` の承認待ちを消さないため）。「Googleログインできない／メールが既に使われている」系の調査は、まずこのゲートと `auth.identities` を疑う |
| 2026-06-30 | **承認待ちの表現は「行を作る（パターンA）」が正**: 新規owner承認制を「承認まで members/admin_users を作らない（パターンB）」で作ると、その承認待ちユーザーが Google ログインした瞬間に **oauth-gate が孤児と誤判定して削除**する。company を `approval_status='pending'`・members を `status='pending'`/`is_active=false` で**先に作る**ことで、既存 gate を改修せず安全に両立できる。将来パターンBへ変える場合は gate の削除判定に「保留申請が無いこと」の確認を必ず足す |
| 2026-06-30 | **競合ドメインは「即ブロック」でなく「承認キュー＋⚠警告」**: 自動ブロックは誤検知時に正当な登録を締め出し、しかもユーザー側に理由が伝わらない。全件承認制を土台にし、競合一致は**人手判断を助ける警告フラグ**に留める設計を採用。フリーメール（gmail等）はドメインで判定できないため、そもそも自動判定に頼らず人手審査に委ねる前提が要る |
| 2026-07-15 | **shadcn `SidebarMenuBadge` は既定 `top-1.5` の上寄せ**＝項目40px/バッジ20pxだと中心が4px上にずれる。中央寄せは `top-1/2 + -translate-y-1/2` だが、既定は **peer variant**（`peer-data-[size=default]/menu-button:top-1.5`＝specificity (0,2,0)）で当たるため、**素の `top-1/2`(0,1,0) では勝てない**。同じ peer variant キーで上書きすると tailwind-merge が同一プロパティとして解決し後勝ちになる。またバッジは `rounded-full` にすると globals.css の**本文14px底上げの除外条件**に合致し、文字が肥大化しない（`rounded-md` のままだと 12px→14px に膨らむ） |
| 2026-07-15 | **CSS変数のテーマは一部だけ変えると取り残しが出る**: スーパー管理サイドバーは背景だけ紺→無彩色(`0 0% 10%`)に変更済みで、`--sidebar-accent`(紺 `213 45% 35%`)・`--sidebar-border` が紺のまま残り、アクティブ項目が**明度+25pt・色相ありで浮いていた**。通常管理画面の既存関係＝**「背景と同色相・明度+8pt」**（`220 13% 18%`→`218 14% 26%`）が事実上のルールなので、それに合わせて `0 0% 18%` に。**UIの「揃った/馴染む」は computed値の実測で確認する**（明度差・コントラスト比13.6:1 を数値で確認してから断言） |
| 2026-07-15 | **ファビコン/PWAアイコンの正本は `public/logo-mark.png`、生成は `node scripts/gen-pwa-icons.mjs`**（画像リサイズ方式。旧＝スクリプト内のベクターパス＋BG/FG定数で描画する方式は廃止）。マーク変更時は logo-mark.png を差し替えて再実行するだけ。**Next.js の `app/icon.*` は1形式に絞る**（`icon.svg` と `icon.png` が両方あると両方 `<link rel=icon>` が出て不整合）→ svg を削除して png に置換。**`.ico` は ImageMagick 無しでも PNG-in-ICO をNodeで組める**（6byteヘッダ＋16byteエントリ＋PNG本体。全モダンブラウザ対応）。検証は `sips -s format png` で読めるか＋`xxd -l 8` で type=1/count=1。**iOSは既存インストール分のアイコンが更新されない**（ホーム画面に再追加が必要） |
| 2026-07-15 | **「画像を使って」は再現SVGでは代替にならない**: 添付画像と同形状のパスがリポ内(`app/icon.svg`)にあったためインラインSVGで再現したが、北川さんの要件は**提供された正式データそのもの**。チャット添付は私からファイル化できないが、**元ファイルは Mac 上にある**（今回は `~/Desktop/Group 8963.png`）ので、`find`＋`sips`(寸法)＋`Read`(目視) で特定し `cp`→`cmp` でバイト一致を確認して採用するのが正解。再現物は削除して二重管理を残さない |
| 2026-07-15 その2 | **Next.js `<Link href="#xxx">` はハッシュだけのナビゲーションで自動スクロールが不安定**: SPA ルーターが介在すると `location.hash` は更新されるが `scrollIntoView` は呼ばれず、`html { scroll-behavior:smooth }` があっても機能しない。**対策**＝ページ内ハッシュリンクは `<a href="#xxx" onClick={e => { const t=document.getElementById('xxx'); if(t){ e.preventDefault(); t.scrollIntoView({behavior:'smooth'}); history.replaceState(null,'','#xxx') } }}>` のように onClick で明示的に `scrollIntoView` を呼ぶ。別ページからの `/features#feature-xxx` 遷移は普通のブラウザナビゲーションなので `[id] { scroll-margin-top:5rem }` が効いて 80px オフセット着地する（別ページ遷移とページ内リンクで挙動が違う） |
| 2026-07-15 その2 | **ネイティブ `<details>` の open 切替は瞬時＝CSS transition だけでは滑らかに開けない**: open 属性が付くと中身が瞬時に display される（DOM上は非表示→表示）ため、`transition: height` を当てても効かない。**Grid-rows[0fr→1fr] トリック**＝親を `grid transition-[grid-template-rows] duration-300` にし、中身を `overflow-hidden` の div でラップ→state で `grid-rows-[0fr]/[1fr]` 切替（`useState` + button で `<details>` の代わりに構築）。実測: 0px → 111px を 300ms cubic-bezier(0,0,0.2,1) で滑らかに補間 |
| 2026-07-15 その2 | **管理系ゲート画面は 3つの Provider (Admin/Portal/SuperAdmin) で重複**: 各画面が同じ「白背景+リキッドグラスダークカード+リキッドスペキュラ+BottomLogo」を独自に組んでいたため保守負債。`components/admin/GateShell.tsx` にリキッドグラス構造を切り出し（cardStyle は `/admin/login` と完全一致）、`icon` は `ReactNode` で受けて絵文字/lucide 両対応（`<ShieldAlert size={48} />` を呼び出し側から渡す）、`primary/secondary` の CTA ボタンで use case を吸収。1ファイル直せば 5画面（+404/500）に反映される。**スプラッシュ側**は `components/admin/AuthSplash.tsx`（白背景+`text-black/40 text-sm`）で認証中間8ページ（OAuth callback 2 + 後方互換リダイレクト 5 + loading）を統一 |
| 2026-07-15 その2 | **UIの「揃った」発言は実数値で証明する**（[[feedback-verify-ui-claims-numerically]]）: 「揃った」「同じ」と言う前に `preview_inspect` で font-size/color/margin/line-height/boundingBox を並べて完全一致を確認する。今回、PageHero の note prop を14px/white40 として実装しリードの18px/white60 と役割で分けたが、北川さんに「（news のリードと terms の最終更新日が）同じなのか？」と指摘されて初めて役割分割≠揃えたと気づいた。**役割で分けた要素は「揃えた」と呼ばない**、揃えるなら全対象要素のスタイルを同一にする（今回は note を廃止して children に一本化＝両者が同じ 18px/white60/mt-24 で解決）。**preview_screenshot が取れない/viewport 0px の場合も `preview_resize width=1280` で md+ 再現できる**＝検証サボりの言い訳にしない |
| 2026-07-15 その2 | **`/admin/members` 部分適用のトレードオフ**: 全 admin をダーク化するか、1画面だけダーク化するかは大きい判断。今回は北川さんの明示指示「A（部分適用）」で /admin/members だけダーク化したが、他 admin ページ（`/admin/dashboard`, `/admin/brand/*` 等）と AdminHeader/AppSidebar はライト基調のまま＝入るとちぐはぐが残る。全体ダーク化に舵を切るなら C 方針（別セッション大工事）へ切替判断が必要 |
| 2026-07-16 | **LPコピーと実装の食い違いを4ツール横断で解消（月次3回）**: 「無料で3回」LP コピーの実装が3ツール（stp/persona/personality）は**通算3回**でブロック・colors だけ**未実装で事実上無制限**という三重の乖離。統一方針=`MONTHLY_FREE_LIMIT=3` を毎月1日 0:00 JST リセット、カウント起点は `status='completed'` の `updated_at`。「有料SaaS本体を 0円と偽らない」ポリシーがあるため、後述 (④) の SoftwareApplication schema でも `offers` は同じ理由で省略した。**教訓**＝LPコピーの数値・回数は必ずコードの `const` と付き合わせる。定数だけ書いて消費側で参照されていない「デッドコード」の混在（`monthlyGenerations` が定義のみで参照ゼロ）に注意 |
| 2026-07-16 | **Google FAQ リッチリザルトは現行仕様で取れない（2023-08 以降）**: FAQPage schema を実装しても検索結果に Q&A 展開表示されない。Google は 2023-08 に FAQ リッチリザルトを政府・医療機関の公式サイトに**限定**した。一般企業サイトはどれだけ正しく書いても展開されない。ただし今回の実装は残す判断＝(a) 可視 FAQ セクション自体はロングテール検索・CV に効く（schema と無関係）(b) FAQPage schema 自体は害なし・将来 Google が方針を戻せば効く (c) 同時実装した BreadcrumbList/SoftwareApplication は実際に有効検出＝SERP 改善に効く。**教訓**＝schema を書くときは Google リッチリザルトテストで「検出されるか」を実測し、無いなら残す/削るを情報開示した上で判断する。「schema 書いたらリッチリザルトが出る」は今の常識ではない |
| 2026-07-16 | **API route の createClient はモジュールトップレベルで呼ばない（Preview ビルド落ちる）**: Next.js の `next build` は "Collecting page data" フェーズで各 route ハンドラのモジュールを評価するため、`const supabaseAdmin = createClient(URL, SERVICE_ROLE_KEY!)` を関数外で書くと `SUPABASE_SERVICE_ROLE_KEY` が undefined な環境（Vercel Preview 等）でビルドが落ちる。**対策**＝既存 `lib/supabase-admin.ts` の `getSupabaseAdmin()`（singleton lazy）を handler 内で呼ぶ形に統一。判定方法＝`grep -rE "^(const\|let\|var)[^=]*=[[:space:]]*createClient\(" app/api --include=route.ts` で行頭 const/let/var + createClient を機械抽出できるが、**helper 関数内の呼び出しは false positive** になるので、実装は「`createClient(` の最初の登場行が最初の `export function` より前か」を目視で追加確認する。**関連する潜在リスク**: `lib/supabase.ts` が module-top-level で `createBrowserClient(URL!, ANON_KEY!)` を実行しており、`import { supabase } from '@/lib/supabase'` する module を評価する環境で `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` が未設定だと落ちる構造（現状は env が入っているので実害ゼロだが、他プロジェクト流用時に再発しうる） |
| 2026-07-16 | **3ドメインシナジー: sameAs は3サイトで完全同一配列にする**: include.bz / branding.bz / designnow.design を「同一運営者の1エンティティ」として Google/AI に名寄せさせるには、3サイトの Organization.sameAs を**同じ URL・同じ順序**の配列にする。バラバラだと名寄せが弱まる。あわせて `parentOrganization`/`publisher` で親 Organization ノード（`https://include.bz/#organization`）を参照させると、系統関係が明示できる。**教訓**＝ドメインを跨ぐ mesh は「schema」「相互リンク」「共通の運営者記載」の3辺が揃って初めて機能する（今回はフッターに DESIGN NOW リンクを追加して可視相互リンクも3辺完成） |
| 2026-07-16 | **他セッションと状態が違う可能性を疑う**: 北川さんから「ローカルが 10コミット遅れ、未追跡ファイル散乱、pull できない」と復旧指示が来たが、実測すると behind=0/ahead=0・未追跡なし・**前回セッションの実装だけが未コミットで残っていた**、という状況だった（複数セッション並行運用の情報ズレ）。**対策**＝指示に書かれた前提と実測が食い違う場合、指示に盲従せず先に `git status`/`git rev-list --count HEAD..origin/main`/`git stash list` で実測 → 指示の意図（「必ずコミット push まで完了」等）だけを汲んで手順を最適化する。指示のステップを字面どおり実行すると余計な stash 操作が入る |


---

## 6. プロジェクト間の依存

| 依存先 | 内容 |
|--------|------|
| dots.bz | Supabase認証パターンの共通化検討中 |
| include.bz | branding.bzのサービス説明ページからリンク |
