# 作業ツリーWIP 仕分け表 v1（Phase 1・報告のみ）

- 対象: branding-bz リポジトリの未コミット変更（2026-06-08 時点）
- 種別: **Phase 1＝棚卸し報告のみ。破壊的操作（restore/clean/stash drop/checkout）は一切未実施**。
- 前提: `260608_低リスクRLS是正_指示書_v1.md`（順序2〜4）完了済み。ローカルmainはorigin/mainと同期（未pushコミットなし）。
- 全体ビルド: 現在の作業ツリー（全WIP込み）で `npm run build` ＝ **Compiled successfully**（直近検証）。＝全WIPはコンパイル可（機能完成とは別）。

## サマリ（4分類）

| 分類 | 件数 | 概要 |
|------|------|------|
| **A コミットすべき** | 2群(7ファイル) | brand_values→value_propositions リネーム追従（**本番バグ修正含む**）／管理ログインのボタン高さ調整 |
| **B 退避すべき** | 1群(9ファイル+新規) | ビデオラーニング「構造化（カテゴリ→テーマ）」機能（進行中・新規API多数） |
| **C 破棄すべき** | 7ファイル | sw重複ビルド成果物・孤立コンポーネント・使い捨てcodemod |
| **D 判断保留** | stash 4本ほか | 別セッション/別ブランチのstash・codemodの扱い |

---

## A コミットすべき（完成・価値あり）

### A1. brand_values → value_propositions リネーム追従（⚠ 本番バグ修正含む）
旧テーブル `brand_values` は `value_propositions` にリネーム済み（**本番DBに brand_values は既に存在しない**）。リネーム時にコード追従が漏れたファイル群。

| ファイル | 内容 | 重要度 |
|---------|------|--------|
| `app/portal/values/page.tsx` | `.from('brand_values')` → `.from('value_propositions')`（**実クエリ。本番で存在しないテーブルを叩いており現在壊れている**） | **高（本番バグ）** |
| `app/portal/guidelines/page.tsx` | コメントのみ（brand_values→value_propositions） | 低 |
| `app/admin/brand/values/page.tsx` | コメントのみ | 低 |
| `scripts/cleanup-demo-data.sql` | DELETE文の brand_values→value_propositions | 低（dev script） |
| `scripts/rollback-rls-step1.sql` | ポリシー名等の brand_values→value_propositions | 低（dev script） |
| `scripts/seed-demo-data-expand.sql` | CREATE/INSERTの brand_values→value_propositions | 低（dev script） |

- **分割コミット案（1コミット）**: 「brand_values→value_propositionsリネームのコード/スクリプト追従（portal/values の壊れた実クエリ修正を含む）」
- 補足: 私の直近コミット `07b7f96` で portal/**strategy** の同種参照は修正済み。本件は**残りの参照**（特に portal/**values** は別ファイルで未修正＝現在も本番で壊れている）。**Phase 2 で最優先コミット＋デプロイ推奨**。

### A2. 管理ログイン ボタン高さのモバイル調整
| ファイル | 内容 |
|---------|------|
| `app/admin/login/page.tsx` | Googleログイン/送信ボタン `h-12` → `h-14`（モバイルサイズ基準） |

- 北川さんのモバイルサイズ統一作業の一部。小・完結。
- **コミット案**: 「管理ログインのボタン高さをモバイル基準(h-14)に統一」

---

## B 退避すべき（進行中・未完成）

### B1. ビデオラーニング「構造化（カテゴリ→テーマ→動画）」機能
既存のビデオラーニングに、**カテゴリ／テーマの階層構造**を追加する進行中の大型機能。

| 種別 | ファイル |
|------|---------|
| 修正(tracked) | `app/admin/learning/page.tsx`(±340), `app/admin/learning/LearningVideoDialog.tsx`(±143), `lib/types/learning.ts`(+LearningCategory/LearningTheme/LearningStructure型・theme_id/category_id), `app/api/learning/videos/route.ts`, `app/api/learning/videos/[id]/route.ts` |
| 新規(untracked) | `app/admin/learning/LearningStructureManager.tsx`, `app/api/learning/categories/`, `app/api/learning/structure/`, `app/api/learning/themes/` |

- 状態: **コンパイルは通る**（全体build通過）が、新規API（categories/structure/themes）＋構造マネージャを伴う**進行中機能**。DBに `learning_categories`/`learning_themes` テーブルが必要なはず（要確認）。
- **退避案**: 作業ブランチ `wip/learning-structure` を main から作成し、上記ファイル群を退避 → main の作業ツリーをクリーン化。
- 関連doc: `feature-requirements.md`(+97) はビデオラーニング仕様の追記。完成済み機能の記述＋本構造化の記述が混在。→ **B1と同じブランチに同梱**を推奨（doc単独commitでも可だが進行中機能の記述を含むため）。
- **判断ポイント**: 北川さんが「この機能は完成・リリース可」と判断するなら **A（main へ機能コミット）** に格上げ。未完成なら B（退避）。

---

## C 破棄すべき（古い・重複・使い捨て残骸）

| ファイル | 破棄理由 |
|---------|---------|
| `public/sw 2.js` / `public/sw 3.js` | serwistビルド成果物 `public/sw.js`（gitignore）の **macOS重複コピー**（「 2」「 3」サフィックス）。中身は minified service worker。完全な不要物 |
| `app/tools/stp/app/[sessionId]/components/StepPlaceholder.tsx` | **どこからも未参照（孤立）**。実験残骸 |
| `app/tools/persona/app/[sessionId]/components/StepPlaceholder.tsx` | 同上（孤立） |
| `app/tools/colors/app/components/ProgressBar.tsx` | 同上（孤立。`components/ProgressBar` 参照なし） |

※ 破棄は Phase 2 で `git clean`（未追跡）により実施。**承認リストにあるもののみ**。

---

## D 判断保留（北川さん確認）

| 項目 | 内容 | 確認したいこと |
|------|------|----------------|
| **D1 codemod 4本** | `scripts/codemod-{card-padding,icon-sizes,mobile-body,spacing}.mjs`（未追跡） | モバイル装飾の**使い捨てcodemod**（成果は本番反映済）。**破棄してよいか**／記録として `scripts/` にコミットして残すか |
| **D2 stash@{1}** | 「並行WIP(PWA/評価軸/next16移行) - 私のpush中の一時退避」 | 北川さんの並行作業。**保持で良いか**（中身確認しますか） |
| **D3 stash@{2}** | 「On chore/cleanup-dead-code: review-fixes + 提供価値reorg backup 17c314f」（別ブランチ由来） | 用途・要否の確認 |
| **D4 stash@{3}** | 「On feature/rls-step1: WIP stp-shared-profile作業」（別ブランチ由来） | 用途・要否の確認 |
| **D5 stash@{0}** | 「WIP styling (push rebase 2026-06-08)」＝**私(Claude)がpush時に作った一時退避** | 中身は現作業ツリーのstyling WIPと重複の可能性。**drop してよいか**（要中身照合） |

※ stash は全て Phase 2 でも **明示承認がない限り drop しない**（破壊的のため）。

---

## Phase 2 実行案（承認後）

1. **A1** を最優先コミット（本番バグ修正）→ 北川さん指示があれば push（/portal/values 復旧）
2. **A2** をコミット
3. **B1** を `wip/learning-structure` ブランチへ退避（＋feature-requirements.md 同梱）
4. **C** の承認分のみ `git clean -f`（sw 2/3.js・孤立3コンポーネント）
5. **D** は回答に従って処理（codemod・各stash）
6. 完了後 `git status` クリーン確認 → 報告
7. push は別途指示（本指示書はコミットまで）

各コミットで「無関係ファイルの混入なし」を `git show --stat` で確認。
