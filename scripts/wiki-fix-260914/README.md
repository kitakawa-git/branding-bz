# §8 用語集 8語の文言修正（2026-09）

対象: Supabase `public.wiki_terms` の8語。

**実行済み。** 2026-09-14 に Cowork 側から本番へ適用しました（wiki_terms 8件）。
このファイル一式は実行記録として残しています。再実行は不要です。

対象外: `ブランドパーソナリティの5次元`（Ruggedness の訳語を意味から照合する必要があるため、監修担当へ引き継ぎ済み）

## 実行順序

| 順 | ファイル | 内容 | 変更 |
|---|---|---|---|
| 1 | `01_backup_and_precheck.sql` | 退避表 `wiki_backup_260914` 作成・件数・ハッシュ確認 | なし |
| 2 | `02_update.sql` | 8語を更新（1トランザクション） | **あり** |
| 3 | `03_postcheck.sql` | 事後確認＋スナップショット `wiki_after_260914` 作成 | なし |
| 4 | `04_restore.sql` | 戻すときだけ | **あり** |

- 01 の `backup_rows` が **8** でなければ 02 を実行しない
- 03「古い表現が残っていないこと」は全項目 **0** が正常

## 中止される条件（02）

1. 退避表が無い / 8件でない
2. 01 実行後に short_def・long_def が変わっている
3. 置換対象が見つからない（適用済みの可能性）
4. 置換後に古い表現が残っている
5. 更新された件数が8件でない

## 変更する列

`short_def` / `long_def` / `updated_at` のみ。
`slug` / `term` / `status` / `categories` / `aliases` / `id` は変更しません。

## 後片付け

**実行しないこと。** 退避表4つ（`news_backup_260914` / `news_after_260914` / `wiki_backup_260914` / `wiki_after_260914`）は維持すると決定済みです。削除しないでください。

以下は SQL を書いた時点の記載で、参考として残しています。

```sql
drop table if exists public.wiki_after_260914;
drop table if exists public.wiki_backup_260914;
```

## ⚠️ terms.json との同期が未完了です

`scripts/wiki-seed/terms.json` には**別セッションの未コミット変更**（+468 / −326行、最終更新 2026-08-19）があり、
DB の最終更新（2026-08-18）より新しい状態です。今回の8語は触れていません。

**同期が終わるまで、古い `terms.json` からの再シードを行わないでください。**
再シードすると今回の修正が消えます。

適用手順は `TERMS-JSON-SYNC.md` を参照。
