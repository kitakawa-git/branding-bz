---
name: sync-status
description: BRD-PROJECT-STATUS.md を現在のコード状態と突き合わせて自動更新する
---

# branding.bz ステータス更新

BRD-PROJECT-STATUS.md は Claude Code・Cowork・Claude Projects の共通ハブファイル。
このスキルは開発作業の区切りに実行し、ステータスを最新に保つ。

**ファイルパス:** `../BRD-PROJECT-STATUS.md`（branding-bzルートからの相対パス）

## 手順

### 1. 現状の確認
以下を実行して現在のコード状態を把握する:
- `git log --oneline -10` — 直近のコミット内容
- `npx tsc --noEmit 2>&1 | head -5` — TypeScriptエラー数
- `git diff --stat HEAD~1` — 直近の変更ファイル

### 2. ステータスファイルを読み込む
- `../BRD-PROJECT-STATUS.md` を読む

### 3. 残タスクを更新
コード確認結果に基づいて:
- 完了したタスクの `[ ]` を `[x]` に変更し、取り消し線＋✅を追加
- 新たに発見した問題があれば適切なカテゴリに追記

### 4. 完了済み機能を更新（該当がある場合のみ）
新しい機能を実装した場合、セクション3に追記。

### 5. 技術メモを更新（該当がある場合のみ）
今回のセッションで発見した教訓・ハマりポイントがあれば追記。

### 6. プロジェクト間の依存を更新（該当がある場合のみ）
他プロジェクトとの依存関係に変更があれば更新。

### 7. メタデータを更新
- 冒頭の `**最終更新:**` を今日の日付に
- `**更新者:**` を `Claude Code（/sync-status）` に

### 8. 完了報告
```
## ステータス更新完了
- プロジェクト: branding.bz
- 完了にしたタスク: ○○, ○○
- 新規追加タスク: ○○（あれば）
- 新規教訓: ○○（あれば）
- TSエラー: N件
```
